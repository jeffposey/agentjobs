{
  "components": {
    "schemas": {
      "AcceptanceCriterion": {
        "additionalProperties": false,
        "description": "One element of the definition of done.\n\nTwo fields describe how it is verified and they are not the same kind of thing\n(task-147). ``verify`` is prose, addressed to a person: it says what somebody should\ndo to satisfy themselves the criterion holds, and nothing executes it. ``check`` is\nan argv list, addressed to this machine: its exit code decides ``met`` or ``failed``,\nand it is the only one of the two that anything runs.\n\nThey are kept apart rather than merged because most criteria can only have the\nfirst. \"The dashboard reads clearly on a phone\" is verifiable and not executable,\nand a schema that offered one field would either lose that criterion or invite a\ncommand written where prose belongs.",
        "properties": {
          "check": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional argv list whose exit code decides this criterion: 0 is met, anything else is failed. A list, never a string -- nothing splits it and no shell sees it.",
            "examples": [
              [
                "poetry",
                "run",
                "pytest",
                "tests/test_concurrency.py",
                "-q"
              ]
            ],
            "title": "Check"
          },
          "id": {
            "description": "Identifier scoped to the task, e.g. ac-1.",
            "title": "Id",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/AcceptanceStatus",
            "default": "pending"
          },
          "text": {
            "description": "What must be true.",
            "title": "Text",
            "type": "string"
          },
          "verify": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional prose for a person: how someone would satisfy themselves this criterion holds. Never executed -- an executable check is `check`.",
            "title": "Verify"
          }
        },
        "required": [
          "id",
          "text"
        ],
        "title": "AcceptanceCriterion",
        "type": "object"
      },
      "AcceptanceStatus": {
        "description": "State of one acceptance criterion.\n\nDeliberately distinct from DeliverableStatus: a criterion is *verified* (met), a\ndeliverable is *produced* (done). Collapsing them recreates the v1 problem of one\nword straining across meanings (design doc section 3).",
        "enum": [
          "pending",
          "met",
          "failed",
          "dropped"
        ],
        "title": "AcceptanceStatus",
        "type": "string"
      },
      "AgeBucket": {
        "description": "One band of the age distribution, emitted whether or not it holds anything.",
        "properties": {
          "label": {
            "enum": [
              "0-6d",
              "7-29d",
              "30-89d",
              "90d+"
            ],
            "title": "Label",
            "type": "string"
          },
          "mean_age_days": {
            "title": "Mean Age Days",
            "type": "number"
          },
          "tasks": {
            "title": "Tasks",
            "type": "integer"
          }
        },
        "required": [
          "label",
          "tasks",
          "mean_age_days"
        ],
        "title": "AgeBucket",
        "type": "object"
      },
      "AgingTask": {
        "description": "One of the ten oldest open, unarchived tasks.",
        "properties": {
          "age_days": {
            "title": "Age Days",
            "type": "number"
          },
          "ball": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ball"
          },
          "ball_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ball Reason"
          },
          "priority": {
            "title": "Priority",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "title": {
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "title",
          "priority",
          "age_days"
        ],
        "title": "AgingTask",
        "type": "object"
      },
      "AnalyticsCoverage": {
        "description": "What the store is prepared to claim about its own history (section 3.6).\n\nPart of the payload rather than something the page infers, so no panel has to guess\nhow far back it is allowed to draw. ``baseline_at`` is null for exactly one state --\na project with no events at all -- which is what section 9.1 renders as a sentence\nrather than as an empty axis.",
        "properties": {
          "baseline_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Null means the store makes no claim at all.",
            "title": "Baseline At"
          },
          "baseline_kind": {
            "default": "unknown",
            "enum": [
              "native",
              "reconstructed",
              "backfilled",
              "unknown"
            ],
            "title": "Baseline Kind",
            "type": "string"
          },
          "complete": {
            "default": false,
            "description": "True when the whole window is covered by native events.",
            "title": "Complete",
            "type": "boolean"
          },
          "events": {
            "additionalProperties": {
              "type": "integer"
            },
            "description": "Event counts by source.",
            "title": "Events",
            "type": "object"
          },
          "native_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "First event written by a verb at the moment of the change.",
            "title": "Native From"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "One sentence, rendered as the coverage caption.",
            "title": "Note"
          },
          "reconstructed_before": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Draw the hatch left of this instant.",
            "title": "Reconstructed Before"
          }
        },
        "title": "AnalyticsCoverage",
        "type": "object"
      },
      "AnalyticsRange": {
        "description": "The window every panel in one response was computed over.\n\n``bucket`` and ``throughput_bucket`` are two grains rather than one, per section 8.3\nand section 8.4: the backlog level is read a day at a time while throughput is read\na week or a month at a time, and a client that had to infer the second from the\ndates would be holding a copy of a rule that lives here.",
        "properties": {
          "bucket": {
            "description": "Grain of the backlog and holder spines.",
            "enum": [
              "day",
              "week",
              "month"
            ],
            "title": "Bucket",
            "type": "string"
          },
          "end": {
            "description": "Exclusive, UTC.",
            "format": "date-time",
            "title": "End",
            "type": "string"
          },
          "key": {
            "enum": [
              "30d",
              "90d",
              "12m",
              "all"
            ],
            "title": "Key",
            "type": "string"
          },
          "start": {
            "description": "Inclusive, UTC. Clipped to the coverage baseline.",
            "format": "date-time",
            "title": "Start",
            "type": "string"
          },
          "throughput_bucket": {
            "description": "Grain of the throughput and cycle-time series.",
            "enum": [
              "day",
              "week",
              "month"
            ],
            "title": "Throughput Bucket",
            "type": "string"
          },
          "timezone": {
            "description": "IANA zone name the buckets were computed in.",
            "title": "Timezone",
            "type": "string"
          }
        },
        "required": [
          "key",
          "start",
          "end",
          "bucket",
          "throughput_bucket",
          "timezone"
        ],
        "title": "AnalyticsRange",
        "type": "object"
      },
      "AnalyticsResponse": {
        "description": "One request for the whole analytics page (section 7.1).\n\nNot seventeen endpoints: the panels share a range, a timezone and a coverage\nstatement that must be identical across all of them, and the storage cost of the\nwhole set was measured at 6.3 ms -- so splitting it would buy nothing and cost\nseventeen chances to render half a page.\n\nThe second set (section 21) adds the process series. Each carries its own\n``SeriesCoverage``; the page-level ``coverage`` still describes the task history.",
        "properties": {
          "aging": {
            "items": {
              "$ref": "#/components/schemas/AgeBucket"
            },
            "title": "Aging",
            "type": "array"
          },
          "backlog": {
            "description": "One entry per bucket in range, gaps filled.",
            "items": {
              "$ref": "#/components/schemas/BacklogPoint"
            },
            "title": "Backlog",
            "type": "array"
          },
          "cost_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "cost_per_task": {
            "items": {
              "$ref": "#/components/schemas/CostPerTaskPoint"
            },
            "title": "Cost Per Task",
            "type": "array"
          },
          "coverage": {
            "$ref": "#/components/schemas/AnalyticsCoverage"
          },
          "estimates": {
            "items": {
              "$ref": "#/components/schemas/EstimatePoint"
            },
            "title": "Estimates",
            "type": "array"
          },
          "estimates_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "estimator": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/EstimatorState"
              },
              {
                "type": "null"
              }
            ]
          },
          "finishes": {
            "items": {
              "$ref": "#/components/schemas/FinishPoint"
            },
            "title": "Finishes",
            "type": "array"
          },
          "finishes_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "gates": {
            "items": {
              "$ref": "#/components/schemas/GatePoint"
            },
            "title": "Gates",
            "type": "array"
          },
          "gates_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "holders": {
            "description": "The same spine.",
            "items": {
              "$ref": "#/components/schemas/HolderPoint"
            },
            "title": "Holders",
            "type": "array"
          },
          "in_review": {
            "items": {
              "$ref": "#/components/schemas/InReview"
            },
            "title": "In Review",
            "type": "array"
          },
          "machine": {
            "items": {
              "$ref": "#/components/schemas/MachinePoint"
            },
            "title": "Machine",
            "type": "array"
          },
          "machine_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "oldest": {
            "description": "Ten, open and not archived.",
            "items": {
              "$ref": "#/components/schemas/AgingTask"
            },
            "title": "Oldest",
            "type": "array"
          },
          "open_questions": {
            "items": {
              "$ref": "#/components/schemas/OpenQuestion"
            },
            "title": "Open Questions",
            "type": "array"
          },
          "range": {
            "$ref": "#/components/schemas/AnalyticsRange"
          },
          "review": {
            "items": {
              "$ref": "#/components/schemas/ReviewPoint"
            },
            "title": "Review",
            "type": "array"
          },
          "review_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "runs": {
            "items": {
              "$ref": "#/components/schemas/RunPoint"
            },
            "title": "Runs",
            "type": "array"
          },
          "runs_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "segments": {
            "items": {
              "$ref": "#/components/schemas/SegmentPoint"
            },
            "title": "Segments",
            "type": "array"
          },
          "segments_coverage": {
            "$ref": "#/components/schemas/SeriesCoverage"
          },
          "stuck": {
            "items": {
              "$ref": "#/components/schemas/StuckGroup"
            },
            "title": "Stuck",
            "type": "array"
          },
          "throughput": {
            "items": {
              "$ref": "#/components/schemas/ThroughputPoint"
            },
            "title": "Throughput",
            "type": "array"
          },
          "totals": {
            "$ref": "#/components/schemas/AnalyticsTotals"
          }
        },
        "required": [
          "range",
          "coverage",
          "totals"
        ],
        "title": "AnalyticsResponse",
        "type": "object"
      },
      "AnalyticsTotals": {
        "description": "Identical, field for field, to ``build_dashboard_snapshot()[\"stats\"]``.\n\nPlus ``open``, which is the number the backlog series ends on. The two are computed\nby different code -- SQL here, Python there -- and\n``tests/test_analytics_api.py`` asserts they agree, which is the whole point:\na page whose headline counts disagreed with the Dashboard's would discredit both.",
        "properties": {
          "awaiting_input": {
            "title": "Awaiting Input",
            "type": "integer"
          },
          "blocked": {
            "title": "Blocked",
            "type": "integer"
          },
          "completed": {
            "title": "Completed",
            "type": "integer"
          },
          "in_progress": {
            "title": "In Progress",
            "type": "integer"
          },
          "open": {
            "title": "Open",
            "type": "integer"
          },
          "total": {
            "title": "Total",
            "type": "integer"
          },
          "waiting_for_human": {
            "title": "Waiting For Human",
            "type": "integer"
          }
        },
        "required": [
          "total",
          "in_progress",
          "blocked",
          "waiting_for_human",
          "awaiting_input",
          "completed",
          "open"
        ],
        "title": "AnalyticsTotals",
        "type": "object"
      },
      "AnswerActionRequest": {
        "description": "Answers to the task's open questions, plus optional prose (task-017).\n\n``feedback`` is optional **here and nowhere else**, which is the one way this differs\nfrom every other send-back. Requiring it would defeat the point of the task: answering\nfour questions by tapping four options, on a phone, without typing. Something has to\nhave been said, though -- a request with neither answers nor prose is refused, because\nit would hand the ball back carrying nothing.\n\nFree text and options are not alternatives. Both can be sent, per question and for the\ntask as a whole, because the session that motivated this had every option on one\nquestion rejected in favour of a typed reply.",
        "properties": {
          "answers": {
            "description": "One entry per question answered. Written atomically with the handoff.",
            "items": {
              "$ref": "#/components/schemas/AnswerSubmission"
            },
            "title": "Answers",
            "type": "array"
          },
          "attachments": {
            "description": "Images evidencing the answer, stored as sidecar files.",
            "items": {
              "$ref": "#/components/schemas/AttachmentUpload"
            },
            "title": "Attachments",
            "type": "array"
          },
          "feedback": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Prose to carry alongside the answers. Optional when answers are sent.",
            "examples": [
              "Also: do not start this until task-016 merges."
            ],
            "title": "Feedback"
          },
          "user": {
            "description": "User performing the action",
            "examples": [
              "jeff"
            ],
            "title": "User",
            "type": "string"
          }
        },
        "required": [
          "user"
        ],
        "title": "AnswerActionRequest",
        "type": "object"
      },
      "AnswerSubmission": {
        "additionalProperties": false,
        "description": "One question answered, as the browser sends it (task-017).",
        "properties": {
          "other": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What was typed into the always-present free-text box.",
            "title": "Other"
          },
          "re": {
            "description": "Id of the question log entry being answered.",
            "minimum": 1.0,
            "title": "Re",
            "type": "integer"
          },
          "selected": {
            "description": "Labels of the options tapped.",
            "items": {
              "type": "string"
            },
            "title": "Selected",
            "type": "array"
          }
        },
        "required": [
          "re"
        ],
        "title": "AnswerSubmission",
        "type": "object"
      },
      "ApproveActionRequest": {
        "description": "An approval, plus the gate the person believed they were approving (task-001).\n\n``gate`` is a guard, never a source. The route derives the gate from the record's\n``ball_reason`` and refuses with 409 when this disagrees, so a page rendered at a plan\ngate and clicked after the agent re-handed to review cannot merge on a button that\nsaid *proceed* -- and the reverse cannot turn a merge approval into a plan approval.\nOmitted, the derived gate is accepted, which is exactly what the route did before the\nfield existed.",
        "properties": {
          "gate": {
            "anyOf": [
              {
                "enum": [
                  "plan",
                  "final"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The gate the approver was shown: `plan` at human/plan, `final` at human/review or human/approval. Refused with 409 when it disagrees with the record; omitted, the record decides.",
            "examples": [
              "final"
            ],
            "title": "Gate"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional note, recorded verbatim in the handoff prompt and the log.",
            "examples": [
              "Fold the naming nit in before you merge."
            ],
            "title": "Note"
          },
          "user": {
            "description": "User performing the action",
            "examples": [
              "jeff"
            ],
            "title": "User",
            "type": "string"
          }
        },
        "required": [
          "user"
        ],
        "title": "ApproveActionRequest",
        "type": "object"
      },
      "ArmedProjectView": {
        "description": "One project the pull mode is armed for, as the slot board draws it (task-462).\n\nMachine-wide, beside the runs and the waiting dispatches, because that is the scope\nof the thing being described: the pull mode competes for the same `max_concurrent_runs`\nevery other row here is about, and a board that learned \"armed\" from a per-project\ncall would show an arming that is not the one taking the slot in front of it.",
        "properties": {
          "armed_at": {
            "description": "When they armed it, UTC.",
            "title": "Armed At",
            "type": "string"
          },
          "armed_by": {
            "default": "",
            "description": "The person who armed it.",
            "title": "Armed By",
            "type": "string"
          },
          "arming_id": {
            "title": "Arming Id",
            "type": "string"
          },
          "bound": {
            "description": "What is left of the bound, as one phrase -- e.g. `1 of 3 starts used`, `until 2026-09-19T06:00:00Z`, `until disarmed`. Composed on the server so the board, the CLI and the dispatch panel say the same words.",
            "title": "Bound",
            "type": "string"
          },
          "next_task_id": {
            "default": "",
            "description": "What `task_next` says it would start next. Sent so a person can see it *before* it happens and move something else to the top if they would rather. Empty when nothing in that backlog is claimable right now.",
            "title": "Next Task Id",
            "type": "string"
          },
          "next_task_title": {
            "default": "",
            "title": "Next Task Title",
            "type": "string"
          },
          "next_task_url": {
            "default": "",
            "description": "Where that task is, in this app.",
            "title": "Next Task Url",
            "type": "string"
          },
          "paused_by": {
            "default": "",
            "description": "The incident id holding this arming's starts off (task-463), or empty. The arming keeps its bound and its authority while this is set and starts again on the first tick after the incident closes; `next_task_id` still says what it will start then.",
            "title": "Paused By",
            "type": "string"
          },
          "posture": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The envelope pulled runs get, or null for the project default.",
            "title": "Posture"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "project_name": {
            "default": "",
            "title": "Project Name",
            "type": "string"
          },
          "starts_left": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runs it may still start, or null when the bound is a moment.",
            "title": "Starts Left"
          }
        },
        "required": [
          "project_id",
          "arming_id",
          "armed_at",
          "bound"
        ],
        "title": "ArmedProjectView",
        "type": "object"
      },
      "Assignment": {
        "additionalProperties": false,
        "description": "Live ownership and authoring-time eligibility.\n\nv1's `assigned_to` conflated these: documented as \"currently assigned\" and used as\na static label.",
        "properties": {
          "eligible": {
            "description": "Actor ids that may claim this task. Empty means anyone.",
            "items": {
              "type": "string"
            },
            "title": "Eligible",
            "type": "array"
          },
          "owner": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Actor id holding the task. Set on claim, cleared on release/close.",
            "title": "Owner"
          }
        },
        "title": "Assignment",
        "type": "object"
      },
      "Attachment": {
        "additionalProperties": false,
        "description": "One image stored beside the tasks, referenced from the entry it illustrates.\n\nThe blob lives in a sidecar file; only this metadata is in the YAML. That is the\nwhole point of the storage decision: a task file stays something a person can read\nin a text editor and git can diff line by line, which a base64 blob would end.\n\n``path`` is relative to the project's tasks directory, not to the repository root,\nbecause that is the directory storage already owns and resolves safely. ``sha256``\nis both the file's name and its integrity check: a read that does not hash to this\nis refused rather than rendered.",
        "properties": {
          "label": {
            "description": "Accessible label; alt text where it renders.",
            "title": "Label",
            "type": "string"
          },
          "media_type": {
            "description": "Image media type, derived from the bytes.",
            "title": "Media Type",
            "type": "string"
          },
          "path": {
            "description": "Sidecar path relative to the tasks directory.",
            "examples": [
              "attachments/task-042/6f4b...c1.png"
            ],
            "title": "Path",
            "type": "string"
          },
          "sha256": {
            "description": "Content hash.",
            "maxLength": 64,
            "minLength": 64,
            "title": "Sha256",
            "type": "string"
          },
          "size_bytes": {
            "description": "Size of the stored file.",
            "minimum": 1.0,
            "title": "Size Bytes",
            "type": "integer"
          }
        },
        "required": [
          "path",
          "media_type",
          "sha256",
          "size_bytes",
          "label"
        ],
        "title": "Attachment",
        "type": "object"
      },
      "AttachmentUpload": {
        "description": "One pasted image, on its way to a sidecar file.\n\nBase64 in the request body, never in the stored record: the transport needs the\nbytes inline and the YAML must stay readable, and those are different problems with\ndifferent right answers. ``media_type`` is deliberately absent -- the server reads\nthe type from the bytes, because a declared type is a claim and the magic number is\nthe blob.",
        "properties": {
          "data_base64": {
            "description": "Base64-encoded image bytes (PNG, JPEG or WebP).",
            "minLength": 1,
            "title": "Data Base64",
            "type": "string"
          },
          "label": {
            "default": "",
            "description": "Accessible label used as alt text where the image renders.",
            "title": "Label",
            "type": "string"
          }
        },
        "required": [
          "data_base64"
        ],
        "title": "AttachmentUpload",
        "type": "object"
      },
      "AttentionAckRequest": {
        "description": "The episode a person has just deliberately acted on.\n\nThe id is required rather than implied by \"the current one\", so a click made\nagainst a screen one poll out of date acknowledges the episode the person actually\nsaw, or nothing at all -- never whichever episode happens to be open when the\nrequest lands.",
        "properties": {
          "episode_id": {
            "title": "Episode Id",
            "type": "string"
          }
        },
        "required": [
          "episode_id"
        ],
        "title": "AttentionAckRequest",
        "type": "object"
      },
      "AttentionEpisodeView": {
        "description": "The current attention episode, as much of it as a client needs to act.\n\n``id`` is what a client compares against the last episode it notified for, so one\ndesktop notification is drawn per episode however often the page polls. ``tasks``\nis the membership in inbox order; ``lead_task_id`` and ``lead_task_title`` are its\nhead, repeated as fields so a notification can name the task without a client\nre-deriving \"first\" from an array it did not sort.",
        "properties": {
          "acknowledged": {
            "title": "Acknowledged",
            "type": "boolean"
          },
          "deep_link": {
            "default": "",
            "title": "Deep Link",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "string"
          },
          "lead_ask": {
            "default": "",
            "title": "Lead Ask",
            "type": "string"
          },
          "lead_task_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Lead Task Id"
          },
          "lead_task_title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Lead Task Title"
          },
          "started_at": {
            "format": "date-time",
            "title": "Started At",
            "type": "string"
          },
          "tasks": {
            "items": {
              "type": "string"
            },
            "title": "Tasks",
            "type": "array"
          }
        },
        "required": [
          "id",
          "started_at",
          "acknowledged",
          "tasks"
        ],
        "title": "AttentionEpisodeView",
        "type": "object"
      },
      "AttentionResponse": {
        "description": "How much of this project is stopped waiting on a person, and whether they know.\n\nIts own endpoint rather than a field of the dashboard, because the header renders\nit on every surface and the dashboard projection is 900KB of task records --\nmeasured against this repository's own corpus, 2026-09-05. A badge that cost that\non the Tasks tab would not be worth having. The episode added on task-422 keeps\nthat property: it is one small object, never the records.\n\n``blocking`` is unchanged and still the badge number. ``episode`` is ``null``\nexactly when nothing is waiting.",
        "properties": {
          "blocking": {
            "title": "Blocking",
            "type": "integer"
          },
          "episode": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/AttentionEpisodeView"
              },
              {
                "type": "null"
              }
            ]
          },
          "stalled": {
            "description": "The members of the waiting set that are there because nobody is working them, quietest first. A subset of `episode.tasks`, never a second list of its own -- there is one waiting set and one episode over it.",
            "items": {
              "$ref": "#/components/schemas/StalledTaskRead"
            },
            "title": "Stalled",
            "type": "array"
          }
        },
        "required": [
          "blocking"
        ],
        "title": "AttentionResponse",
        "type": "object"
      },
      "BacklogPoint": {
        "description": "One bucket of the backlog level and both flows, on a filled calendar spine.",
        "properties": {
          "closed": {
            "description": "Departures in this bucket.",
            "title": "Closed",
            "type": "integer"
          },
          "day": {
            "format": "date",
            "title": "Day",
            "type": "string"
          },
          "estimated": {
            "description": "This bucket contains a reconstructed or backfilled event.",
            "title": "Estimated",
            "type": "boolean"
          },
          "open_count": {
            "title": "Open Count",
            "type": "integer"
          },
          "opened": {
            "description": "Arrivals in this bucket.",
            "title": "Opened",
            "type": "integer"
          }
        },
        "required": [
          "day",
          "open_count",
          "opened",
          "closed",
          "estimated"
        ],
        "title": "BacklogPoint",
        "type": "object"
      },
      "Ball": {
        "description": "Who acts next. Required while a task is open; null only when closed.",
        "enum": [
          "agent",
          "human",
          "external"
        ],
        "title": "Ball",
        "type": "string"
      },
      "BallReason": {
        "description": "Why the ball holder holds it. Scoped to the holder -- see BALL_REASONS.",
        "enum": [
          "available",
          "work",
          "revise",
          "answer",
          "redirect",
          "hold",
          "spec",
          "review",
          "plan",
          "decision",
          "approval",
          "input",
          "dependency",
          "service"
        ],
        "title": "BallReason",
        "type": "string"
      },
      "Branch": {
        "additionalProperties": false,
        "description": "Git branch lifecycle metadata.",
        "properties": {
          "merged_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Merged At"
          },
          "name": {
            "description": "Branch name.",
            "title": "Name",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/BranchStatus",
            "default": "active"
          }
        },
        "required": [
          "name"
        ],
        "title": "Branch",
        "type": "object"
      },
      "BranchStatus": {
        "description": "Git branch lifecycle. Unchanged from v1 -- genuinely distinct.",
        "enum": [
          "active",
          "merged",
          "abandoned"
        ],
        "title": "BranchStatus",
        "type": "string"
      },
      "BrokenTaskFile": {
        "description": "An on-disk task record that could not be loaded.",
        "properties": {
          "filename": {
            "title": "Filename",
            "type": "string"
          },
          "path": {
            "title": "Path",
            "type": "string"
          },
          "reason": {
            "title": "Reason",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "path",
          "filename",
          "reason"
        ],
        "title": "BrokenTaskFile",
        "type": "object"
      },
      "ChainAuthorizeRequest": {
        "description": "Authorise a bounded chain of dispatches against one task (task-150).\n\n**There is no field here for the digest, the chain id, or which criteria are\ncovered**, and that absence is the whole of dispatch design section 2 applied to a\nloop. Those three are computed from the stored task at the moment of the write; a\nrequest that supplied them would be asking the server to take the definition of done\nfrom the caller, which is precisely the move the digest exists to defeat.\n\nThe two bounds *are* here, because they are the thing a person is choosing. Both are\nchecked against the design's ceilings on the way in and again on the payload model.",
        "properties": {
          "max_iterations": {
            "default": 5,
            "description": "How many dispatches this authorisation buys, at most.",
            "maximum": 20.0,
            "minimum": 1.0,
            "title": "Max Iterations",
            "type": "integer"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What the person wants the chain to achieve, for the record.",
            "title": "Note"
          },
          "user": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The human authorising this. Validated against the project's configured actors and refused unless `kind: human`, exactly as a dispatch's is.",
            "title": "User"
          },
          "wall_clock_seconds": {
            "default": 14400,
            "description": "How long the chain may run from the moment it is authorised.",
            "maximum": 43200.0,
            "minimum": 1.0,
            "title": "Wall Clock Seconds",
            "type": "integer"
          }
        },
        "title": "ChainAuthorizeRequest",
        "type": "object"
      },
      "ChainIteration-Input": {
        "description": "One turn of a chain, as the task page reads it back.",
        "properties": {
          "entry_id": {
            "description": "The `check_result` entry this turn wrote.",
            "title": "Entry Id",
            "type": "integer"
          },
          "iteration": {
            "description": "0 is the baseline recorded at authorisation.",
            "title": "Iteration",
            "type": "integer"
          },
          "results": {
            "description": "The result vector, in the task's own order.",
            "items": {
              "$ref": "#/components/schemas/CheckOutcome"
            },
            "title": "Results",
            "type": "array"
          },
          "ts": {
            "description": "When the pass was recorded.",
            "format": "date-time",
            "title": "Ts",
            "type": "string"
          },
          "unchecked": {
            "description": "Criteria this pass did not decide.",
            "items": {
              "type": "string"
            },
            "title": "Unchecked",
            "type": "array"
          }
        },
        "required": [
          "iteration",
          "entry_id",
          "ts"
        ],
        "title": "ChainIteration",
        "type": "object"
      },
      "ChainIteration-Output": {
        "description": "One turn of a chain, as the task page reads it back.",
        "properties": {
          "entry_id": {
            "description": "The `check_result` entry this turn wrote.",
            "title": "Entry Id",
            "type": "integer"
          },
          "iteration": {
            "description": "0 is the baseline recorded at authorisation.",
            "title": "Iteration",
            "type": "integer"
          },
          "results": {
            "description": "The result vector, in the task's own order.",
            "items": {
              "$ref": "#/components/schemas/CheckOutcome"
            },
            "title": "Results",
            "type": "array"
          },
          "ts": {
            "description": "When the pass was recorded.",
            "format": "date-time",
            "title": "Ts",
            "type": "string"
          },
          "unchecked": {
            "description": "Criteria this pass did not decide.",
            "items": {
              "type": "string"
            },
            "title": "Unchecked",
            "type": "array"
          }
        },
        "required": [
          "iteration",
          "entry_id",
          "ts"
        ],
        "title": "ChainIteration",
        "type": "object"
      },
      "ChainList": {
        "description": "Every chain a task has ever had, oldest first.",
        "properties": {
          "chains": {
            "items": {
              "$ref": "#/components/schemas/ChainRead"
            },
            "title": "Chains",
            "type": "array"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          }
        },
        "required": [
          "task_id"
        ],
        "title": "ChainList",
        "type": "object"
      },
      "ChainRead": {
        "description": "One chain and its whole history, for the panel on the task page (task-150).\n\nDerived from the task's log rather than stored anywhere: the ``chain_authorized``\nentry, its revocation if it has one, and every ``check_result`` carrying its id. A\nsecond copy of any of that could disagree with the entries it was derived from, and\nthe entries are what a person auditing a loop next week will read.",
        "properties": {
          "authorized_at": {
            "format": "date-time",
            "title": "Authorized At",
            "type": "string"
          },
          "authorized_by": {
            "description": "Whose act this was.",
            "title": "Authorized By",
            "type": "string"
          },
          "chain_id": {
            "title": "Chain Id",
            "type": "string"
          },
          "check_digest": {
            "title": "Check Digest",
            "type": "string"
          },
          "criteria": {
            "items": {
              "type": "string"
            },
            "title": "Criteria",
            "type": "array"
          },
          "deadline": {
            "description": "When the wall-clock bound expires.",
            "format": "date-time",
            "title": "Deadline",
            "type": "string"
          },
          "digest_matches": {
            "description": "Whether the task's checks are still the ones this authorised.",
            "title": "Digest Matches",
            "type": "boolean"
          },
          "entry_id": {
            "description": "The `chain_authorized` entry.",
            "title": "Entry Id",
            "type": "integer"
          },
          "expired": {
            "description": "Whether the wall-clock bound has passed.",
            "title": "Expired",
            "type": "boolean"
          },
          "iterations": {
            "items": {
              "$ref": "#/components/schemas/ChainIteration-Output"
            },
            "title": "Iterations",
            "type": "array"
          },
          "live": {
            "description": "Whether an iteration could still start: authorised, not revoked, not expired, and the task's checks still match the digest. This is what a Revoke button is offered on.",
            "title": "Live",
            "type": "boolean"
          },
          "max_iterations": {
            "title": "Max Iterations",
            "type": "integer"
          },
          "revoked": {
            "description": "Whether the authorisation has been withdrawn.",
            "title": "Revoked",
            "type": "boolean"
          },
          "revoked_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revoked At"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "wall_clock_seconds": {
            "title": "Wall Clock Seconds",
            "type": "integer"
          }
        },
        "required": [
          "chain_id",
          "task_id",
          "entry_id",
          "authorized_by",
          "authorized_at",
          "max_iterations",
          "wall_clock_seconds",
          "deadline",
          "check_digest",
          "revoked",
          "expired",
          "live",
          "digest_matches"
        ],
        "title": "ChainRead",
        "type": "object"
      },
      "ChainRevokeRequest": {
        "description": "Stop a chain. Everything on it is optional, which is the point (task-150).\n\nSection 9 asks for a kill switch as blunt as ``agentjobs dispatch stop``. A revoke\nthat required a chain id copied off a log entry would not be one, so an empty body\nstops whatever is live.",
        "properties": {
          "chain_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Which chain, when it is not simply the live one.",
            "title": "Chain Id"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why, for the record.",
            "title": "Note"
          },
          "user": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who is stopping it.",
            "title": "User"
          }
        },
        "title": "ChainRevokeRequest",
        "type": "object"
      },
      "CheckOutcome": {
        "additionalProperties": false,
        "description": "What one acceptance criterion's ``check`` did on one pass (task-147).\n\n``status`` is only ever ``met`` or ``failed``: there is no third value, because a\ncheck that could not be started is ``failed`` and not skipped. That is the whole\nsecurity argument for the loop this feeds -- if a broken check were \"unknown\" and\nunknown were tolerated, a loop could converge by breaking its own tests.",
        "properties": {
          "cause": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why it failed, when the exit code does not say: `timeout`, `not_started`, or `pass_timeout` for a check the pass budget cut short.",
            "title": "Cause"
          },
          "duration_seconds": {
            "description": "Wall-clock time this check took, including a timeout.",
            "minimum": 0.0,
            "title": "Duration Seconds",
            "type": "number"
          },
          "exit_code": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The process's exit code, or absent when there was no process -- a timeout killed it, or it could not be started at all.",
            "title": "Exit Code"
          },
          "id": {
            "description": "The acceptance criterion this is the result for.",
            "title": "Id",
            "type": "string"
          },
          "output_tail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Last lines of the check's combined output. Absent when it printed nothing.",
            "title": "Output Tail"
          },
          "status": {
            "$ref": "#/components/schemas/AcceptanceStatus",
            "description": "`met` on exit 0, `failed` on anything else."
          }
        },
        "required": [
          "id",
          "status",
          "duration_seconds"
        ],
        "title": "CheckOutcome",
        "type": "object"
      },
      "CheckRunResult": {
        "description": "What one evaluation pass over a task's acceptance checks did (task-147).\n\nThe same vector ``agentjobs check`` prints, so the two triggers cannot come to\ndescribe a pass differently. ``entry_id`` names the one ``check_result`` entry the\npass wrote, which is how a client reads the evidence back without guessing which\nentry it was.",
        "properties": {
          "entry_id": {
            "description": "Id of the `check_result` entry this pass wrote.",
            "title": "Entry Id",
            "type": "integer"
          },
          "ok": {
            "description": "True when every check exited 0.",
            "title": "Ok",
            "type": "boolean"
          },
          "results": {
            "description": "One outcome per criterion that has a check, in the task's order.",
            "items": {
              "$ref": "#/components/schemas/CheckOutcome"
            },
            "title": "Results",
            "type": "array"
          },
          "task_id": {
            "description": "The task whose checks were run.",
            "title": "Task Id",
            "type": "string"
          },
          "unchecked": {
            "description": "Criteria with no check, which this pass did not decide.",
            "items": {
              "type": "string"
            },
            "title": "Unchecked",
            "type": "array"
          }
        },
        "required": [
          "task_id",
          "results",
          "ok",
          "entry_id"
        ],
        "title": "CheckRunResult",
        "type": "object"
      },
      "ClaimRequest": {
        "additionalProperties": false,
        "description": "An agent takes ownership of a ready task.",
        "properties": {
          "agent": {
            "description": "Actor id claiming the task.",
            "title": "Agent",
            "type": "string"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "session_cwd": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The directory that session runs in. Where the driver keeps its transcript store, so the task page can show the session's log. Defaults to the project root.",
            "title": "Session Cwd"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The claiming session's own id, when the claim comes from an agent session AgentJobs did not start (task-354). With it, the claim writes an interactive run record so the work shows as running; without it, a claim is exactly what it was. Claude Code's MCP server and the CLI fill it from CLAUDE_CODE_SESSION_ID.",
            "title": "Session Id"
          }
        },
        "required": [
          "agent"
        ],
        "title": "ClaimRequest",
        "type": "object"
      },
      "CloseRequest": {
        "additionalProperties": false,
        "description": "End the task with an outcome.",
        "properties": {
          "actor": {
            "description": "Who is closing the task.",
            "title": "Actor",
            "type": "string"
          },
          "archive": {
            "default": false,
            "description": "Also hide the task from listings.",
            "title": "Archive",
            "type": "boolean"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional log entry body.",
            "title": "Body"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "outcome": {
            "$ref": "#/components/schemas/Outcome",
            "description": "How it ended."
          }
        },
        "required": [
          "actor",
          "outcome"
        ],
        "title": "CloseRequest",
        "type": "object"
      },
      "ClosureView": {
        "description": "One finished task, in the fields a row on the Dashboard renders.",
        "properties": {
          "age_seconds": {
            "description": "Seconds since it closed, computed on the server. The phone reading this page is not on the clock that wrote the stamp, and *how long ago* is what the row is actually read for.",
            "title": "Age Seconds",
            "type": "number"
          },
          "closed_at": {
            "description": "When the task closed, in UTC. This is the store's `closed_at`, stamped at the close and untouched by later edits -- not `updated`, which an edit after closing would move.",
            "title": "Closed At",
            "type": "string"
          },
          "display_status": {
            "description": "The chip's word, from the function every task read draws its label from (`models_v2.closed_status`), so this row says exactly what the task list says about the same task.",
            "title": "Display Status",
            "type": "string"
          },
          "outcome": {
            "description": "How it ended: `completed`, `cancelled`, `superseded` or `duplicate`. Shown as written -- a region that printed every row as *done* would be hiding the difference the reader is scanning for.",
            "title": "Outcome",
            "type": "string"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "project_name": {
            "default": "",
            "description": "The project's display name, falling back to its id.",
            "title": "Project Name",
            "type": "string"
          },
          "status_category": {
            "$ref": "#/components/schemas/StatusCategory",
            "description": "The chip's colour: `closed`, or `closed_unfinished` for an outcome that was not a finish -- drawn struck through. Same derivation as the label."
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "task_title": {
            "title": "Task Title",
            "type": "string"
          },
          "task_url": {
            "description": "Where this task is, in this app.",
            "title": "Task Url",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "task_title",
          "project_id",
          "outcome",
          "closed_at",
          "age_seconds",
          "display_status",
          "status_category",
          "task_url"
        ],
        "title": "ClosureView",
        "type": "object"
      },
      "ContextPointer": {
        "additionalProperties": false,
        "description": "A curated \"read this first\" pointer, with the reason it matters.",
        "properties": {
          "path": {
            "description": "Repository-relative path.",
            "title": "Path",
            "type": "string"
          },
          "why": {
            "description": "Why a reader should open this first.",
            "title": "Why",
            "type": "string"
          }
        },
        "required": [
          "path",
          "why"
        ],
        "title": "ContextPointer",
        "type": "object"
      },
      "CostPerTaskPoint": {
        "description": "What a completed task cost the machine: runs, finishes and gate minutes (S4, F5, G4).\n\nBucketed by the task's close. ``without_gate`` counts the tasks with no full gate\nat all -- hand closes and recorded decisions -- so a low median is not read as a\nfast gate.",
        "properties": {
          "bucket": {
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "estimated": {
            "default": false,
            "title": "Estimated",
            "type": "boolean"
          },
          "finishes_mean": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finishes Mean"
          },
          "gate_minutes_p50": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gate Minutes P50"
          },
          "gate_minutes_p90": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gate Minutes P90"
          },
          "runs_mean": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Runs Mean"
          },
          "runs_mode": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Runs Mode"
          },
          "sample": {
            "description": "Completed tasks closed in this bucket.",
            "title": "Sample",
            "type": "integer"
          },
          "without_gate": {
            "default": 0,
            "title": "Without Gate",
            "type": "integer"
          }
        },
        "required": [
          "bucket",
          "sample"
        ],
        "title": "CostPerTaskPoint",
        "type": "object"
      },
      "DashboardRecentUpdate": {
        "description": "A compact task-log record for the recent activity list.",
        "properties": {
          "author": {
            "title": "Author",
            "type": "string"
          },
          "summary": {
            "title": "Summary",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "task_title": {
            "title": "Task Title",
            "type": "string"
          },
          "timestamp": {
            "format": "date-time",
            "title": "Timestamp",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "task_title",
          "timestamp",
          "summary",
          "author"
        ],
        "title": "DashboardRecentUpdate",
        "type": "object"
      },
      "DashboardResponse": {
        "description": "The complete Python-computed dashboard contract.\n\nEvery task list here is a :class:`TaskCardRead`, not a whole record. It answered\nwith whole ``TaskRead`` records until task-495 -- spec prose, acceptance criteria and\nthe complete log of every task on the page -- which measured 10,888 bytes per record\nand 5.2 MB in total against a generated corpus of 480, to draw cards showing a title,\na summary line, a priority chip and a dependency badge.",
        "properties": {
          "backlog_tasks": {
            "items": {
              "$ref": "#/components/schemas/TaskCardRead-Output"
            },
            "title": "Backlog Tasks",
            "type": "array"
          },
          "broken_files": {
            "items": {
              "$ref": "#/components/schemas/BrokenTaskFile"
            },
            "title": "Broken Files",
            "type": "array"
          },
          "identity": {
            "$ref": "#/components/schemas/ReviewIdentity"
          },
          "next_action": {
            "enum": [
              "blocked",
              "backlog",
              "queue_broken",
              "next_up",
              "nothing_claimable",
              "empty_project"
            ],
            "title": "Next Action",
            "type": "string"
          },
          "next_task": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskCardRead-Output"
              },
              {
                "type": "null"
              }
            ]
          },
          "queue_broken": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueueBrokenRead"
              },
              {
                "type": "null"
              }
            ]
          },
          "queue_preview": {
            "description": "The head of the claimable queue, in the queue's own order. `next_task` is its first element. Sent as a list because the slot board offers a way to *start* each of them, one per free run slot -- so it is at least `QUEUE_PREVIEW_LIMIT` long and grows with this machine's `max_concurrent_runs`, which is what decides how many cells the board has.",
            "items": {
              "$ref": "#/components/schemas/TaskCardRead-Output"
            },
            "title": "Queue Preview",
            "type": "array"
          },
          "recent_updates": {
            "items": {
              "$ref": "#/components/schemas/DashboardRecentUpdate"
            },
            "title": "Recent Updates",
            "type": "array"
          },
          "stalled": {
            "description": "Which of `waiting_tasks` are there because nobody is working them. The same list `/attention` carries, from the same derivation, so the panel and the badge above it cannot disagree.",
            "items": {
              "$ref": "#/components/schemas/StalledTaskRead"
            },
            "title": "Stalled",
            "type": "array"
          },
          "stats": {
            "$ref": "#/components/schemas/DashboardStats"
          },
          "waiting_tasks": {
            "items": {
              "$ref": "#/components/schemas/TaskCardRead-Output"
            },
            "title": "Waiting Tasks",
            "type": "array"
          }
        },
        "required": [
          "stats",
          "recent_updates",
          "waiting_tasks",
          "backlog_tasks",
          "next_task",
          "next_action",
          "broken_files",
          "identity"
        ],
        "title": "DashboardResponse",
        "type": "object"
      },
      "DashboardStats": {
        "description": "Counts rendered by the dashboard stat tiles.",
        "properties": {
          "awaiting_input": {
            "title": "Awaiting Input",
            "type": "integer"
          },
          "blocked": {
            "title": "Blocked",
            "type": "integer"
          },
          "completed": {
            "title": "Completed",
            "type": "integer"
          },
          "in_progress": {
            "title": "In Progress",
            "type": "integer"
          },
          "total": {
            "title": "Total",
            "type": "integer"
          },
          "waiting_for_human": {
            "title": "Waiting For Human",
            "type": "integer"
          }
        },
        "required": [
          "total",
          "in_progress",
          "blocked",
          "waiting_for_human",
          "awaiting_input",
          "completed"
        ],
        "title": "DashboardStats",
        "type": "object"
      },
      "Deliverable": {
        "additionalProperties": false,
        "description": "An artifact this task produces.",
        "properties": {
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What it is.",
            "title": "Note"
          },
          "path": {
            "description": "Repository-relative path.",
            "title": "Path",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/DeliverableStatus",
            "default": "pending"
          }
        },
        "required": [
          "path"
        ],
        "title": "Deliverable",
        "type": "object"
      },
      "DeliverableDocument": {
        "description": "One Markdown deliverable, read at the head of the task's active branch (task-594).",
        "properties": {
          "branch": {
            "description": "The active branch it was read from.",
            "title": "Branch",
            "type": "string"
          },
          "commit": {
            "description": "Short id of the commit the branch pointed at.",
            "title": "Commit",
            "type": "string"
          },
          "index": {
            "description": "Position in the task's deliverables[].",
            "title": "Index",
            "type": "integer"
          },
          "path": {
            "description": "Repository-relative path, as the record lists it.",
            "title": "Path",
            "type": "string"
          },
          "size": {
            "description": "Size of the file in bytes.",
            "title": "Size",
            "type": "integer"
          },
          "text": {
            "description": "The file's text, unrendered.",
            "title": "Text",
            "type": "string"
          }
        },
        "required": [
          "index",
          "path",
          "branch",
          "commit",
          "size",
          "text"
        ],
        "title": "DeliverableDocument",
        "type": "object"
      },
      "DeliverableStatus": {
        "description": "State of one deliverable.",
        "enum": [
          "pending",
          "done",
          "dropped"
        ],
        "title": "DeliverableStatus",
        "type": "string"
      },
      "Dependency": {
        "additionalProperties": false,
        "description": "A typed relationship to another task.",
        "properties": {
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why the relationship exists.",
            "title": "Note"
          },
          "task": {
            "description": "Referenced task id.",
            "title": "Task",
            "type": "string"
          },
          "type": {
            "$ref": "#/components/schemas/DependencyType",
            "default": "needs"
          }
        },
        "required": [
          "task"
        ],
        "title": "Dependency",
        "type": "object"
      },
      "DependencyRelation": {
        "description": "Resolved dependency relation shown on a task detail page.",
        "properties": {
          "exists": {
            "title": "Exists",
            "type": "boolean"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "The other task's kind, so a detail page can say what a design task is implemented by without fetching every task. Null when unset or missing; absent means implementation (task-593)."
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Note"
          },
          "reason": {
            "title": "Reason",
            "type": "string"
          },
          "state": {
            "enum": [
              "open",
              "done",
              "missing"
            ],
            "title": "State",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          }
        },
        "required": [
          "task_id",
          "title",
          "exists",
          "state",
          "note",
          "reason"
        ],
        "title": "DependencyRelation",
        "type": "object"
      },
      "DependencyType": {
        "description": "Relationship to another task. v1's `depends_on` is renamed `needs`.",
        "enum": [
          "needs",
          "blocks",
          "related"
        ],
        "title": "DependencyType",
        "type": "string"
      },
      "DispatchCancelResult": {
        "description": "What cancelling asked for, and whether it happened.\n\nOne result for two things that can be cancelled at that id, because they are two\nstates of one act: a dispatch that is waiting for a slot (task-459) and the run it\nbecomes. ``run`` is null for the first and ``queued`` for the second, so a caller\nthat stops reading here still cannot mistake one for the other.",
        "properties": {
          "detail": {
            "title": "Detail",
            "type": "string"
          },
          "queued": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueuedDispatchView"
              },
              {
                "type": "null"
              }
            ],
            "description": "The removed queue entry, with its final status. Null when a run was cancelled."
          },
          "run": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchRunView"
              },
              {
                "type": "null"
              }
            ],
            "description": "The cancelled run. Null when a queued entry was removed."
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "stopped": {
            "title": "Stopped",
            "type": "boolean"
          }
        },
        "required": [
          "run_id",
          "stopped",
          "detail"
        ],
        "title": "DispatchCancelResult",
        "type": "object"
      },
      "DispatchEnableRequest": {
        "description": "Point a project at a runner this machine already defines, and turn it on.\n\n``runner`` names an existing runner; it never creates one. Omitted, the project\nkeeps the runner it already names, or takes the only one defined -- the same rule\n``agentjobs dispatch enable`` follows, so the two surfaces cannot disagree.",
        "properties": {
          "group": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner group to point this project at, instead of a single runner. Names an existing group; it never creates one. Mutually exclusive with `runner`.",
            "title": "Group"
          },
          "runner": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Runner"
          }
        },
        "title": "DispatchEnableRequest",
        "type": "object"
      },
      "DispatchPosture": {
        "description": "What the run was permitted to do (design doc section 4, task-076).\n\nMirrors ``dispatch.config.Posture``, and must keep mirroring it: this is the value\nwritten into the task's dispatch log entry, so a posture missing here cannot be\nrecorded even though a run was started under it. ``auto`` was added by task-020.",
        "enum": [
          "read_only",
          "auto",
          "supervised",
          "autonomous"
        ],
        "title": "DispatchPosture",
        "type": "string"
      },
      "DispatchRefusalView": {
        "description": "The gate that currently refuses this project, in the API's own vocabulary.\n\nCarried on the state rather than raised as an error, because \"you cannot dispatch,\nhere is exactly why\" is the normal answer for a project nobody has enabled -- not a\nfailure. The GUI renders ``reason``-specific copy from it; ``message`` is the\nlibrary's own sentence and is always safe to show.",
        "properties": {
          "message": {
            "description": "The refusal, in words.",
            "title": "Message",
            "type": "string"
          },
          "reason": {
            "description": "Stable machine-readable code, e.g. 'disabled'.",
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason",
          "message"
        ],
        "title": "DispatchRefusalView",
        "type": "object"
      },
      "DispatchRequestBody": {
        "description": "Ask AgentJobs to start an agent on this task.\n\nThere is still no ``actor`` field, and that absence is still the design. The actor\nrecorded on a dispatch is the author of the log entry that *caused* it, never\nwhoever posted the request.\n\n``user`` is not that field, and the distinction is the whole of task-188. It does\nnot name the cause of the dispatch; it names the person whose authorising entry the\nserver should **write** before dispatching. The entry is persisted, then re-read\nfrom storage, then put through the human-clocked check like any other -- so the\nevidence remains a row in the append-only log, and a request that tried to supply\nits own justification still gets nowhere. The identity claim itself is validated\nagainst the project's configured actors and refused unless it is ``kind: human``,\nexactly as ``POST /log`` and ``POST /approve`` have always validated theirs.\n\nOmit ``user`` and nothing changes: the causing entry is whatever the log already\nholds, which is what the CLI, MCP and auto-dispatch do.",
        "properties": {
          "caused_by": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Log entry id authorising this dispatch. Defaults to the newest entry. Its actor must be a configured human.",
            "title": "Caused By"
          },
          "group": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner group to choose from, overriding the project's. Names a group this machine already defines; it never creates one, and it cannot open a gate that is closed.",
            "title": "Group"
          },
          "if_full": {
            "default": "refuse",
            "description": "What to do when every machine slot is taken. 'refuse' is the historical behaviour and the default, so a caller that predates task-459 is unchanged: 409 'concurrency_limit'. 'queue' accepts the dispatch into the machine's dispatch queue instead -- 202 with 'queued': true and a 'queue_id' -- and the server starts it when a slot frees, with every dispatch gate judged at that moment rather than this one.",
            "enum": [
              "refuse",
              "queue"
            ],
            "title": "If Full",
            "type": "string"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What the human typed, when the record could not brief an agent on its own. Becomes the body of the authorising entry. Only meaningful alongside 'user'.",
            "title": "Note"
          },
          "over_ceiling": {
            "default": false,
            "description": "Start this run although every machine slot is taken (task-461). A deliberate overage of limits.max_concurrent_runs for this one dispatch, and nothing else: every other gate binds as it always did, including the hourly cap, because an overage is a dispatch. Honoured only for a human principal -- a run credential sending it is refused 403 'capability_denied' under 'dispatch.over_ceiling'. Mutually exclusive with if_full=queue: asking to wait for a slot and asking to start without one are opposite answers to the same question.",
            "title": "Over Ceiling",
            "type": "boolean"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "What this one run may do, overriding both the project default and any posture on the task record. Refused with 'posture_above_ceiling' when it exceeds the project's machine-local max_posture -- populate a chooser from the dispatch state view's 'offerable_postures' so the refusal is never reachable by clicking (task-308)."
          },
          "runner": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Specific machine-local runner for this run, overriding group and project defaults. Mutually exclusive with group.",
            "title": "Runner"
          },
          "user": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The signed-in human clicking Dispatch. Their authorising entry is written to the task before the run starts, and the dispatch is attributed to it. Must be an actor this project configures with 'kind: human'. Mutually exclusive with caused_by.",
            "title": "User"
          }
        },
        "title": "DispatchRequestBody",
        "type": "object"
      },
      "DispatchRunTailView": {
        "description": "The end of a run's output, for a page that is watching it happen.\n\nBounded on purpose. A session transcript grows for as long as the session does, and a\npane on the task page that renders all of it turns the page a reader came to for the\ntask record into a terminal emulator with a task record somewhere above it.",
        "properties": {
          "lines": {
            "description": "How many lines this tail is bounded to.",
            "title": "Lines",
            "type": "integer"
          },
          "live": {
            "description": "Nothing has declared this run over.",
            "title": "Live",
            "type": "boolean"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "source": {
            "description": "Where the text came from: 'session-transcript' (the session's own output), 'captured-output' (what the process wrote to stdout/stderr), or 'none'.",
            "title": "Source",
            "type": "string"
          },
          "text": {
            "description": "The tail itself, escape sequences already removed.",
            "title": "Text",
            "type": "string"
          },
          "updated_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the file behind this text last changed.",
            "title": "Updated At"
          }
        },
        "required": [
          "run_id",
          "live",
          "source",
          "lines",
          "text"
        ],
        "title": "DispatchRunTailView",
        "type": "object"
      },
      "DispatchRunTranscriptView": {
        "description": "A run's transcript as structured entries, for a panel that renders rather than dumps.\n\nDistinct from ``DispatchRunTailView`` and not a replacement for it. That one serves\nthe terminal capture, which stays the only evidence when a session dies in a way no\nrenderer models; this one serves what the session recorded about itself.",
        "properties": {
          "entries": {
            "items": {
              "$ref": "#/components/schemas/TranscriptEntryView"
            },
            "title": "Entries",
            "type": "array"
          },
          "live": {
            "description": "Nothing has declared this run over.",
            "title": "Live",
            "type": "boolean"
          },
          "note": {
            "default": "",
            "description": "Why there is nothing structured, in a sentence.",
            "title": "Note",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "source": {
            "description": "'session-jsonl' when the session's own structured transcript was read, 'none' when there was nothing to read -- in which case 'note' says why and the caller should fall back to the tail.",
            "title": "Source",
            "type": "string"
          },
          "total_entries": {
            "default": 0,
            "description": "How many entries the transcript holds.",
            "title": "Total Entries",
            "type": "integer"
          },
          "truncated": {
            "default": false,
            "description": "Earlier entries exist and were not returned.",
            "title": "Truncated",
            "type": "boolean"
          },
          "updated_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the file behind these entries last changed.",
            "title": "Updated At"
          }
        },
        "required": [
          "run_id",
          "live",
          "source"
        ],
        "title": "DispatchRunTranscriptView",
        "type": "object"
      },
      "DispatchRunView": {
        "description": "One run, as the browser sees it.",
        "properties": {
          "caused_by": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Log entry id this run was attributed to.",
            "title": "Caused By"
          },
          "elapsed_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Seconds since start for a live run; the total it ran for once it ended, measured to its finish time so it stops moving. Null when a concluded run has no recorded finish time -- render that as unknown, never as a number.",
            "title": "Elapsed Seconds"
          },
          "live": {
            "description": "Nothing has declared this run over.",
            "title": "Live",
            "type": "boolean"
          },
          "mode": {
            "title": "Mode",
            "type": "string"
          },
          "outcome": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Outcome"
          },
          "output_url": {
            "description": "Where this run's captured output is readable.",
            "title": "Output Url",
            "type": "string"
          },
          "posture": {
            "title": "Posture",
            "type": "string"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Session Id"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "task_id",
          "project_id",
          "mode",
          "posture",
          "status",
          "live",
          "output_url"
        ],
        "title": "DispatchRunView",
        "type": "object"
      },
      "DispatchStarted": {
        "description": "What a successful dispatch reports back -- or, since task-459, a queued one.\n\n**Read ``queued`` before anything else.** Both answers are 202, because both mean\n\"accepted, and how it ends arrives later on the task\". They are not the same event,\nand a client that renders \"Dispatched\" over a queued one has told somebody a run is\nworking when nothing has started.",
        "properties": {
          "caused_by": {
            "description": "The log entry this dispatch is attributed to. 0 while a dispatch is queued -- the authorising entry is written when it starts, exactly as for a click.",
            "title": "Caused By",
            "type": "integer"
          },
          "group": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner group it was selected from, when one participated.",
            "title": "Group"
          },
          "mode": {
            "description": "session or batch. Empty while a dispatch is queued.",
            "title": "Mode",
            "type": "string"
          },
          "over_ceiling": {
            "default": false,
            "description": "True when this run was started above limits.max_concurrent_runs because a person chose to (task-461). The machine is over its ceiling until it ends, and the slot board says so rather than clipping the count.",
            "title": "Over Ceiling",
            "type": "boolean"
          },
          "posture": {
            "description": "What the run is permitted to do. Empty while a dispatch is queued: the posture is resolved by the gates when it starts, not when it was queued.",
            "title": "Posture",
            "type": "string"
          },
          "queue_position": {
            "default": 0,
            "description": "1-based place in line while queued; 0 for a dispatch that started.",
            "title": "Queue Position",
            "type": "integer"
          },
          "queued": {
            "default": false,
            "description": "True when the machine was full and the caller sent if_full=queue: nothing has started, and the entry waits in the machine's dispatch queue.",
            "title": "Queued",
            "type": "boolean"
          },
          "queued_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the entry joined the queue, UTC.",
            "title": "Queued At"
          },
          "run_id": {
            "description": "AgentJobs' identifier for this run. On a queued dispatch this is the queue entry's id, which is what the cancel route takes while it waits.",
            "title": "Run Id",
            "type": "string"
          },
          "runner": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner that was selected and started.",
            "title": "Runner"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Session mode only, and assigned by the CLI rather than by us.",
            "title": "Session Id"
          },
          "task_id": {
            "description": "The task the run is working.",
            "title": "Task Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "mode",
          "posture",
          "task_id",
          "caused_by"
        ],
        "title": "DispatchStarted",
        "type": "object"
      },
      "DispatchStateView": {
        "description": "Everything the GUI needs to decide what to offer, and what to explain.",
        "properties": {
          "auto_dispatch": {
            "default": false,
            "description": "Auto-dispatch on approval (task-074).",
            "title": "Auto Dispatch",
            "type": "boolean"
          },
          "available_groups": {
            "description": "Runner group names this machine defines. Read-only on the same terms as available_runners: pointing a project at an existing group is selecting among machine-local definitions, and authoring one is not reachable here.",
            "items": {
              "type": "string"
            },
            "title": "Available Groups",
            "type": "array"
          },
          "available_runners": {
            "description": "Runner names this machine defines. Read-only: the browser may point a project at one of these and can never create one.",
            "items": {
              "type": "string"
            },
            "title": "Available Runners",
            "type": "array"
          },
          "can_dispatch": {
            "description": "Every gate is open right now.",
            "title": "Can Dispatch",
            "type": "boolean"
          },
          "config_path": {
            "description": "Where a human edits any of this.",
            "title": "Config Path",
            "type": "string"
          },
          "configured": {
            "description": "A dispatch.yaml exists on this machine.",
            "title": "Configured",
            "type": "boolean"
          },
          "default_group": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Machine-wide group, used by any project that names none of its own.",
            "title": "Default Group"
          },
          "finish_enabled": {
            "default": false,
            "description": "Whether the scripted finish (task-241) is on for this project, which is what an autonomous merge runs through. task-021 accepted the consequence that without it there is no sanctioned mechanism for one -- so a chooser offers `autonomous` disabled here rather than granting an envelope whose merge cannot be performed.",
            "title": "Finish Enabled",
            "type": "boolean"
          },
          "group": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner group this project is pointed at, if any.",
            "title": "Group"
          },
          "machine_ceiling": {
            "default": 0,
            "description": "`limits.max_concurrent_runs` from ~/.agentjobs/dispatch.yaml.",
            "title": "Machine Ceiling",
            "type": "integer"
          },
          "machine_full": {
            "default": false,
            "description": "Every slot is taken, so a dispatch would be refused `concurrency_limit` (task-461). Beside `can_dispatch` rather than folded into it, and that is the whole point: the four configuration gates are reasons the button should not be offered, while a full machine is a question to ask the person pressing it -- wait for a slot, take one anyway, or think better of it. Withholding the button would answer it for them.",
            "title": "Machine Full",
            "type": "boolean"
          },
          "machine_occupied": {
            "default": 0,
            "description": "Run slots in use on this machine right now, counted exactly as the concurrency guard counts them (task-461). Machine-wide rather than this project's: the ceiling is the machine's.",
            "title": "Machine Occupied",
            "type": "integer"
          },
          "master_enabled": {
            "description": "The machine-wide 'enabled:' switch.",
            "title": "Master Enabled",
            "type": "boolean"
          },
          "max_posture": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The widest posture any run here may get, whatever asks for it (task-308). Equal to `posture` on a project that has not set a ceiling of its own, because that is the only default that cannot silently widen an existing machine.",
            "title": "Max Posture"
          },
          "offerable_postures": {
            "description": "Every posture at or below `max_posture`, narrowest first. Sent rather than derived, for the same reason `resolved_from` is: a browser that re-implements the ceiling is the one place in the system that could offer a choice the dispatch API will refuse.",
            "items": {
              "type": "string"
            },
            "title": "Offerable Postures",
            "type": "array"
          },
          "posture": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What a run here gets when nothing else names a posture.",
            "title": "Posture"
          },
          "posture_merge_policies": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "What each posture does to the *branch*, keyed by posture value (task-021: `read_only` -> none, `auto`/`supervised` -> review, `autonomous` -> automatic). Sent rather than hardcoded in the client for the same reason `offerable_postures` is, and the stake is higher: this is the difference between 'stops for your review' and 'merges without you', so a browser that carried its own copy could tell an operator the opposite of what the posture they picked will actually do.",
            "title": "Posture Merge Policies",
            "type": "object"
          },
          "project_enabled": {
            "description": "This project is enabled for dispatch.",
            "title": "Project Enabled",
            "type": "boolean"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "pull": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PullModeView"
              },
              {
                "type": "null"
              }
            ],
            "description": "The pull mode's state for this project (task-462). Null only on a machine whose execution store could not be read at all; an unarmed project sends `armed: false` rather than nothing, so a control can tell 'off' from 'unknown'."
          },
          "push": {
            "default": false,
            "description": "Whether this project permits pushing. Per project and never a posture property (task-021), and false everywhere today. Surfaced because 'this project will merge my work without asking me, and publish it' is the one thing worth knowing beside a Dispatch button.",
            "title": "Push",
            "type": "boolean"
          },
          "refusal": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchRefusalView"
              },
              {
                "type": "null"
              }
            ],
            "description": "Which gate refuses, when can_dispatch is false."
          },
          "resolved_from": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Which rung of the precedence ladder decided: 'project' for the project's own group, 'machine' for default_group, 'project_runner' for a plain runner. Sent rather than derived, because a browser that re-implements the ladder is the one place in the system that would disagree with the dispatcher about what runs.",
            "title": "Resolved From"
          },
          "resolved_group": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The group that chose `resolved_runner`, when one participated. Null for a flat config, so a machine with no groups reads exactly as it did before they existed.",
            "title": "Resolved Group"
          },
          "resolved_runner": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The runner that would actually start right now. Null when a gate refuses, because there is then no answer rather than a stale one.",
            "title": "Resolved Runner"
          },
          "runner": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner this project is pointed at.",
            "title": "Runner"
          },
          "runner_labels": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Human-facing model names for available_runners, keyed by the stable machine-local runner id. The browser submits the id and displays the label, so presentation never changes dispatch authority.",
            "title": "Runner Labels",
            "type": "object"
          },
          "sentinel_active": {
            "description": "DISPATCH_DISABLED exists; all runs refused.",
            "title": "Sentinel Active",
            "type": "boolean"
          },
          "sentinel_file": {
            "description": "Path of the kill-switch sentinel.",
            "title": "Sentinel File",
            "type": "string"
          },
          "slot_holders": {
            "default": "",
            "description": "The runs holding the slots, in the same sentence the `concurrency_limit` refusal uses, so the prompt before the click and the refusal after one name the same runs in the same words. Empty when the machine is not full.",
            "title": "Slot Holders",
            "type": "string"
          }
        },
        "required": [
          "project_id",
          "configured",
          "master_enabled",
          "sentinel_active",
          "project_enabled",
          "can_dispatch",
          "config_path",
          "sentinel_file"
        ],
        "title": "DispatchStateView",
        "type": "object"
      },
      "EmergencyStopItem": {
        "description": "One thing the stop acted on.",
        "properties": {
          "detail": {
            "title": "Detail",
            "type": "string"
          },
          "id": {
            "description": "The run, walk, pull arming or queue entry id.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "description": "`run`, `walk`, `pull` or `queue`.",
            "title": "Kind",
            "type": "string"
          },
          "stopped": {
            "description": "False when it could not be confirmed stopped.",
            "title": "Stopped",
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "kind",
          "stopped",
          "detail"
        ],
        "title": "EmergencyStopItem",
        "type": "object"
      },
      "EmergencyStopResult": {
        "description": "The state after the stop, and everything it acted on.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/EmergencyStopItem"
            },
            "title": "Items",
            "type": "array"
          },
          "note": {
            "default": "",
            "description": "The sentinel's first line: who stopped dispatch, from where, and when.",
            "title": "Note",
            "type": "string"
          },
          "sentinel_file": {
            "description": "Where the sentinel lives on this machine.",
            "title": "Sentinel File",
            "type": "string"
          },
          "stopped": {
            "description": "`~/.agentjobs/DISPATCH_DISABLED` exists, so every dispatch is refused.",
            "title": "Stopped",
            "type": "boolean"
          }
        },
        "required": [
          "stopped",
          "sentinel_file"
        ],
        "title": "EmergencyStopResult",
        "type": "object"
      },
      "EmergencyStopState": {
        "description": "Whether the machine's kill switch is down, and what it says about who put it there.",
        "properties": {
          "note": {
            "default": "",
            "description": "The sentinel's first line: who stopped dispatch, from where, and when.",
            "title": "Note",
            "type": "string"
          },
          "sentinel_file": {
            "description": "Where the sentinel lives on this machine.",
            "title": "Sentinel File",
            "type": "string"
          },
          "stopped": {
            "description": "`~/.agentjobs/DISPATCH_DISABLED` exists, so every dispatch is refused.",
            "title": "Stopped",
            "type": "boolean"
          }
        },
        "required": [
          "stopped",
          "sentinel_file"
        ],
        "title": "EmergencyStopState",
        "type": "object"
      },
      "EpicWalkView": {
        "description": "One epic walk that is still open, as the board draws it (task-523).\n\nA walk is the only thing on this machine that dispatches work with no human act at\nthe moment of dispatch, and since task-458 it is hosted by the server rather than by\na blocking process -- so its own ``mode: \"walk\"`` run is normally over before any\nboard is drawn and there was nothing on the page to explain the children arriving.\nThis row is that explanation.\n\n**It holds no slot**, exactly as its run did not. Nothing here is counted against\n``occupied`` or takes a cell from the board; a walk is supervision, and the slots it\nfills are filled by the children's own runs, which are already in ``runs``.",
        "properties": {
          "children_completed": {
            "default": 0,
            "description": "Children closed with outcome `completed`.",
            "title": "Children Completed",
            "type": "integer"
          },
          "children_in_flight": {
            "default": 0,
            "description": "Children this walk has in the air now -- admitted or flying, and still open. Each one is a run of its own and appears in `runs` on its own terms.",
            "title": "Children In Flight",
            "type": "integer"
          },
          "children_remaining": {
            "default": 0,
            "description": "Open children that are not in flight: what is still to come.",
            "title": "Children Remaining",
            "type": "integer"
          },
          "children_total": {
            "default": 0,
            "description": "Every child of the parent, counted from the task graph rather than from the walk's own rows. The walk only has a row for a child it has already touched, so counting its rows would report a five-child epic that has flown two as having three children.",
            "title": "Children Total",
            "type": "integer"
          },
          "detail": {
            "default": "",
            "description": "What the walk last recorded about its stop.",
            "title": "Detail",
            "type": "string"
          },
          "grounded": {
            "description": "The walk has stopped taking off. **The field this section exists for**: a grounded walk and a quiet one look identical from the outside, and a reader who cannot tell them apart reads a stall as progress.",
            "title": "Grounded",
            "type": "boolean"
          },
          "grounded_reason": {
            "default": "",
            "description": "The `WalkStop` that grounded it, or empty while it is still flying.",
            "title": "Grounded Reason",
            "type": "string"
          },
          "grounded_word": {
            "default": "",
            "description": "How that reason reads in a sentence, composed on the server so the board and the CLI say the same words.",
            "title": "Grounded Word",
            "type": "string"
          },
          "in_flight_task_ids": {
            "description": "The children in the air, so a reader can follow them.",
            "items": {
              "type": "string"
            },
            "title": "In Flight Task Ids",
            "type": "array"
          },
          "parent_task_id": {
            "description": "The epic being walked.",
            "title": "Parent Task Id",
            "type": "string"
          },
          "parent_task_title": {
            "default": "",
            "description": "Resolved server-side, empty when the record cannot be read.",
            "title": "Parent Task Title",
            "type": "string"
          },
          "parent_task_url": {
            "default": "",
            "description": "Where that task is, in this app.",
            "title": "Parent Task Url",
            "type": "string"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "project_name": {
            "default": "",
            "description": "Falls back to the id for a project no longer registered.",
            "title": "Project Name",
            "type": "string"
          },
          "resumes_by_itself": {
            "default": false,
            "description": "True when the thing it is waiting on clears without anybody deciding it has, so the board may say the walk takes off again on its own. False on a real stop, where the epic moves again only if a person does something.",
            "title": "Resumes By Itself",
            "type": "boolean"
          },
          "started_at": {
            "default": "",
            "description": "When the walk was authorised, UTC. The age of a walk is most of what a reader wants from it: an epic that has been supervising for four hours is a different fact from one that started a minute ago.",
            "title": "Started At",
            "type": "string"
          },
          "waiting_on_task_id": {
            "default": "",
            "description": "The open child this walk is merely *waiting* on, or empty when the grounding is a real stop. Answered against the live records by `epic.waiting_child`, which is the same function the walk itself asks, so the board cannot disagree with the tick about whether this clears.",
            "title": "Waiting On Task Id",
            "type": "string"
          },
          "waiting_on_task_title": {
            "default": "",
            "title": "Waiting On Task Title",
            "type": "string"
          },
          "waiting_on_task_url": {
            "default": "",
            "title": "Waiting On Task Url",
            "type": "string"
          },
          "walk_id": {
            "title": "Walk Id",
            "type": "string"
          }
        },
        "required": [
          "walk_id",
          "project_id",
          "parent_task_id",
          "grounded"
        ],
        "title": "EpicWalkView",
        "type": "object"
      },
      "EstimatePoint": {
        "description": "How good the landing estimate was, per week of start (F6, task-586).\n\nScored at the prediction given as the gate started, against what the landing then\nreally took. ``raw_error_p50_pct`` scores the uncorrected medians the same way, so\nthe distance between the two is what the learned correction is worth.",
        "properties": {
          "bias_p50": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Median correction factor in force when those ETAs were given.",
            "title": "Bias P50"
          },
          "bucket": {
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "error_p50_pct": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Median absolute error of the ETA shown, as a percentage.",
            "title": "Error P50 Pct"
          },
          "outliers": {
            "default": 0,
            "description": "Scored landings with a gate retry or a runway wait: not taught from.",
            "title": "Outliers",
            "type": "integer"
          },
          "raw_error_p50_pct": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "The same, for the ETA before the learned correction.",
            "title": "Raw Error P50 Pct"
          },
          "sample": {
            "default": 0,
            "description": "Finished landings scored in this bucket.",
            "title": "Sample",
            "type": "integer"
          },
          "within_20": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Share of landings that ended within 20% of the ETA shown, 0 to 1.",
            "title": "Within 20"
          }
        },
        "required": [
          "bucket"
        ],
        "title": "EstimatePoint",
        "type": "object"
      },
      "EstimatorResetResult": {
        "description": "What resetting the landing estimate's correction did: from when it learns again.",
        "properties": {
          "reset_at": {
            "title": "Reset At",
            "type": "string"
          }
        },
        "required": [
          "reset_at"
        ],
        "title": "EstimatorResetResult",
        "type": "object"
      },
      "EstimatorState": {
        "description": "The landing estimate's learned correction as it stands now (task-586).\n\nShown beside the accuracy chart so the loop is never invisible: what factor is\napplied, how many landings taught it, whether the clamp is holding it, and when it\nwas last reset.",
        "properties": {
          "active": {
            "default": false,
            "description": "Enough measured landings to apply a correction.",
            "title": "Active",
            "type": "boolean"
          },
          "ceiling": {
            "title": "Ceiling",
            "type": "number"
          },
          "clamped": {
            "default": false,
            "title": "Clamped",
            "type": "boolean"
          },
          "excluded": {
            "default": 0,
            "description": "Recent measured landings left out as outliers.",
            "title": "Excluded",
            "type": "integer"
          },
          "factor": {
            "default": 1.0,
            "title": "Factor",
            "type": "number"
          },
          "floor": {
            "title": "Floor",
            "type": "number"
          },
          "learned": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "The median before the clamp.",
            "title": "Learned"
          },
          "min_sample": {
            "title": "Min Sample",
            "type": "integer"
          },
          "reset_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reset At"
          },
          "sample": {
            "default": 0,
            "title": "Sample",
            "type": "integer"
          },
          "window": {
            "title": "Window",
            "type": "integer"
          }
        },
        "required": [
          "floor",
          "ceiling",
          "min_sample",
          "window"
        ],
        "title": "EstimatorState",
        "type": "object"
      },
      "FeedbackActionRequest": {
        "description": "Request changes with feedback.",
        "properties": {
          "attachments": {
            "description": "Images evidencing the feedback, stored as sidecar files.",
            "items": {
              "$ref": "#/components/schemas/AttachmentUpload"
            },
            "title": "Attachments",
            "type": "array"
          },
          "feedback": {
            "description": "Feedback text",
            "examples": [
              "Please add error handling"
            ],
            "minLength": 1,
            "title": "Feedback",
            "type": "string"
          },
          "user": {
            "description": "User performing the action",
            "examples": [
              "jeff"
            ],
            "title": "User",
            "type": "string"
          }
        },
        "required": [
          "user",
          "feedback"
        ],
        "title": "FeedbackActionRequest",
        "type": "object"
      },
      "FinishGateRetryView": {
        "description": "The one retry a red gate stage gets (task-322), as the page renders it.",
        "properties": {
          "classification": {
            "default": "",
            "description": "'inputs_changed' (a proven change explains the red), 'flaky_test', or, once the retry has failed the same way, 'deterministic_in_context'.",
            "title": "Classification",
            "type": "string"
          },
          "explanation": {
            "default": "",
            "title": "Explanation",
            "type": "string"
          },
          "failed_stage": {
            "default": "",
            "title": "Failed Stage",
            "type": "string"
          }
        },
        "title": "FinishGateRetryView",
        "type": "object"
      },
      "FinishGateView": {
        "description": "How far into the gate a finish is, when the gate is what it is doing.",
        "properties": {
          "failed_stage": {
            "default": "",
            "title": "Failed Stage",
            "type": "string"
          },
          "passed": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "Null while the gate is still running.",
            "title": "Passed"
          },
          "running": {
            "default": false,
            "title": "Running",
            "type": "boolean"
          },
          "seconds": {
            "default": 0.0,
            "title": "Seconds",
            "type": "number"
          },
          "stage": {
            "default": "",
            "description": "The stage running now; empty once it ended.",
            "title": "Stage",
            "type": "string"
          },
          "stages_run": {
            "default": 0,
            "title": "Stages Run",
            "type": "integer"
          },
          "stages_total": {
            "default": 0,
            "title": "Stages Total",
            "type": "integer"
          }
        },
        "title": "FinishGateView",
        "type": "object"
      },
      "FinishHistoryWrite": {
        "additionalProperties": false,
        "description": "Body of ``PUT /history/finishes/{finish_id}``: the row and every step so far.",
        "properties": {
          "record": {
            "$ref": "#/components/schemas/FinishRecordWrite"
          },
          "steps": {
            "items": {
              "$ref": "#/components/schemas/FinishStepWrite"
            },
            "title": "Steps",
            "type": "array"
          }
        },
        "required": [
          "record"
        ],
        "title": "FinishHistoryWrite",
        "type": "object"
      },
      "FinishPoint": {
        "description": "The scripted finish, per week of start (F1 to F4).\n\nDuration percentiles are over ``finished`` rows only: a finish that stopped at the\ngate is measuring the gate. ``steps_p50_s`` lists the steps with a nonzero median,\nand ``runway_waited`` is how many finishes queued behind another for the merge\nrunway (task-223).",
        "properties": {
          "bucket": {
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "declined": {
            "default": 0,
            "title": "Declined",
            "type": "integer"
          },
          "duration_p50_min": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration P50 Min"
          },
          "duration_p90_min": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration P90 Min"
          },
          "escalated": {
            "default": 0,
            "title": "Escalated",
            "type": "integer"
          },
          "estimated": {
            "default": false,
            "title": "Estimated",
            "type": "boolean"
          },
          "finished": {
            "default": 0,
            "title": "Finished",
            "type": "integer"
          },
          "interrupted": {
            "default": 0,
            "title": "Interrupted",
            "type": "integer"
          },
          "reasons": {
            "additionalProperties": {
              "type": "integer"
            },
            "description": "Escalation reasons.",
            "title": "Reasons",
            "type": "object"
          },
          "runway_p90_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Runway P90 S"
          },
          "runway_waited": {
            "default": 0,
            "title": "Runway Waited",
            "type": "integer"
          },
          "sample": {
            "default": 0,
            "title": "Sample",
            "type": "integer"
          },
          "steps_p50_s": {
            "additionalProperties": {
              "type": "number"
            },
            "title": "Steps P50 S",
            "type": "object"
          }
        },
        "required": [
          "bucket"
        ],
        "title": "FinishPoint",
        "type": "object"
      },
      "FinishRecordWrite": {
        "additionalProperties": false,
        "description": "The ``finish`` row a scripted finish indexes itself as (task-472).\n\nDerived from the finish's ``meta.yaml`` by ``agentjobs.history``; the field names are\nthe column names. ``source`` says who is writing: ``native`` upserts, ``imported``\nnever overwrites a row that exists.",
        "properties": {
          "authority": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Authority"
          },
          "dispatched_run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Dispatched Run Id"
          },
          "finished_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finished At"
          },
          "merge_commit": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Merge Commit"
          },
          "merged": {
            "default": false,
            "title": "Merged",
            "type": "boolean"
          },
          "outcome": {
            "enum": [
              "finished",
              "escalated",
              "declined",
              "interrupted",
              "running"
            ],
            "title": "Outcome",
            "type": "string"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          },
          "run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run Id"
          },
          "seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Seconds"
          },
          "source": {
            "default": "native",
            "enum": [
              "native",
              "imported"
            ],
            "title": "Source",
            "type": "string"
          },
          "started_at": {
            "title": "Started At",
            "type": "string"
          },
          "stopped_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stopped At"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "started_at",
          "outcome"
        ],
        "title": "FinishRecordWrite",
        "type": "object"
      },
      "FinishRetryRequest": {
        "additionalProperties": false,
        "description": "Ask AgentJobs to retry a stopped finish on the approval already given (task-575).\n\nSent by the agent that repaired whatever stopped the finish. The server judges the\nrepair against the head the approver saw and either starts the finish itself or hands\nthe task to review; the caller never runs the merge.",
        "properties": {
          "actor": {
            "description": "Actor id of the agent asking for the retry.",
            "title": "Actor",
            "type": "string"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "summary": {
            "default": "",
            "description": "What the repair was, in the agent's words. Quoted on the record.",
            "title": "Summary",
            "type": "string"
          }
        },
        "required": [
          "actor"
        ],
        "title": "FinishRetryRequest",
        "type": "object"
      },
      "FinishRetryResult": {
        "description": "What a retry request did.",
        "properties": {
          "data": {
            "additionalProperties": true,
            "description": "The facts recorded: approval, heads, bases and each path's verdict.",
            "title": "Data",
            "type": "object"
          },
          "detail": {
            "description": "The outcome in a sentence or two, for the agent.",
            "title": "Detail",
            "type": "string"
          },
          "outcome": {
            "description": "`retrying`: the finish was started. `handed_back`: the repair changed reviewed work or the bound was reached, so the task went to human/review. `declined`: nothing was written, and `reason` says why. `replayed`: already acted on.",
            "title": "Outcome",
            "type": "string"
          },
          "project_id": {
            "description": "Project the request addressed.",
            "title": "Project Id",
            "type": "string"
          },
          "reason": {
            "description": "A stable code for the outcome.",
            "title": "Reason",
            "type": "string"
          },
          "task": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskRead-Output"
              },
              {
                "type": "null"
              }
            ],
            "description": "The task as it now stands."
          }
        },
        "required": [
          "project_id",
          "outcome",
          "reason",
          "detail"
        ],
        "title": "FinishRetryResult",
        "type": "object"
      },
      "FinishStepView": {
        "description": "One step of a scripted finish, as the task page renders it.",
        "properties": {
          "detail": {
            "default": "",
            "title": "Detail",
            "type": "string"
          },
          "meaning": {
            "default": "",
            "description": "What this step is, for a reader who has not read ENGINEERING.md.",
            "title": "Meaning",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "seconds": {
            "default": 0.0,
            "title": "Seconds",
            "type": "number"
          },
          "state": {
            "description": "'done', 'skipped', 'stopped' (this is where the finish gave up), or 'running' (inferred from the fixed order, and true of a finish that is between steps as well as one in the middle of this one).",
            "title": "State",
            "type": "string"
          }
        },
        "required": [
          "name",
          "state"
        ],
        "title": "FinishStepView",
        "type": "object"
      },
      "FinishStepWrite": {
        "additionalProperties": false,
        "description": "One ``finish_step`` row: a step of the sequence and what it cost.",
        "properties": {
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Detail"
          },
          "ok": {
            "title": "Ok",
            "type": "boolean"
          },
          "seconds": {
            "default": 0.0,
            "title": "Seconds",
            "type": "number"
          },
          "seq": {
            "minimum": 1.0,
            "title": "Seq",
            "type": "integer"
          },
          "skipped": {
            "default": false,
            "title": "Skipped",
            "type": "boolean"
          },
          "step": {
            "title": "Step",
            "type": "string"
          },
          "ts": {
            "title": "Ts",
            "type": "string"
          }
        },
        "required": [
          "seq",
          "step",
          "ok",
          "ts"
        ],
        "title": "FinishStepWrite",
        "type": "object"
      },
      "GateHistoryWrite": {
        "additionalProperties": false,
        "description": "Body of ``PUT /history/gates/{gate_id}``: the row and every stage so far.",
        "properties": {
          "record": {
            "$ref": "#/components/schemas/GateRunWrite"
          },
          "stages": {
            "items": {
              "$ref": "#/components/schemas/GateStageWrite"
            },
            "title": "Stages",
            "type": "array"
          }
        },
        "required": [
          "record"
        ],
        "title": "GateHistoryWrite",
        "type": "object"
      },
      "GatePoint": {
        "description": "Full gates, per week of start (G1 to G3).\n\nDuration percentiles are over green full gates: a red gate stops early and says\nnothing about cost. ``failed_stages`` is where the red ones stopped and\n``origins`` says whose gates they were: the finisher's, a run's, or somebody's at a\nshell.",
        "properties": {
          "bucket": {
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "duration_p50_min": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration P50 Min"
          },
          "duration_p90_min": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration P90 Min"
          },
          "estimated": {
            "default": false,
            "title": "Estimated",
            "type": "boolean"
          },
          "failed_stages": {
            "additionalProperties": {
              "type": "integer"
            },
            "title": "Failed Stages",
            "type": "object"
          },
          "full": {
            "default": 0,
            "title": "Full",
            "type": "integer"
          },
          "origins": {
            "additionalProperties": {
              "type": "integer"
            },
            "title": "Origins",
            "type": "object"
          },
          "passed": {
            "default": 0,
            "title": "Passed",
            "type": "integer"
          },
          "sample": {
            "default": 0,
            "title": "Sample",
            "type": "integer"
          },
          "stages_p50_s": {
            "additionalProperties": {
              "type": "number"
            },
            "title": "Stages P50 S",
            "type": "object"
          }
        },
        "required": [
          "bucket"
        ],
        "title": "GatePoint",
        "type": "object"
      },
      "GateRunWrite": {
        "additionalProperties": false,
        "description": "The ``gate_run`` row a gate indexes itself as (task-472).\n\n``origin`` says whose gate it was: the finisher's, a dispatched run's before its\nhandoff, or somebody's at a shell. ``scope`` is the gate's own vocabulary with one\nrename -- the gate calls a reduced ``--since-gate`` run ``necessity``, the store\ncalls it ``since_gate`` -- and a partial run is a row, not an omitted one.",
        "properties": {
          "branch": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Branch"
          },
          "checkout": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkout"
          },
          "failed_stage": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Failed Stage"
          },
          "finish_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finish Id"
          },
          "finished_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finished At"
          },
          "free_mb_low": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The lowest available memory, MB, the gate saw.",
            "title": "Free Mb Low"
          },
          "free_mb_start": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Available physical memory, MB, when the gate started.",
            "title": "Free Mb Start"
          },
          "low_memory": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "Whether the gate ran below its memory floor (task-548).",
            "title": "Low Memory"
          },
          "origin": {
            "enum": [
              "finish",
              "run",
              "manual"
            ],
            "title": "Origin",
            "type": "string"
          },
          "passed": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Passed"
          },
          "run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run Id"
          },
          "scope": {
            "enum": [
              "full",
              "partial",
              "since_gate",
              "concurrent"
            ],
            "title": "Scope",
            "type": "string"
          },
          "seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Seconds"
          },
          "source": {
            "default": "native",
            "enum": [
              "native",
              "imported"
            ],
            "title": "Source",
            "type": "string"
          },
          "stages_run": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stages Run"
          },
          "stages_total": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stages Total"
          },
          "started_at": {
            "title": "Started At",
            "type": "string"
          },
          "task_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Task Id"
          },
          "tree": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Tree"
          }
        },
        "required": [
          "origin",
          "scope",
          "started_at"
        ],
        "title": "GateRunWrite",
        "type": "object"
      },
      "GateStageWrite": {
        "additionalProperties": false,
        "description": "One ``gate_stage`` row. ``seconds`` is null for the stage that failed.",
        "properties": {
          "finished_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finished At"
          },
          "free_mb_end": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Free Mb End"
          },
          "free_mb_low": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Free Mb Low"
          },
          "free_mb_start": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Free Mb Start"
          },
          "low_memory": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Low Memory"
          },
          "passed": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Passed"
          },
          "seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Seconds"
          },
          "seq": {
            "minimum": 1.0,
            "title": "Seq",
            "type": "integer"
          },
          "stage": {
            "title": "Stage",
            "type": "string"
          },
          "started_at": {
            "title": "Started At",
            "type": "string"
          }
        },
        "required": [
          "seq",
          "stage",
          "started_at"
        ],
        "title": "GateStageWrite",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "HandoffRequest": {
        "additionalProperties": false,
        "description": "The ball moves; the ask travels with it.",
        "properties": {
          "actor": {
            "description": "Who is handing the work over.",
            "title": "Actor",
            "type": "string"
          },
          "ball": {
            "$ref": "#/components/schemas/Ball",
            "description": "Who acts next."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to the new holder. Required except agent/available.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "$ref": "#/components/schemas/BallReason",
            "description": "Why they hold it."
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Log entry body; defaults to the ball_prompt.",
            "title": "Body"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "questions": {
            "description": "Questions to pose alongside this handoff, each optionally offering options. Written in the same mutation, so the human never opens a half-populated form.",
            "items": {
              "$ref": "#/components/schemas/QuestionDraft"
            },
            "title": "Questions",
            "type": "array"
          }
        },
        "required": [
          "actor",
          "ball",
          "ball_reason"
        ],
        "title": "HandoffRequest",
        "type": "object"
      },
      "HistoryWriteResult": {
        "description": "What a history write did.\n\n``written`` false is an answer, not an error: ``exists`` means an imported write met\na row that was already there, and ``unknown_task`` means a finish named a task this\nproject does not have and was refused rather than inserted with the foreign key\noff.",
        "properties": {
          "reason": {
            "anyOf": [
              {
                "enum": [
                  "exists",
                  "unknown_task"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          },
          "written": {
            "title": "Written",
            "type": "boolean"
          }
        },
        "required": [
          "written"
        ],
        "title": "HistoryWriteResult",
        "type": "object"
      },
      "HolderPoint": {
        "description": "Who held the open work, per bucket, on the backlog's spine.",
        "properties": {
          "agent": {
            "title": "Agent",
            "type": "integer"
          },
          "day": {
            "format": "date",
            "title": "Day",
            "type": "string"
          },
          "external": {
            "title": "External",
            "type": "integer"
          },
          "human": {
            "title": "Human",
            "type": "integer"
          }
        },
        "required": [
          "day",
          "agent",
          "human",
          "external"
        ],
        "title": "HolderPoint",
        "type": "object"
      },
      "HumanActionResponse": {
        "description": "A manager-backed human action returns the newly persisted task state.",
        "properties": {
          "task": {
            "$ref": "#/components/schemas/Task"
          }
        },
        "required": [
          "task"
        ],
        "title": "HumanActionResponse",
        "type": "object"
      },
      "IdleSessionEventView": {
        "properties": {
          "at": {
            "title": "At",
            "type": "string"
          },
          "auth_incidents": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "On a mode event: the auth incident count at the switch-over.",
            "title": "Auth Incidents"
          },
          "cwd": {
            "default": "",
            "title": "Cwd",
            "type": "string"
          },
          "detail": {
            "default": "",
            "title": "Detail",
            "type": "string"
          },
          "event_id": {
            "title": "Event Id",
            "type": "string"
          },
          "idle_seconds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idle Seconds"
          },
          "kind": {
            "description": "stop, or mode for a change of enforcement.",
            "title": "Kind",
            "type": "string"
          },
          "last_activity": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Activity"
          },
          "name": {
            "default": "",
            "title": "Name",
            "type": "string"
          },
          "outcome": {
            "description": "stopped, failed or declined; enforce or report.",
            "title": "Outcome",
            "type": "string"
          },
          "reason": {
            "default": "",
            "title": "Reason",
            "type": "string"
          },
          "resume_commands": {
            "items": {
              "type": "string"
            },
            "title": "Resume Commands",
            "type": "array"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Session Id"
          },
          "short_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Short Id"
          },
          "trigger": {
            "default": "",
            "title": "Trigger",
            "type": "string"
          }
        },
        "required": [
          "event_id",
          "kind",
          "at",
          "outcome"
        ],
        "title": "IdleSessionEventView",
        "type": "object"
      },
      "IdleSessionSettingsUpdate": {
        "properties": {
          "enforce": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Enforce"
          },
          "idle_minutes": {
            "anyOf": [
              {
                "exclusiveMinimum": 0.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idle Minutes"
          }
        },
        "title": "IdleSessionSettingsUpdate",
        "type": "object"
      },
      "IdleSessionSettingsView": {
        "properties": {
          "configured": {
            "description": "This machine has a dispatch config to hold them.",
            "title": "Configured",
            "type": "boolean"
          },
          "enforce": {
            "description": "Idle, resumable sessions are stopped automatically.",
            "title": "Enforce",
            "type": "boolean"
          },
          "idle_minutes": {
            "title": "Idle Minutes",
            "type": "integer"
          },
          "max_stops_per_sweep": {
            "title": "Max Stops Per Sweep",
            "type": "integer"
          },
          "sweep_interval_seconds": {
            "title": "Sweep Interval Seconds",
            "type": "integer"
          }
        },
        "required": [
          "configured",
          "enforce",
          "idle_minutes",
          "max_stops_per_sweep",
          "sweep_interval_seconds"
        ],
        "title": "IdleSessionSettingsView",
        "type": "object"
      },
      "IdleSessionView": {
        "properties": {
          "cwd": {
            "default": "",
            "title": "Cwd",
            "type": "string"
          },
          "idle_seconds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idle Seconds"
          },
          "kind": {
            "description": "daemon, background, remote_control_host, ...",
            "title": "Kind",
            "type": "string"
          },
          "last_activity": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Activity"
          },
          "ledger_status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ledger Status"
          },
          "name": {
            "default": "",
            "title": "Name",
            "type": "string"
          },
          "pid": {
            "title": "Pid",
            "type": "integer"
          },
          "reason": {
            "title": "Reason",
            "type": "string"
          },
          "resume_commands": {
            "items": {
              "type": "string"
            },
            "title": "Resume Commands",
            "type": "array"
          },
          "run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Run Id"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Session Id"
          },
          "short_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Short Id"
          },
          "verdict": {
            "description": "protected, in_use, idle, report_only or candidate.",
            "title": "Verdict",
            "type": "string"
          }
        },
        "required": [
          "pid",
          "kind",
          "verdict",
          "reason"
        ],
        "title": "IdleSessionView",
        "type": "object"
      },
      "IdleSessionsView": {
        "properties": {
          "auth_incidents": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Auth Incidents"
          },
          "errors": {
            "items": {
              "type": "string"
            },
            "title": "Errors",
            "type": "array"
          },
          "events": {
            "items": {
              "$ref": "#/components/schemas/IdleSessionEventView"
            },
            "title": "Events",
            "type": "array"
          },
          "generated_at": {
            "title": "Generated At",
            "type": "string"
          },
          "sessions": {
            "items": {
              "$ref": "#/components/schemas/IdleSessionView"
            },
            "title": "Sessions",
            "type": "array"
          },
          "settings": {
            "$ref": "#/components/schemas/IdleSessionSettingsView"
          }
        },
        "required": [
          "settings",
          "generated_at",
          "sessions",
          "events"
        ],
        "title": "IdleSessionsView",
        "type": "object"
      },
      "InReview": {
        "description": "One open task waiting on review, with how long the ball has sat there (R1).",
        "properties": {
          "hours_waiting": {
            "title": "Hours Waiting",
            "type": "number"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "title": {
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "title",
          "hours_waiting"
        ],
        "title": "InReview",
        "type": "object"
      },
      "LandingEstimate": {
        "additionalProperties": false,
        "description": "How far along a live finish is and roughly when it will be done (task-586).\n\nComputed once, on the server, by ``agentjobs.finish_estimate.estimate`` -- the task\nlist's ``live_finish``, the task page's finish view and the slot board all carry this\nsame shape from that same function, so no two surfaces can draw a different bar for\none finish, and no client re-derives the model.\n\nTwo numbers and a sentence, not the step list, which is why it may ride on every row.",
        "properties": {
          "basis": {
            "default": "",
            "title": "Basis",
            "type": "string"
          },
          "elapsed_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Elapsed Seconds"
          },
          "eta_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Eta Seconds"
          },
          "kind": {
            "enum": [
              "estimate",
              "runway",
              "no_history"
            ],
            "title": "Kind",
            "type": "string"
          },
          "overrun": {
            "default": false,
            "title": "Overrun",
            "type": "boolean"
          },
          "progress": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Progress"
          },
          "typical_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Typical Seconds"
          }
        },
        "required": [
          "kind"
        ],
        "title": "LandingEstimate",
        "type": "object"
      },
      "Lifecycle": {
        "description": "Where a task is in its life (design doc section 3).",
        "enum": [
          "draft",
          "ready",
          "active",
          "closed"
        ],
        "title": "Lifecycle",
        "type": "string"
      },
      "Link": {
        "additionalProperties": false,
        "description": "An external resource.",
        "properties": {
          "rel": {
            "$ref": "#/components/schemas/LinkRel",
            "default": "other",
            "description": "What it points at."
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Display title.",
            "title": "Title"
          },
          "url": {
            "description": "Validated URL.",
            "format": "uri",
            "maxLength": 2083,
            "minLength": 1,
            "title": "Url",
            "type": "string"
          }
        },
        "required": [
          "url"
        ],
        "title": "Link",
        "type": "object"
      },
      "LinkRel": {
        "description": "What an external link points at.",
        "enum": [
          "pr",
          "issue",
          "doc",
          "design",
          "build",
          "other"
        ],
        "title": "LinkRel",
        "type": "string"
      },
      "LiveFinishState": {
        "additionalProperties": false,
        "description": "A scripted finish running against this task's branch right now (task-509).\n\nDerived on read from the finish's own records on disk and never stored on the task,\nfor the reason ``QueuedDispatchState`` above is: the finish moves nothing on the\nrecord. Approving a task hands the ball to ``agent``/``work`` and there it stays for\nthe three to four minutes of rebase, gate and merge, so every surface but the task\npage's ``FinishPanel`` showed a task mid-merge as an ordinary agent-held one.\n\nThe fields are the ones a *list* needs -- a chip to draw, a sentence to explain a\ndisabled button, and an id to link with. Everything else a watcher wants is on\n``GET /dispatch/finishes/{task_id}``, which the panel already polls; duplicating the\nstep list and the gate counter here would put a second copy of a fact on every row\nof a list nothing draws it on.\n\n``estimate`` is the exception that proves the rule (task-586): two numbers a row does\ndraw -- a thin progress bar and \"about N min left\" -- computed from the step list on\nthe server so the row does not need it.",
        "properties": {
          "branch": {
            "default": "",
            "title": "Branch",
            "type": "string"
          },
          "current_step": {
            "default": "",
            "title": "Current Step",
            "type": "string"
          },
          "estimate": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LandingEstimate"
              },
              {
                "type": "null"
              }
            ]
          },
          "finish_id": {
            "default": "",
            "title": "Finish Id",
            "type": "string"
          },
          "started_at": {
            "default": "",
            "title": "Started At",
            "type": "string"
          },
          "state": {
            "title": "State",
            "type": "string"
          },
          "step_meaning": {
            "default": "",
            "title": "Step Meaning",
            "type": "string"
          }
        },
        "required": [
          "state"
        ],
        "title": "LiveFinishState",
        "type": "object"
      },
      "LiveRunView": {
        "description": "One run that is happening now, said in words a person can act on.",
        "properties": {
          "elapsed_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Seconds since this run started, computed on the server. The phone reading this page is not on the clock that wrote `started_at`.",
            "title": "Elapsed Seconds"
          },
          "finish_estimate": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LandingEstimate"
              },
              {
                "type": "null"
              }
            ],
            "description": "When `health` is `finishing`: progress and time remaining (task-586)."
          },
          "finish_id": {
            "default": "",
            "description": "The live finish for this run's task, when `health` is `finishing`.",
            "title": "Finish Id",
            "type": "string"
          },
          "finish_step": {
            "default": "",
            "description": "The step that finish is on, in the finish's own vocabulary (`runway`, `gate`...). `runway` means it is queued for the repository's merge runway.",
            "title": "Finish Step",
            "type": "string"
          },
          "health": {
            "description": "What the run is actually doing: working, starting, parked, silent, handback, work_done, finishing, orphaned or unknown. `live` means only that nothing has declared the run over, so this is the field a surface renders. `finishing` comes from the same live-finish lookup as the task read's `live_finish`, so it and the task's Finishing chip cannot disagree (task-533).",
            "title": "Health",
            "type": "string"
          },
          "holds_slot": {
            "description": "Whether this run is one of the `occupied` slots. False for an interactive session and a walk, which never took one, and for a run whose task has closed while its session stayed open (task-482) -- that one released its slot and reads `work_done` in `health`. Sent rather than re-derived from `mode`, so a board can never draw a run in a slot cell the server does not count.",
            "title": "Holds Slot",
            "type": "boolean"
          },
          "mode": {
            "title": "Mode",
            "type": "string"
          },
          "output_url": {
            "description": "Where this run's captured output is readable.",
            "title": "Output Url",
            "type": "string"
          },
          "over_ceiling": {
            "default": false,
            "description": "This run was started above `max_concurrent_runs` because a person chose to (task-461). While it lives, `occupied` exceeds the ceiling -- and a board that could not say which run explains that would be showing a count nobody can reconcile.",
            "title": "Over Ceiling",
            "type": "boolean"
          },
          "posture": {
            "title": "Posture",
            "type": "string"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "project_name": {
            "default": "",
            "description": "The project's display name. Falls back to the id for a run whose project is no longer registered, rather than rendering a blank cell.",
            "title": "Project Name",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "runway_behind": {
            "default": "",
            "description": "When the finish is queued for the runway: the task whose finish holds it, if that can be told. Empty otherwise.",
            "title": "Runway Behind",
            "type": "string"
          },
          "session": {
            "description": "A session run, as opposed to a batch one.",
            "title": "Session",
            "type": "boolean"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "status": {
            "description": "The ledger's own word. Prefer `health` for display.",
            "title": "Status",
            "type": "string"
          },
          "task_display_status": {
            "default": "",
            "description": "When `health` is `work_done`: the closed task's own status word -- Completed, Cancelled, Superseded or Duplicate -- so the run's chip says what the task's chip says rather than a word of its own (task-577). Empty when the task cannot be read, or the run's task is still open.",
            "title": "Task Display Status",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "task_status_category": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/StatusCategory"
              },
              {
                "type": "null"
              }
            ],
            "description": "The category `task_display_status` is drawn in, beside it."
          },
          "task_title": {
            "default": "",
            "description": "The task's title, resolved server-side from its project's storage. Empty when the record cannot be read -- a run whose task file has been renamed or removed is exactly the kind of thing this surface should still show.",
            "title": "Task Title",
            "type": "string"
          },
          "task_url": {
            "description": "Where this run's task is, in this app.",
            "title": "Task Url",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "task_id",
          "project_id",
          "mode",
          "session",
          "posture",
          "status",
          "health",
          "task_url",
          "output_url",
          "holds_slot"
        ],
        "title": "LiveRunView",
        "type": "object"
      },
      "LiveRunsView": {
        "description": "Everything both machine-wide surfaces need, in one response.\n\nThe capacity numbers ride along with the list rather than being fetched separately\nor recomputed in TypeScript: a browser that divided a count by a ceiling it read from\nsomewhere else would be the one place in the system able to disagree with\n``dispatch/guards.py`` about whether the machine is full.",
        "properties": {
          "armed": {
            "description": "Projects the pull mode is armed for (task-462), oldest arming first. Empty on a machine where nobody has armed one. Filtered by what this caller may see, exactly as `runs` is.",
            "items": {
              "$ref": "#/components/schemas/ArmedProjectView"
            },
            "title": "Armed",
            "type": "array"
          },
          "dispatch_configured": {
            "description": "False on a machine with no dispatch.yaml at all, where the ceiling below is a default rather than a setting anyone chose.",
            "title": "Dispatch Configured",
            "type": "boolean"
          },
          "generated_at": {
            "description": "When this answer was assembled, in UTC.",
            "title": "Generated At",
            "type": "string"
          },
          "holders": {
            "items": {
              "$ref": "#/components/schemas/MachineHolderView"
            },
            "title": "Holders",
            "type": "array"
          },
          "max_concurrent_runs": {
            "description": "`limits.max_concurrent_runs` from ~/.agentjobs/dispatch.yaml.",
            "title": "Max Concurrent Runs",
            "type": "integer"
          },
          "occupied": {
            "description": "Run slots in use. Counted exactly as the concurrency guard counts them -- `len(slot_runs(home))` -- so this surface and a refused dispatch can never disagree. An interactive run (mode `interactive`) is in `runs` and not in this count: it holds its task, not a slot (task-354). Nor is a run whose task has closed while its session stayed open, which released its slot (task-482) -- `holds_slot` is false on both. Finishes and the runway are not in it either: they hold locks, not run slots.",
            "title": "Occupied",
            "type": "integer"
          },
          "paused": {
            "description": "Why the machine is starting nothing on a credential (task-463). Empty on the ordinary machine, which is the point: a board that says nothing here is a board with nothing to explain. **Unfiltered by project**, on the same argument as `occupied`: the rows answer 'what may I read' and this answers 'why is nothing starting', and a machine out of quota is out of quota whether or not the run that spent it is one this caller may see.",
            "items": {
              "$ref": "#/components/schemas/StartPauseView"
            },
            "title": "Paused",
            "type": "array"
          },
          "queue_limit": {
            "default": 0,
            "description": "`limits.dispatch_queue_limit` from ~/.agentjobs/dispatch.yaml: how many dispatches may wait at once.",
            "title": "Queue Limit",
            "type": "integer"
          },
          "queued": {
            "description": "Dispatches waiting for a slot, in the order they will start (task-459). Empty on a machine where nobody has queued one. Filtered by what this caller may see, exactly as `runs` is.",
            "items": {
              "$ref": "#/components/schemas/QueuedDispatchView"
            },
            "title": "Queued",
            "type": "array"
          },
          "runs": {
            "items": {
              "$ref": "#/components/schemas/LiveRunView"
            },
            "title": "Runs",
            "type": "array"
          },
          "walks": {
            "description": "Epic walks still open on this machine (task-523), oldest first. Empty on a machine where none is, which is the ordinary case. Filtered by what this caller may see, exactly as `runs` is -- and **not** counted in `occupied`: a walk holds no run slot, and drawing one as though it did would make this surface disagree with the concurrency guard.",
            "items": {
              "$ref": "#/components/schemas/EpicWalkView"
            },
            "title": "Walks",
            "type": "array"
          }
        },
        "required": [
          "occupied",
          "max_concurrent_runs",
          "dispatch_configured",
          "runs",
          "holders",
          "generated_at"
        ],
        "title": "LiveRunsView",
        "type": "object"
      },
      "LiveWalkState": {
        "additionalProperties": false,
        "description": "An open epic walk supervising this task right now (task-591).\n\nDerived on read from the machine's execution store and never stored on the task, for\nthe reason ``LiveFinishState`` is: a walk moves nothing on the record it supervises.\nThe epic is claimed and reads ``agent``/``work`` for as long as the walk is open, so\nwithout this every surface drew a walked epic as \"Working\" -- a word that says an\nagent is editing it, when nothing is and the server is starting its children.\n\nOnly what a row needs: whether to say \"Walking\", and whether to animate the chip.\nThe counts and the child being waited on are on ``GET /api/runs/live``'s ``walks``.",
        "properties": {
          "grounded": {
            "default": false,
            "title": "Grounded",
            "type": "boolean"
          },
          "walk_id": {
            "title": "Walk Id",
            "type": "string"
          }
        },
        "required": [
          "walk_id"
        ],
        "title": "LiveWalkState",
        "type": "object"
      },
      "LogAppendRequest": {
        "additionalProperties": false,
        "description": "Append one entry to the unified log.",
        "properties": {
          "actor": {
            "description": "Actor id writing the entry.",
            "title": "Actor",
            "type": "string"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Prose, markdown.",
            "title": "Body"
          },
          "data": {
            "additionalProperties": true,
            "description": "Optional structured payload.",
            "title": "Data",
            "type": "object"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "re": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional id of the earlier entry this threads to.",
            "title": "Re"
          },
          "type": {
            "$ref": "#/components/schemas/LogEntryType",
            "default": "note",
            "description": "Entry type. Transitions are manager-only and rejected here."
          }
        },
        "required": [
          "actor"
        ],
        "title": "LogAppendRequest",
        "type": "object"
      },
      "LogEntry": {
        "additionalProperties": false,
        "description": "One entry in the unified append-only log (design doc section 4).\n\nReplaces v1's `status_updates`, `comments` and `prompts.followups` -- three\nappend-only authored lists with an implied but unenforced role split.",
        "properties": {
          "actor": {
            "description": "Actor id. A bare reference; kind is resolved from config (D4).",
            "title": "Actor",
            "type": "string"
          },
          "attachments": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/Attachment"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Images evidencing this entry, stored as sidecar files.",
            "title": "Attachments"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Prose, markdown.",
            "title": "Body"
          },
          "data": {
            "additionalProperties": true,
            "description": "Optional structured payload, typed per entry type.",
            "title": "Data",
            "type": "object"
          },
          "id": {
            "description": "Per-task integer, assigned by the manager.",
            "minimum": 1.0,
            "title": "Id",
            "type": "integer"
          },
          "re": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional id of the entry this threads to.",
            "title": "Re"
          },
          "ts": {
            "description": "When the entry was written.",
            "format": "date-time",
            "title": "Ts",
            "type": "string"
          },
          "type": {
            "$ref": "#/components/schemas/LogEntryType",
            "description": "Entry type."
          }
        },
        "required": [
          "id",
          "ts",
          "actor",
          "type"
        ],
        "title": "LogEntry",
        "type": "object"
      },
      "LogEntryType": {
        "description": "Type of a log entry (design doc section 4).",
        "enum": [
          "note",
          "progress",
          "transition",
          "handoff",
          "decision",
          "question",
          "answer",
          "instruction",
          "dispatch",
          "dispatch_result",
          "queue_move",
          "authorization",
          "check_result",
          "chain_authorized",
          "chain_revoked"
        ],
        "title": "LogEntryType",
        "type": "string"
      },
      "MachineHolderView": {
        "description": "Something holding a lock on this machine that is not a dispatched run.\n\nA scripted finish (task-241) and the merge runway (task-223) are both real work\ncompeting for this machine, both leave a lock behind, and neither has a run record\n-- so neither is reachable through ``live_runs``. Leaving them out would make the\nsurface answer \"nothing is happening\" during the three minutes a merge takes.",
        "properties": {
          "detail": {
            "default": "",
            "description": "What it is doing right now, e.g. the finish step it is on.",
            "title": "Detail",
            "type": "string"
          },
          "elapsed_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Elapsed Seconds"
          },
          "estimate": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LandingEstimate"
              },
              {
                "type": "null"
              }
            ],
            "description": "For a live finish: progress and time remaining (task-586)."
          },
          "finish_id": {
            "default": "",
            "title": "Finish Id",
            "type": "string"
          },
          "kind": {
            "description": "`finish` or `runway`.",
            "title": "Kind",
            "type": "string"
          },
          "lock_name": {
            "description": "The lock file's stem: a task id, or a runway key. For a finish drawn from its record because it holds no finish lock yet (task-533), `project:task`.",
            "title": "Lock Name",
            "type": "string"
          },
          "overtaken": {
            "default": false,
            "description": "True when this finish holds a task that is already closed and it merged nothing, so it is not the finish that finished it (task-514).",
            "title": "Overtaken",
            "type": "boolean"
          },
          "pid": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pid"
          },
          "project_id": {
            "default": "",
            "title": "Project Id",
            "type": "string"
          },
          "project_name": {
            "default": "",
            "title": "Project Name",
            "type": "string"
          },
          "runway_behind": {
            "default": "",
            "description": "For a finish queued for the runway: the task whose finish holds it, if that can be told (task-533).",
            "title": "Runway Behind",
            "type": "string"
          },
          "started_at": {
            "default": "",
            "title": "Started At",
            "type": "string"
          },
          "task_id": {
            "default": "",
            "description": "The task. For a runway, the task whose live finish holds it, or empty when no live finish names that runway's finish id (task-533).",
            "title": "Task Id",
            "type": "string"
          },
          "task_title": {
            "default": "",
            "title": "Task Title",
            "type": "string"
          },
          "task_url": {
            "default": "",
            "description": "Empty when there is no task to link to.",
            "title": "Task Url",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "lock_name"
        ],
        "title": "MachineHolderView",
        "type": "object"
      },
      "MachinePoint": {
        "description": "What the execution journal says about this project, per week (R-5, R-6).\n\nIts own series because its source is another database with its own baseline.\n``start_latency`` is admission to launch; ``queue_wait`` is the time a queued\ndispatch waited for a slot; ``paused_run_hours`` is run-hours lost to usage-limit\npauses, not wall-clock -- three runs stalled for one reset count three times.",
        "properties": {
          "admitted": {
            "default": 0,
            "title": "Admitted",
            "type": "integer"
          },
          "bucket": {
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "paused_run_hours": {
            "default": 0.0,
            "title": "Paused Run Hours",
            "type": "number"
          },
          "paused_waiters": {
            "default": 0,
            "title": "Paused Waiters",
            "type": "integer"
          },
          "queue_wait_p50_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Queue Wait P50 S"
          },
          "queue_wait_p90_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Queue Wait P90 S"
          },
          "queued": {
            "default": 0,
            "title": "Queued",
            "type": "integer"
          },
          "start_latency_p50_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Start Latency P50 S"
          },
          "start_latency_p90_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Start Latency P90 S"
          }
        },
        "required": [
          "bucket"
        ],
        "title": "MachinePoint",
        "type": "object"
      },
      "ModelStatusResponse": {
        "description": "Whether this machine can draft a task spec right now, and why not when it cannot.\n\n**There is no field here that a credential could occupy**, which is the design's\nrule expressed as a shape rather than as a promise (`docs/model-access-design.md`\n§4): the status route answers a boolean, never the key, a prefix of it, or its\nlength. `model` is a configured identifier and is shown because a person who asked\nfor a draft is entitled to know what drafted it.\n\n`reason` is one of the closed set in `agentjobs.modelaccess.config.REASONS`, and\n`detail` is a sentence written in this repository. Neither is ever built from a\nprovider's response.",
        "properties": {
          "available": {
            "description": "Whether a drafting call can be made now.",
            "title": "Available",
            "type": "boolean"
          },
          "calls_per_hour": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "This machine's hourly cap.",
            "title": "Calls Per Hour"
          },
          "calls_used": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Calls made inside the rolling hour.",
            "title": "Calls Used"
          },
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "One sentence a person can act on.",
            "title": "Detail"
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The configured model id, when there is one.",
            "title": "Model"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why not, as a stable code. Null when available.",
            "title": "Reason"
          }
        },
        "required": [
          "available"
        ],
        "title": "ModelStatusResponse",
        "type": "object"
      },
      "MutationResult-Input": {
        "description": "What a mutation did, for callers that need more than the new task.\n\nReturned only when a request asks for it with `?envelope=true`, so the existing\ntask-shaped responses stay exactly as they were. `replayed` is the field that\ncannot be derived any other way: a caller retrying after a timeout has no way to\ntell \"I did that\" from \"you already had\".",
        "properties": {
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The operation id the caller supplied, echoed back.",
            "title": "Operation Id"
          },
          "project_id": {
            "description": "Project the mutation addressed.",
            "title": "Project Id",
            "type": "string"
          },
          "queue_undo": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueueMovePlacement"
              },
              {
                "type": "null"
              }
            ],
            "description": "The placement that puts the task back where the move took it from, offered only alongside a queue warning and only for a single-task move."
          },
          "queue_warnings": {
            "description": "What the queue-move check found about a reorder that has already landed. Empty for every other verb, and empty for most moves -- silence is the normal outcome. A finding here never means the move was refused.",
            "items": {
              "$ref": "#/components/schemas/QueueMoveWarning"
            },
            "title": "Queue Warnings",
            "type": "array"
          },
          "replayed": {
            "description": "True when this operation had already been applied, so nothing was written and no log entry was added.",
            "title": "Replayed",
            "type": "boolean"
          },
          "task": {
            "$ref": "#/components/schemas/TaskRead-Input",
            "description": "The task as persisted and reloaded."
          },
          "warnings": {
            "description": "Post-commit side effects that failed, such as webhook delivery. A warning never means the task write failed; that would be an error.",
            "items": {
              "type": "string"
            },
            "title": "Warnings",
            "type": "array"
          }
        },
        "required": [
          "project_id",
          "replayed",
          "task"
        ],
        "title": "MutationResult",
        "type": "object"
      },
      "MutationResult-Output": {
        "description": "What a mutation did, for callers that need more than the new task.\n\nReturned only when a request asks for it with `?envelope=true`, so the existing\ntask-shaped responses stay exactly as they were. `replayed` is the field that\ncannot be derived any other way: a caller retrying after a timeout has no way to\ntell \"I did that\" from \"you already had\".",
        "properties": {
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The operation id the caller supplied, echoed back.",
            "title": "Operation Id"
          },
          "project_id": {
            "description": "Project the mutation addressed.",
            "title": "Project Id",
            "type": "string"
          },
          "queue_undo": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueueMovePlacement"
              },
              {
                "type": "null"
              }
            ],
            "description": "The placement that puts the task back where the move took it from, offered only alongside a queue warning and only for a single-task move."
          },
          "queue_warnings": {
            "description": "What the queue-move check found about a reorder that has already landed. Empty for every other verb, and empty for most moves -- silence is the normal outcome. A finding here never means the move was refused.",
            "items": {
              "$ref": "#/components/schemas/QueueMoveWarning"
            },
            "title": "Queue Warnings",
            "type": "array"
          },
          "replayed": {
            "description": "True when this operation had already been applied, so nothing was written and no log entry was added.",
            "title": "Replayed",
            "type": "boolean"
          },
          "task": {
            "$ref": "#/components/schemas/TaskRead-Output",
            "description": "The task as persisted and reloaded."
          },
          "warnings": {
            "description": "Post-commit side effects that failed, such as webhook delivery. A warning never means the task write failed; that would be an error.",
            "items": {
              "type": "string"
            },
            "title": "Warnings",
            "type": "array"
          }
        },
        "required": [
          "project_id",
          "replayed",
          "task"
        ],
        "title": "MutationResult",
        "type": "object"
      },
      "NextExplanationResponse": {
        "description": "Why this task is next -- the answer plus the work it stands in front of.\n\n``task`` is null when nothing is claimable, in which case ``skipped`` lists every\nopen task with the rule that excluded it. That is the listing a reader wants\nprecisely when a tool has just told them there is nothing to do.",
        "properties": {
          "band": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Band"
          },
          "empty_bands_above": {
            "items": {
              "type": "string"
            },
            "title": "Empty Bands Above",
            "type": "array"
          },
          "queue_position": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Queue Position"
          },
          "skipped": {
            "items": {
              "$ref": "#/components/schemas/SkippedTaskRead"
            },
            "title": "Skipped",
            "type": "array"
          },
          "task": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Task"
          }
        },
        "title": "NextExplanationResponse",
        "type": "object"
      },
      "NoteActionRequest": {
        "description": "A human action carrying an optional note.\n\nDistinct from ``FeedbackActionRequest`` in exactly one way, and it is the one that\nmatters: the text is optional. Approving, and releasing a hold, are complete acts\non their own -- a required field here would make \"yes, go\" impossible to say\nwithout saying more, which is what pushed approvals-with-a-comment onto the\nrequest-changes path in the first place (task-228). ``None`` and ``\"\"`` both mean\nno note, and the route then writes precisely what it wrote before this existed.",
        "properties": {
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional note, recorded verbatim in the handoff prompt and the log.",
            "examples": [
              "Fold the naming nit in before you merge."
            ],
            "title": "Note"
          },
          "user": {
            "description": "User performing the action",
            "examples": [
              "jeff"
            ],
            "title": "User",
            "type": "string"
          }
        },
        "required": [
          "user"
        ],
        "title": "NoteActionRequest",
        "type": "object"
      },
      "OpenQuestion": {
        "description": "One question on an open task with no threaded answer (Q-1).",
        "properties": {
          "entry_id": {
            "title": "Entry Id",
            "type": "integer"
          },
          "hours_open": {
            "title": "Hours Open",
            "type": "number"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "entry_id",
          "hours_open"
        ],
        "title": "OpenQuestion",
        "type": "object"
      },
      "Outcome": {
        "description": "How a task ended. Set if and only if lifecycle is closed.",
        "enum": [
          "completed",
          "cancelled",
          "superseded",
          "duplicate"
        ],
        "title": "Outcome",
        "type": "string"
      },
      "PlaybookAcceptance": {
        "additionalProperties": false,
        "description": "One acceptance criterion the run task is created with.",
        "properties": {
          "text": {
            "minLength": 1,
            "title": "Text",
            "type": "string"
          },
          "verify": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional prose for a person, mirroring the task field: how somebody would satisfy themselves this criterion holds. Never executed.",
            "title": "Verify"
          }
        },
        "required": [
          "text"
        ],
        "title": "PlaybookAcceptance",
        "type": "object"
      },
      "PlaybookCollection": {
        "description": "What a project's playbooks directory holds.\n\nInvalid files are reported beside valid ones for the reason ``/tasks/broken``\nexists: a file that fails validation and then vanishes from the listing reads as a\nplaybook nobody ever wrote.",
        "properties": {
          "directory": {
            "title": "Directory",
            "type": "string"
          },
          "exists": {
            "description": "False when the directory has not been created. Not an error: it is the state every project starts in, and `agentjobs playbook init` leaves it.",
            "title": "Exists",
            "type": "boolean"
          },
          "identity": {
            "$ref": "#/components/schemas/ReviewIdentity",
            "description": "Who a run started from this listing would be attributed to, or why nobody can be. Carried on the listing because the Run button has to be disabled with a reason rather than pressable into a refusal, and this is the only response the playbooks page reads."
          },
          "playbooks": {
            "items": {
              "$ref": "#/components/schemas/PlaybookRead"
            },
            "title": "Playbooks",
            "type": "array"
          },
          "problems": {
            "items": {
              "$ref": "#/components/schemas/PlaybookProblemRead"
            },
            "title": "Problems",
            "type": "array"
          }
        },
        "required": [
          "directory",
          "exists",
          "identity"
        ],
        "title": "PlaybookCollection",
        "type": "object"
      },
      "PlaybookDifficulty": {
        "description": "The capability the work needs, in the task-156 vocabulary (§7.2).",
        "enum": [
          "routine",
          "standard",
          "hard"
        ],
        "title": "PlaybookDifficulty",
        "type": "string"
      },
      "PlaybookGate": {
        "additionalProperties": false,
        "description": "Where a run must stop for a human, stated declaratively (§3.2).",
        "properties": {
          "before": {
            "description": "The verb this gate stands in front of.",
            "minLength": 1,
            "title": "Before",
            "type": "string"
          },
          "what": {
            "description": "What must be true before it is passed.",
            "minLength": 1,
            "title": "What",
            "type": "string"
          }
        },
        "required": [
          "before",
          "what"
        ],
        "title": "PlaybookGate",
        "type": "object"
      },
      "PlaybookProblemRead": {
        "description": "One reason a file in the playbooks directory is not a valid playbook.",
        "properties": {
          "field": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Field"
          },
          "filename": {
            "title": "Filename",
            "type": "string"
          },
          "message": {
            "title": "Message",
            "type": "string"
          }
        },
        "required": [
          "filename",
          "message"
        ],
        "title": "PlaybookProblemRead",
        "type": "object"
      },
      "PlaybookRead": {
        "additionalProperties": false,
        "description": "A playbook's contract, plus where it was read from and (optionally) its brief.\n\nSubclasses the contract rather than restating it, so a field added to the\nfrontmatter model reaches the API without a second edit that can be forgotten.",
        "properties": {
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The brief: opaque markdown prose an agent reads. Present on the single-playbook route and omitted from the collection, which is a discovery listing rather than a way to fetch every brief at once.",
            "title": "Body"
          },
          "description": {
            "minLength": 1,
            "title": "Description",
            "type": "string"
          },
          "difficulty": {
            "$ref": "#/components/schemas/PlaybookDifficulty"
          },
          "filename": {
            "description": "The file this was read from, e.g. groom.md.",
            "title": "Filename",
            "type": "string"
          },
          "gates": {
            "items": {
              "$ref": "#/components/schemas/PlaybookGate"
            },
            "title": "Gates",
            "type": "array"
          },
          "name": {
            "description": "Must equal the filename stem.",
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "run_task": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PlaybookRunTask"
              },
              {
                "type": "null"
              }
            ]
          },
          "target": {
            "$ref": "#/components/schemas/PlaybookTarget"
          },
          "verbs": {
            "items": {
              "type": "string"
            },
            "title": "Verbs",
            "type": "array"
          }
        },
        "required": [
          "name",
          "description",
          "target",
          "difficulty",
          "filename"
        ],
        "title": "PlaybookRead",
        "type": "object"
      },
      "PlaybookRunRequestBody": {
        "description": "Ask AgentJobs to run a playbook.\n\nThe same three fields the dispatch endpoint takes, because this **is** that\nendpoint with a brief attached (design section 7.1). There is no field for the\nbrief, no field naming a runner, and no field naming argv: a playbook declares\ndifficulty and the machine binds it, which is decision P6 and is not negotiable\nfrom a request body.",
        "properties": {
          "group": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner group to choose from, overriding the project's. Names a group this machine already defines; it cannot open a gate that is closed.",
            "title": "Group"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What the human typed, when a task-target run's record could not brief an agent on its own. Becomes the body of the authorising entry.",
            "title": "Note"
          },
          "task": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The task to run a task-target playbook against. Refused for a project-target playbook, which creates its own run task.",
            "title": "Task"
          },
          "user": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The signed-in human asking for this run. Must be an actor this project configures with 'kind: human'. Required for a project-target playbook, which creates a run task attributed to them -- that creation entry is the authorisation. For a task-target playbook it is task-188's authorising entry, written onto the target task exactly as POST /dispatch writes it.",
            "title": "User"
          }
        },
        "title": "PlaybookRunRequestBody",
        "type": "object"
      },
      "PlaybookRunStarted": {
        "description": "A started playbook run: the dispatch, plus which brief it was given.",
        "properties": {
          "caused_by": {
            "description": "The log entry this dispatch is attributed to. 0 while a dispatch is queued -- the authorising entry is written when it starts, exactly as for a click.",
            "title": "Caused By",
            "type": "integer"
          },
          "created_run_task": {
            "description": "True when this run created the task it dispatched, which is every project-target run. False for a task-target run, which dispatches the task you named.",
            "title": "Created Run Task",
            "type": "boolean"
          },
          "group": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner group it was selected from, when one participated.",
            "title": "Group"
          },
          "mode": {
            "description": "session or batch. Empty while a dispatch is queued.",
            "title": "Mode",
            "type": "string"
          },
          "over_ceiling": {
            "default": false,
            "description": "True when this run was started above limits.max_concurrent_runs because a person chose to (task-461). The machine is over its ceiling until it ends, and the slot board says so rather than clipping the count.",
            "title": "Over Ceiling",
            "type": "boolean"
          },
          "playbook": {
            "description": "The playbook whose brief this run has.",
            "title": "Playbook",
            "type": "string"
          },
          "playbook_hash": {
            "description": "Algorithm-prefixed content hash of the file at instantiation, pinned on the task's dispatch entry so the record says which brief ran.",
            "title": "Playbook Hash",
            "type": "string"
          },
          "playbook_path": {
            "description": "Where it was read from, project-relative.",
            "title": "Playbook Path",
            "type": "string"
          },
          "posture": {
            "description": "What the run is permitted to do. Empty while a dispatch is queued: the posture is resolved by the gates when it starts, not when it was queued.",
            "title": "Posture",
            "type": "string"
          },
          "queue_position": {
            "default": 0,
            "description": "1-based place in line while queued; 0 for a dispatch that started.",
            "title": "Queue Position",
            "type": "integer"
          },
          "queued": {
            "default": false,
            "description": "True when the machine was full and the caller sent if_full=queue: nothing has started, and the entry waits in the machine's dispatch queue.",
            "title": "Queued",
            "type": "boolean"
          },
          "queued_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the entry joined the queue, UTC.",
            "title": "Queued At"
          },
          "run_id": {
            "description": "AgentJobs' identifier for this run. On a queued dispatch this is the queue entry's id, which is what the cancel route takes while it waits.",
            "title": "Run Id",
            "type": "string"
          },
          "runner": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runner that was selected and started.",
            "title": "Runner"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Session mode only, and assigned by the CLI rather than by us.",
            "title": "Session Id"
          },
          "task_id": {
            "description": "The task the run is working.",
            "title": "Task Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "mode",
          "posture",
          "task_id",
          "caused_by",
          "playbook",
          "playbook_path",
          "playbook_hash",
          "created_run_task"
        ],
        "title": "PlaybookRunStarted",
        "type": "object"
      },
      "PlaybookRunTask": {
        "additionalProperties": false,
        "description": "Defaults for the run task a ``target: project`` run creates (§3.2).\n\nNothing in this module creates that task -- instantiation is task-215. These are\nthe values it will read.",
        "properties": {
          "acceptance": {
            "items": {
              "$ref": "#/components/schemas/PlaybookAcceptance"
            },
            "title": "Acceptance",
            "type": "array"
          },
          "category": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Project taxonomy for the run task. Not validated against the project's configured categories here: a playbook is read without reference to any one project's config, and the manager validates the category at creation.",
            "title": "Category"
          },
          "priority": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Priority"
              },
              {
                "type": "null"
              }
            ]
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "minLength": 1,
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "title"
        ],
        "title": "PlaybookRunTask",
        "type": "object"
      },
      "PlaybookTarget": {
        "description": "What a run is aimed at (§3.2).",
        "enum": [
          "project",
          "task"
        ],
        "title": "PlaybookTarget",
        "type": "string"
      },
      "Priority": {
        "description": "Relative urgency.",
        "enum": [
          "low",
          "medium",
          "high",
          "critical"
        ],
        "title": "Priority",
        "type": "string"
      },
      "ProgressUpdateRequest": {
        "additionalProperties": false,
        "description": "Progress update payload appended to the task log.",
        "properties": {
          "author": {
            "title": "Author",
            "type": "string"
          },
          "details": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Details"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "summary": {
            "title": "Summary",
            "type": "string"
          }
        },
        "required": [
          "author",
          "summary"
        ],
        "title": "ProgressUpdateRequest",
        "type": "object"
      },
      "ProjectActor": {
        "description": "One actor a project's config defines.",
        "properties": {
          "display_name": {
            "title": "Display Name",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "id",
          "kind",
          "display_name"
        ],
        "title": "ProjectActor",
        "type": "object"
      },
      "ProjectInitializationRequest": {
        "description": "Create a project in an existing directory, then register it.",
        "properties": {
          "id": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Id"
          },
          "path": {
            "minLength": 1,
            "title": "Path",
            "type": "string"
          },
          "playbooks_directory": {
            "default": "playbooks",
            "minLength": 1,
            "title": "Playbooks Directory",
            "type": "string"
          },
          "port": {
            "default": 8765,
            "maximum": 65535.0,
            "minimum": 1.0,
            "title": "Port",
            "type": "integer"
          },
          "project_name": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Project Name"
          },
          "prompts_directory": {
            "default": "prompts",
            "minLength": 1,
            "title": "Prompts Directory",
            "type": "string"
          },
          "tasks_directory": {
            "default": "tasks",
            "minLength": 1,
            "title": "Tasks Directory",
            "type": "string"
          },
          "user": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "User"
          }
        },
        "required": [
          "path"
        ],
        "title": "ProjectInitializationRequest",
        "type": "object"
      },
      "ProjectInspection": {
        "description": "What the onboarding page should do with an inspected path.",
        "properties": {
          "action": {
            "enum": [
              "register",
              "initialize"
            ],
            "title": "Action",
            "type": "string"
          },
          "path": {
            "title": "Path",
            "type": "string"
          },
          "project_name": {
            "title": "Project Name",
            "type": "string"
          },
          "suggested_id": {
            "title": "Suggested Id",
            "type": "string"
          }
        },
        "required": [
          "path",
          "action",
          "project_name",
          "suggested_id"
        ],
        "title": "ProjectInspection",
        "type": "object"
      },
      "ProjectInspectionRequest": {
        "description": "A path to inspect before the user confirms a write or registration.",
        "properties": {
          "path": {
            "minLength": 1,
            "title": "Path",
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "title": "ProjectInspectionRequest",
        "type": "object"
      },
      "ProjectRegistrationRequest": {
        "description": "Register a directory that is already an AgentJobs project.",
        "properties": {
          "id": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Id"
          },
          "name": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Name"
          },
          "path": {
            "minLength": 1,
            "title": "Path",
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "title": "ProjectRegistrationRequest",
        "type": "object"
      },
      "ProjectResponse": {
        "description": "A registered project as exposed to API consumers.",
        "properties": {
          "actors": {
            "items": {
              "$ref": "#/components/schemas/ProjectActor"
            },
            "title": "Actors",
            "type": "array"
          },
          "default_user": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The configured human. Present so a client can address a person; it is never an agent's mutation identity.",
            "title": "Default User"
          },
          "id": {
            "title": "Id",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "root": {
            "title": "Root",
            "type": "string"
          },
          "task_count": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Task Count"
          },
          "tasks_directory": {
            "title": "Tasks Directory",
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "root",
          "tasks_directory",
          "task_count"
        ],
        "title": "ProjectResponse",
        "type": "object"
      },
      "ProjectRevisionResponse": {
        "description": "Small file-derived change signal for one project's task collection.",
        "properties": {
          "revision": {
            "title": "Revision",
            "type": "string"
          },
          "task_count": {
            "title": "Task Count",
            "type": "integer"
          }
        },
        "required": [
          "revision",
          "task_count"
        ],
        "title": "ProjectRevisionResponse",
        "type": "object"
      },
      "PromoteRequest": {
        "additionalProperties": false,
        "description": "A draft's spec is finished; the task becomes claimable.",
        "properties": {
          "actor": {
            "description": "Who is promoting the task.",
            "title": "Actor",
            "type": "string"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional log entry body.",
            "title": "Body"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          }
        },
        "required": [
          "actor"
        ],
        "title": "PromoteRequest",
        "type": "object"
      },
      "PullArmRequest": {
        "description": "What a person chooses when they arm the pull mode.\n\nThe bound is required and has no default, which is the one piece of validation worth\narguing for: the difference between \"three runs\" and \"all night\" is the whole of the\ndecision being made, and a server-side default would make it on the person's behalf\nin the one place they are entitled to be asked.",
        "properties": {
          "bound_kind": {
            "default": "",
            "description": "`starts`, `until` or `open`. Required -- there is no default bound.",
            "title": "Bound Kind",
            "type": "string"
          },
          "posture": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The envelope pulled runs get. Omitted, they get the project's own default. Refused here, where a person is waiting for the answer, when it exceeds this project's machine-local ceiling.",
            "title": "Posture"
          },
          "starts": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "How many runs, for a `starts` bound.",
            "title": "Starts"
          },
          "until": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "An ISO-8601 moment to stop at, for an `until` bound.",
            "title": "Until"
          },
          "user": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The person arming it. Validated against the principal this request resolved to, exactly as a dispatch's `user` is: the identity every pulled run will be attributed to must be the caller's own, not one read out of a listing.",
            "title": "User"
          }
        },
        "title": "PullArmRequest",
        "type": "object"
      },
      "PullModeView": {
        "description": "Whether the pull mode is armed for this project, and what it would do (task-462).\n\nOn the dispatch state rather than in a call of its own, for the reason the capacity\nnumbers ride with the live runs: a control that read \"armed\" from one endpoint and\n\"two starts left\" from another would have two answers about one arming, taken a\nsecond apart.",
        "properties": {
          "armed": {
            "description": "Whether this project is pulling right now.",
            "title": "Armed",
            "type": "boolean"
          },
          "armed_at": {
            "default": "",
            "description": "When they armed it, UTC.",
            "title": "Armed At",
            "type": "string"
          },
          "armed_by": {
            "default": "",
            "description": "The person who armed it.",
            "title": "Armed By",
            "type": "string"
          },
          "arming_id": {
            "default": "",
            "description": "The arming's id. Empty when not armed.",
            "title": "Arming Id",
            "type": "string"
          },
          "bound": {
            "default": "",
            "description": "What is left of the bound, as one phrase a person reads on a card -- e.g. `1 of 3 starts used`. Composed on the server so the board and the CLI say the same words about the same row.",
            "title": "Bound",
            "type": "string"
          },
          "bound_kind": {
            "default": "",
            "description": "`starts` (a number of runs), `until` (a moment) or `open` (until disarmed).",
            "title": "Bound Kind",
            "type": "string"
          },
          "last_detail": {
            "default": "",
            "description": "Why it ended, in the mode's own words.",
            "title": "Last Detail",
            "type": "string"
          },
          "last_state": {
            "default": "",
            "description": "How the previous arming ended, when there was one and this project is not armed now: `disarmed`, `spent`, `expired` or `faulted`. The four are kept apart because *you turned it off* and *it kept failing* are different things to read.",
            "title": "Last State",
            "type": "string"
          },
          "next_task_id": {
            "default": "",
            "description": "What `task_next` says it would start next, so a person can reorder the queue before it happens. Empty when nothing is claimable, and empty when the project is not armed -- this field is about an arming, not a backlog.",
            "title": "Next Task Id",
            "type": "string"
          },
          "next_task_title": {
            "default": "",
            "title": "Next Task Title",
            "type": "string"
          },
          "posture": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The envelope pulled runs get, or null for the project's own default.",
            "title": "Posture"
          },
          "starts_left": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Runs it may still start, or null when the bound is not a count.",
            "title": "Starts Left"
          },
          "starts_used": {
            "default": 0,
            "description": "Runs this arming has already started.",
            "title": "Starts Used",
            "type": "integer"
          }
        },
        "required": [
          "armed"
        ],
        "title": "PullModeView",
        "type": "object"
      },
      "PushDeviceView": {
        "description": "One registered device, with its endpoint deliberately absent.\n\nA push endpoint is a URL anybody holding it can send a notification to, so it is\ntreated as a secret: the API answers with the push service's host, a label the\nperson chose, and what happened to the last attempt. ``healthy`` is false once a\ndevice has failed enough times in a row to be worth mentioning -- it is a report,\nnot a removal.",
        "properties": {
          "consecutive_failures": {
            "default": 0,
            "title": "Consecutive Failures",
            "type": "integer"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "detail": {
            "title": "Detail",
            "type": "string"
          },
          "healthy": {
            "default": true,
            "title": "Healthy",
            "type": "boolean"
          },
          "id": {
            "title": "Id",
            "type": "string"
          },
          "label": {
            "title": "Label",
            "type": "string"
          },
          "last_attempt_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Attempt At"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Error"
          },
          "last_status": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status"
          },
          "service": {
            "title": "Service",
            "type": "string"
          }
        },
        "required": [
          "id",
          "service",
          "label",
          "detail",
          "created_at"
        ],
        "title": "PushDeviceView",
        "type": "object"
      },
      "PushSendResult": {
        "description": "What one deliberate test push did.\n\n``outcome`` rather than the status alone: ``gone`` means the device was forgotten\nas a result, which a status code on its own would not tell the page.",
        "properties": {
          "error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Error"
          },
          "outcome": {
            "title": "Outcome",
            "type": "string"
          },
          "status": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Status"
          },
          "subscription_id": {
            "title": "Subscription Id",
            "type": "string"
          }
        },
        "required": [
          "subscription_id",
          "outcome"
        ],
        "title": "PushSendResult",
        "type": "object"
      },
      "PushStatusResponse": {
        "description": "What the notifications panel needs to render itself.\n\n``vapid_public_key`` is the application-server key a browser must subscribe\nagainst. It is public by construction -- the private half never leaves this\nprocess -- and reading it is what creates the machine's keypair on first use.",
        "properties": {
          "contact": {
            "title": "Contact",
            "type": "string"
          },
          "devices": {
            "items": {
              "$ref": "#/components/schemas/PushDeviceView"
            },
            "title": "Devices",
            "type": "array"
          },
          "poll_seconds": {
            "title": "Poll Seconds",
            "type": "number"
          },
          "vapid_public_key": {
            "title": "Vapid Public Key",
            "type": "string"
          },
          "watching": {
            "title": "Watching",
            "type": "boolean"
          }
        },
        "required": [
          "vapid_public_key",
          "contact",
          "watching",
          "poll_seconds"
        ],
        "title": "PushStatusResponse",
        "type": "object"
      },
      "PushSubscribeRequest": {
        "description": "A device asking to be woken.\n\nExactly the shape ``PushSubscription.toJSON()`` produces, plus two fields of ours,\nso the client can pass the browser's object through without reassembling it.\n\n``label`` is for the person -- \"Pixel 9\", \"iPad\" -- and is the only way to tell two\nrows apart in the UI, because the endpoint is never shown. ``detail`` is the\nlock-screen privacy setting for this device: ``task`` names the waiting task and is\nthe default, ``count`` withholds the name for a device with bystanders. Re-posting\nan endpoint that is already registered is how a device changes it -- the row keeps\nits id and its episode, so a toggle is not a re-arm.",
        "properties": {
          "detail": {
            "default": "task",
            "title": "Detail",
            "type": "string"
          },
          "endpoint": {
            "title": "Endpoint",
            "type": "string"
          },
          "keys": {
            "$ref": "#/components/schemas/PushSubscriptionKeys"
          },
          "label": {
            "default": "",
            "title": "Label",
            "type": "string"
          }
        },
        "required": [
          "endpoint",
          "keys"
        ],
        "title": "PushSubscribeRequest",
        "type": "object"
      },
      "PushSubscriptionKeys": {
        "description": "The two values a browser hands out with a subscription, base64url as it gives them.",
        "properties": {
          "auth": {
            "title": "Auth",
            "type": "string"
          },
          "p256dh": {
            "title": "P256Dh",
            "type": "string"
          }
        },
        "required": [
          "p256dh",
          "auth"
        ],
        "title": "PushSubscriptionKeys",
        "type": "object"
      },
      "PushTestRequest": {
        "description": "Which device to prove delivery to. Omitted means every registered device.",
        "properties": {
          "subscription_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subscription Id"
          }
        },
        "title": "PushTestRequest",
        "type": "object"
      },
      "PushTestResponse": {
        "description": "One result per device the test was aimed at.",
        "properties": {
          "results": {
            "items": {
              "$ref": "#/components/schemas/PushSendResult"
            },
            "title": "Results",
            "type": "array"
          }
        },
        "title": "PushTestResponse",
        "type": "object"
      },
      "PushUnsubscribeRequest": {
        "description": "A device asking to be forgotten, by whichever handle the caller has.\n\nThe page has the id; a service worker reacting to ``pushsubscriptionchange`` has\nonly the endpoint it is losing. Either identifies a row, and neither is an error\nwhen it matches nothing -- a device is routinely removed from both ends at once.",
        "properties": {
          "endpoint": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Endpoint"
          },
          "subscription_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subscription Id"
          }
        },
        "title": "PushUnsubscribeRequest",
        "type": "object"
      },
      "QuestionDraft": {
        "additionalProperties": false,
        "description": "One question an agent asks, before the manager has made an entry of it.\n\nDistinct from ``QuestionData`` by exactly one field -- the prose -- because the\nentry keeps its question in ``body`` where every other entry keeps its prose, and a\npayload model that duplicated it would give a reader two places to look. This is the\nargument shape; that is the stored shape.",
        "properties": {
          "body": {
            "description": "The question, addressed to a human.",
            "minLength": 1,
            "title": "Body",
            "type": "string"
          },
          "multi_select": {
            "default": false,
            "description": "Whether more than one option may be chosen.",
            "title": "Multi Select",
            "type": "boolean"
          },
          "options": {
            "description": "Offered answers, in display order.",
            "items": {
              "$ref": "#/components/schemas/QuestionOption"
            },
            "title": "Options",
            "type": "array"
          },
          "placeholder": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Hint for the free-text box, e.g. 'a number of minutes'.",
            "title": "Placeholder"
          }
        },
        "required": [
          "body"
        ],
        "title": "QuestionDraft",
        "type": "object"
      },
      "QuestionOption": {
        "additionalProperties": false,
        "description": "One answer an agent is offering for a ``question`` entry (task-017).\n\nThe label is what the human taps and what an ``answer`` entry records having been\nchosen, so it is the identity of the option and not a caption over one. There is\ndeliberately no separate id: a log is append-only, so a stored label cannot drift\naway from the option it names, and an id would only add a second thing to read.",
        "properties": {
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who holds the task once this option is chosen. Required with `dispatches: false` and forbidden otherwise."
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why that holder has it. Travels with `ball`."
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What picking this means. Shown under the label.",
            "title": "Description"
          },
          "dispatches": {
            "default": true,
            "description": "Whether choosing this hands work back to an agent. False for an answer that means *wait*, *stop* or *decline*, which records the choice and starts nothing.",
            "title": "Dispatches",
            "type": "boolean"
          },
          "label": {
            "description": "The choice itself. Short enough to be a button.",
            "minLength": 1,
            "title": "Label",
            "type": "string"
          },
          "recommended": {
            "default": false,
            "description": "The agent's own recommendation. Marks the option; it never preselects it, because a prefilled answer is one a tired reader submits without reading.",
            "title": "Recommended",
            "type": "boolean"
          }
        },
        "required": [
          "label"
        ],
        "title": "QuestionOption",
        "type": "object"
      },
      "QueueAssignmentRead": {
        "description": "One position a repair or a compaction wrote.",
        "properties": {
          "band": {
            "title": "Band",
            "type": "string"
          },
          "position": {
            "title": "Position",
            "type": "integer"
          },
          "task": {
            "title": "Task",
            "type": "string"
          }
        },
        "required": [
          "task",
          "band",
          "position"
        ],
        "title": "QueueAssignmentRead",
        "type": "object"
      },
      "QueueBandRead-Input": {
        "description": "One priority band, in queue order. Listed even when empty.",
        "properties": {
          "band": {
            "title": "Band",
            "type": "string"
          },
          "entries": {
            "items": {
              "$ref": "#/components/schemas/QueueEntryRead"
            },
            "title": "Entries",
            "type": "array"
          }
        },
        "required": [
          "band"
        ],
        "title": "QueueBandRead",
        "type": "object"
      },
      "QueueBandRead-Output": {
        "description": "One priority band, in queue order. Listed even when empty.",
        "properties": {
          "band": {
            "title": "Band",
            "type": "string"
          },
          "entries": {
            "items": {
              "$ref": "#/components/schemas/QueueEntryRead"
            },
            "title": "Entries",
            "type": "array"
          }
        },
        "required": [
          "band"
        ],
        "title": "QueueBandRead",
        "type": "object"
      },
      "QueueBrokenRead": {
        "description": "The queue could not be read, what is wrong with it, and what repairs it.\n\nNull on a healthy corpus. Present, the dashboard renders a banner naming the\noffending tasks instead of a next action computed from an order that does not\nexist -- design section 8's React row.",
        "properties": {
          "problems": {
            "items": {
              "$ref": "#/components/schemas/QueueProblemRead"
            },
            "title": "Problems",
            "type": "array"
          },
          "repair_command": {
            "title": "Repair Command",
            "type": "string"
          }
        },
        "required": [
          "repair_command"
        ],
        "title": "QueueBrokenRead",
        "type": "object"
      },
      "QueueCompactRequest": {
        "additionalProperties": false,
        "description": "Renumber one band back to 100, 200, 300...",
        "properties": {
          "actor": {
            "description": "Actor id asking for the operation.",
            "minLength": 1,
            "title": "Actor",
            "type": "string"
          },
          "band": {
            "$ref": "#/components/schemas/Priority",
            "description": "The band to compact. One band per request."
          },
          "operation_id": {
            "description": "Caller-generated UUID identifying this attempt.",
            "minLength": 1,
            "title": "Operation Id",
            "type": "string"
          }
        },
        "required": [
          "actor",
          "operation_id",
          "band"
        ],
        "title": "QueueCompactRequest",
        "type": "object"
      },
      "QueueCompactResponse": {
        "description": "The tasks a compaction renumbered, and what they were renumbered to.",
        "properties": {
          "band": {
            "title": "Band",
            "type": "string"
          },
          "moved": {
            "items": {
              "$ref": "#/components/schemas/QueueAssignmentRead"
            },
            "title": "Moved",
            "type": "array"
          }
        },
        "required": [
          "band"
        ],
        "title": "QueueCompactResponse",
        "type": "object"
      },
      "QueueEntryRead": {
        "description": "One task's place in line, and whether it can be taken.",
        "properties": {
          "ball": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ball"
          },
          "claimable": {
            "title": "Claimable",
            "type": "boolean"
          },
          "last_move": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueueMoveProvenanceRead"
              },
              {
                "type": "null"
              }
            ],
            "description": "The move that set this place. Null if it never moved."
          },
          "lifecycle": {
            "title": "Lifecycle",
            "type": "string"
          },
          "queue_position": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Queue Position"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why it is not claimable. Null when it is.",
            "title": "Reason"
          },
          "task": {
            "title": "Task",
            "type": "string"
          },
          "title": {
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "task",
          "title",
          "lifecycle",
          "claimable"
        ],
        "title": "QueueEntryRead",
        "type": "object"
      },
      "QueueKeepRequest": {
        "additionalProperties": false,
        "description": "Keep a queue position that was moved over a stated warning.\n\nThe other half of the notice the queue-move check raises: `undo` is an ordinary\nmove back, and this is what \"keep\" sends. It records the strong anchor -- a place a\nperson defended against an objection they had read -- which `reorder` may not move.",
        "properties": {
          "actor": {
            "description": "Actor id keeping the position.",
            "minLength": 1,
            "title": "Actor",
            "type": "string"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why it was kept. Omit it and the manager writes its own sentence.",
            "title": "Body"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "operation_id": {
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing a second anchor.",
            "minLength": 1,
            "title": "Operation Id",
            "type": "string"
          }
        },
        "required": [
          "operation_id",
          "actor"
        ],
        "title": "QueueKeepRequest",
        "type": "object"
      },
      "QueueMaintenanceRequest": {
        "additionalProperties": false,
        "description": "Repair or compaction: attributed, and required to say who asked.\n\n``operation_id`` is required for the same reason it is on a move -- a caller that\ncannot name its attempt cannot be told apart from one retrying -- but it is not\nreplayed from a ledger, because the ledger is per-task and these two operations act\non a whole band or a whole corpus. They do not need one: both are idempotent by\nconstruction, since repairing a repaired queue finds nothing to repair and\ncompacting a compacted band renumbers it to the numbers it already has. Running\ntwice and running once leave the same corpus, which is a stronger property than\nreplay rather than a weaker substitute for it.",
        "properties": {
          "actor": {
            "description": "Actor id asking for the operation.",
            "minLength": 1,
            "title": "Actor",
            "type": "string"
          },
          "operation_id": {
            "description": "Caller-generated UUID identifying this attempt.",
            "minLength": 1,
            "title": "Operation Id",
            "type": "string"
          }
        },
        "required": [
          "actor",
          "operation_id"
        ],
        "title": "QueueMaintenanceRequest",
        "type": "object"
      },
      "QueueMovePlacement": {
        "description": "A placement, in the shape the queue-move route accepts one.\n\nReturned as `queue_undo` so a client can offer one-click undo without deriving the\ninverse itself. It names a neighbour rather than a number, because a number can be\nrewritten by a rebalance while \"behind task-063\" cannot.",
        "properties": {
          "kind": {
            "description": "top, bottom, before or after.",
            "title": "Kind",
            "type": "string"
          },
          "target": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The neighbour, for before and after.",
            "title": "Target"
          }
        },
        "required": [
          "kind"
        ],
        "title": "QueueMovePlacement",
        "type": "object"
      },
      "QueueMoveProvenanceRead": {
        "description": "Where a task's place came from: the last ``queue_move`` that set it.\n\nThe anchor evidence a ``reorder`` run reads (``playbooks/reorder.md``), so it does\nnot have to fetch and scan every task's log to learn who put each task where.\n\nEvery field is present on every record. ``kind`` and ``anchor`` are nullable rather\nthan absent, so a reader always finds the key and only ever has to judge its value\n-- an absent ``kind`` and a ``kind`` of ``null`` would mean the same thing here and\nonly one of them can be checked in a single expression.",
        "properties": {
          "actor": {
            "description": "Actor id that wrote the move. Who executed it.",
            "title": "Actor",
            "type": "string"
          },
          "anchor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "'strong' when a human kept this place after reading the move's warnings. Null otherwise. A strong anchor is never moved by a reorder run.",
            "title": "Anchor"
          },
          "at": {
            "description": "When the move was written, ISO-8601.",
            "title": "At",
            "type": "string"
          },
          "body": {
            "description": "The reason recorded with the move. Where an agent-executed human decision says whose decision it was, which the actor alone cannot tell you.",
            "title": "Body",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "'human' or 'agent' per the project's actors vocabulary. Null when config does not define the id -- unknown, and not to be read as either kind.",
            "title": "Kind"
          }
        },
        "required": [
          "actor",
          "kind",
          "at",
          "body",
          "anchor"
        ],
        "title": "QueueMoveProvenanceRead",
        "type": "object"
      },
      "QueueMoveRequest": {
        "additionalProperties": false,
        "description": "Where in its band a task is being put. Exactly one placement, never two.\n\n\"before task-063 and also at the top\" is two answers to one question, and choosing\none of them on the caller's behalf is how a reorder ends up somewhere nobody asked\nfor. The manager refuses it; this refuses it a layer earlier, where the caller can\nstill see which fields it sent.",
        "properties": {
          "actor": {
            "description": "Actor id performing the move.",
            "minLength": 1,
            "title": "Actor",
            "type": "string"
          },
          "after": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Place it behind this task.",
            "title": "After"
          },
          "before": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Place it ahead of this task.",
            "title": "Before"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Log body for the queue_move entry. Omit it and the manager writes its own.",
            "title": "Body"
          },
          "bottom": {
            "default": false,
            "description": "Place it last in its band.",
            "title": "Bottom",
            "type": "boolean"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "operation_id": {
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of moving the task twice.",
            "minLength": 1,
            "title": "Operation Id",
            "type": "string"
          },
          "top": {
            "default": false,
            "description": "Place it first in its band.",
            "title": "Top",
            "type": "boolean"
          },
          "with_children": {
            "default": false,
            "description": "Carry the task's open same-band descendants with it, contiguously.",
            "title": "With Children",
            "type": "boolean"
          }
        },
        "required": [
          "operation_id",
          "actor"
        ],
        "title": "QueueMoveRequest",
        "type": "object"
      },
      "QueueMoveWarning": {
        "description": "One deterministic finding about a move that has already landed.\n\nComputed synchronously by `agentjobs.queue_check` from facts the move handler had\nalready read -- no model, no dispatch, no delay. `kind` is the closed vocabulary a\nclient branches on; `message` is the sentence every surface shows, written once in\nthe check so the CLI, this response and the browser cannot describe one fact three\nways.",
        "properties": {
          "kind": {
            "description": "One of: queue_broken, promoted_unclaimable, above_prerequisite, demoted_blocker, no_op.",
            "title": "Kind",
            "type": "string"
          },
          "message": {
            "description": "The finding, as a sentence meant to be shown as-is.",
            "title": "Message",
            "type": "string"
          },
          "tasks": {
            "description": "Every task this finding is about, even when the message names fewer.",
            "items": {
              "type": "string"
            },
            "title": "Tasks",
            "type": "array"
          }
        },
        "required": [
          "kind",
          "message"
        ],
        "title": "QueueMoveWarning",
        "type": "object"
      },
      "QueueProblemRead": {
        "description": "One broken queue rule, named well enough to fix by hand.",
        "properties": {
          "band": {
            "title": "Band",
            "type": "string"
          },
          "kind": {
            "title": "Kind",
            "type": "string"
          },
          "message": {
            "title": "Message",
            "type": "string"
          },
          "position": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Position"
          },
          "tasks": {
            "items": {
              "type": "string"
            },
            "title": "Tasks",
            "type": "array"
          }
        },
        "required": [
          "kind",
          "band",
          "message"
        ],
        "title": "QueueProblemRead",
        "type": "object"
      },
      "QueueRepairResponse": {
        "description": "What a repair did. Everything it guessed is named, which is the point.",
        "properties": {
          "assigned": {
            "items": {
              "$ref": "#/components/schemas/QueueAssignmentRead"
            },
            "title": "Assigned",
            "type": "array"
          },
          "changed": {
            "title": "Changed",
            "type": "boolean"
          },
          "rebalanced": {
            "items": {
              "type": "string"
            },
            "title": "Rebalanced",
            "type": "array"
          },
          "report": {
            "description": "The same result rendered for a terminal.",
            "title": "Report",
            "type": "string"
          },
          "unrepairable": {
            "items": {
              "type": "string"
            },
            "title": "Unrepairable",
            "type": "array"
          }
        },
        "required": [
          "changed",
          "report"
        ],
        "title": "QueueRepairResponse",
        "type": "object"
      },
      "QueueResponse": {
        "description": "The whole ordered backlog. This is the list a human reviews.\n\nIt renders a broken queue rather than refusing to: ``problems`` says what is wrong\nand ``repair_command`` says what to type. That is design section 8's deliberate\nexception -- you have to be able to see a broken queue in order to fix it.",
        "properties": {
          "bands": {
            "items": {
              "$ref": "#/components/schemas/QueueBandRead-Output"
            },
            "title": "Bands",
            "type": "array"
          },
          "problems": {
            "items": {
              "$ref": "#/components/schemas/QueueProblemRead"
            },
            "title": "Problems",
            "type": "array"
          },
          "repair_command": {
            "title": "Repair Command",
            "type": "string"
          }
        },
        "required": [
          "repair_command"
        ],
        "title": "QueueResponse",
        "type": "object"
      },
      "QueuedDispatchState": {
        "additionalProperties": false,
        "description": "A dispatch of this task waiting for a free slot on this machine (task-459).\n\nDerived on read from the machine's execution store and never stored on the task, for\nthe reason ``enqueue()`` deliberately does not claim the task: every dispatch gate is\njudged when a slot frees, so the task genuinely is ``ready``/``agent``/``available``\nuntil something starts. A flag written onto the record would be a second copy of a\nfact the queue owns, and it would go stale the moment an entry is cancelled, refused\nat start, or started -- none of which writes a counterpart to the record.\n\nThe fields are the queued rail's, minus the ones a task already knows about itself\n(its id, its project, its own URL), so the slot board and a task page cannot disagree\nabout what a waiting dispatch is called.",
        "properties": {
          "detail": {
            "default": "",
            "title": "Detail",
            "type": "string"
          },
          "paused_by": {
            "default": "",
            "title": "Paused By",
            "type": "string"
          },
          "position": {
            "title": "Position",
            "type": "integer"
          },
          "queue_id": {
            "title": "Queue Id",
            "type": "string"
          },
          "queued_at": {
            "title": "Queued At",
            "type": "string"
          },
          "queued_by": {
            "default": "",
            "title": "Queued By",
            "type": "string"
          },
          "source": {
            "default": "manual",
            "title": "Source",
            "type": "string"
          },
          "status": {
            "default": "queued",
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "queue_id",
          "position",
          "queued_at"
        ],
        "title": "QueuedDispatchState",
        "type": "object"
      },
      "QueuedDispatchView": {
        "description": "One authorised dispatch waiting for a slot (task-459).\n\nBeside the live runs rather than in a call of its own, for the reason the capacity\nnumbers are: a board that read \"three of three busy\" from one endpoint and \"two\nwaiting\" from another would have two answers about one machine, taken a second apart.",
        "properties": {
          "detail": {
            "default": "",
            "description": "Why it is still waiting, when a start has already been tried and refused for a condition that clears on its own.",
            "title": "Detail",
            "type": "string"
          },
          "paused_by": {
            "default": "",
            "description": "The incident id holding this entry off (task-463), or empty when nothing is. An entry with this set is not being tried at all: no attempt, no hourly cap, nothing written to its task. It keeps its position.",
            "title": "Paused By",
            "type": "string"
          },
          "position": {
            "description": "1-based place in line. FIFO by when it was queued.",
            "title": "Position",
            "type": "integer"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "project_name": {
            "default": "",
            "title": "Project Name",
            "type": "string"
          },
          "queue_id": {
            "description": "The entry's id. It is what `POST .../dispatch/runs/{run_id}/cancel` takes while the dispatch is waiting -- a queued entry cancels through the same route as the run it has not become.",
            "title": "Queue Id",
            "type": "string"
          },
          "queued_at": {
            "description": "When it joined the queue, UTC.",
            "title": "Queued At",
            "type": "string"
          },
          "queued_by": {
            "default": "",
            "description": "Who asked for it. Empty for a dispatch nobody signed.",
            "title": "Queued By",
            "type": "string"
          },
          "source": {
            "default": "manual",
            "description": "`manual` today; `pull` is reserved.",
            "title": "Source",
            "type": "string"
          },
          "status": {
            "description": "`queued`, or `starting` for the seconds a tick is putting it through the dispatch gates. A `starting` entry is not yet a run and may still be refused.",
            "title": "Status",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "task_title": {
            "default": "",
            "title": "Task Title",
            "type": "string"
          },
          "task_url": {
            "description": "Where this entry's task is, in this app.",
            "title": "Task Url",
            "type": "string"
          },
          "waiting_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "How long it has been waiting, computed on the server. The phone reading this page is not on the clock that wrote `queued_at`.",
            "title": "Waiting Seconds"
          }
        },
        "required": [
          "queue_id",
          "position",
          "task_id",
          "project_id",
          "queued_at",
          "status",
          "task_url"
        ],
        "title": "QueuedDispatchView",
        "type": "object"
      },
      "RecentClosuresView": {
        "description": "The region's whole answer, with the bounds it was computed under.",
        "properties": {
          "closures": {
            "items": {
              "$ref": "#/components/schemas/ClosureView"
            },
            "title": "Closures",
            "type": "array"
          },
          "generated_at": {
            "description": "When this answer was assembled, in UTC.",
            "title": "Generated At",
            "type": "string"
          },
          "limit": {
            "description": "The most rows this answer could have held.",
            "title": "Limit",
            "type": "integer"
          },
          "window_days": {
            "description": "How far back it looked. On the wire because the empty state says it -- *nothing has finished in the last 7 days* -- and a page that hard-coded the number would keep saying seven after this endpoint stopped meaning it.",
            "title": "Window Days",
            "type": "integer"
          }
        },
        "required": [
          "closures",
          "limit",
          "window_days",
          "generated_at"
        ],
        "title": "RecentClosuresView",
        "type": "object"
      },
      "RedactRequest": {
        "additionalProperties": false,
        "description": "Replace the text of one prose region with a stated redaction (task-376).\n\nReachable over HTTP because the CLI is a service client. It is the only verb that\nreaches into the append-only log, and it stays that: the request names one region,\nsupplies the replacement, and states a reason, all of which the manager records.",
        "properties": {
          "actor": {
            "description": "Who is redacting.",
            "title": "Actor",
            "type": "string"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "field": {
            "description": "The region: a task field name, or 'log[<id>].body'.",
            "examples": [
              "spec.description",
              "log[12].body"
            ],
            "title": "Field",
            "type": "string"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "reason": {
            "description": "Why the text was removed. Recorded verbatim.",
            "title": "Reason",
            "type": "string"
          },
          "replacement": {
            "description": "What the region says instead.",
            "title": "Replacement",
            "type": "string"
          }
        },
        "required": [
          "actor",
          "field",
          "replacement",
          "reason"
        ],
        "title": "RedactRequest",
        "type": "object"
      },
      "RejectActionRequest": {
        "description": "Reject task with reason.",
        "properties": {
          "reason": {
            "description": "Rejection reason",
            "examples": [
              "Out of scope"
            ],
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          },
          "user": {
            "description": "User performing the action",
            "examples": [
              "jeff"
            ],
            "title": "User",
            "type": "string"
          }
        },
        "required": [
          "user",
          "reason"
        ],
        "title": "RejectActionRequest",
        "type": "object"
      },
      "RelayAuthorizationRequest": {
        "additionalProperties": false,
        "description": "Relay a human's authorisation of a dispatch, as the agent they said it to (task-506).\n\nIts own request rather than a field on :class:`LogAppendRequest`, because it is its\nown capability: a ``run`` holds the log route and may not hold this. The route table\nis keyed by endpoint, so a separate endpoint is how one caller is refused what the\nother is allowed.\n\n``actor`` is the agent writing the entry -- checked against the principal like every\nother actor, so a run cannot name a person here either. ``authorized_by`` is the\nhuman, and is refused unless this project configures them ``kind: human``.",
        "properties": {
          "actor": {
            "description": "Actor id writing the entry: the agent relaying.",
            "title": "Actor",
            "type": "string"
          },
          "ask": {
            "description": "What they asked for, in the relaying agent's words. Becomes the entry body, where the repository's paraphrase rule reaches it. Never a quotation.",
            "minLength": 1,
            "title": "Ask",
            "type": "string"
          },
          "authorized_by": {
            "description": "Actor id of the human who authorised it. Must be kind: human.",
            "title": "Authorized By",
            "type": "string"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "surface": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Where they said it, for a reader. Never read back by a check.",
            "title": "Surface"
          }
        },
        "required": [
          "actor",
          "authorized_by",
          "ask"
        ],
        "title": "RelayAuthorizationRequest",
        "type": "object"
      },
      "ReleaseRequest": {
        "additionalProperties": false,
        "description": "An agent bows out; the task returns to the pool.",
        "properties": {
          "actor": {
            "description": "Who is releasing the task.",
            "title": "Actor",
            "type": "string"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional log entry body.",
            "title": "Body"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          }
        },
        "required": [
          "actor"
        ],
        "title": "ReleaseRequest",
        "type": "object"
      },
      "ReprioritizeRequest": {
        "additionalProperties": false,
        "description": "Change a task's band, and optionally where it lands inside the new one.\n\nThe default placement is the bottom of the target band. A band change already says\neverything about urgency; where inside the new band it goes is a separate question\nthe caller may answer, and \"bottom\" is the answer that assumes least.",
        "properties": {
          "actor": {
            "description": "Actor id performing the move.",
            "minLength": 1,
            "title": "Actor",
            "type": "string"
          },
          "after": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Place it behind this task.",
            "title": "After"
          },
          "before": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Place it ahead of this task.",
            "title": "Before"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Log body for the queue_move entry. Omit it and the manager writes its own.",
            "title": "Body"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "operation_id": {
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of moving the task twice.",
            "minLength": 1,
            "title": "Operation Id",
            "type": "string"
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "description": "The band to move the task into."
          },
          "top": {
            "default": false,
            "description": "Place it first in the target band.",
            "title": "Top",
            "type": "boolean"
          }
        },
        "required": [
          "operation_id",
          "actor",
          "priority"
        ],
        "title": "ReprioritizeRequest",
        "type": "object"
      },
      "ReviewIdentity": {
        "description": "Configured human identity used by review mutations, or why none is safe.",
        "properties": {
          "detail": {
            "title": "Detail",
            "type": "string"
          },
          "ok": {
            "title": "Ok",
            "type": "boolean"
          },
          "problem": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Problem"
          },
          "user": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "User"
          }
        },
        "required": [
          "ok",
          "user",
          "problem",
          "detail"
        ],
        "title": "ReviewIdentity",
        "type": "object"
      },
      "ReviewPoint": {
        "description": "How long a person took, per week (R2, R3, Q-2).\n\n``exits`` is every departure from review in the bucket, approval or not, because a\nrevise request is also the owner answering; ``wait_*`` is measured over all of them.\nAn approval is an exit to agent/work or a close from review (section 17.2), and a\nfirst-time approval is one on the task's first review round. Questions are\nbucketed by when they were asked.",
        "properties": {
          "answer_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Answer P50 Hours"
          },
          "answer_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Answer P90 Hours"
          },
          "answered": {
            "default": 0,
            "title": "Answered",
            "type": "integer"
          },
          "approvals": {
            "default": 0,
            "title": "Approvals",
            "type": "integer"
          },
          "bucket": {
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "estimated": {
            "default": false,
            "title": "Estimated",
            "type": "boolean"
          },
          "exits": {
            "default": 0,
            "title": "Exits",
            "type": "integer"
          },
          "first_time_approvals": {
            "default": 0,
            "title": "First Time Approvals",
            "type": "integer"
          },
          "questions": {
            "default": 0,
            "title": "Questions",
            "type": "integer"
          },
          "wait_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Wait P50 Hours"
          },
          "wait_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Wait P90 Hours"
          }
        },
        "required": [
          "bucket"
        ],
        "title": "ReviewPoint",
        "type": "object"
      },
      "RunPoint": {
        "description": "Dispatched runs per bucket of start, at the spine grain (R-1 to R-4).\n\nA run is attributed whole to the bucket it started in. ``in_flight`` is the runs\nwith no end yet, which are in ``runs`` and in no outcome.",
        "properties": {
          "agent_hours": {
            "default": 0.0,
            "title": "Agent Hours",
            "type": "number"
          },
          "bucket": {
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "duration_p50_min": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration P50 Min"
          },
          "duration_p90_min": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration P90 Min"
          },
          "estimated": {
            "default": false,
            "title": "Estimated",
            "type": "boolean"
          },
          "in_flight": {
            "default": 0,
            "title": "In Flight",
            "type": "integer"
          },
          "outcomes": {
            "additionalProperties": {
              "type": "integer"
            },
            "title": "Outcomes",
            "type": "object"
          },
          "runs": {
            "default": 0,
            "title": "Runs",
            "type": "integer"
          },
          "sample": {
            "default": 0,
            "title": "Sample",
            "type": "integer"
          },
          "triggers": {
            "additionalProperties": {
              "type": "integer"
            },
            "title": "Triggers",
            "type": "object"
          }
        },
        "required": [
          "bucket"
        ],
        "title": "RunPoint",
        "type": "object"
      },
      "ScopedDependencyEdge": {
        "description": "A sequence arrow in one umbrella's contained task graph.",
        "properties": {
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Note"
          },
          "source": {
            "title": "Source",
            "type": "string"
          },
          "source_contained": {
            "title": "Source Contained",
            "type": "boolean"
          },
          "source_exists": {
            "title": "Source Exists",
            "type": "boolean"
          },
          "target": {
            "title": "Target",
            "type": "string"
          },
          "target_contained": {
            "title": "Target Contained",
            "type": "boolean"
          },
          "target_exists": {
            "title": "Target Exists",
            "type": "boolean"
          }
        },
        "required": [
          "source",
          "target",
          "note",
          "source_exists",
          "target_exists",
          "source_contained",
          "target_contained"
        ],
        "title": "ScopedDependencyEdge",
        "type": "object"
      },
      "SegmentAmong": {
        "description": "Per segment: how many of the bucket's tasks had any time in it, and their median.",
        "properties": {
          "p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "P50 Hours"
          },
          "tasks": {
            "title": "Tasks",
            "type": "integer"
          }
        },
        "required": [
          "tasks"
        ],
        "title": "SegmentAmong",
        "type": "object"
      },
      "SegmentPoint": {
        "description": "Where a completed task's time went, per week of close (sections 17 and 18.1).\n\nFive segments -- queue, work, waiting, review, finish -- partition each task's open\nlife exactly, so ``queue + work + waiting + review + finish = total`` per task to\nthe second. The percentiles here are per segment over the bucket's tasks, so the\nstack's height is a sum of medians and not the median total; ``total_p50_hours``\nis the latter. ``first_review_*`` is the owner's dispatch-to-handoff (S3): first\nclaim to first review entry, bucketed by the review entry.",
        "properties": {
          "among": {
            "additionalProperties": {
              "$ref": "#/components/schemas/SegmentAmong"
            },
            "description": "Per segment, the tasks with a nonzero value.",
            "title": "Among",
            "type": "object"
          },
          "bucket": {
            "description": "First day of the week, in the reporting zone.",
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "estimated": {
            "description": "A task in this bucket has a reconstructed boundary row.",
            "title": "Estimated",
            "type": "boolean"
          },
          "excluded": {
            "description": "Closed by an import row: close time unknown.",
            "title": "Excluded",
            "type": "integer"
          },
          "finish_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finish P50 Hours"
          },
          "finish_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finish P90 Hours"
          },
          "first_review_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "First Review P50 Hours"
          },
          "first_review_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "First Review P90 Hours"
          },
          "first_review_sample": {
            "default": 0,
            "title": "First Review Sample",
            "type": "integer"
          },
          "queue_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Queue P50 Hours"
          },
          "queue_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Queue P90 Hours"
          },
          "review_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Review P50 Hours"
          },
          "review_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Review P90 Hours"
          },
          "sample": {
            "description": "Completed tasks closed in this bucket, in the sample.",
            "title": "Sample",
            "type": "integer"
          },
          "total_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total P50 Hours"
          },
          "total_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total P90 Hours"
          },
          "unreviewed": {
            "description": "Tasks with no review handoff (section 17.3).",
            "title": "Unreviewed",
            "type": "integer"
          },
          "waiting_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Waiting P50 Hours"
          },
          "waiting_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Waiting P90 Hours"
          },
          "work_p50_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Work P50 Hours"
          },
          "work_p90_hours": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Work P90 Hours"
          }
        },
        "required": [
          "bucket",
          "sample",
          "excluded",
          "unreviewed",
          "estimated"
        ],
        "title": "SegmentPoint",
        "type": "object"
      },
      "SelfClearingWait": {
        "additionalProperties": false,
        "description": "Why an ``external``/``service`` park needs nobody, and when it ends.\n\nDerived from the task's newest auth-recovery handoff and never stored, so it cannot\ndisagree with the log entry it comes from.",
        "properties": {
          "kind": {
            "title": "Kind",
            "type": "string"
          },
          "resets_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Resets At"
          }
        },
        "required": [
          "kind"
        ],
        "title": "SelfClearingWait",
        "type": "object"
      },
      "SendBackActionRequest": {
        "description": "Feedback whose meaning is carried by which route received it.\n\nThe same shape as requesting changes, on purpose: every send-back is a note the\nhuman wrote plus the ball moving to the agent, and the only thing that differs is\nwhat the note *means*. That difference is the route and the ball_reason it writes,\nnot a discriminator inside the payload -- one act per route is what /approve,\n/request-changes and /reject already do, and it is what makes a network log or a\nserver log readable without cross-referencing a body.",
        "properties": {
          "attachments": {
            "description": "Images evidencing the feedback, stored as sidecar files.",
            "items": {
              "$ref": "#/components/schemas/AttachmentUpload"
            },
            "title": "Attachments",
            "type": "array"
          },
          "feedback": {
            "description": "Feedback text",
            "examples": [
              "Please add error handling"
            ],
            "minLength": 1,
            "title": "Feedback",
            "type": "string"
          },
          "user": {
            "description": "User performing the action",
            "examples": [
              "jeff"
            ],
            "title": "User",
            "type": "string"
          }
        },
        "required": [
          "user",
          "feedback"
        ],
        "title": "SendBackActionRequest",
        "type": "object"
      },
      "SeriesCoverage": {
        "description": "What one series can honestly claim, independent of the page-level coverage.\n\nA series whose source is younger than the range starts where the source starts and\nsays so here, rather than drawing zero over history nobody recorded (section 21.1).\n``bucket`` is the grain the series is aggregated at, so a client never infers it.",
        "properties": {
          "bucket": {
            "default": "week",
            "description": "The grain this series is aggregated at.",
            "enum": [
              "day",
              "week",
              "month"
            ],
            "title": "Bucket",
            "type": "string"
          },
          "complete": {
            "default": false,
            "description": "True when the window starts at or after native_from.",
            "title": "Complete",
            "type": "boolean"
          },
          "native_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "First row written at the moment it happened.",
            "title": "Native From"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "One sentence for the caption, or null when none is needed.",
            "title": "Note"
          },
          "recorded_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "First row of any source. Null means no rows at all.",
            "title": "Recorded From"
          }
        },
        "title": "SeriesCoverage",
        "type": "object"
      },
      "SkippedTaskRead": {
        "description": "One open task the queue passed over, and the rule that did it.",
        "properties": {
          "position": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Position"
          },
          "reason": {
            "title": "Reason",
            "type": "string"
          },
          "task": {
            "title": "Task",
            "type": "string"
          }
        },
        "required": [
          "task",
          "reason"
        ],
        "title": "SkippedTaskRead",
        "type": "object"
      },
      "Spec": {
        "additionalProperties": false,
        "description": "The specification, split along the questions agents actually ask.\n\nv1 had one `description` blob plus a `prompts.starter` that largely restated it.",
        "properties": {
          "constraints": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Hard requirements and prohibitions (markdown).",
            "title": "Constraints"
          },
          "context": {
            "description": "Read-this-first pointers with reasons.",
            "items": {
              "$ref": "#/components/schemas/ContextPointer"
            },
            "title": "Context",
            "type": "array"
          },
          "description": {
            "description": "WHAT to do (markdown) -- the working spec.",
            "title": "Description",
            "type": "string"
          },
          "intent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "WHY this task exists (markdown).",
            "title": "Intent"
          },
          "out_of_scope": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Explicit non-goals, so agents do not wander.",
            "title": "Out Of Scope"
          },
          "summary": {
            "description": "One or two sentences. The only summary, for every audience.",
            "title": "Summary",
            "type": "string"
          }
        },
        "required": [
          "summary",
          "description"
        ],
        "title": "Spec",
        "type": "object"
      },
      "SpecDraftRequest": {
        "description": "What the human typed, and nothing about where the task should land.\n\nThere is deliberately no field for lifecycle, ball, priority, parent, dependencies\nor actor -- not because the server would refuse them, but so that no caller can form\nthe request that would ask a model to choose one.",
        "properties": {
          "description": {
            "default": "",
            "description": "The rough description the human typed.",
            "title": "Description",
            "type": "string"
          },
          "title": {
            "default": "",
            "description": "The title the human typed, possibly empty.",
            "title": "Title",
            "type": "string"
          }
        },
        "title": "SpecDraftRequest",
        "type": "object"
      },
      "SpecDraftResponse": {
        "description": "One draft, for a form to fill its fields from -- or a refusal saying why not.\n\nSix spec fields, matching `agentjobs.modelaccess.draft.SpecDraft`. A reply that\nnamed a priority or a parent produced a draft in which those values do not exist;\nthere is nowhere in this model to put one.\n\n**A refusal is a 200 with `drafted: false`**, carrying the same `reason`/`detail`\npair as the status route. That is a decision rather than laziness: the person is\nstanding in a form they are about to file by hand, and \"no draft, here is why\" is\nnot the same event as \"your task could not be filed\". Answering with an error status\nwould route it through the form's filing-failed path. It also puts the refusal in\nthe OpenAPI document, so the generated client is typed for it -- which the 409\nrefusals elsewhere in this API are not.",
        "properties": {
          "acceptance": {
            "items": {
              "type": "string"
            },
            "title": "Acceptance",
            "type": "array"
          },
          "constraints": {
            "default": "",
            "title": "Constraints",
            "type": "string"
          },
          "description": {
            "default": "",
            "title": "Description",
            "type": "string"
          },
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "One sentence a person can act on.",
            "title": "Detail"
          },
          "drafted": {
            "default": true,
            "description": "False when no draft was produced.",
            "title": "Drafted",
            "type": "boolean"
          },
          "intent": {
            "default": "",
            "title": "Intent",
            "type": "string"
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Which model produced this draft.",
            "title": "Model"
          },
          "out_of_scope": {
            "default": "",
            "title": "Out Of Scope",
            "type": "string"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why there is no draft, as a stable code.",
            "title": "Reason"
          },
          "summary": {
            "default": "",
            "title": "Summary",
            "type": "string"
          }
        },
        "title": "SpecDraftResponse",
        "type": "object"
      },
      "StalledTaskRead": {
        "description": "Why a task in the waiting set is there because nobody is working it (task-499).\n\nStructure rather than a sentence, for the reason ``self_clearing_wait`` is: the\nDashboard draws a chip from ``reason`` and a duration from ``quiet_seconds``, and a\nclient matching on the words of a label is what ENGINEERING.md's rendered-value rule\nexists to prevent.\n\nDerived on every read and never stored. The task itself reads ``agent``/``work``\nthroughout -- that is precisely why nothing noticed for twenty-two hours on\n2026-09-19 -- so there is no field on the record for this to be a stale copy of, and\na log entry landing makes it disappear with nothing to retract.",
        "properties": {
          "quiet_seconds": {
            "title": "Quiet Seconds",
            "type": "number"
          },
          "quiet_since": {
            "description": "The newest log entry the threshold is measured from.",
            "format": "date-time",
            "title": "Quiet Since",
            "type": "string"
          },
          "reason": {
            "enum": [
              "no_agent",
              "undelivered_handback"
            ],
            "title": "Reason",
            "type": "string"
          },
          "run_id": {
            "default": "",
            "description": "The live run the undelivered feedback is addressed to. Empty for `no_agent`, where the point is that there is no run.",
            "title": "Run Id",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "threshold_seconds": {
            "title": "Threshold Seconds",
            "type": "number"
          }
        },
        "required": [
          "task_id",
          "reason",
          "quiet_since",
          "quiet_seconds",
          "threshold_seconds"
        ],
        "title": "StalledTaskRead",
        "type": "object"
      },
      "StartPauseView": {
        "description": "One open incident, as the reason the machine is starting nothing on a credential.\n\n**Machine-wide and about starts, not about runs.** The runs it already parked are\ntask-417's business and appear on their own tasks; what this says is why the two\nclickless starters -- the dispatch queue and the pull mode -- are quiet, which is a\nquestion a person asks of the board and of nothing else.\n\nIt is a list rather than one object because the pause is per credential: a machine\nwith a Claude home out of quota and a working Codex runner has one pause, and the\nCodex rows carry on starting beside it.",
        "properties": {
          "detail": {
            "default": "",
            "description": "The same sentence the tick's own report prints, in UTC.",
            "title": "Detail",
            "type": "string"
          },
          "incident_id": {
            "description": "task-417's incident. The same id its park names.",
            "title": "Incident Id",
            "type": "string"
          },
          "kind": {
            "description": "`usage_limit`, `auth` or `spend_limit`. Only `usage_limit` ends by itself, which is why `resets_at` is null for the other two.",
            "title": "Kind",
            "type": "string"
          },
          "kind_word": {
            "description": "How the kind reads in a sentence: `usage limit`, `login`, `spend limit`.",
            "title": "Kind Word",
            "type": "string"
          },
          "opened_at": {
            "description": "When the incident opened, UTC.",
            "title": "Opened At",
            "type": "string"
          },
          "projects": {
            "description": "Armed projects whose pull mode is held by it, by project id.",
            "items": {
              "type": "string"
            },
            "title": "Projects",
            "type": "array"
          },
          "queued": {
            "default": 0,
            "description": "Queued entries waiting on this incident, over the whole machine.",
            "title": "Queued",
            "type": "integer"
          },
          "resets_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the subscription window reopens, UTC, or null. **Render it in the reader's own zone**: the limit is a wall-clock fact to whoever is waiting for it. Null means nothing has undertaken to clear this on its own and the board must not print a time.",
            "title": "Resets At"
          },
          "resumes_by_itself": {
            "description": "True when the reset arrives with no person involved, so the board may say 'paused until <time>' rather than 'until the incident clears'.",
            "title": "Resumes By Itself",
            "type": "boolean"
          },
          "runner": {
            "description": "The runner whose starts are held. Two runners sharing one login each report the same incident under their own name, because a board shows rows rather than credentials.",
            "title": "Runner",
            "type": "string"
          }
        },
        "required": [
          "incident_id",
          "kind",
          "kind_word",
          "runner",
          "opened_at",
          "resumes_by_itself"
        ],
        "title": "StartPauseView",
        "type": "object"
      },
      "StatusCategory": {
        "description": "Which colour a status chip is drawn in: one colour per category (task-562).\n\nDerived on read beside ``display_status`` and by the same function, so the word and\nthe colour cannot disagree. Which label belongs to which category, and each\ncategory's colours, are in ``status_vocabulary.json`` -- the one file both the\nserver and the React app read. ``closed`` and ``closed_unfinished`` are both grey:\nCompleted is drawn solid, and Superseded, Cancelled and Duplicate hollow.\n\nA plain ``Enum`` rather than a tolerant ``ValueEnum``: nothing ever writes one, so\nthere is no older writer whose value a reader has to survive. Its members must be\nexactly the file's categories, which tests/test_status_vocabulary.py holds.",
        "enum": [
          "ready",
          "queued",
          "working",
          "finishing",
          "needs_you",
          "not_now",
          "draft",
          "closed",
          "closed_unfinished"
        ],
        "title": "StatusCategory",
        "type": "string"
      },
      "StuckGroup": {
        "description": "Open work grouped by who holds it and why, with how long they have held it.",
        "properties": {
          "ball": {
            "title": "Ball",
            "type": "string"
          },
          "ball_reason": {
            "title": "Ball Reason",
            "type": "string"
          },
          "max_days_held": {
            "title": "Max Days Held",
            "type": "number"
          },
          "mean_days_held": {
            "title": "Mean Days Held",
            "type": "number"
          },
          "oldest_task_id": {
            "title": "Oldest Task Id",
            "type": "string"
          },
          "tasks": {
            "title": "Tasks",
            "type": "integer"
          }
        },
        "required": [
          "ball",
          "ball_reason",
          "tasks",
          "mean_days_held",
          "max_days_held",
          "oldest_task_id"
        ],
        "title": "StuckGroup",
        "type": "object"
      },
      "Task": {
        "additionalProperties": false,
        "description": "A task, in schema v2.\n\nThe five consistency rules from design doc section 3 are enforced below in\n``_check_consistency``. They are what make limbo unrepresentable: an open task with\nnobody responsible, or a handoff with no stated ask, cannot be written down.",
        "properties": {
          "acceptance": {
            "items": {
              "$ref": "#/components/schemas/AcceptanceCriterion"
            },
            "title": "Acceptance",
            "type": "array"
          },
          "archived": {
            "default": false,
            "description": "Visibility flag, orthogonal to how the task ended.",
            "title": "Archived",
            "type": "boolean"
          },
          "assignment": {
            "$ref": "#/components/schemas/Assignment"
          },
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who acts next. Required while open."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to whoever holds the ball. Required when ball is set.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why they hold it, scoped to the holder."
          },
          "branches": {
            "items": {
              "$ref": "#/components/schemas/Branch"
            },
            "title": "Branches",
            "type": "array"
          },
          "category": {
            "description": "Project taxonomy; validated against config.",
            "title": "Category",
            "type": "string"
          },
          "created": {
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "deliverables": {
            "items": {
              "$ref": "#/components/schemas/Deliverable"
            },
            "title": "Deliverables",
            "type": "array"
          },
          "dependencies": {
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "display_status": {
            "description": "One human-readable label, derived on read and never stored.\n\nStoring it was rejected in design doc section 3: a denormalized copy of three\nfields is a drift bug waiting for its moment, and the derivation is this.\nA computed field rather than a plain property so API responses carry it;\nstorage excludes it on write, and a file that contains it is rejected by\nname (extra=\"forbid\") rather than silently round-tripped.",
            "readOnly": true,
            "title": "Display Status",
            "type": "string"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free text. An estimate, not a contract.",
            "title": "Effort"
          },
          "id": {
            "description": "Unique task identifier.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Whether this is a design pass or an implementation task. Absent means implementation. Grants nothing: what an approval authorises is the gate's business, not this field's (task-592)."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft"
          },
          "links": {
            "items": {
              "$ref": "#/components/schemas/Link"
            },
            "title": "Links",
            "type": "array"
          },
          "log": {
            "items": {
              "$ref": "#/components/schemas/LogEntry"
            },
            "title": "Log",
            "type": "array"
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Outcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How it ended. Set only when closed."
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Task id of the umbrella task, if any.",
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "What a run dispatched at this task may do, when this task wants something other than its project's default. Bounded by the project's machine-local ceiling: a value above it is clamped, never honoured (task-308)."
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium"
          },
          "queue_position": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Order within the priority band. Present if and only if the task is open.",
            "title": "Queue Position"
          },
          "schema": {
            "default": 2,
            "description": "Schema version stamp. Always 2 for this model (D3).",
            "title": "Schema",
            "type": "integer"
          },
          "spec": {
            "$ref": "#/components/schemas/Spec"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title.",
            "title": "Title",
            "type": "string"
          },
          "updated": {
            "format": "date-time",
            "title": "Updated",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "created",
          "updated",
          "category",
          "spec",
          "display_status"
        ],
        "title": "Task",
        "type": "object"
      },
      "TaskCardRead-Input": {
        "additionalProperties": false,
        "description": "A dashboard card: a listing row, plus the one line a card prints.\n\nThe dashboard is a page of cards rather than a table, and every one of them --\n``TaskCard`` on the panels, the free cells of the slot board -- draws the task's\none-sentence summary under its title. That is the single field the listing row does\nnot carry, so it is the single field added here.\n\n**Why a fourth read model rather than putting ``summary`` on ``TaskSummary``.** That\nwould have put it on ``GET /tasks`` as well, where nothing draws it: the summary\naverages 293 bytes on this repository's own backlog, which is a third again on top\nof a row that task-484 had just cut to 785, spent on a field no consumer reads. A\nsurface gets the fields it draws, which is the whole of task-483's argument, and\n\"one more field, everywhere\" is how a projection grows back into a record.\n\nBuilt from rows, not from records. It used to say the opposite -- that the dashboard\nhad whole records in hand anyway, because the recent-updates panel needed logs -- and\nthat was the last thing holding the whole-corpus read in place: the panel asks the\nstore for the ten entries it draws now, and nothing else in the snapshot wants a log\n(task-498). See :func:`agentjobs.dashboard.build_dashboard_snapshot`.",
        "properties": {
          "actionable": {
            "default": false,
            "title": "Actionable",
            "type": "boolean"
          },
          "archived": {
            "default": false,
            "description": "Visibility flag, orthogonal to how the task ended.",
            "title": "Archived",
            "type": "boolean"
          },
          "assignment": {
            "$ref": "#/components/schemas/Assignment"
          },
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who acts next. Required while open."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to whoever holds the ball. Required when ball is set.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why they hold it, scoped to the holder."
          },
          "can_brief": {
            "title": "Can Brief",
            "type": "boolean"
          },
          "category": {
            "description": "Project taxonomy; validated against config.",
            "title": "Category",
            "type": "string"
          },
          "created": {
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "dependencies": {
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free text. An estimate, not a contract.",
            "title": "Effort"
          },
          "id": {
            "description": "Unique task identifier.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Design pass or implementation. Absent means implementation."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft"
          },
          "live_finish": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveFinishState"
              },
              {
                "type": "null"
              }
            ]
          },
          "live_run_health": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Live Run Health"
          },
          "live_walk": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveWalkState"
              },
              {
                "type": "null"
              }
            ]
          },
          "needs_cycles": {
            "items": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "title": "Needs Cycles",
            "type": "array"
          },
          "open_children_count": {
            "default": 0,
            "title": "Open Children Count",
            "type": "integer"
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Outcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How it ended. Set only when closed."
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Task id of the umbrella task, if any.",
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "This task's request for a dispatch envelope. A request, not a grant."
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium"
          },
          "queue_position": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Order within the priority band. Present if and only if the task is open.",
            "title": "Queue Position"
          },
          "queued_dispatch": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueuedDispatchState"
              },
              {
                "type": "null"
              }
            ]
          },
          "schema": {
            "default": 2,
            "description": "Schema version stamp. Always 2 for this model (D3).",
            "title": "Schema",
            "type": "integer"
          },
          "self_clearing_wait": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SelfClearingWait"
              },
              {
                "type": "null"
              }
            ]
          },
          "summary": {
            "title": "Summary",
            "type": "string"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title.",
            "title": "Title",
            "type": "string"
          },
          "unblocks_count": {
            "default": 0,
            "title": "Unblocks Count",
            "type": "integer"
          },
          "unmet_needs": {
            "items": {
              "type": "string"
            },
            "title": "Unmet Needs",
            "type": "array"
          },
          "updated": {
            "format": "date-time",
            "title": "Updated",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "created",
          "updated",
          "category",
          "summary",
          "can_brief"
        ],
        "title": "TaskCardRead",
        "type": "object"
      },
      "TaskCardRead-Output": {
        "additionalProperties": false,
        "description": "A dashboard card: a listing row, plus the one line a card prints.\n\nThe dashboard is a page of cards rather than a table, and every one of them --\n``TaskCard`` on the panels, the free cells of the slot board -- draws the task's\none-sentence summary under its title. That is the single field the listing row does\nnot carry, so it is the single field added here.\n\n**Why a fourth read model rather than putting ``summary`` on ``TaskSummary``.** That\nwould have put it on ``GET /tasks`` as well, where nothing draws it: the summary\naverages 293 bytes on this repository's own backlog, which is a third again on top\nof a row that task-484 had just cut to 785, spent on a field no consumer reads. A\nsurface gets the fields it draws, which is the whole of task-483's argument, and\n\"one more field, everywhere\" is how a projection grows back into a record.\n\nBuilt from rows, not from records. It used to say the opposite -- that the dashboard\nhad whole records in hand anyway, because the recent-updates panel needed logs -- and\nthat was the last thing holding the whole-corpus read in place: the panel asks the\nstore for the ten entries it draws now, and nothing else in the snapshot wants a log\n(task-498). See :func:`agentjobs.dashboard.build_dashboard_snapshot`.",
        "properties": {
          "actionable": {
            "default": false,
            "title": "Actionable",
            "type": "boolean"
          },
          "archived": {
            "default": false,
            "description": "Visibility flag, orthogonal to how the task ended.",
            "title": "Archived",
            "type": "boolean"
          },
          "assignment": {
            "$ref": "#/components/schemas/Assignment"
          },
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who acts next. Required while open."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to whoever holds the ball. Required when ball is set.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why they hold it, scoped to the holder."
          },
          "can_brief": {
            "title": "Can Brief",
            "type": "boolean"
          },
          "category": {
            "description": "Project taxonomy; validated against config.",
            "title": "Category",
            "type": "string"
          },
          "created": {
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "dependencies": {
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "display_status": {
            "description": "The chip's word: the one label every surface shows for this task.",
            "readOnly": true,
            "title": "Display Status",
            "type": "string"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free text. An estimate, not a contract.",
            "title": "Effort"
          },
          "id": {
            "description": "Unique task identifier.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Design pass or implementation. Absent means implementation."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft"
          },
          "live_finish": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveFinishState"
              },
              {
                "type": "null"
              }
            ]
          },
          "live_run_health": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Live Run Health"
          },
          "live_walk": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveWalkState"
              },
              {
                "type": "null"
              }
            ]
          },
          "needs_cycles": {
            "items": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "title": "Needs Cycles",
            "type": "array"
          },
          "open_children_count": {
            "default": 0,
            "title": "Open Children Count",
            "type": "integer"
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Outcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How it ended. Set only when closed."
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Task id of the umbrella task, if any.",
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "This task's request for a dispatch envelope. A request, not a grant."
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium"
          },
          "queue_position": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Order within the priority band. Present if and only if the task is open.",
            "title": "Queue Position"
          },
          "queued_dispatch": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueuedDispatchState"
              },
              {
                "type": "null"
              }
            ]
          },
          "schema": {
            "default": 2,
            "description": "Schema version stamp. Always 2 for this model (D3).",
            "title": "Schema",
            "type": "integer"
          },
          "self_clearing_wait": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SelfClearingWait"
              },
              {
                "type": "null"
              }
            ]
          },
          "status_category": {
            "$ref": "#/components/schemas/StatusCategory",
            "description": "The chip's colour, from the same derivation as ``display_status``.",
            "readOnly": true
          },
          "summary": {
            "title": "Summary",
            "type": "string"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title.",
            "title": "Title",
            "type": "string"
          },
          "unblocks_count": {
            "default": 0,
            "title": "Unblocks Count",
            "type": "integer"
          },
          "unmet_needs": {
            "items": {
              "type": "string"
            },
            "title": "Unmet Needs",
            "type": "array"
          },
          "updated": {
            "format": "date-time",
            "title": "Updated",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "created",
          "updated",
          "category",
          "summary",
          "can_brief",
          "display_status",
          "status_category"
        ],
        "title": "TaskCardRead",
        "type": "object"
      },
      "TaskCreateRequest": {
        "additionalProperties": false,
        "description": "Payload for creating a new task.",
        "properties": {
          "acceptance": {
            "description": "Checklist defining done.",
            "items": {
              "$ref": "#/components/schemas/AcceptanceCriterion"
            },
            "title": "Acceptance",
            "type": "array"
          },
          "actor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Configured actor id to record as the creator. Written to the creation log entry, so a task can say who filed it. Refused when the project does not define the id.",
            "title": "Actor"
          },
          "attachments": {
            "description": "Images evidencing the report, stored as sidecar files.",
            "items": {
              "$ref": "#/components/schemas/AttachmentUpload"
            },
            "title": "Attachments",
            "type": "array"
          },
          "branches": {
            "description": "Git branches associated with the task lifecycle.",
            "items": {
              "$ref": "#/components/schemas/Branch"
            },
            "title": "Branches",
            "type": "array"
          },
          "category": {
            "default": "general",
            "description": "Classification category used for filtering in the UI.",
            "title": "Category",
            "type": "string"
          },
          "constraints": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Hard requirements and prohibitions (spec.constraints).",
            "title": "Constraints"
          },
          "context": {
            "description": "Curated read-this-first paths and why each one matters (spec.context).",
            "items": {
              "$ref": "#/components/schemas/ContextPointer"
            },
            "title": "Context",
            "type": "array"
          },
          "deliverables": {
            "description": "Deliverables to be produced for task completion.",
            "items": {
              "$ref": "#/components/schemas/Deliverable"
            },
            "title": "Deliverables",
            "type": "array"
          },
          "dependencies": {
            "description": "Task dependencies tracked in the system.",
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "description": {
            "description": "Markdown working spec (spec.description).",
            "title": "Description",
            "type": "string"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Estimated effort (time or complexity). Free text.",
            "title": "Effort"
          },
          "eligible": {
            "description": "Actor ids that may claim the task. Empty means anyone.",
            "items": {
              "type": "string"
            },
            "title": "Eligible",
            "type": "array"
          },
          "id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional explicit task identifier (e.g., task-042).",
            "title": "Id"
          },
          "intent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why the task exists (spec.intent).",
            "title": "Intent"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "'design' for a design pass. Omit it for an implementation task: absent means implementation (task-592)."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft",
            "description": "Initial lifecycle: draft (ball human/spec) or ready (agent/available)."
          },
          "links": {
            "description": "External references relevant to the task.",
            "items": {
              "$ref": "#/components/schemas/Link"
            },
            "title": "Links",
            "type": "array"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "out_of_scope": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Explicit non-goals (spec.out_of_scope).",
            "title": "Out Of Scope"
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Umbrella task id, if any.",
            "title": "Parent"
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium",
            "description": "Relative urgency for the new task."
          },
          "summary": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "One-or-two sentence spec.summary. Defaults to the title.",
            "title": "Summary"
          },
          "tags": {
            "description": "Arbitrary tag labels.",
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title summarising the work to be done.",
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "title",
          "description"
        ],
        "title": "TaskCreateRequest",
        "type": "object"
      },
      "TaskDetailResponse": {
        "description": "Everything the React detail page needs to resume and review one task.",
        "properties": {
          "blocks": {
            "items": {
              "$ref": "#/components/schemas/DependencyRelation"
            },
            "title": "Blocks",
            "type": "array"
          },
          "child_dependency_edges": {
            "items": {
              "$ref": "#/components/schemas/ScopedDependencyEdge"
            },
            "title": "Child Dependency Edges",
            "type": "array"
          },
          "children": {
            "items": {
              "$ref": "#/components/schemas/TaskRead-Output"
            },
            "title": "Children",
            "type": "array"
          },
          "identity": {
            "$ref": "#/components/schemas/ReviewIdentity"
          },
          "needs": {
            "items": {
              "$ref": "#/components/schemas/DependencyRelation"
            },
            "title": "Needs",
            "type": "array"
          },
          "parent_task": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskRead-Output"
              },
              {
                "type": "null"
              }
            ]
          },
          "related": {
            "items": {
              "$ref": "#/components/schemas/DependencyRelation"
            },
            "title": "Related",
            "type": "array"
          },
          "task": {
            "$ref": "#/components/schemas/TaskRead-Output"
          }
        },
        "required": [
          "task",
          "parent_task",
          "children",
          "needs",
          "blocks",
          "related",
          "child_dependency_edges",
          "identity"
        ],
        "title": "TaskDetailResponse",
        "type": "object"
      },
      "TaskFinishView": {
        "description": "What is happening to this task's branch right now, or last happened to it.\n\nThe answer to a question the API could not previously be asked. Approving a task on\na project with the scripted finish switched on starts a process that takes minutes\nand, until task-321, said nothing to the page that started it: the ball moved to\n``agent``/``work`` and the reader was left to guess whether anything had picked the\napproval up.",
        "properties": {
          "branch": {
            "default": "",
            "title": "Branch",
            "type": "string"
          },
          "current_step": {
            "default": "",
            "title": "Current Step",
            "type": "string"
          },
          "earlier_merge_commit": {
            "default": "",
            "description": "A merge an earlier attempt made of this branch, when this attempt made none (task-322). A separate fact: state and merge_commit still describe this attempt.",
            "title": "Earlier Merge Commit",
            "type": "string"
          },
          "earlier_merge_finish_id": {
            "default": "",
            "title": "Earlier Merge Finish Id",
            "type": "string"
          },
          "elapsed_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Seconds since it started while live; the total it took once it ended. Computed on the server, because started_at is this machine's clock and the phone reading the page is not on it.",
            "title": "Elapsed Seconds"
          },
          "estimate": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LandingEstimate"
              },
              {
                "type": "null"
              }
            ],
            "description": "Progress and time remaining while live (task-586). The same computation the task list's live_finish carries, so the page and the row cannot disagree."
          },
          "finish_id": {
            "default": "",
            "title": "Finish Id",
            "type": "string"
          },
          "finished_at": {
            "default": "",
            "title": "Finished At",
            "type": "string"
          },
          "gate": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FinishGateView"
              },
              {
                "type": "null"
              }
            ]
          },
          "gate_retry": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FinishGateRetryView"
              },
              {
                "type": "null"
              }
            ],
            "description": "The one gate retry this attempt made, when it made one."
          },
          "live": {
            "description": "Something is working on this task's branch now.",
            "title": "Live",
            "type": "boolean"
          },
          "merge_commit": {
            "default": "",
            "description": "The merge this attempt made, if any.",
            "title": "Merge Commit",
            "type": "string"
          },
          "next_action": {
            "default": "",
            "description": "What a person should do about this finish now; empty when nothing.",
            "title": "Next Action",
            "type": "string"
          },
          "output_source": {
            "default": "none",
            "description": "Where the text came from: 'finish-log' (the spawned process's own output, which is the whole step table and exists only once it has ended), 'gate-log' (the gate's output, for a finish run inside a session), or 'none' -- which is the normal answer while one is still running.",
            "title": "Output Source",
            "type": "string"
          },
          "output_tail": {
            "default": "",
            "description": "The end of that text, bounded.",
            "title": "Output Tail",
            "type": "string"
          },
          "output_url": {
            "description": "Where the whole of it is readable.",
            "title": "Output Url",
            "type": "string"
          },
          "project_id": {
            "title": "Project Id",
            "type": "string"
          },
          "reason": {
            "default": "",
            "title": "Reason",
            "type": "string"
          },
          "started_at": {
            "default": "",
            "title": "Started At",
            "type": "string"
          },
          "state": {
            "description": "'starting' (spawned, nothing written yet), 'running', 'finished', 'escalated' (it stopped and handed back), 'declined' (never a candidate), or 'interrupted' (it wrote no ending and its process is gone).",
            "title": "State",
            "type": "string"
          },
          "steps": {
            "items": {
              "$ref": "#/components/schemas/FinishStepView"
            },
            "title": "Steps",
            "type": "array"
          },
          "stopped_at": {
            "default": "",
            "description": "Which step an escalation stopped at.",
            "title": "Stopped At",
            "type": "string"
          },
          "task_id": {
            "title": "Task Id",
            "type": "string"
          },
          "worktree": {
            "default": "",
            "title": "Worktree",
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "project_id",
          "state",
          "live",
          "output_url"
        ],
        "title": "TaskFinishView",
        "type": "object"
      },
      "TaskKind": {
        "description": "What a task *is*: a design pass or the building of something (task-592).\n\nAbsent means ``implementation``. It changes how a task is shown and how an approval\nis worded, and never what an approval authorises -- the handoff's gate does that --\nwhich is why any actor may edit it. See docs/task-kind-design.md section 1.",
        "enum": [
          "design",
          "implementation"
        ],
        "title": "TaskKind",
        "type": "string"
      },
      "TaskRead-Input": {
        "additionalProperties": false,
        "description": "Task plus server-computed dependency state for read surfaces.",
        "properties": {
          "acceptance": {
            "items": {
              "$ref": "#/components/schemas/AcceptanceCriterion"
            },
            "title": "Acceptance",
            "type": "array"
          },
          "actionable": {
            "default": false,
            "title": "Actionable",
            "type": "boolean"
          },
          "archived": {
            "default": false,
            "description": "Visibility flag, orthogonal to how the task ended.",
            "title": "Archived",
            "type": "boolean"
          },
          "assignment": {
            "$ref": "#/components/schemas/Assignment"
          },
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who acts next. Required while open."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to whoever holds the ball. Required when ball is set.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why they hold it, scoped to the holder."
          },
          "branches": {
            "items": {
              "$ref": "#/components/schemas/Branch"
            },
            "title": "Branches",
            "type": "array"
          },
          "category": {
            "description": "Project taxonomy; validated against config.",
            "title": "Category",
            "type": "string"
          },
          "created": {
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "deliverables": {
            "items": {
              "$ref": "#/components/schemas/Deliverable"
            },
            "title": "Deliverables",
            "type": "array"
          },
          "dependencies": {
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free text. An estimate, not a contract.",
            "title": "Effort"
          },
          "id": {
            "description": "Unique task identifier.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Whether this is a design pass or an implementation task. Absent means implementation. Grants nothing: what an approval authorises is the gate's business, not this field's (task-592)."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft"
          },
          "links": {
            "items": {
              "$ref": "#/components/schemas/Link"
            },
            "title": "Links",
            "type": "array"
          },
          "live_finish": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveFinishState"
              },
              {
                "type": "null"
              }
            ]
          },
          "live_run_health": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Live Run Health"
          },
          "live_walk": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveWalkState"
              },
              {
                "type": "null"
              }
            ]
          },
          "log": {
            "items": {
              "$ref": "#/components/schemas/LogEntry"
            },
            "title": "Log",
            "type": "array"
          },
          "needs_cycles": {
            "items": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "title": "Needs Cycles",
            "type": "array"
          },
          "open_children_count": {
            "default": 0,
            "title": "Open Children Count",
            "type": "integer"
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Outcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How it ended. Set only when closed."
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Task id of the umbrella task, if any.",
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "What a run dispatched at this task may do, when this task wants something other than its project's default. Bounded by the project's machine-local ceiling: a value above it is clamped, never honoured (task-308)."
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium"
          },
          "queue_position": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Order within the priority band. Present if and only if the task is open.",
            "title": "Queue Position"
          },
          "queued_dispatch": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueuedDispatchState"
              },
              {
                "type": "null"
              }
            ]
          },
          "schema": {
            "default": 2,
            "description": "Schema version stamp. Always 2 for this model (D3).",
            "title": "Schema",
            "type": "integer"
          },
          "self_clearing_wait": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SelfClearingWait"
              },
              {
                "type": "null"
              }
            ]
          },
          "spec": {
            "$ref": "#/components/schemas/Spec"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title.",
            "title": "Title",
            "type": "string"
          },
          "unblocks_count": {
            "default": 0,
            "title": "Unblocks Count",
            "type": "integer"
          },
          "unmet_needs": {
            "items": {
              "type": "string"
            },
            "title": "Unmet Needs",
            "type": "array"
          },
          "updated": {
            "format": "date-time",
            "title": "Updated",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "created",
          "updated",
          "category",
          "spec"
        ],
        "title": "TaskRead",
        "type": "object"
      },
      "TaskRead-Output": {
        "additionalProperties": false,
        "description": "Task plus server-computed dependency state for read surfaces.",
        "properties": {
          "acceptance": {
            "items": {
              "$ref": "#/components/schemas/AcceptanceCriterion"
            },
            "title": "Acceptance",
            "type": "array"
          },
          "actionable": {
            "default": false,
            "title": "Actionable",
            "type": "boolean"
          },
          "archived": {
            "default": false,
            "description": "Visibility flag, orthogonal to how the task ended.",
            "title": "Archived",
            "type": "boolean"
          },
          "assignment": {
            "$ref": "#/components/schemas/Assignment"
          },
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who acts next. Required while open."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to whoever holds the ball. Required when ball is set.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why they hold it, scoped to the holder."
          },
          "branches": {
            "items": {
              "$ref": "#/components/schemas/Branch"
            },
            "title": "Branches",
            "type": "array"
          },
          "category": {
            "description": "Project taxonomy; validated against config.",
            "title": "Category",
            "type": "string"
          },
          "created": {
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "deliverables": {
            "items": {
              "$ref": "#/components/schemas/Deliverable"
            },
            "title": "Deliverables",
            "type": "array"
          },
          "dependencies": {
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "display_status": {
            "description": "The chip's word: the one label every surface shows for this task.",
            "readOnly": true,
            "title": "Display Status",
            "type": "string"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free text. An estimate, not a contract.",
            "title": "Effort"
          },
          "id": {
            "description": "Unique task identifier.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Whether this is a design pass or an implementation task. Absent means implementation. Grants nothing: what an approval authorises is the gate's business, not this field's (task-592)."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft"
          },
          "links": {
            "items": {
              "$ref": "#/components/schemas/Link"
            },
            "title": "Links",
            "type": "array"
          },
          "live_finish": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveFinishState"
              },
              {
                "type": "null"
              }
            ]
          },
          "live_run_health": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Live Run Health"
          },
          "live_walk": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveWalkState"
              },
              {
                "type": "null"
              }
            ]
          },
          "log": {
            "items": {
              "$ref": "#/components/schemas/LogEntry"
            },
            "title": "Log",
            "type": "array"
          },
          "needs_cycles": {
            "items": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "title": "Needs Cycles",
            "type": "array"
          },
          "open_children_count": {
            "default": 0,
            "title": "Open Children Count",
            "type": "integer"
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Outcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How it ended. Set only when closed."
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Task id of the umbrella task, if any.",
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "What a run dispatched at this task may do, when this task wants something other than its project's default. Bounded by the project's machine-local ceiling: a value above it is clamped, never honoured (task-308)."
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium"
          },
          "queue_position": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Order within the priority band. Present if and only if the task is open.",
            "title": "Queue Position"
          },
          "queued_dispatch": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueuedDispatchState"
              },
              {
                "type": "null"
              }
            ]
          },
          "schema": {
            "default": 2,
            "description": "Schema version stamp. Always 2 for this model (D3).",
            "title": "Schema",
            "type": "integer"
          },
          "self_clearing_wait": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SelfClearingWait"
              },
              {
                "type": "null"
              }
            ]
          },
          "spec": {
            "$ref": "#/components/schemas/Spec"
          },
          "status_category": {
            "$ref": "#/components/schemas/StatusCategory",
            "description": "The chip's colour, from the same derivation as ``display_status``.",
            "readOnly": true
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title.",
            "title": "Title",
            "type": "string"
          },
          "unblocks_count": {
            "default": 0,
            "title": "Unblocks Count",
            "type": "integer"
          },
          "unmet_needs": {
            "items": {
              "type": "string"
            },
            "title": "Unmet Needs",
            "type": "array"
          },
          "updated": {
            "format": "date-time",
            "title": "Updated",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "created",
          "updated",
          "category",
          "spec",
          "display_status",
          "status_category"
        ],
        "title": "TaskRead",
        "type": "object"
      },
      "TaskSummaryRead-Input": {
        "additionalProperties": false,
        "description": "A listing row: the task without its prose, its criteria or its log.\n\nWhat ``GET /tasks`` returns. It carries the same server-computed dependency state\n``TaskRead`` does, because a list is where the claim gate is drawn -- a row is greyed\nout by ``actionable`` and explained by ``unmet_needs``, and a client that had to work\nthose out for itself would need the corpus the projection exists to avoid sending.\n\nWhole records are still on offer, at ``GET /tasks/full``; see the route.",
        "properties": {
          "actionable": {
            "default": false,
            "title": "Actionable",
            "type": "boolean"
          },
          "archived": {
            "default": false,
            "description": "Visibility flag, orthogonal to how the task ended.",
            "title": "Archived",
            "type": "boolean"
          },
          "assignment": {
            "$ref": "#/components/schemas/Assignment"
          },
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who acts next. Required while open."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to whoever holds the ball. Required when ball is set.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why they hold it, scoped to the holder."
          },
          "category": {
            "description": "Project taxonomy; validated against config.",
            "title": "Category",
            "type": "string"
          },
          "created": {
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "dependencies": {
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free text. An estimate, not a contract.",
            "title": "Effort"
          },
          "id": {
            "description": "Unique task identifier.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Design pass or implementation. Absent means implementation."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft"
          },
          "live_finish": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveFinishState"
              },
              {
                "type": "null"
              }
            ]
          },
          "live_run_health": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Live Run Health"
          },
          "live_walk": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveWalkState"
              },
              {
                "type": "null"
              }
            ]
          },
          "needs_cycles": {
            "items": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "title": "Needs Cycles",
            "type": "array"
          },
          "open_children_count": {
            "default": 0,
            "title": "Open Children Count",
            "type": "integer"
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Outcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How it ended. Set only when closed."
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Task id of the umbrella task, if any.",
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "This task's request for a dispatch envelope. A request, not a grant."
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium"
          },
          "queue_position": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Order within the priority band. Present if and only if the task is open.",
            "title": "Queue Position"
          },
          "queued_dispatch": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueuedDispatchState"
              },
              {
                "type": "null"
              }
            ]
          },
          "schema": {
            "default": 2,
            "description": "Schema version stamp. Always 2 for this model (D3).",
            "title": "Schema",
            "type": "integer"
          },
          "self_clearing_wait": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SelfClearingWait"
              },
              {
                "type": "null"
              }
            ]
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title.",
            "title": "Title",
            "type": "string"
          },
          "unblocks_count": {
            "default": 0,
            "title": "Unblocks Count",
            "type": "integer"
          },
          "unmet_needs": {
            "items": {
              "type": "string"
            },
            "title": "Unmet Needs",
            "type": "array"
          },
          "updated": {
            "format": "date-time",
            "title": "Updated",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "created",
          "updated",
          "category"
        ],
        "title": "TaskSummaryRead",
        "type": "object"
      },
      "TaskSummaryRead-Output": {
        "additionalProperties": false,
        "description": "A listing row: the task without its prose, its criteria or its log.\n\nWhat ``GET /tasks`` returns. It carries the same server-computed dependency state\n``TaskRead`` does, because a list is where the claim gate is drawn -- a row is greyed\nout by ``actionable`` and explained by ``unmet_needs``, and a client that had to work\nthose out for itself would need the corpus the projection exists to avoid sending.\n\nWhole records are still on offer, at ``GET /tasks/full``; see the route.",
        "properties": {
          "actionable": {
            "default": false,
            "title": "Actionable",
            "type": "boolean"
          },
          "archived": {
            "default": false,
            "description": "Visibility flag, orthogonal to how the task ended.",
            "title": "Archived",
            "type": "boolean"
          },
          "assignment": {
            "$ref": "#/components/schemas/Assignment"
          },
          "ball": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ball"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who acts next. Required while open."
          },
          "ball_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The ask, addressed to whoever holds the ball. Required when ball is set.",
            "title": "Ball Prompt"
          },
          "ball_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BallReason"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why they hold it, scoped to the holder."
          },
          "category": {
            "description": "Project taxonomy; validated against config.",
            "title": "Category",
            "type": "string"
          },
          "created": {
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "dependencies": {
            "items": {
              "$ref": "#/components/schemas/Dependency"
            },
            "title": "Dependencies",
            "type": "array"
          },
          "display_status": {
            "description": "The chip's word: the one label every surface shows for this task.",
            "readOnly": true,
            "title": "Display Status",
            "type": "string"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free text. An estimate, not a contract.",
            "title": "Effort"
          },
          "id": {
            "description": "Unique task identifier.",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Design pass or implementation. Absent means implementation."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/Lifecycle",
            "default": "draft"
          },
          "live_finish": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveFinishState"
              },
              {
                "type": "null"
              }
            ]
          },
          "live_run_health": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Live Run Health"
          },
          "live_walk": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LiveWalkState"
              },
              {
                "type": "null"
              }
            ]
          },
          "needs_cycles": {
            "items": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "title": "Needs Cycles",
            "type": "array"
          },
          "open_children_count": {
            "default": 0,
            "title": "Open Children Count",
            "type": "integer"
          },
          "outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Outcome"
              },
              {
                "type": "null"
              }
            ],
            "description": "How it ended. Set only when closed."
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Task id of the umbrella task, if any.",
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "This task's request for a dispatch envelope. A request, not a grant."
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "medium"
          },
          "queue_position": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Order within the priority band. Present if and only if the task is open.",
            "title": "Queue Position"
          },
          "queued_dispatch": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/QueuedDispatchState"
              },
              {
                "type": "null"
              }
            ]
          },
          "schema": {
            "default": 2,
            "description": "Schema version stamp. Always 2 for this model (D3).",
            "title": "Schema",
            "type": "integer"
          },
          "self_clearing_wait": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/SelfClearingWait"
              },
              {
                "type": "null"
              }
            ]
          },
          "status_category": {
            "$ref": "#/components/schemas/StatusCategory",
            "description": "The chip's colour, from the same derivation as ``display_status``.",
            "readOnly": true
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Task title.",
            "title": "Title",
            "type": "string"
          },
          "unblocks_count": {
            "default": 0,
            "title": "Unblocks Count",
            "type": "integer"
          },
          "unmet_needs": {
            "items": {
              "type": "string"
            },
            "title": "Unmet Needs",
            "type": "array"
          },
          "updated": {
            "format": "date-time",
            "title": "Updated",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "created",
          "updated",
          "category",
          "display_status",
          "status_category"
        ],
        "title": "TaskSummaryRead",
        "type": "object"
      },
      "TaskUpdateRequest": {
        "additionalProperties": false,
        "description": "Payload for partially updating a task.\n\nThe state axes are deliberately absent: lifecycle, ball and outcome move only\nthrough the claim/handoff/release/close verbs, which log their transitions.",
        "properties": {
          "acceptance": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/AcceptanceCriterion"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Acceptance"
          },
          "branches": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/Branch"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Branches"
          },
          "category": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Category"
          },
          "deliverables": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/Deliverable"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deliverables"
          },
          "dependencies": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/Dependency"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Dependencies"
          },
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Effort"
          },
          "expected_revision": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The `updated` value from a prior read. When supplied, the request is refused if the task changed in the meantime, and the current task is returned so the caller can decide again.",
            "title": "Expected Revision"
          },
          "kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TaskKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "Design pass or implementation. Any actor may change it: it grants nothing, because what an approval authorises is the gate's business (task-592). Send null to clear it back to implementation."
          },
          "links": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/Link"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Links"
          },
          "operation_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Caller-generated UUID. Resending the same request with the same id replays the original result instead of writing again; reusing it for a different request is a conflict and writes nothing.",
            "title": "Operation Id"
          },
          "parent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent"
          },
          "posture": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DispatchPosture"
              },
              {
                "type": "null"
              }
            ],
            "description": "What a run dispatched at this task may do. Content, not a state axis: it is a request bounded by the project's machine-local ceiling, never a grant, so it needs no verb of its own (task-308). Send null to clear it."
          },
          "priority": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Priority"
              },
              {
                "type": "null"
              }
            ]
          },
          "spec": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Spec"
              },
              {
                "type": "null"
              }
            ]
          },
          "tags": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Tags"
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          }
        },
        "title": "TaskUpdateRequest",
        "type": "object"
      },
      "ThroughputPoint": {
        "description": "Completions, cancellations and reopenings for one throughput bucket (T1).\n\n``tasks_completed`` and ``completion_events`` differ whenever a task was reopened\nand closed again (section 3.3). The chart plots the first; the second is returned so\na reader whose arithmetic does not work out has an answer.\n\n**The cycle-time percentiles are gone**, per section 21.2 and section 19.2: they\nmeasured ``created_at`` to ``closed_at``, which is mostly queue wait, and section\n18.1's segment series answers the question they stood in for. They were removed\nrather than deprecated in place -- a client cannot misread a field that is absent,\nand the generated client is regenerated by the same task that removed them.",
        "properties": {
          "bucket": {
            "description": "First day of the bucket, in the reporting zone.",
            "format": "date",
            "title": "Bucket",
            "type": "string"
          },
          "cancelled": {
            "description": "Closed with any outcome other than completed.",
            "title": "Cancelled",
            "type": "integer"
          },
          "completion_events": {
            "description": "COUNT(*).",
            "title": "Completion Events",
            "type": "integer"
          },
          "estimated": {
            "default": false,
            "description": "This bucket contains a reconstructed close or reopen.",
            "title": "Estimated",
            "type": "boolean"
          },
          "reopened": {
            "default": 0,
            "description": "Tasks reopened in this bucket (T1).",
            "title": "Reopened",
            "type": "integer"
          },
          "tasks_completed": {
            "description": "COUNT(DISTINCT task_id).",
            "title": "Tasks Completed",
            "type": "integer"
          }
        },
        "required": [
          "bucket",
          "tasks_completed",
          "completion_events",
          "cancelled"
        ],
        "title": "ThroughputPoint",
        "type": "object"
      },
      "TranscriptCallView": {
        "description": "One tool call inside a run of them, for the disclosure that holds the detail.",
        "properties": {
          "added": {
            "default": 0,
            "title": "Added",
            "type": "integer"
          },
          "detail": {
            "default": "",
            "description": "What was actually run or written, bounded.",
            "title": "Detail",
            "type": "string"
          },
          "failed": {
            "default": false,
            "description": "The call answered with an error.",
            "title": "Failed",
            "type": "boolean"
          },
          "name": {
            "description": "The tool, as the runner names it.",
            "title": "Name",
            "type": "string"
          },
          "output": {
            "default": "",
            "description": "The end of what came back, bounded.",
            "title": "Output",
            "type": "string"
          },
          "removed": {
            "default": 0,
            "title": "Removed",
            "type": "integer"
          },
          "title": {
            "default": "",
            "description": "The one line that stands for this call.",
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "TranscriptCallView",
        "type": "object"
      },
      "TranscriptEntryView": {
        "description": "One thing the agent did.\n\n``kind`` is ``'narration'`` (the agent's own prose), ``'tools'`` (a run of\nconsecutive calls, summarized, with ``calls`` holding the detail), or ``'prompt'``\n(what a human -- or the dispatcher -- asked for).",
        "properties": {
          "added": {
            "default": 0,
            "title": "Added",
            "type": "integer"
          },
          "calls": {
            "items": {
              "$ref": "#/components/schemas/TranscriptCallView"
            },
            "title": "Calls",
            "type": "array"
          },
          "failed": {
            "default": 0,
            "description": "How many calls in this run answered with an error.",
            "title": "Failed",
            "type": "integer"
          },
          "kind": {
            "title": "Kind",
            "type": "string"
          },
          "removed": {
            "default": 0,
            "title": "Removed",
            "type": "integer"
          },
          "summary": {
            "default": "",
            "description": "A run of calls in one line.",
            "title": "Summary",
            "type": "string"
          },
          "text": {
            "default": "",
            "description": "Prompt or narration text; empty for tools.",
            "title": "Text",
            "type": "string"
          }
        },
        "required": [
          "kind"
        ],
        "title": "TranscriptEntryView",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "VersionResponse": {
        "description": "What a client needs to decide whether it can talk to this service.\n\nTwo independent numbers. ``version`` is the installed AgentJobs distribution and\ngoverns the shape of the REST surface; ``schema_version`` is the task-record\nschema and governs the shape of the documents that surface returns. A client can\nmatch one and not the other, so neither is derivable from the other.",
        "properties": {
          "api_digest": {
            "description": "SHA-256 of the OpenAPI document this process serves. The generated TypeScript client is built from that same document, so a bundle carries the digest it was generated against and can tell, in one comparison, whether the server answering it is the one it was built for. Neither `version` nor `schema_version` can: in the incident this was added for both matched exactly while a response field had been added underneath a running process. See agentjobs.api.contract.",
            "title": "Api Digest",
            "type": "string"
          },
          "bundle_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Which build of the React app this process is serving from disk, or null when no bundle is present or it was built before this field existed. Derived from the built asset bytes, so unlike `api_digest` it also moves for the many rebuilds that change no API route -- which is what lets an open tab notice it is running the previous build and offer a reload.",
            "title": "Bundle Id"
          },
          "frontend_bundle": {
            "description": "Whether this process can serve the React app at /app/. The bundle is gitignored and no install step builds it, so a clone that has never run `npm run build` answers every REST call correctly and 404s the one URL a new user is told to open. Reported here so `agentjobs open` can say so before opening a browser rather than after.",
            "enum": [
              "present",
              "missing"
            ],
            "title": "Frontend Bundle",
            "type": "string"
          },
          "schema_version": {
            "description": "Task record schema version served.",
            "title": "Schema Version",
            "type": "integer"
          },
          "source_commit": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Git commit this process's source was at when the process started, or null when the source is not a checkout. Captured at startup and never recomputed, so it describes the code in memory rather than the files on disk -- which is what makes it evidence that a merge is actually live rather than merely committed.",
            "title": "Source Commit"
          },
          "source_root": {
            "description": "Directory this process imported its own code from. Startup refuses when that is the wrong checkout, but the answer is reported here too: on a machine with several worktrees it is the difference between a stale server and a wrongly-installed one, and guessing costs a forensic session.",
            "title": "Source Root",
            "type": "string"
          },
          "started_at": {
            "description": "When this process fixed its identity, in UTC. Independent of the commit: a process that started before a merge cannot be serving it, whatever any file on disk now says.",
            "title": "Started At",
            "type": "string"
          },
          "version": {
            "description": "Installed AgentJobs package version.",
            "title": "Version",
            "type": "string"
          },
          "yaml_loader": {
            "description": "Which YAML parser reads task files. The pure-Python fallback is about thirteen times slower than libyaml and is the usual explanation for a sluggish install, so it is reported rather than left to be guessed at.",
            "title": "Yaml Loader",
            "type": "string"
          }
        },
        "required": [
          "version",
          "schema_version",
          "yaml_loader",
          "source_root",
          "started_at",
          "api_digest",
          "frontend_bundle"
        ],
        "title": "VersionResponse",
        "type": "object"
      },
      "Webhook": {
        "description": "Webhook configuration for task event notifications.",
        "properties": {
          "active": {
            "default": true,
            "description": "Whether this webhook is active.",
            "title": "Active",
            "type": "boolean"
          },
          "created": {
            "description": "When the webhook was created.",
            "format": "date-time",
            "title": "Created",
            "type": "string"
          },
          "events": {
            "description": "List of events to trigger this webhook.",
            "items": {
              "type": "string"
            },
            "title": "Events",
            "type": "array"
          },
          "id": {
            "description": "Unique webhook identifier.",
            "title": "Id",
            "type": "string"
          },
          "last_triggered": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Last time this webhook was successfully triggered.",
            "title": "Last Triggered"
          },
          "secret": {
            "description": "Secret for HMAC signature verification.",
            "title": "Secret",
            "type": "string"
          },
          "url": {
            "description": "Target URL for webhook delivery.",
            "format": "uri",
            "maxLength": 2083,
            "minLength": 1,
            "title": "Url",
            "type": "string"
          }
        },
        "required": [
          "id",
          "url",
          "events",
          "secret",
          "created"
        ],
        "title": "Webhook",
        "type": "object"
      },
      "WebhookCreateRequest": {
        "description": "Request to create a webhook.",
        "properties": {
          "active": {
            "default": true,
            "description": "Whether webhook is active",
            "title": "Active",
            "type": "boolean"
          },
          "events": {
            "description": "Events to subscribe to",
            "items": {
              "type": "string"
            },
            "title": "Events",
            "type": "array"
          },
          "secret": {
            "description": "Secret for HMAC verification",
            "title": "Secret",
            "type": "string"
          },
          "url": {
            "description": "Webhook target URL",
            "format": "uri",
            "maxLength": 2083,
            "minLength": 1,
            "title": "Url",
            "type": "string"
          }
        },
        "required": [
          "url",
          "events",
          "secret"
        ],
        "title": "WebhookCreateRequest",
        "type": "object"
      },
      "WhoAmIResponse": {
        "description": "The principal this request resolved to, or the reason none did.\n\nThe observable half of task-329's resolution and task-331's credential: without a\nroute that says what the application concluded, \"a dispatched run identifies as its\nrun\" is only assertable against a synthesised header in a unit test. Here a real\ndispatched agent can ask, over the same transport it makes every other call on, and\nget the answer the middleware actually reached.\n\nIt reports identity; it enforces nothing. Every route answers exactly as it did\nbefore, including for a caller this resolves nothing for -- that is task-332's\nquestion, and answering it here would smuggle enforcement into a change meant to\nestablish identity.\n\n**It never echoes the credential.** A caller learns which run it is, never the token\nthat proved it. There is nothing here a caller did not already present.",
        "properties": {
          "actor_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The configured actor this principal maps to. Null until task-330 owns that mapping, and null forever for a run -- a run is not a human and must never be attributed as one.",
            "title": "Actor Id"
          },
          "describe": {
            "description": "One line naming the kind and the evidence, as a log would record it.",
            "title": "Describe",
            "type": "string"
          },
          "detail": {
            "default": "",
            "description": "A sentence a person can act on when nothing resolved.",
            "title": "Detail",
            "type": "string"
          },
          "kind": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "tailnet, owner, or run. Null when nothing resolved.",
            "title": "Kind"
          },
          "login": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The raw identity the front door proved. Set for kind tailnet only.",
            "title": "Login"
          },
          "problem": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why nothing resolved: no_proven_identity, unverified_run_credential, or expired_run_credential. An expired credential is reported as its own problem rather than resolving the owner -- falling back to a more capable principal on expiry would invert the control.",
            "title": "Problem"
          },
          "run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The dispatched run asking. Set for kind run only.",
            "title": "Run Id"
          },
          "source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "How the identity was established: run_credential, proven_header, or loopback. Kept separate from the kind because an audit trail that records only what a caller is cannot answer on what evidence.",
            "title": "Source"
          },
          "task_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The task that run is working. Set for kind run only.",
            "title": "Task Id"
          }
        },
        "required": [
          "describe"
        ],
        "title": "WhoAmIResponse",
        "type": "object"
      }
    }
  },
  "info": {
    "description": "REST API for interacting with AgentJobs tasks, including task management, status tracking, prompt coordination, and search.",
    "title": "AgentJobs API",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/all/tasks": {
      "get": {
        "description": "Every task this caller may see, each tagged with the project it belongs to.\n\nMounted at ``/api/all/tasks`` rather than as a magic id under ``/api/tasks/``,\nbecause ``/api/tasks/all`` would be indistinguishable from a task whose id is\nliterally \"all\" and would shadow it.\n\nRead-only by design. Writes always address one project explicitly, so there stays\nexactly one code path that mutates a file.\n\n**Filtered by exposure, not denied** (task-333). The audit that found this route\nreturning 3.9 MB of every project's tasks to the tailnet proposed blocking it at the\nproxy; filtering is strictly better, because the route keeps working for the\nprojects a phone is meant to see and stays correct the day there is a second front\ndoor -- a proxy rule protects only the door it is written on.",
        "operationId": "get_all_tasks_api_all_tasks_get",
        "parameters": [
          {
            "in": "query",
            "name": "lifecycle",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Lifecycle"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Lifecycle"
            }
          },
          {
            "in": "query",
            "name": "ball",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Ball"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Ball"
            }
          },
          {
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Project"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  "title": "Response Get All Tasks Api All Tasks Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get All Tasks",
        "tags": [
          "projects"
        ]
      }
    },
    "/api/analytics": {
      "get": {
        "description": "Backlog, throughput, aging and where work is stuck, over one window.\n\n**A project with no history is a 200, not an error** (§7.4): the totals are\npopulated from ``task``, every series is empty and ``coverage.baseline_at`` is null,\nwhich is what lets the page tell \"nothing has happened here\" from \"we do not know\nwhat happened\". Those are different sentences and the page renders them differently.",
        "operationId": "get_analytics_api_analytics_get",
        "parameters": [
          {
            "description": "How far back to look. Four presets are the whole surface (§7.1).",
            "in": "query",
            "name": "range",
            "required": false,
            "schema": {
              "default": "30d",
              "description": "How far back to look. Four presets are the whole surface (§7.1).",
              "pattern": "^(30d|90d|12m|all)$",
              "title": "Range",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalyticsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Analytics",
        "tags": [
          "analytics"
        ]
      }
    },
    "/api/analytics/finish-estimator/reset": {
      "post": {
        "description": "Forget the landing estimate's learned correction (task-586).\n\nDeletes nothing: every prediction and every finish stays, and the accuracy chart\nkeeps scoring them. Only landings that start after this moment teach the bias factor,\nso the estimate returns to the uncorrected medians until three of them have ended.\nAn owner's act, because it changes what every Landing row shows.",
        "operationId": "reset_finish_estimator_api_analytics_finish_estimator_reset_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EstimatorResetResult"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Reset Finish Estimator",
        "tags": [
          "analytics"
        ]
      }
    },
    "/api/attention": {
      "get": {
        "description": "Return how many tasks are stopped waiting on a person, plus the episode.\n\nThe header polls this on every surface, so it deliberately answers with one\ninteger and one small object instead of the records behind them -- see\n:class:`AttentionResponse`. It is the same predicate the dashboard's own tile and\nthe legacy header use, from the same function, because a badge that disagrees with\nthe page it links to is worse than no badge.\n\n**This read reconciles**, which is the one thing about it worth knowing. The\nepisode is a fact about the waiting set, so it is brought up to date here rather\nthan on a clock of its own: the header was polling anyway, and a reconcile is\nidempotent, so polling cannot manufacture attention. Nothing in the answer depends\non a notification having been delivered.",
        "operationId": "get_attention_api_attention_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AttentionResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Attention",
        "tags": [
          "dashboard"
        ]
      }
    },
    "/api/attention/ack": {
      "post": {
        "description": "Record that a person deliberately acted on the episode they were shown.\n\nAcknowledgment stops the *interruption*, not the indicator: the badge tracks the\nwaiting set and stays up until it empties. What it buys is that the next task to\nstop on this person may interrupt again, which is the whole of the anti-fatigue\nrule (task-422's decision entry).\n\nAn id that is no longer current is not an error -- see\n:func:`agentjobs.attention.acknowledge`. The caller gets the current state back and\nrenders it.",
        "operationId": "acknowledge_attention_api_attention_ack_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AttentionAckRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AttentionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Acknowledge Attention",
        "tags": [
          "dashboard"
        ]
      }
    },
    "/api/dashboard": {
      "get": {
        "description": "Return the dashboard projection, including its single next action.\n\n``identity`` rides along for the same reason it rides along on the task detail and\nthe playbook listing: the next-up panel offers a Dispatch button, a run has to be\nattributed to a real person, and a button that cannot name one must be disabled with\nthe reason rather than pressable into a refusal.",
        "operationId": "get_dashboard_api_dashboard_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DashboardResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Dashboard",
        "tags": [
          "dashboard"
        ]
      }
    },
    "/api/dispatch": {
      "get": {
        "description": "Whether this project may dispatch right now, and which gate says otherwise.",
        "operationId": "get_dispatch_state_api_dispatch_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Dispatch State",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/arm": {
      "post": {
        "description": "Arm the pull mode for this project. Starts nothing; the server's tick does.\n\n**Human-only, and enforced in one place rather than here.** ``DISPATCH_ADMIN`` is not\nin a run's grant, so a run credential is refused 403 by the capability dependency\nbefore this function is entered -- which is the property that matters: a run must not\nbe able to arm the machine to keep starting runs, and that is a rule about the table\nrather than about anyone remembering to check it in a handler.\n\nThe ``user`` is checked against the principal the request resolved to, on the same\nterms a dispatch's is (task-332): every run this arming buys will carry that person's\nname, so naming somebody else would be signing their authorisation.",
        "operationId": "arm_pull_mode_api_dispatch_arm_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PullArmRequest",
                "default": {
                  "bound_kind": ""
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Arm Pull Mode",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/disable": {
      "post": {
        "description": "Stop dispatching for this project. Takes nothing, asks nothing.\n\nRefuses only when there is no config file at all, which is already a state in which\nnothing can dispatch -- so there is no reachable case where a human wants this off\nand cannot have it.",
        "operationId": "disable_dispatch_api_dispatch_disable_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Disable Dispatch",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/disarm": {
      "post": {
        "description": "Stop the pull mode starting anything more. **Kills nothing.**\n\nTakes nothing and asks nothing, like ``/disable`` and for the same reason: a switch\nyou cannot reach is not one, and one that argues with you is worse than none. Runs\nalready going are untouched -- each was authorised individually and has its own merge\ngate, and destroying work somebody's arming already bought is not what \"stop\" means.\n\nQuiet when the project was not armed, which covers a second press and a press that\nraced a bound running out.",
        "operationId": "disarm_pull_mode_api_dispatch_disarm_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Disarm Pull Mode",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/enable": {
      "post": {
        "description": "Enable dispatch for this project against an already-defined runner.",
        "operationId": "enable_dispatch_api_dispatch_enable_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DispatchEnableRequest",
                "default": {}
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Enable Dispatch",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/finishes/{task_id}": {
      "get": {
        "description": "What is happening to this task's branch, or last happened to it.\n\nKeyed on the task rather than on a finish id, because the question a page asks is\n\"what is happening to *this*\", and the reader pressing Approve has no finish id to\nask with -- the finish that answers it does not exist yet at the moment they press.\n\n``null`` is the ordinary answer for almost every task, and means no finish has run\nfor it on this machine. The page renders nothing at all for that, which is why it is\na null body rather than a 404: an absent finish is not a missing resource, and a\ntask page that logged a 404 every two seconds would teach its reader to ignore them.",
        "operationId": "read_task_finish_api_dispatch_finishes__task_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/TaskFinishView"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "title": "Response Read Task Finish Api Dispatch Finishes  Task Id  Get"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Task Finish",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/finishes/{task_id}/output": {
      "get": {
        "description": "A finish's output in full, as text a browser tab can show.\n\nText rather than JSON, for the same reason a run's is: this is read by a person, and\na step table wrapped in a JSON string escape is unreadable.",
        "operationId": "read_task_finish_output_api_dispatch_finishes__task_id__output_get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Task Finish Output",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/runs": {
      "get": {
        "description": "Runs belonging to this project, newest first.\n\nFiltered by project rather than returning the machine's whole ledger: a run\ndirectory records which project it belongs to, and a page about one project has no\nbusiness showing another's.",
        "operationId": "list_dispatch_runs_api_dispatch_runs_get",
        "parameters": [
          {
            "description": "Only runs for this task. Omitted, every run in the project.",
            "in": "query",
            "name": "task_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only runs for this task. Omitted, every run in the project.",
              "title": "Task Id"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 20,
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/DispatchRunView"
                  },
                  "title": "Response List Dispatch Runs Api Dispatch Runs Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Dispatch Runs",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/runs/{run_id}/cancel": {
      "post": {
        "description": "Stop one run, or take one queued dispatch out of the line, and record it.\n\n**The queue is tried first, and only a *waiting* entry answers here.** A queued\ndispatch that started a second ago is a run, its queue row says ``started``, and this\nfalls through to the run path with the run's own id -- which is the one that stops\nsomething. The reverse ordering would report a cancellation over a live agent.\n\nSame route for both because they are the same act from where the person is standing:\nthe card they are cancelling is the same card, before and after a slot freed under it.",
        "operationId": "cancel_dispatch_run_api_dispatch_runs__run_id__cancel_post",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchCancelResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Cancel Dispatch Run",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/runs/{run_id}/output": {
      "get": {
        "description": "A run's output in full, as text a browser tab can show.\n\nText rather than JSON because this is the one dispatch response a human reads\ndirectly, and a transcript wrapped in a JSON string escape is unreadable.",
        "operationId": "read_dispatch_run_output_api_dispatch_runs__run_id__output_get",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Dispatch Run Output",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/runs/{run_id}/tail": {
      "get": {
        "description": "The end of a run's output, for a page watching it while it happens.\n\nA separate endpoint from ``/output`` rather than a parameter on it, because the two\nare read by different things: ``/output`` is a link a human opens, so it answers in\ntext and gives them everything, while this is polled by a page and answers in the\nshape the generated client already understands.\n\n**No subprocess is involved.** The text comes from a file the session poller wrote,\nso a hundred people watching one run costs a hundred file reads and no more processes\nthan nobody watching it -- and the tail can never be fresher than the poller's own\ninterval, which is the point rather than a limitation.",
        "operationId": "read_dispatch_run_tail_api_dispatch_runs__run_id__tail_get",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "lines",
            "required": false,
            "schema": {
              "default": 40,
              "maximum": 200,
              "minimum": 1,
              "title": "Lines",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchRunTailView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Dispatch Run Tail",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/dispatch/runs/{run_id}/transcript": {
      "get": {
        "description": "What the session recorded about itself, as entries rather than as a screen.\n\nPolled on the same clock as ``/tail`` and read the same way -- a file, no subprocess\n-- but from a different file. ``/tail`` serves the pty capture, which is a *repaint*\nof a terminal and loses every space and line break the moment its escape sequences\nare removed. This serves the JSONL the runner writes beside it, where the agent's\nprose, each tool call and whether it failed are all still separate things.\n\n``source: 'none'`` is an ordinary answer, not an error: a batch run, a session that\nhas not reported its id yet, and a driver that keeps no such file all reach it. The\n``note`` says which, and the caller falls back to the tail.",
        "operationId": "read_dispatch_run_transcript_api_dispatch_runs__run_id__transcript_get",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "entries",
            "required": false,
            "schema": {
              "default": 40,
              "maximum": 200,
              "minimum": 1,
              "title": "Entries",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchRunTranscriptView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Dispatch Run Transcript",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/health": {
      "get": {
        "description": "Simple health check endpoint for API consumers.",
        "operationId": "api_health_check_api_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Api Health Check Api Health Get",
                  "type": "object"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Api Health Check",
        "tags": [
          "system"
        ]
      }
    },
    "/api/history/finishes/{finish_id}": {
      "put": {
        "description": "Index one scripted finish and the steps it has taken so far.\n\n``written`` is false, with a reason, when an imported record met a row already there\nor when the finish names a task this project does not have. Both are answers rather\nthan errors: the importer reports them, and the finisher never sends either.",
        "operationId": "record_finish_history_api_history_finishes__finish_id__put",
        "parameters": [
          {
            "in": "path",
            "name": "finish_id",
            "required": true,
            "schema": {
              "title": "Finish Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FinishHistoryWrite"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HistoryWriteResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Record Finish History",
        "tags": [
          "history"
        ]
      }
    },
    "/api/history/gates/{gate_id}": {
      "put": {
        "description": "Index one gate run and the stages it has finished so far.",
        "operationId": "record_gate_history_api_history_gates__gate_id__put",
        "parameters": [
          {
            "in": "path",
            "name": "gate_id",
            "required": true,
            "schema": {
              "title": "Gate Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GateHistoryWrite"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HistoryWriteResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Record Gate History",
        "tags": [
          "history"
        ]
      }
    },
    "/api/model": {
      "get": {
        "description": "Whether a drafting call can be made on this machine, and why not when it cannot.\n\nNames no project: what is configured is a property of the machine, and a per-project\nanswer would imply a per-project credential, which is the one arrangement the design\nrules out (``docs/model-access-design.md`` §4).",
        "operationId": "get_model_status_api_model_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ModelStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Model Status",
        "tags": [
          "model"
        ]
      }
    },
    "/api/model/draft": {
      "post": {
        "description": "Expand what a person typed into spec fields. Writes nothing.\n\nProject-scoped because the draft is told which project it is being filed into, which\nis context a spec legitimately reflects. It reads nothing else about the project and\nwrites nothing to it.\n\nEvery refusal comes back as a 200 with ``drafted: false``; see\n:class:`~agentjobs.api.models.SpecDraftResponse` for why that is the shape.",
        "operationId": "draft_task_spec_api_model_draft_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SpecDraftRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SpecDraftResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Draft Task Spec",
        "tags": [
          "model"
        ]
      }
    },
    "/api/playbooks": {
      "get": {
        "description": "Every playbook this project holds, with the files that would not load.",
        "operationId": "get_playbooks_api_playbooks_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaybookCollection"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Playbooks",
        "tags": [
          "playbooks"
        ]
      }
    },
    "/api/playbooks/{name}": {
      "get": {
        "description": "One playbook by name, including its brief.\n\n422 rather than 404 for a file that exists and does not validate, matching how a\nstored task that will not parse is reported: the thing is there and repairable,\nand \"not found\" would send its author looking for something already on disk.",
        "operationId": "get_playbook_api_playbooks__name__get",
        "parameters": [
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "title": "Name",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaybookRead"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Playbook",
        "tags": [
          "playbooks"
        ]
      }
    },
    "/api/playbooks/{name}/run": {
      "post": {
        "description": "Start an agent on a playbook: create its run task if it has one, then dispatch.\n\n202 rather than 200, and for the same reason ``POST /tasks/{id}/dispatch`` is: the\nrun has started, and how it ends arrives later as ``dispatch_result`` entries on the\ntask rather than in this response.\n\n**Every dispatch gate binds, unchanged.** This endpoint opens nothing: it resolves a\nfile, checks the invocation's shape against the playbook's ``target``, and then\ncalls the same ``dispatch_task`` the Dispatch button calls, with a pointer attached\nthat no guard reads. A machine with dispatch off refuses here as it refuses there,\nand refuses *before* a run task is created.\n\n``user`` reaches the record by one of two routes, and which one depends on the\nplaybook rather than on this endpoint. A project-target run creates the run task\nattributed to that human, and their creation entry is the newest stored entry the\nhuman-clocked rule then reads. A task-target run creates nothing, so ``user`` is\npassed to the dispatcher as task-188's ``authorized_by`` and its authorising entry\nis written onto the task named. Neither path substitutes the project's\n``default_user`` for a human nobody named.",
        "operationId": "run_playbook_endpoint_api_playbooks__name__run_post",
        "parameters": [
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "title": "Name",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaybookRunRequestBody",
                "default": {}
              }
            }
          }
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaybookRunStarted"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Run Playbook Endpoint",
        "tags": [
          "playbooks"
        ]
      }
    },
    "/api/projects": {
      "get": {
        "description": "List every project this server can serve *this caller*, with task counts.\n\nA project whose directory has gone missing is reported with a null task_count\nrather than failing the whole listing -- the registry is machine-local and a\ncheckout can legitimately disappear.\n\nA project marked ``visibility: local`` is simply not in the list for a remote\ncaller (task-333). Omitted rather than returned with a flag, because a flag would\ndisclose the name and the root path of the thing being hidden, and because a client\nthat has to know about a hidden-project state is a client that will one day render\none.",
        "operationId": "get_projects_api_projects_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ProjectResponse"
                  },
                  "title": "Response Get Projects Api Projects Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Projects",
        "tags": [
          "projects"
        ]
      },
      "post": {
        "description": "Register an existing AgentJobs project without changing its files.",
        "operationId": "register_project_api_projects_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProjectRegistrationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Register Project",
        "tags": [
          "projects"
        ]
      }
    },
    "/api/projects/init": {
      "post": {
        "description": "Initialize an existing untracked directory and register the new project.",
        "operationId": "initialize_and_register_project_api_projects_init_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProjectInitializationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Initialize And Register Project",
        "tags": [
          "projects"
        ]
      }
    },
    "/api/projects/inspect": {
      "post": {
        "description": "Tell the UI whether confirmation should register or initialize a path.",
        "operationId": "inspect_project_path_api_projects_inspect_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProjectInspectionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectInspection"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Inspect Project Path",
        "tags": [
          "projects"
        ]
      }
    },
    "/api/projects/{project_id}/analytics": {
      "get": {
        "description": "Backlog, throughput, aging and where work is stuck, over one window.\n\n**A project with no history is a 200, not an error** (§7.4): the totals are\npopulated from ``task``, every series is empty and ``coverage.baseline_at`` is null,\nwhich is what lets the page tell \"nothing has happened here\" from \"we do not know\nwhat happened\". Those are different sentences and the page renders them differently.",
        "operationId": "get_analytics_api_projects__project_id__analytics_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "How far back to look. Four presets are the whole surface (§7.1).",
            "in": "query",
            "name": "range",
            "required": false,
            "schema": {
              "default": "30d",
              "description": "How far back to look. Four presets are the whole surface (§7.1).",
              "pattern": "^(30d|90d|12m|all)$",
              "title": "Range",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnalyticsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Analytics",
        "tags": [
          "analytics"
        ]
      }
    },
    "/api/projects/{project_id}/analytics/finish-estimator/reset": {
      "post": {
        "description": "Forget the landing estimate's learned correction (task-586).\n\nDeletes nothing: every prediction and every finish stays, and the accuracy chart\nkeeps scoring them. Only landings that start after this moment teach the bias factor,\nso the estimate returns to the uncorrected medians until three of them have ended.\nAn owner's act, because it changes what every Landing row shows.",
        "operationId": "reset_finish_estimator_api_projects__project_id__analytics_finish_estimator_reset_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EstimatorResetResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Reset Finish Estimator",
        "tags": [
          "analytics"
        ]
      }
    },
    "/api/projects/{project_id}/attention": {
      "get": {
        "description": "Return how many tasks are stopped waiting on a person, plus the episode.\n\nThe header polls this on every surface, so it deliberately answers with one\ninteger and one small object instead of the records behind them -- see\n:class:`AttentionResponse`. It is the same predicate the dashboard's own tile and\nthe legacy header use, from the same function, because a badge that disagrees with\nthe page it links to is worse than no badge.\n\n**This read reconciles**, which is the one thing about it worth knowing. The\nepisode is a fact about the waiting set, so it is brought up to date here rather\nthan on a clock of its own: the header was polling anyway, and a reconcile is\nidempotent, so polling cannot manufacture attention. Nothing in the answer depends\non a notification having been delivered.",
        "operationId": "get_attention_api_projects__project_id__attention_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AttentionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Attention",
        "tags": [
          "dashboard"
        ]
      }
    },
    "/api/projects/{project_id}/attention/ack": {
      "post": {
        "description": "Record that a person deliberately acted on the episode they were shown.\n\nAcknowledgment stops the *interruption*, not the indicator: the badge tracks the\nwaiting set and stays up until it empties. What it buys is that the next task to\nstop on this person may interrupt again, which is the whole of the anti-fatigue\nrule (task-422's decision entry).\n\nAn id that is no longer current is not an error -- see\n:func:`agentjobs.attention.acknowledge`. The caller gets the current state back and\nrenders it.",
        "operationId": "acknowledge_attention_api_projects__project_id__attention_ack_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AttentionAckRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AttentionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Acknowledge Attention",
        "tags": [
          "dashboard"
        ]
      }
    },
    "/api/projects/{project_id}/dashboard": {
      "get": {
        "description": "Return the dashboard projection, including its single next action.\n\n``identity`` rides along for the same reason it rides along on the task detail and\nthe playbook listing: the next-up panel offers a Dispatch button, a run has to be\nattributed to a real person, and a button that cannot name one must be disabled with\nthe reason rather than pressable into a refusal.",
        "operationId": "get_dashboard_api_projects__project_id__dashboard_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DashboardResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Dashboard",
        "tags": [
          "dashboard"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch": {
      "get": {
        "description": "Whether this project may dispatch right now, and which gate says otherwise.",
        "operationId": "get_dispatch_state_api_projects__project_id__dispatch_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Dispatch State",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/arm": {
      "post": {
        "description": "Arm the pull mode for this project. Starts nothing; the server's tick does.\n\n**Human-only, and enforced in one place rather than here.** ``DISPATCH_ADMIN`` is not\nin a run's grant, so a run credential is refused 403 by the capability dependency\nbefore this function is entered -- which is the property that matters: a run must not\nbe able to arm the machine to keep starting runs, and that is a rule about the table\nrather than about anyone remembering to check it in a handler.\n\nThe ``user`` is checked against the principal the request resolved to, on the same\nterms a dispatch's is (task-332): every run this arming buys will carry that person's\nname, so naming somebody else would be signing their authorisation.",
        "operationId": "arm_pull_mode_api_projects__project_id__dispatch_arm_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PullArmRequest",
                "default": {
                  "bound_kind": ""
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Arm Pull Mode",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/disable": {
      "post": {
        "description": "Stop dispatching for this project. Takes nothing, asks nothing.\n\nRefuses only when there is no config file at all, which is already a state in which\nnothing can dispatch -- so there is no reachable case where a human wants this off\nand cannot have it.",
        "operationId": "disable_dispatch_api_projects__project_id__dispatch_disable_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Disable Dispatch",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/disarm": {
      "post": {
        "description": "Stop the pull mode starting anything more. **Kills nothing.**\n\nTakes nothing and asks nothing, like ``/disable`` and for the same reason: a switch\nyou cannot reach is not one, and one that argues with you is worse than none. Runs\nalready going are untouched -- each was authorised individually and has its own merge\ngate, and destroying work somebody's arming already bought is not what \"stop\" means.\n\nQuiet when the project was not armed, which covers a second press and a press that\nraced a bound running out.",
        "operationId": "disarm_pull_mode_api_projects__project_id__dispatch_disarm_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Disarm Pull Mode",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/enable": {
      "post": {
        "description": "Enable dispatch for this project against an already-defined runner.",
        "operationId": "enable_dispatch_api_projects__project_id__dispatch_enable_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DispatchEnableRequest",
                "default": {}
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStateView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Enable Dispatch",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/finishes/{task_id}": {
      "get": {
        "description": "What is happening to this task's branch, or last happened to it.\n\nKeyed on the task rather than on a finish id, because the question a page asks is\n\"what is happening to *this*\", and the reader pressing Approve has no finish id to\nask with -- the finish that answers it does not exist yet at the moment they press.\n\n``null`` is the ordinary answer for almost every task, and means no finish has run\nfor it on this machine. The page renders nothing at all for that, which is why it is\na null body rather than a 404: an absent finish is not a missing resource, and a\ntask page that logged a 404 every two seconds would teach its reader to ignore them.",
        "operationId": "read_task_finish_api_projects__project_id__dispatch_finishes__task_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/TaskFinishView"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "title": "Response Read Task Finish Api Projects  Project Id  Dispatch Finishes  Task Id  Get"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Task Finish",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/finishes/{task_id}/output": {
      "get": {
        "description": "A finish's output in full, as text a browser tab can show.\n\nText rather than JSON, for the same reason a run's is: this is read by a person, and\na step table wrapped in a JSON string escape is unreadable.",
        "operationId": "read_task_finish_output_api_projects__project_id__dispatch_finishes__task_id__output_get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Task Finish Output",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/runs": {
      "get": {
        "description": "Runs belonging to this project, newest first.\n\nFiltered by project rather than returning the machine's whole ledger: a run\ndirectory records which project it belongs to, and a page about one project has no\nbusiness showing another's.",
        "operationId": "list_dispatch_runs_api_projects__project_id__dispatch_runs_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Only runs for this task. Omitted, every run in the project.",
            "in": "query",
            "name": "task_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only runs for this task. Omitted, every run in the project.",
              "title": "Task Id"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 20,
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/DispatchRunView"
                  },
                  "title": "Response List Dispatch Runs Api Projects  Project Id  Dispatch Runs Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Dispatch Runs",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/runs/{run_id}/cancel": {
      "post": {
        "description": "Stop one run, or take one queued dispatch out of the line, and record it.\n\n**The queue is tried first, and only a *waiting* entry answers here.** A queued\ndispatch that started a second ago is a run, its queue row says ``started``, and this\nfalls through to the run path with the run's own id -- which is the one that stops\nsomething. The reverse ordering would report a cancellation over a live agent.\n\nSame route for both because they are the same act from where the person is standing:\nthe card they are cancelling is the same card, before and after a slot freed under it.",
        "operationId": "cancel_dispatch_run_api_projects__project_id__dispatch_runs__run_id__cancel_post",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchCancelResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Cancel Dispatch Run",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/runs/{run_id}/output": {
      "get": {
        "description": "A run's output in full, as text a browser tab can show.\n\nText rather than JSON because this is the one dispatch response a human reads\ndirectly, and a transcript wrapped in a JSON string escape is unreadable.",
        "operationId": "read_dispatch_run_output_api_projects__project_id__dispatch_runs__run_id__output_get",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Dispatch Run Output",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/runs/{run_id}/tail": {
      "get": {
        "description": "The end of a run's output, for a page watching it while it happens.\n\nA separate endpoint from ``/output`` rather than a parameter on it, because the two\nare read by different things: ``/output`` is a link a human opens, so it answers in\ntext and gives them everything, while this is polled by a page and answers in the\nshape the generated client already understands.\n\n**No subprocess is involved.** The text comes from a file the session poller wrote,\nso a hundred people watching one run costs a hundred file reads and no more processes\nthan nobody watching it -- and the tail can never be fresher than the poller's own\ninterval, which is the point rather than a limitation.",
        "operationId": "read_dispatch_run_tail_api_projects__project_id__dispatch_runs__run_id__tail_get",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "lines",
            "required": false,
            "schema": {
              "default": 40,
              "maximum": 200,
              "minimum": 1,
              "title": "Lines",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchRunTailView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Dispatch Run Tail",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/dispatch/runs/{run_id}/transcript": {
      "get": {
        "description": "What the session recorded about itself, as entries rather than as a screen.\n\nPolled on the same clock as ``/tail`` and read the same way -- a file, no subprocess\n-- but from a different file. ``/tail`` serves the pty capture, which is a *repaint*\nof a terminal and loses every space and line break the moment its escape sequences\nare removed. This serves the JSONL the runner writes beside it, where the agent's\nprose, each tool call and whether it failed are all still separate things.\n\n``source: 'none'`` is an ordinary answer, not an error: a batch run, a session that\nhas not reported its id yet, and a driver that keeps no such file all reach it. The\n``note`` says which, and the caller falls back to the tail.",
        "operationId": "read_dispatch_run_transcript_api_projects__project_id__dispatch_runs__run_id__transcript_get",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "entries",
            "required": false,
            "schema": {
              "default": 40,
              "maximum": 200,
              "minimum": 1,
              "title": "Entries",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchRunTranscriptView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Dispatch Run Transcript",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/projects/{project_id}/history/finishes/{finish_id}": {
      "put": {
        "description": "Index one scripted finish and the steps it has taken so far.\n\n``written`` is false, with a reason, when an imported record met a row already there\nor when the finish names a task this project does not have. Both are answers rather\nthan errors: the importer reports them, and the finisher never sends either.",
        "operationId": "record_finish_history_api_projects__project_id__history_finishes__finish_id__put",
        "parameters": [
          {
            "in": "path",
            "name": "finish_id",
            "required": true,
            "schema": {
              "title": "Finish Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FinishHistoryWrite"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HistoryWriteResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Record Finish History",
        "tags": [
          "history"
        ]
      }
    },
    "/api/projects/{project_id}/history/gates/{gate_id}": {
      "put": {
        "description": "Index one gate run and the stages it has finished so far.",
        "operationId": "record_gate_history_api_projects__project_id__history_gates__gate_id__put",
        "parameters": [
          {
            "in": "path",
            "name": "gate_id",
            "required": true,
            "schema": {
              "title": "Gate Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GateHistoryWrite"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HistoryWriteResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Record Gate History",
        "tags": [
          "history"
        ]
      }
    },
    "/api/projects/{project_id}/model": {
      "get": {
        "description": "Whether a drafting call can be made on this machine, and why not when it cannot.\n\nNames no project: what is configured is a property of the machine, and a per-project\nanswer would imply a per-project credential, which is the one arrangement the design\nrules out (``docs/model-access-design.md`` §4).",
        "operationId": "get_model_status_api_projects__project_id__model_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ModelStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Model Status",
        "tags": [
          "model"
        ]
      }
    },
    "/api/projects/{project_id}/model/draft": {
      "post": {
        "description": "Expand what a person typed into spec fields. Writes nothing.\n\nProject-scoped because the draft is told which project it is being filed into, which\nis context a spec legitimately reflects. It reads nothing else about the project and\nwrites nothing to it.\n\nEvery refusal comes back as a 200 with ``drafted: false``; see\n:class:`~agentjobs.api.models.SpecDraftResponse` for why that is the shape.",
        "operationId": "draft_task_spec_api_projects__project_id__model_draft_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SpecDraftRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SpecDraftResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Draft Task Spec",
        "tags": [
          "model"
        ]
      }
    },
    "/api/projects/{project_id}/playbooks": {
      "get": {
        "description": "Every playbook this project holds, with the files that would not load.",
        "operationId": "get_playbooks_api_projects__project_id__playbooks_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaybookCollection"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Playbooks",
        "tags": [
          "playbooks"
        ]
      }
    },
    "/api/projects/{project_id}/playbooks/{name}": {
      "get": {
        "description": "One playbook by name, including its brief.\n\n422 rather than 404 for a file that exists and does not validate, matching how a\nstored task that will not parse is reported: the thing is there and repairable,\nand \"not found\" would send its author looking for something already on disk.",
        "operationId": "get_playbook_api_projects__project_id__playbooks__name__get",
        "parameters": [
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "title": "Name",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaybookRead"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Playbook",
        "tags": [
          "playbooks"
        ]
      }
    },
    "/api/projects/{project_id}/playbooks/{name}/run": {
      "post": {
        "description": "Start an agent on a playbook: create its run task if it has one, then dispatch.\n\n202 rather than 200, and for the same reason ``POST /tasks/{id}/dispatch`` is: the\nrun has started, and how it ends arrives later as ``dispatch_result`` entries on the\ntask rather than in this response.\n\n**Every dispatch gate binds, unchanged.** This endpoint opens nothing: it resolves a\nfile, checks the invocation's shape against the playbook's ``target``, and then\ncalls the same ``dispatch_task`` the Dispatch button calls, with a pointer attached\nthat no guard reads. A machine with dispatch off refuses here as it refuses there,\nand refuses *before* a run task is created.\n\n``user`` reaches the record by one of two routes, and which one depends on the\nplaybook rather than on this endpoint. A project-target run creates the run task\nattributed to that human, and their creation entry is the newest stored entry the\nhuman-clocked rule then reads. A task-target run creates nothing, so ``user`` is\npassed to the dispatcher as task-188's ``authorized_by`` and its authorising entry\nis written onto the task named. Neither path substitutes the project's\n``default_user`` for a human nobody named.",
        "operationId": "run_playbook_endpoint_api_projects__project_id__playbooks__name__run_post",
        "parameters": [
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "title": "Name",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaybookRunRequestBody",
                "default": {}
              }
            }
          }
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaybookRunStarted"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Run Playbook Endpoint",
        "tags": [
          "playbooks"
        ]
      }
    },
    "/api/projects/{project_id}/push": {
      "get": {
        "description": "The application-server key, and every device registered for this project.",
        "operationId": "get_push_status_api_projects__project_id__push_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Push Status",
        "tags": [
          "push"
        ]
      }
    },
    "/api/projects/{project_id}/push/subscribe": {
      "post": {
        "description": "Register a device, or refresh one that already exists.\n\n**A device registered mid-episode is not immediately pushed.** The new row inherits\nnothing, so ``last_episode_id`` is seeded with whatever episode is currently open:\na person who opts in from a page that is already showing \"3 waiting\" has seen the\nthree, and interrupting them about it a second later would be the alert fatigue the\nepisode model exists to prevent. The *next* episode wakes them, which is the thing\nthey subscribed for; ``POST /push/test`` is how they confirm it works before then.\n\nRe-subscribing an endpoint that is already registered keeps its id and its episode\nid -- see :func:`agentjobs.push.subscriptions.upsert`.",
        "operationId": "subscribe_push_device_api_projects__project_id__push_subscribe_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PushSubscribeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Subscribe Push Device",
        "tags": [
          "push"
        ]
      }
    },
    "/api/projects/{project_id}/push/test": {
      "post": {
        "description": "Prove delivery to one device, or to all of them.\n\nThe answer to \"did that actually work\", which nothing else gives: a device that\nopted in while nothing was waiting would otherwise find out whether push reaches it\nat the moment it matters least. It does not touch the episode -- a test must not be\nable to consume the interruption a real episode is owed.",
        "operationId": "send_test_push_api_projects__project_id__push_test_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PushTestRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushTestResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Send Test Push",
        "tags": [
          "push"
        ]
      }
    },
    "/api/projects/{project_id}/push/unsubscribe": {
      "post": {
        "description": "Forget a device, by id from the page or by endpoint from a service worker.\n\nIdempotent, and silent about whether anything matched. A device is routinely\nremoved from both ends at once -- the button here and the browser's own permission\nreset -- and a 404 for the second of those would be an error message about a race.",
        "operationId": "unsubscribe_push_device_api_projects__project_id__push_unsubscribe_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PushUnsubscribeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Unsubscribe Push Device",
        "tags": [
          "push"
        ]
      }
    },
    "/api/projects/{project_id}/queue": {
      "get": {
        "description": "The whole ordered backlog, band by band. This is the list a human reviews.\n\nEvery band appears, including the empty ones: \"critical is empty\" is a fact a\nreader of an ordered backlog wants stated rather than inferred from a heading that\nis not there. Every open task carries its claimability and, when it is not\nclaimable, the rule that excluded it -- the same sentence ``/tasks/next/explain``\ngives, from the same place, so the two can never disagree.\n\n**This does not 409 on a broken queue**, unlike ``/tasks/next``. It is one of the\ntwo deliberate exceptions in design section 8: you have to be able to see a broken\nqueue in order to fix it, so the offending bands still render and ``problems``\nnames what is wrong beside ``repair_command``.\n\nEach entry also carries ``last_move`` -- the move that set its place, with the\nactor, that actor's kind from this project's vocabulary, the reason recorded with\nit, and whether a human kept the place over a warning. That is the anchor evidence\n``reorder`` reads, and it is here so a run costs one request instead of one record\nfetch per open task.",
        "operationId": "get_queue_api_projects__project_id__queue_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Judge claimability for this agent, so a task restricted to somebody else is marked with that as its reason. Omit it to judge for any agent.",
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Judge claimability for this agent, so a task restricted to somebody else is marked with that as its reason. Omit it to judge for any agent.",
              "title": "Agent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueueResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Queue",
        "tags": [
          "queue"
        ]
      }
    },
    "/api/projects/{project_id}/queue/compact": {
      "post": {
        "description": "Renumber one band back to 100, 200, 300..., changing nobody's place.\n\nPurely cosmetic, and explicit only -- there is no automatic compaction, because a\nbackground process quietly rewriting forty task files is exactly the kind of thing\nthat should require somebody to ask for it. One band per request for the same\nreason: compacting the whole corpus is four decisions, not one.",
        "operationId": "compact_queue_api_projects__project_id__queue_compact_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueCompactRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueueCompactResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Compact Queue",
        "tags": [
          "queue"
        ]
      }
    },
    "/api/projects/{project_id}/queue/repair": {
      "post": {
        "description": "Make a broken queue into a queue again, and say exactly what was guessed.\n\nOperates on a corrupt corpus by definition, so it reads the raw files rather than\nloaded tasks -- the records it most needs are the ones the open-task-has-a-position\nrule refuses to load. Open tasks with no usable position, and the losing claimants\nof a shared one, go to the bottom of their band ordered by ``created`` then id.\n\nIt never invents an opinion it does not have. A duplicate position carries no\nrecord of who was meant to be first, so that tie-break is arbitrary by necessity --\nand every task it touched comes back in ``assigned``, which is what makes the guess\nreviewable rather than silent.",
        "operationId": "repair_queue_api_projects__project_id__queue_repair_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueMaintenanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueueRepairResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Repair Queue",
        "tags": [
          "queue"
        ]
      }
    },
    "/api/projects/{project_id}/revision": {
      "get": {
        "description": "Answer whether this project changed, without loading the task collection.",
        "operationId": "get_project_revision_api_projects__project_id__revision_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectRevisionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Project Revision",
        "tags": [
          "live updates"
        ]
      }
    },
    "/api/projects/{project_id}/search": {
      "get": {
        "description": "Search tasks using a case-insensitive substring query.\n\nRows, in the same shape and for the same reason ``GET /tasks`` returns rows: a\nresult list draws a title, a badge and a place in line. It answered with whole\n``TaskRead`` records until task-495 -- every matched task's spec prose, acceptance\ncriteria and complete log -- which was 10,781 bytes per record and 5.2 MB for 480\nmatches. Whole records are at ``GET /search/full``.\n\nRows carry the same computed dependency facts as ``GET /tasks`` -- ``actionable``,\n``unmet_needs``, ``open_children_count`` -- so a result can be acted on without a\nsecond request per row.",
        "operationId": "search_tasks_api_projects__project_id__search_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "q",
            "required": true,
            "schema": {
              "title": "Q",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskSummaryRead-Output"
                  },
                  "title": "Response Search Tasks Api Projects  Project Id  Search Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Search Tasks",
        "tags": [
          "search"
        ]
      }
    },
    "/api/projects/{project_id}/search/full": {
      "get": {
        "description": "The same hits, in the same order, as whole records.\n\nNamed so the expensive shape is the one a caller asks for by name -- task-484's\ndecision, applied to the second endpoint that had it by default.\n``TaskClient.search_tasks`` is the caller that needs it: it parses each item into a\n``Task``, and a row would parse with an empty log, which is a wrong answer rather\nthan an error.",
        "operationId": "search_tasks_full_api_projects__project_id__search_full_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "q",
            "required": true,
            "schema": {
              "title": "Q",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskRead-Output"
                  },
                  "title": "Response Search Tasks Full Api Projects  Project Id  Search Full Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Search Tasks Full",
        "tags": [
          "search"
        ]
      }
    },
    "/api/projects/{project_id}/tasks": {
      "get": {
        "description": "List tasks filtered along the state axes, as listing rows.\n\n``?ball=human`` is the human inbox: everything waiting on a person, each row\ncarrying its ``ball_prompt``. ``?ball=external`` is the blocked list.\n``?parent=task-063-schema-v2`` is one umbrella's children.\n\n**A row, not a record.** This used to answer with complete tasks -- spec prose,\nacceptance criteria and the entire log of each -- which on the agentjobs backlog was\n10.4 MB and four full corpus loads to draw a column of titles, and grew with every\nlog entry appended to any task (task-484). It now answers with the fields a listing\nreads. Open a task and ``/{task_id}/detail`` carries the rest, which is the fetch\nevery surface that shows a task already makes.\n\nWhole records are at ``/tasks/full``, unchanged. Narrowing *this* route rather than\nadding a narrow sibling is deliberate: the expensive shape should be the one a caller\nasks for by name, so a new surface that never thought about it gets the cheap one.",
        "operationId": "list_tasks_api_projects__project_id__tasks_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "lifecycle",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Lifecycle"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Lifecycle"
            }
          },
          {
            "in": "query",
            "name": "ball",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Ball"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Ball"
            }
          },
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "description": "Return only the children of this umbrella task.",
            "in": "query",
            "name": "parent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only the children of this umbrella task.",
              "title": "Parent"
            }
          },
          {
            "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/TaskKind"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
              "title": "Kind"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskSummaryRead-Output"
                  },
                  "title": "Response List Tasks Api Projects  Project Id  Tasks Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Tasks",
        "tags": [
          "tasks"
        ]
      },
      "post": {
        "description": "Create a new task record.\n\nWith an operation_id the create runs under the project-wide creation lock and a\nretry resolves to the task the first attempt made, rather than producing a second\none with a different generated id.",
        "operationId": "create_task_api_projects__project_id__tasks_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskCreateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Create Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/broken": {
      "get": {
        "description": "Files in the task directory that exist but cannot be loaded.\n\nDeclared before /{task_id} so \"broken\" is not captured as a task id. These used to\nbe invisible: storage returned None for them and every listing simply omitted the\ntask. An unmigrated v1 file shows up here by filename.",
        "operationId": "list_broken_tasks_api_projects__project_id__tasks_broken_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/BrokenTaskFile"
                  },
                  "title": "Response List Broken Tasks Api Projects  Project Id  Tasks Broken Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Broken Tasks",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/claimable": {
      "get": {
        "description": "Every task that may be worked now, in the queue's order. ``/next`` is its head.\n\nThe epic walk needs the *set* rather than the winner: it starts every eligible child\nup to its slot count, and asking for the winner repeatedly would either re-offer the\nchild it just claimed or report the epic as deadlocked. It is here because the CLI\nis a service client now that only the server opens the store, and the walk must not\nbe the one caller that re-implements the claimability filter -- that is exactly how\nthe walk and the dashboard would come to disagree about what is next, with the walk\nwinning silently because it is the one that spends money.\n\nDeclared before ``/{task_id}`` for the same reason ``/next`` is: otherwise\n\"claimable\" is captured as a task id.",
        "operationId": "get_claimable_tasks_api_projects__project_id__tasks_claimable_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent"
            }
          },
          {
            "in": "query",
            "name": "parent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Parent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Task"
                  },
                  "title": "Response Get Claimable Tasks Api Projects  Project Id  Tasks Claimable Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Claimable Tasks",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/full": {
      "get": {
        "description": "Every matching task as a complete record -- what ``GET /tasks`` used to return.\n\nKept because two callers genuinely need whole records for a listing, and neither is\na browser: ``TaskClient.list_tasks`` is the surface the CLI's ``TaskManager``\nequivalent is built on, so it must return ``Task`` and not a shape missing a log;\nand ``agentjobs branches`` reads ``branches[]`` off every task, which no listing\nprojection carries.\n\nExpensive, and named so a caller has to choose it. Declared before ``/{task_id}``\nfor the reason ``/next`` and ``/broken`` are: otherwise \"full\" is captured as a task\nid.",
        "operationId": "list_full_tasks_api_projects__project_id__tasks_full_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "lifecycle",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Lifecycle"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Lifecycle"
            }
          },
          {
            "in": "query",
            "name": "ball",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Ball"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Ball"
            }
          },
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "description": "Return only the children of this umbrella task.",
            "in": "query",
            "name": "parent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only the children of this umbrella task.",
              "title": "Parent"
            }
          },
          {
            "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/TaskKind"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
              "title": "Kind"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskRead-Output"
                  },
                  "title": "Response List Full Tasks Api Projects  Project Id  Tasks Full Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Full Tasks",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/next": {
      "get": {
        "description": "The next claimable task: ready, eligible for the agent, no unmet needs.\n\nAnswers 409 rather than guessing when the queue it would have to read is broken.",
        "operationId": "get_next_task_api_projects__project_id__tasks_next_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/Task"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "title": "Response Get Next Task Api Projects  Project Id  Tasks Next Get"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Next Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/next/explain": {
      "get": {
        "description": "Why this task is next, and every open task it stands in front of.\n\nDeclared before ``/{task_id}`` so \"next\" is not captured as a task id, exactly as\n``/next`` and ``/broken`` are. The body is design section 9's structure, produced\nby the manager rather than assembled here: the route is a transcription.",
        "operationId": "explain_next_task_api_projects__project_id__tasks_next_explain_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NextExplanationResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Explain Next Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}": {
      "delete": {
        "description": "Archive a task. An open task is closed as cancelled first.",
        "operationId": "archive_task_api_projects__project_id__tasks__task_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Archive Task",
        "tags": [
          "tasks"
        ]
      },
      "get": {
        "description": "Retrieve a specific task by identifier.",
        "operationId": "get_task_api_projects__project_id__tasks__task_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Task",
        "tags": [
          "tasks"
        ]
      },
      "patch": {
        "description": "Apply a partial update to a task. State axes move through the verbs, not here.\n\nThe ``actor`` is validated whenever one is sent, exactly as ``create_task``\nvalidates its own (D2): this route writes an entry into an append-only log, and an\nattribution nobody can resolve later is worse than a refused request. It was the one\nsubmitted actor in this module that went to the manager unchecked, so a browser edit\ncould name anybody and a typo became permanent.",
        "operationId": "update_task_api_projects__project_id__tasks__task_id__patch",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Actor recorded on the manager-owned note an operation_id creates.",
            "in": "query",
            "name": "actor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Actor recorded on the manager-owned note an operation_id creates.",
              "title": "Actor"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskUpdateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Update Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/answer": {
      "post": {
        "description": "Supply what the agent was waiting for and hand the ball back (agent/answer).\n\nNot a revision: nothing the agent did was wrong, and a record that says otherwise\nmakes the next reader reconstruct which it was. The answer rides in the ball_prompt\nand the log verbatim, exactly as requested changes do.\n\nSince task-017 it may also carry ``answers``, each naming the question entry it\nanswers. Those become ``answer`` entries threaded by ``re``, written in the same\nmutation as the handoff, and the ball_prompt is composed from them when the human\ntapped options and typed nothing -- which is the whole point of the feature, and\nwould otherwise leave the agent a handoff whose ask is blank.",
        "operationId": "answer_task_api_projects__project_id__tasks__task_id__answer_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AnswerActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Answer Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/approve": {
      "post": {
        "description": "Record human approval and hand the ball back to the agent (agent/work).\n\nNothing here merges anything. The GUI cannot run git, and a button that implied\notherwise would lie. It records the approval in the task record -- which is the\npoint, since approvals used to live only in chat -- and moves the ball: the agent\nmust now rebase, merge --no-ff, mark the branch merged, and close the task\n(ENGINEERING.md, \"The Merge Gate\").\n\n``note`` is optional and strictly additive (task-228). With none, this writes byte\nfor byte what it wrote before the field existed. With one, the note rides *after*\nthe merge clearance, verbatim and unsummarised, followed by a sentence saying it is\ncontext to carry into the merge -- because an agent that reads an approval note as\na fresh review round has inverted the point of attaching one. Before this, an\napproval carrying a sentence had to go through Request Changes: a round trip the\nhuman did not ask for, and a record that said `revise` about work that was approved.\n\n**Which gate the task is at decides what the approval says** (task-001). At\n``human/plan`` nothing is built yet, so it writes ``PLAN_APPROVAL``, records\n``gate: plan`` on the receipt and is never finishable: the approval goes through\n``deliver_handback`` like a Request Changes does. Anywhere else it is the final gate\nand writes ``APPROVAL_CLEARANCE`` as before -- or ``DESIGN_APPROVAL_CLEARANCE`` on a\n``kind: design`` task, which merges the document and authorises nothing further.\n``payload.gate`` only guards against a stale page; see ``ApproveActionRequest``.",
        "operationId": "approve_task_api_projects__project_id__tasks__task_id__approve_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApproveActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Approve Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/attachments/{filename}": {
      "get": {
        "description": "Serve one image a log entry references.\n\nResolved through the task's own record rather than straight off the filesystem, so\nthe only files this can ever return are ones an entry actually points at. That also\nsupplies the recorded hash, which is checked before the bytes are handed back: a\nfile edited or corrupted since it was stored is refused rather than rendered.",
        "operationId": "get_attachment_api_projects__project_id__tasks__task_id__attachments__filename__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "filename",
            "required": true,
            "schema": {
              "title": "Filename",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Attachment",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/authorization": {
      "post": {
        "description": "Record that a human authorised a dispatch, as the agent they told (task-506).\n\nThe third shape of an authorising entry, and the only one whose author and subject\nare different people. A person clicking Dispatch gets an entry under their own name.\nA person writing a note by hand gets the same. An agent told in a chat session to\nstart the task it has just filed had neither, and the workaround was to write the\nnote as *them* -- a signature in an append-only log that its owner did not put\nthere, indistinguishable by any reader from a click. This writes an entry that says\nwhat actually happened: ``actor`` is the agent, ``authorized_by`` is the person,\n``body`` is what the agent was asked for in its own words.\n\n**It is a separate route because it is a separate capability.**\n``dispatch.relay_authorization`` is held by the two human kinds and by no ``run``,\nand the route table is keyed by endpoint -- so putting this on ``POST /log``, which\nevery run may call, would have made the gate a condition inside a handler instead of\na row in the table ``tests/test_authorization.py`` checks for completeness.\n\n**It starts nothing.** It is not an approval, it releases no merge gate, it arms\nnothing, and it spends no run slot. A dispatch afterwards is an ordinary ``manual``\ndispatch, judged by every gate in ``dispatch/guards.py`` and counted against every\ncap in ``dispatch/budget.py``. Nor is the entry consumed: it clocks exactly as many\nruns as a human's own note would, which is to say as many as those caps allow.\n\n**What it is not is proof.** The server cannot tell whether the person said anything,\njust as it cannot verify the ``user`` field on a dispatch (design section 2's P1-2).\nWhat it buys is a record that is true about who typed it, and a write a dispatched\nrun is refused -- see ``docs/authorization.md``.",
        "operationId": "relay_authorization_api_projects__project_id__tasks__task_id__authorization_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RelayAuthorizationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Relay Authorization Api Projects  Project Id  Tasks  Task Id  Authorization Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Relay Authorization",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/chain": {
      "post": {
        "description": "Authorise a bounded chain of dispatches against this task's checks (task-150).\n\n**Classified under ``dispatch.start``, which no run holds**, and the alternative was\nargued rather than assumed. A capability of its own -- ``CHAIN_AUTHORIZE`` -- would\ndraw exactly the same line for exactly the same principals, and this module's\ncoarseness is deliberate: *a capability per route would be a role system with extra\nsteps*. What a chain authorisation buys is up to twenty dispatches against one task\nwith nobody clicking again, which is the act ``dispatch.start`` already names, bought\nin advance. ``task.review`` was the other candidate and is wrong for the opposite\nreason: this is not a judgement about work somebody did, it is a purchase.\n\nThe digest, the chain id and the covered criteria are computed here from the **stored\ntask**. There is no request field for any of them, so there is nothing a caller could\nsupply that would move the definition of done the chain converges on.\n\n``user`` is validated the way a dispatch's is: the id must be one this project\nconfigures with ``kind: human``. An agent may not authorise a chain any more than it\nmay authorise a dispatch, and this is the same refusal reached through the same\nfunction.",
        "operationId": "authorize_task_chain_api_projects__project_id__tasks__task_id__chain_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChainAuthorizeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChainRead"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Authorize Task Chain",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/chain/revoke": {
      "post": {
        "description": "Stop a chain before its next iteration. One click, no arguments required.\n\nShares ``dispatch.start`` with the route above for the reason a kill switch always\nshares a capability with its switch: everyone who may start it may stop it. The\nasymmetric alternative -- anyone may stop, only some may start -- reads well and is\nwrong here, because the principal it would newly admit is a dispatched run, and a run\nable to revoke could stop a chain a person is relying on and then report that it\nconverged.\n\n**This does not stop a run that is already executing.** An iteration is a dispatch and\na dispatch is somebody's session; ``POST .../dispatch/runs/{id}/cancel`` ends one of\nthose. What this guarantees is that no further iteration begins.",
        "operationId": "revoke_task_chain_api_projects__project_id__tasks__task_id__chain_revoke_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChainRevokeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChainList"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Revoke Task Chain",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/chains": {
      "get": {
        "description": "Every chain this task has had, oldest first, each with its iteration history.\n\nA read, and deliberately absent from ``ROUTE_CAPABILITIES``: it executes nothing and\nanswers with what the log already holds. What a caller may *do* with a chain is gated\nat the two verbs below.",
        "operationId": "read_task_chains_api_projects__project_id__tasks__task_id__chains_get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChainList"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Task Chains",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/check": {
      "post": {
        "description": "Run this task's executable acceptance checks and answer with what each one did.\n\nThe same vector ``agentjobs check`` prints, written to the record the same way: the\ncriteria's statuses move and exactly one ``check_result`` entry records the pass.\n\n**A POST because it executes commands**, not because it writes -- and that is also\nwhy it is classified under ``dispatch.start`` rather than under the ``task.*``\ncapabilities a run already holds. A run may edit its own task, so a run able to call\nthis could write a check and then have this machine run it, which is starting a\nprocess of its own choosing on somebody else's hardware. That is the act the dispatch\ngates bound, and this goes through the same gate: ``assert_dispatch_permitted`` runs\nbefore any process starts, and its refusals render under their own reason codes.\n\nRefused with ``no_checks`` on a task whose criteria are all prose. Answering an empty\nsuccess would be indistinguishable, to any client, from every check having passed.",
        "operationId": "check_task_acceptance_api_projects__project_id__tasks__task_id__check_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CheckRunResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Check Task Acceptance",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/claim": {
      "post": {
        "description": "Claim a ready task: one winner, everyone else gets a 409.\n\nA claim that names its session also writes an interactive run record (task-354), so\nthe work shows as running on every surface that reads the ledger. Only after the\nclaim landed, only for an agent actor, and never a second one for a task that has a\nlive run -- which is what makes a replayed claim harmless.",
        "operationId": "claim_task_api_projects__project_id__tasks__task_id__claim_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClaimRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Claim Task Api Projects  Project Id  Tasks  Task Id  Claim Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Claim Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/close": {
      "post": {
        "description": "End the task with an outcome.",
        "operationId": "close_task_api_projects__project_id__tasks__task_id__close_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CloseRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Close Task Api Projects  Project Id  Tasks  Task Id  Close Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Close Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/deliverables/{deliverable_path}": {
      "patch": {
        "description": "Mark a deliverable as done for the task.",
        "operationId": "mark_deliverable_api_projects__project_id__tasks__task_id__deliverables__deliverable_path__patch",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "deliverable_path",
            "required": true,
            "schema": {
              "title": "Deliverable Path",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark Deliverable",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/deliverables/{index}": {
      "get": {
        "description": "One Markdown deliverable as it stands at the head of the task's active branch.\n\nAddressed by index into ``deliverables[]`` so no caller-supplied path ever reaches\ngit; every refusal is a structured body the review panel shows in place of the\ndocument. The rules are in :mod:`agentjobs.deliverable_text`. A plain ``def`` so the\ngit calls run on the threadpool rather than the event loop.",
        "operationId": "get_deliverable_api_projects__project_id__tasks__task_id__deliverables__index__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "index",
            "required": true,
            "schema": {
              "title": "Index",
              "type": "integer"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeliverableDocument"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Deliverable",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/detail": {
      "get": {
        "description": "Return the complete resumption and review contract for one task.",
        "operationId": "get_task_detail_api_projects__project_id__tasks__task_id__detail_get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskDetailResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Task Detail",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/dispatch": {
      "post": {
        "description": "Start an agent on this task.\n\nDeliberately **not** merged into any approval endpoint. Approving means \"I agree\";\ndispatching means \"spend money now\", and collapsing the two would turn every\napproval into an implicit purchase (design decision D1).\n\n202 rather than 200: the run has started, and how it ends arrives later as\n``dispatch_result`` entries on the task, not in this response.\n\nThe address handed to the agent is this server's own, taken from the socket the\nrequest arrived on. Until 2026-08-19 nothing was passed and the runner's default\nwon, so a dashboard on any other port dispatched agents at ``:8765`` -- an address\nthat, on the machine this was built for, is deliberately dead (task-154).\n\n**``user`` makes this one click (task-188).** Supplied, the guard layer writes that\nhuman's authorising entry onto the task and dispatches on it, so a person no longer\nhas to know to write a note by hand before every run. It is validated as a\nconfigured human and it is not the dispatch's ``actor``; see\n:class:`DispatchRequestBody` for why those are different things.\n\n**Omitted, this endpoint behaves exactly as it did before.** It does *not* quietly\nsubstitute the project's ``default_user``: a run has to be signed for by whoever\nasked for it, and a server-side default would produce an entry that looks like a\nperson's authorisation and is really just a config value. So a caller with no\nsigned-in user falls back to the pre-existing rule -- the newest stored entry must\nbe a human's -- and is refused with ``not_human_clocked`` if it is not. That is the\nsame answer the CLI gets, and the React app disables the button and says so rather\nthan letting someone press it into a refusal.",
        "operationId": "dispatch_task_endpoint_api_projects__project_id__tasks__task_id__dispatch_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DispatchRequestBody",
                "default": {
                  "if_full": "refuse",
                  "over_ceiling": false
                }
              }
            }
          }
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStarted"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Dispatch Task Endpoint",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/finish-retry": {
      "post": {
        "description": "Retry a stopped scripted finish on the approval already given (task-575).\n\nThe repairing agent calls this instead of running ``agentjobs finish`` from its own\nshell, which the auto-mode classifier rightly refuses as a merge nobody reviewed.\nThe server judges the repair against the head the approver saw: when nothing they\nreviewed changed, it starts the finish itself in a detached process; otherwise it\nhands the task to human/review with each offending path stated. See\n``dispatch/finish_retry.py``.\n\n``task.verb`` rather than ``task.review``, because a run must be able to ask. What\nkeeps the approval meaningful is the judgement and the bound, not who may call.",
        "operationId": "request_finish_retry_api_projects__project_id__tasks__task_id__finish_retry_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FinishRetryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinishRetryResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Request Finish Retry",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/handoff": {
      "post": {
        "description": "Move the ball, with its ask.",
        "operationId": "handoff_task_api_projects__project_id__tasks__task_id__handoff_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/HandoffRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Handoff Task Api Projects  Project Id  Tasks  Task Id  Handoff Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Handoff Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/hold": {
      "post": {
        "description": "Stop the task, with the release condition on the record (agent/hold).\n\nThe condition is prefixed rather than left bare, because this is the one send-back\nwhose prompt must not read as work: an agent that skims it and carries on has done\nthe opposite of what it was told. `hold` is also the one agent-side reason no\ndispatch path will act on, so a held task cannot be started by clicking Dispatch\nbeside the Hold button that stopped it.",
        "operationId": "hold_task_api_projects__project_id__tasks__task_id__hold_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SendBackActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Hold Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/log": {
      "post": {
        "description": "Append a note/progress/decision/question/answer/instruction entry.",
        "operationId": "append_log_entry_api_projects__project_id__tasks__task_id__log_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LogAppendRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Append Log Entry Api Projects  Project Id  Tasks  Task Id  Log Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Append Log Entry",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/progress": {
      "post": {
        "description": "Append a progress update entry for the task.",
        "operationId": "post_progress_update_api_projects__project_id__tasks__task_id__progress_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProgressUpdateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Post Progress Update Api Projects  Project Id  Tasks  Task Id  Progress Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Post Progress Update",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/promote": {
      "post": {
        "description": "Declare a draft's spec finished: it becomes ready and claimable.",
        "operationId": "promote_task_api_projects__project_id__tasks__task_id__promote_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PromoteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Promote Task Api Projects  Project Id  Tasks  Task Id  Promote Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Promote Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/queue-keep": {
      "post": {
        "description": "Keep a place that was moved over a stated warning, and record why that is binding.\n\nThe other button on the queue-move notice. `undo` needs no route of its own -- it\nis an ordinary move back through the route above -- but keeping does, because what\nit writes is not a move: it is the strong anchor, a position a person defended\nagainst an objection they had read, which `reorder` may not overturn.\n\nRefused when the last move produced no warnings. An anchor claims \"informed, and\nkept anyway\", and one written where nothing was ever said would bind `reorder` to a\ndecision nobody took.",
        "operationId": "queue_keep_task_api_projects__project_id__tasks__task_id__queue_keep_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueKeepRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Queue Keep Task Api Projects  Project Id  Tasks  Task Id  Queue Keep Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Queue Keep Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/queue-move": {
      "post": {
        "description": "Change where a task stands in its band. The only way the order changes.\n\nA verb like every other one here, which is why it lives beside them rather than\nwith the read routes: attributed, retry-safe, refused against a stale read, and\nlogged. The route computes no position -- it names a placement and the manager\ndecides what number that is, because the arithmetic has to happen under the queue\nlock and a route holds no locks.\n\nThe move lands and is never blocked by what the check finds. Ask for\n``?envelope=true`` to be told what it found: ``queue_warnings`` carries the\nfindings, ``queue_undo`` the placement that puts the task back where it came from.\nWithout the envelope this answers with the bare task exactly as it always did.",
        "operationId": "queue_move_task_api_projects__project_id__tasks__task_id__queue_move_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueMoveRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Queue Move Task Api Projects  Project Id  Tasks  Task Id  Queue Move Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Queue Move Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/redact": {
      "post": {
        "description": "Replace one prose region with a stated redaction, recording that it happened.",
        "operationId": "redact_task_region_api_projects__project_id__tasks__task_id__redact_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RedactRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Redact Task Region Api Projects  Project Id  Tasks  Task Id  Redact Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Redact Task Region",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/redirect": {
      "post": {
        "description": "Re-brief the agent and hand the ball back (agent/redirect).\n\nThe instructions changed; the work done so far stands. Recorded apart from `revise`\nbecause a reader who cannot tell a re-brief from a rejection has to reconstruct\nwhich it was from prose -- task-222 entry 14 had to supersede entry 13 to say\nexactly that.",
        "operationId": "redirect_task_api_projects__project_id__tasks__task_id__redirect_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SendBackActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Redirect Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/reject": {
      "post": {
        "description": "Reject a task: closed as cancelled, archived, reason on the record.",
        "operationId": "reject_task_api_projects__project_id__tasks__task_id__reject_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RejectActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Reject Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/release": {
      "post": {
        "description": "Return a claimed task to the pool.",
        "operationId": "release_task_api_projects__project_id__tasks__task_id__release_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReleaseRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Release Task Api Projects  Project Id  Tasks  Task Id  Release Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Release Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/reprioritize": {
      "post": {
        "description": "Change a task's band and its place in that band, in one decision.\n\nNot a `PATCH` of `priority`, deliberately. A band change moves a task between two\norderings, so it has to land somewhere in the new one -- and the generic patch has\nno way to say where, no queue lock, and no `queue_move` entry to record the\ndecision. Sending `priority` through `PATCH /tasks/{id}` still works and still\nrejoins the band at the bottom; this is the route that lets a caller say otherwise.",
        "operationId": "reprioritize_task_api_projects__project_id__tasks__task_id__reprioritize_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReprioritizeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Reprioritize Task Api Projects  Project Id  Tasks  Task Id  Reprioritize Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Reprioritize Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/request-changes": {
      "post": {
        "description": "Record requested changes and hand the ball back to the agent (agent/revise).\n\nThe feedback is the payload of the handoff, so it rides in the ball_prompt and the\nlog entry verbatim.",
        "operationId": "request_changes_api_projects__project_id__tasks__task_id__request_changes_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FeedbackActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Request Changes",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/tasks/{task_id}/resume": {
      "post": {
        "description": "Release a hold and put the task back to work (agent/work).\n\nThe counterpart to /hold, and the reason a hold is not a dead end. The note is\noptional because \"carry on\" is a complete instruction on its own; demanding a\nsentence to say it would push the human back onto the send-back controls, which is\nhow one reason came to carry four intents in the first place.",
        "operationId": "resume_task_api_projects__project_id__tasks__task_id__resume_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NoteActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Resume Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/projects/{project_id}/webhooks": {
      "get": {
        "description": "List all registered webhooks.",
        "operationId": "list_webhooks_api_projects__project_id__webhooks_get",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Webhook"
                  },
                  "title": "Response List Webhooks Api Projects  Project Id  Webhooks Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Webhooks",
        "tags": [
          "webhooks"
        ]
      },
      "post": {
        "description": "Create a new webhook.",
        "operationId": "create_webhook_api_projects__project_id__webhooks_post",
        "parameters": [
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WebhookCreateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Webhook"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Create Webhook",
        "tags": [
          "webhooks"
        ]
      }
    },
    "/api/projects/{project_id}/webhooks/{webhook_id}": {
      "delete": {
        "description": "Delete a webhook.",
        "operationId": "delete_webhook_api_projects__project_id__webhooks__webhook_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "webhook_id",
            "required": true,
            "schema": {
              "title": "Webhook Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Delete Webhook",
        "tags": [
          "webhooks"
        ]
      },
      "get": {
        "description": "Get a specific webhook by ID.",
        "operationId": "get_webhook_api_projects__project_id__webhooks__webhook_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "webhook_id",
            "required": true,
            "schema": {
              "title": "Webhook Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Webhook"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Webhook",
        "tags": [
          "webhooks"
        ]
      }
    },
    "/api/projects/{project_id}/webhooks/{webhook_id}/test": {
      "post": {
        "description": "Send a test event to a webhook.",
        "operationId": "test_webhook_api_projects__project_id__webhooks__webhook_id__test_post",
        "parameters": [
          {
            "in": "path",
            "name": "webhook_id",
            "required": true,
            "schema": {
              "title": "Webhook Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "project_id",
            "required": true,
            "schema": {
              "title": "Project Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "title": "Response Test Webhook Api Projects  Project Id  Webhooks  Webhook Id  Test Post",
                  "type": "object"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Test Webhook",
        "tags": [
          "webhooks"
        ]
      }
    },
    "/api/push": {
      "get": {
        "description": "The application-server key, and every device registered for this project.",
        "operationId": "get_push_status_api_push_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Push Status",
        "tags": [
          "push"
        ]
      }
    },
    "/api/push/subscribe": {
      "post": {
        "description": "Register a device, or refresh one that already exists.\n\n**A device registered mid-episode is not immediately pushed.** The new row inherits\nnothing, so ``last_episode_id`` is seeded with whatever episode is currently open:\na person who opts in from a page that is already showing \"3 waiting\" has seen the\nthree, and interrupting them about it a second later would be the alert fatigue the\nepisode model exists to prevent. The *next* episode wakes them, which is the thing\nthey subscribed for; ``POST /push/test`` is how they confirm it works before then.\n\nRe-subscribing an endpoint that is already registered keeps its id and its episode\nid -- see :func:`agentjobs.push.subscriptions.upsert`.",
        "operationId": "subscribe_push_device_api_push_subscribe_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PushSubscribeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Subscribe Push Device",
        "tags": [
          "push"
        ]
      }
    },
    "/api/push/test": {
      "post": {
        "description": "Prove delivery to one device, or to all of them.\n\nThe answer to \"did that actually work\", which nothing else gives: a device that\nopted in while nothing was waiting would otherwise find out whether push reaches it\nat the moment it matters least. It does not touch the episode -- a test must not be\nable to consume the interruption a real episode is owed.",
        "operationId": "send_test_push_api_push_test_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PushTestRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushTestResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Send Test Push",
        "tags": [
          "push"
        ]
      }
    },
    "/api/push/unsubscribe": {
      "post": {
        "description": "Forget a device, by id from the page or by endpoint from a service worker.\n\nIdempotent, and silent about whether anything matched. A device is routinely\nremoved from both ends at once -- the button here and the browser's own permission\nreset -- and a 404 for the second of those would be an error message about a race.",
        "operationId": "unsubscribe_push_device_api_push_unsubscribe_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PushUnsubscribeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PushStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Unsubscribe Push Device",
        "tags": [
          "push"
        ]
      }
    },
    "/api/queue": {
      "get": {
        "description": "The whole ordered backlog, band by band. This is the list a human reviews.\n\nEvery band appears, including the empty ones: \"critical is empty\" is a fact a\nreader of an ordered backlog wants stated rather than inferred from a heading that\nis not there. Every open task carries its claimability and, when it is not\nclaimable, the rule that excluded it -- the same sentence ``/tasks/next/explain``\ngives, from the same place, so the two can never disagree.\n\n**This does not 409 on a broken queue**, unlike ``/tasks/next``. It is one of the\ntwo deliberate exceptions in design section 8: you have to be able to see a broken\nqueue in order to fix it, so the offending bands still render and ``problems``\nnames what is wrong beside ``repair_command``.\n\nEach entry also carries ``last_move`` -- the move that set its place, with the\nactor, that actor's kind from this project's vocabulary, the reason recorded with\nit, and whether a human kept the place over a warning. That is the anchor evidence\n``reorder`` reads, and it is here so a run costs one request instead of one record\nfetch per open task.",
        "operationId": "get_queue_api_queue_get",
        "parameters": [
          {
            "description": "Judge claimability for this agent, so a task restricted to somebody else is marked with that as its reason. Omit it to judge for any agent.",
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Judge claimability for this agent, so a task restricted to somebody else is marked with that as its reason. Omit it to judge for any agent.",
              "title": "Agent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueueResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Queue",
        "tags": [
          "queue"
        ]
      }
    },
    "/api/queue/compact": {
      "post": {
        "description": "Renumber one band back to 100, 200, 300..., changing nobody's place.\n\nPurely cosmetic, and explicit only -- there is no automatic compaction, because a\nbackground process quietly rewriting forty task files is exactly the kind of thing\nthat should require somebody to ask for it. One band per request for the same\nreason: compacting the whole corpus is four decisions, not one.",
        "operationId": "compact_queue_api_queue_compact_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueCompactRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueueCompactResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Compact Queue",
        "tags": [
          "queue"
        ]
      }
    },
    "/api/queue/repair": {
      "post": {
        "description": "Make a broken queue into a queue again, and say exactly what was guessed.\n\nOperates on a corrupt corpus by definition, so it reads the raw files rather than\nloaded tasks -- the records it most needs are the ones the open-task-has-a-position\nrule refuses to load. Open tasks with no usable position, and the losing claimants\nof a shared one, go to the bottom of their band ordered by ``created`` then id.\n\nIt never invents an opinion it does not have. A duplicate position carries no\nrecord of who was meant to be first, so that tie-break is arbitrary by necessity --\nand every task it touched comes back in ``assigned``, which is what makes the guess\nreviewable rather than silent.",
        "operationId": "repair_queue_api_queue_repair_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueMaintenanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueueRepairResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Repair Queue",
        "tags": [
          "queue"
        ]
      }
    },
    "/api/recent/closures": {
      "get": {
        "description": "The newest closed tasks this caller may see, newest first.\n\n**Each project is asked for ``limit`` rows and the merge keeps ``limit``.** Asking\nfor fewer per project would make the answer depend on how the closures happen to be\ndistributed -- one busy project would be cut short while a quiet one contributed a\nweek-old row ahead of it.\n\n**A project that cannot be read contributes nothing and does not fail the request.**\nA store mid-migration, or one whose database has been moved, is a deployment fact;\ntaking the Dashboard down over it would be a worse answer than a list that is one\nproject short.",
        "operationId": "list_recent_closures_api_recent_closures_get",
        "parameters": [
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 5,
              "maximum": 20,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "days",
            "required": false,
            "schema": {
              "default": 7,
              "maximum": 90,
              "minimum": 1,
              "title": "Days",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecentClosuresView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Recent Closures",
        "tags": [
          "dashboard"
        ]
      }
    },
    "/api/revision": {
      "get": {
        "description": "Answer whether this project changed, without loading the task collection.",
        "operationId": "get_project_revision_api_revision_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectRevisionResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Project Revision",
        "tags": [
          "live updates"
        ]
      }
    },
    "/api/runs/emergency-stop": {
      "get": {
        "description": "Whether dispatch is stopped machine-wide. Read by the header on every page.",
        "operationId": "get_emergency_stop_api_runs_emergency_stop_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmergencyStopState"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Emergency Stop",
        "tags": [
          "dispatch"
        ]
      },
      "post": {
        "description": "Refuse every new dispatch, then stop everything AgentJobs started. Takes nothing.\n\nThe browser's twin of ``agentjobs dispatch stop``, and the same call underneath. No\nbody and no questions, for the reason ``disable_dispatch`` gives: a kill switch you\ncannot reach is not one. It works whether or not dispatch is configured, because it\nreads no configuration at all.\n\nA plain ``def`` so Starlette runs it on a worker thread: stopping a batch run may\nwait out its grace period, and the event loop serving every other page must not.",
        "operationId": "press_emergency_stop_api_runs_emergency_stop_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmergencyStopResult"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Press Emergency Stop",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/runs/emergency-stop/resume": {
      "post": {
        "description": "Lift the kill switch. Starts nothing: what the stop ended stays ended.\n\nWalks, pull armings and queued dispatches were ended by the stop rather than paused,\nso resuming only means the next dispatch a person asks for is allowed again.",
        "operationId": "resume_after_emergency_stop_api_runs_emergency_stop_resume_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmergencyStopState"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Resume After Emergency Stop",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/runs/live": {
      "get": {
        "description": "Every run this caller may see, with what is left of the machine's capacity.\n\nOne request answers both surfaces task-328 ships -- the Runs tab and the Dashboard's\ncapacity row -- which is why the ceiling and the occupied count are in the body\nrather than left to a second call.\n\n**``occupied`` counts every run, including the ones not listed** (task-333). The two\nnumbers answer different questions and only one of them is about exposure: the rows\nare \"what may I read\", while the capacity is \"why can I not dispatch\", and a machine\nthat is full because of a hidden project's run is still full. Subtracting the hidden\nrows from the count would make this surface disagree with ``dispatch/guards.py``\nabout whether there is a slot, which is the one thing its docstring says it must\nnever do.\n\n**What the live-finish lookup adds (task-533), measured 2026-09-24** with\n``machine_live_finishes`` against a home of 311 finish directories, median of 15:\n0.09 ms with no lock held; 33 ms with three runs and no finish (the one shared scan\nconfirming none of them is merging); 65 ms with one of them finishing; 67 ms with six\nruns and one finishing. The live home the same day -- three run locks, 311\ndirectories, nothing finishing -- measured 68 ms. The cost follows live locks and\nlive finishes, never tasks or rows, and each live finish adds one store read for\nwhether its task is still open.",
        "operationId": "list_live_runs_api_runs_live_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LiveRunsView"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List Live Runs",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/search": {
      "get": {
        "description": "Search tasks using a case-insensitive substring query.\n\nRows, in the same shape and for the same reason ``GET /tasks`` returns rows: a\nresult list draws a title, a badge and a place in line. It answered with whole\n``TaskRead`` records until task-495 -- every matched task's spec prose, acceptance\ncriteria and complete log -- which was 10,781 bytes per record and 5.2 MB for 480\nmatches. Whole records are at ``GET /search/full``.\n\nRows carry the same computed dependency facts as ``GET /tasks`` -- ``actionable``,\n``unmet_needs``, ``open_children_count`` -- so a result can be acted on without a\nsecond request per row.",
        "operationId": "search_tasks_api_search_get",
        "parameters": [
          {
            "in": "query",
            "name": "q",
            "required": true,
            "schema": {
              "title": "Q",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskSummaryRead-Output"
                  },
                  "title": "Response Search Tasks Api Search Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Search Tasks",
        "tags": [
          "search"
        ]
      }
    },
    "/api/search/full": {
      "get": {
        "description": "The same hits, in the same order, as whole records.\n\nNamed so the expensive shape is the one a caller asks for by name -- task-484's\ndecision, applied to the second endpoint that had it by default.\n``TaskClient.search_tasks`` is the caller that needs it: it parses each item into a\n``Task``, and a row would parse with an empty log, which is a wrong answer rather\nthan an error.",
        "operationId": "search_tasks_full_api_search_full_get",
        "parameters": [
          {
            "in": "query",
            "name": "q",
            "required": true,
            "schema": {
              "title": "Q",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskRead-Output"
                  },
                  "title": "Response Search Tasks Full Api Search Full Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Search Tasks Full",
        "tags": [
          "search"
        ]
      }
    },
    "/api/sessions/idle": {
      "get": {
        "description": "Every Claude Code process on this machine, what the sweep makes of it, and its record.",
        "operationId": "list_idle_sessions_api_sessions_idle_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IdleSessionsView"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List Idle Sessions",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/sessions/idle/settings": {
      "put": {
        "description": "Switch enforcement on or off, or change the threshold. Stops nothing by itself.",
        "operationId": "update_idle_session_settings_api_sessions_idle_settings_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/IdleSessionSettingsUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IdleSessionSettingsView"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Update Idle Session Settings",
        "tags": [
          "dispatch"
        ]
      }
    },
    "/api/tasks": {
      "get": {
        "description": "List tasks filtered along the state axes, as listing rows.\n\n``?ball=human`` is the human inbox: everything waiting on a person, each row\ncarrying its ``ball_prompt``. ``?ball=external`` is the blocked list.\n``?parent=task-063-schema-v2`` is one umbrella's children.\n\n**A row, not a record.** This used to answer with complete tasks -- spec prose,\nacceptance criteria and the entire log of each -- which on the agentjobs backlog was\n10.4 MB and four full corpus loads to draw a column of titles, and grew with every\nlog entry appended to any task (task-484). It now answers with the fields a listing\nreads. Open a task and ``/{task_id}/detail`` carries the rest, which is the fetch\nevery surface that shows a task already makes.\n\nWhole records are at ``/tasks/full``, unchanged. Narrowing *this* route rather than\nadding a narrow sibling is deliberate: the expensive shape should be the one a caller\nasks for by name, so a new surface that never thought about it gets the cheap one.",
        "operationId": "list_tasks_api_tasks_get",
        "parameters": [
          {
            "in": "query",
            "name": "lifecycle",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Lifecycle"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Lifecycle"
            }
          },
          {
            "in": "query",
            "name": "ball",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Ball"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Ball"
            }
          },
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "description": "Return only the children of this umbrella task.",
            "in": "query",
            "name": "parent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only the children of this umbrella task.",
              "title": "Parent"
            }
          },
          {
            "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/TaskKind"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
              "title": "Kind"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskSummaryRead-Output"
                  },
                  "title": "Response List Tasks Api Tasks Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Tasks",
        "tags": [
          "tasks"
        ]
      },
      "post": {
        "description": "Create a new task record.\n\nWith an operation_id the create runs under the project-wide creation lock and a\nretry resolves to the task the first attempt made, rather than producing a second\none with a different generated id.",
        "operationId": "create_task_api_tasks_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskCreateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Create Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/broken": {
      "get": {
        "description": "Files in the task directory that exist but cannot be loaded.\n\nDeclared before /{task_id} so \"broken\" is not captured as a task id. These used to\nbe invisible: storage returned None for them and every listing simply omitted the\ntask. An unmigrated v1 file shows up here by filename.",
        "operationId": "list_broken_tasks_api_tasks_broken_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/BrokenTaskFile"
                  },
                  "title": "Response List Broken Tasks Api Tasks Broken Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List Broken Tasks",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/claimable": {
      "get": {
        "description": "Every task that may be worked now, in the queue's order. ``/next`` is its head.\n\nThe epic walk needs the *set* rather than the winner: it starts every eligible child\nup to its slot count, and asking for the winner repeatedly would either re-offer the\nchild it just claimed or report the epic as deadlocked. It is here because the CLI\nis a service client now that only the server opens the store, and the walk must not\nbe the one caller that re-implements the claimability filter -- that is exactly how\nthe walk and the dashboard would come to disagree about what is next, with the walk\nwinning silently because it is the one that spends money.\n\nDeclared before ``/{task_id}`` for the same reason ``/next`` is: otherwise\n\"claimable\" is captured as a task id.",
        "operationId": "get_claimable_tasks_api_tasks_claimable_get",
        "parameters": [
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent"
            }
          },
          {
            "in": "query",
            "name": "parent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Parent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Task"
                  },
                  "title": "Response Get Claimable Tasks Api Tasks Claimable Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Claimable Tasks",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/full": {
      "get": {
        "description": "Every matching task as a complete record -- what ``GET /tasks`` used to return.\n\nKept because two callers genuinely need whole records for a listing, and neither is\na browser: ``TaskClient.list_tasks`` is the surface the CLI's ``TaskManager``\nequivalent is built on, so it must return ``Task`` and not a shape missing a log;\nand ``agentjobs branches`` reads ``branches[]`` off every task, which no listing\nprojection carries.\n\nExpensive, and named so a caller has to choose it. Declared before ``/{task_id}``\nfor the reason ``/next`` and ``/broken`` are: otherwise \"full\" is captured as a task\nid.",
        "operationId": "list_full_tasks_api_tasks_full_get",
        "parameters": [
          {
            "in": "query",
            "name": "lifecycle",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Lifecycle"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Lifecycle"
            }
          },
          {
            "in": "query",
            "name": "ball",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Ball"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Ball"
            }
          },
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "description": "Return only the children of this umbrella task.",
            "in": "query",
            "name": "parent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only the children of this umbrella task.",
              "title": "Parent"
            }
          },
          {
            "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/TaskKind"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Return only tasks of this kind. 'implementation' includes tasks with no kind set, because absent means implementation.",
              "title": "Kind"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TaskRead-Output"
                  },
                  "title": "Response List Full Tasks Api Tasks Full Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Full Tasks",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/next": {
      "get": {
        "description": "The next claimable task: ready, eligible for the agent, no unmet needs.\n\nAnswers 409 rather than guessing when the queue it would have to read is broken.",
        "operationId": "get_next_task_api_tasks_next_get",
        "parameters": [
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/Task"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "title": "Response Get Next Task Api Tasks Next Get"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Next Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/next/explain": {
      "get": {
        "description": "Why this task is next, and every open task it stands in front of.\n\nDeclared before ``/{task_id}`` so \"next\" is not captured as a task id, exactly as\n``/next`` and ``/broken`` are. The body is design section 9's structure, produced\nby the manager rather than assembled here: the route is a transcription.",
        "operationId": "explain_next_task_api_tasks_next_explain_get",
        "parameters": [
          {
            "in": "query",
            "name": "priority",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/Priority"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Priority"
            }
          },
          {
            "in": "query",
            "name": "agent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NextExplanationResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Explain Next Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}": {
      "delete": {
        "description": "Archive a task. An open task is closed as cancelled first.",
        "operationId": "archive_task_api_tasks__task_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Archive Task",
        "tags": [
          "tasks"
        ]
      },
      "get": {
        "description": "Retrieve a specific task by identifier.",
        "operationId": "get_task_api_tasks__task_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Task",
        "tags": [
          "tasks"
        ]
      },
      "patch": {
        "description": "Apply a partial update to a task. State axes move through the verbs, not here.\n\nThe ``actor`` is validated whenever one is sent, exactly as ``create_task``\nvalidates its own (D2): this route writes an entry into an append-only log, and an\nattribution nobody can resolve later is worse than a refused request. It was the one\nsubmitted actor in this module that went to the manager unchecked, so a browser edit\ncould name anybody and a typo became permanent.",
        "operationId": "update_task_api_tasks__task_id__patch",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Actor recorded on the manager-owned note an operation_id creates.",
            "in": "query",
            "name": "actor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Actor recorded on the manager-owned note an operation_id creates.",
              "title": "Actor"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskUpdateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Update Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/answer": {
      "post": {
        "description": "Supply what the agent was waiting for and hand the ball back (agent/answer).\n\nNot a revision: nothing the agent did was wrong, and a record that says otherwise\nmakes the next reader reconstruct which it was. The answer rides in the ball_prompt\nand the log verbatim, exactly as requested changes do.\n\nSince task-017 it may also carry ``answers``, each naming the question entry it\nanswers. Those become ``answer`` entries threaded by ``re``, written in the same\nmutation as the handoff, and the ball_prompt is composed from them when the human\ntapped options and typed nothing -- which is the whole point of the feature, and\nwould otherwise leave the agent a handoff whose ask is blank.",
        "operationId": "answer_task_api_tasks__task_id__answer_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AnswerActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Answer Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/approve": {
      "post": {
        "description": "Record human approval and hand the ball back to the agent (agent/work).\n\nNothing here merges anything. The GUI cannot run git, and a button that implied\notherwise would lie. It records the approval in the task record -- which is the\npoint, since approvals used to live only in chat -- and moves the ball: the agent\nmust now rebase, merge --no-ff, mark the branch merged, and close the task\n(ENGINEERING.md, \"The Merge Gate\").\n\n``note`` is optional and strictly additive (task-228). With none, this writes byte\nfor byte what it wrote before the field existed. With one, the note rides *after*\nthe merge clearance, verbatim and unsummarised, followed by a sentence saying it is\ncontext to carry into the merge -- because an agent that reads an approval note as\na fresh review round has inverted the point of attaching one. Before this, an\napproval carrying a sentence had to go through Request Changes: a round trip the\nhuman did not ask for, and a record that said `revise` about work that was approved.\n\n**Which gate the task is at decides what the approval says** (task-001). At\n``human/plan`` nothing is built yet, so it writes ``PLAN_APPROVAL``, records\n``gate: plan`` on the receipt and is never finishable: the approval goes through\n``deliver_handback`` like a Request Changes does. Anywhere else it is the final gate\nand writes ``APPROVAL_CLEARANCE`` as before -- or ``DESIGN_APPROVAL_CLEARANCE`` on a\n``kind: design`` task, which merges the document and authorises nothing further.\n``payload.gate`` only guards against a stale page; see ``ApproveActionRequest``.",
        "operationId": "approve_task_api_tasks__task_id__approve_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApproveActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Approve Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/attachments/{filename}": {
      "get": {
        "description": "Serve one image a log entry references.\n\nResolved through the task's own record rather than straight off the filesystem, so\nthe only files this can ever return are ones an entry actually points at. That also\nsupplies the recorded hash, which is checked before the bytes are handed back: a\nfile edited or corrupted since it was stored is refused rather than rendered.",
        "operationId": "get_attachment_api_tasks__task_id__attachments__filename__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "filename",
            "required": true,
            "schema": {
              "title": "Filename",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Attachment",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/authorization": {
      "post": {
        "description": "Record that a human authorised a dispatch, as the agent they told (task-506).\n\nThe third shape of an authorising entry, and the only one whose author and subject\nare different people. A person clicking Dispatch gets an entry under their own name.\nA person writing a note by hand gets the same. An agent told in a chat session to\nstart the task it has just filed had neither, and the workaround was to write the\nnote as *them* -- a signature in an append-only log that its owner did not put\nthere, indistinguishable by any reader from a click. This writes an entry that says\nwhat actually happened: ``actor`` is the agent, ``authorized_by`` is the person,\n``body`` is what the agent was asked for in its own words.\n\n**It is a separate route because it is a separate capability.**\n``dispatch.relay_authorization`` is held by the two human kinds and by no ``run``,\nand the route table is keyed by endpoint -- so putting this on ``POST /log``, which\nevery run may call, would have made the gate a condition inside a handler instead of\na row in the table ``tests/test_authorization.py`` checks for completeness.\n\n**It starts nothing.** It is not an approval, it releases no merge gate, it arms\nnothing, and it spends no run slot. A dispatch afterwards is an ordinary ``manual``\ndispatch, judged by every gate in ``dispatch/guards.py`` and counted against every\ncap in ``dispatch/budget.py``. Nor is the entry consumed: it clocks exactly as many\nruns as a human's own note would, which is to say as many as those caps allow.\n\n**What it is not is proof.** The server cannot tell whether the person said anything,\njust as it cannot verify the ``user`` field on a dispatch (design section 2's P1-2).\nWhat it buys is a record that is true about who typed it, and a write a dispatched\nrun is refused -- see ``docs/authorization.md``.",
        "operationId": "relay_authorization_api_tasks__task_id__authorization_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RelayAuthorizationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Relay Authorization Api Tasks  Task Id  Authorization Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Relay Authorization",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/chain": {
      "post": {
        "description": "Authorise a bounded chain of dispatches against this task's checks (task-150).\n\n**Classified under ``dispatch.start``, which no run holds**, and the alternative was\nargued rather than assumed. A capability of its own -- ``CHAIN_AUTHORIZE`` -- would\ndraw exactly the same line for exactly the same principals, and this module's\ncoarseness is deliberate: *a capability per route would be a role system with extra\nsteps*. What a chain authorisation buys is up to twenty dispatches against one task\nwith nobody clicking again, which is the act ``dispatch.start`` already names, bought\nin advance. ``task.review`` was the other candidate and is wrong for the opposite\nreason: this is not a judgement about work somebody did, it is a purchase.\n\nThe digest, the chain id and the covered criteria are computed here from the **stored\ntask**. There is no request field for any of them, so there is nothing a caller could\nsupply that would move the definition of done the chain converges on.\n\n``user`` is validated the way a dispatch's is: the id must be one this project\nconfigures with ``kind: human``. An agent may not authorise a chain any more than it\nmay authorise a dispatch, and this is the same refusal reached through the same\nfunction.",
        "operationId": "authorize_task_chain_api_tasks__task_id__chain_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChainAuthorizeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChainRead"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Authorize Task Chain",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/chain/revoke": {
      "post": {
        "description": "Stop a chain before its next iteration. One click, no arguments required.\n\nShares ``dispatch.start`` with the route above for the reason a kill switch always\nshares a capability with its switch: everyone who may start it may stop it. The\nasymmetric alternative -- anyone may stop, only some may start -- reads well and is\nwrong here, because the principal it would newly admit is a dispatched run, and a run\nable to revoke could stop a chain a person is relying on and then report that it\nconverged.\n\n**This does not stop a run that is already executing.** An iteration is a dispatch and\na dispatch is somebody's session; ``POST .../dispatch/runs/{id}/cancel`` ends one of\nthose. What this guarantees is that no further iteration begins.",
        "operationId": "revoke_task_chain_api_tasks__task_id__chain_revoke_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChainRevokeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChainList"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Revoke Task Chain",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/chains": {
      "get": {
        "description": "Every chain this task has had, oldest first, each with its iteration history.\n\nA read, and deliberately absent from ``ROUTE_CAPABILITIES``: it executes nothing and\nanswers with what the log already holds. What a caller may *do* with a chain is gated\nat the two verbs below.",
        "operationId": "read_task_chains_api_tasks__task_id__chains_get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChainList"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read Task Chains",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/check": {
      "post": {
        "description": "Run this task's executable acceptance checks and answer with what each one did.\n\nThe same vector ``agentjobs check`` prints, written to the record the same way: the\ncriteria's statuses move and exactly one ``check_result`` entry records the pass.\n\n**A POST because it executes commands**, not because it writes -- and that is also\nwhy it is classified under ``dispatch.start`` rather than under the ``task.*``\ncapabilities a run already holds. A run may edit its own task, so a run able to call\nthis could write a check and then have this machine run it, which is starting a\nprocess of its own choosing on somebody else's hardware. That is the act the dispatch\ngates bound, and this goes through the same gate: ``assert_dispatch_permitted`` runs\nbefore any process starts, and its refusals render under their own reason codes.\n\nRefused with ``no_checks`` on a task whose criteria are all prose. Answering an empty\nsuccess would be indistinguishable, to any client, from every check having passed.",
        "operationId": "check_task_acceptance_api_tasks__task_id__check_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CheckRunResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Check Task Acceptance",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/claim": {
      "post": {
        "description": "Claim a ready task: one winner, everyone else gets a 409.\n\nA claim that names its session also writes an interactive run record (task-354), so\nthe work shows as running on every surface that reads the ledger. Only after the\nclaim landed, only for an agent actor, and never a second one for a task that has a\nlive run -- which is what makes a replayed claim harmless.",
        "operationId": "claim_task_api_tasks__task_id__claim_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClaimRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Claim Task Api Tasks  Task Id  Claim Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Claim Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/close": {
      "post": {
        "description": "End the task with an outcome.",
        "operationId": "close_task_api_tasks__task_id__close_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CloseRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Close Task Api Tasks  Task Id  Close Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Close Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/deliverables/{deliverable_path}": {
      "patch": {
        "description": "Mark a deliverable as done for the task.",
        "operationId": "mark_deliverable_api_tasks__task_id__deliverables__deliverable_path__patch",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "deliverable_path",
            "required": true,
            "schema": {
              "title": "Deliverable Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Task"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark Deliverable",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/deliverables/{index}": {
      "get": {
        "description": "One Markdown deliverable as it stands at the head of the task's active branch.\n\nAddressed by index into ``deliverables[]`` so no caller-supplied path ever reaches\ngit; every refusal is a structured body the review panel shows in place of the\ndocument. The rules are in :mod:`agentjobs.deliverable_text`. A plain ``def`` so the\ngit calls run on the threadpool rather than the event loop.",
        "operationId": "get_deliverable_api_tasks__task_id__deliverables__index__get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "index",
            "required": true,
            "schema": {
              "title": "Index",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeliverableDocument"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Deliverable",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/detail": {
      "get": {
        "description": "Return the complete resumption and review contract for one task.",
        "operationId": "get_task_detail_api_tasks__task_id__detail_get",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskDetailResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Task Detail",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/dispatch": {
      "post": {
        "description": "Start an agent on this task.\n\nDeliberately **not** merged into any approval endpoint. Approving means \"I agree\";\ndispatching means \"spend money now\", and collapsing the two would turn every\napproval into an implicit purchase (design decision D1).\n\n202 rather than 200: the run has started, and how it ends arrives later as\n``dispatch_result`` entries on the task, not in this response.\n\nThe address handed to the agent is this server's own, taken from the socket the\nrequest arrived on. Until 2026-08-19 nothing was passed and the runner's default\nwon, so a dashboard on any other port dispatched agents at ``:8765`` -- an address\nthat, on the machine this was built for, is deliberately dead (task-154).\n\n**``user`` makes this one click (task-188).** Supplied, the guard layer writes that\nhuman's authorising entry onto the task and dispatches on it, so a person no longer\nhas to know to write a note by hand before every run. It is validated as a\nconfigured human and it is not the dispatch's ``actor``; see\n:class:`DispatchRequestBody` for why those are different things.\n\n**Omitted, this endpoint behaves exactly as it did before.** It does *not* quietly\nsubstitute the project's ``default_user``: a run has to be signed for by whoever\nasked for it, and a server-side default would produce an entry that looks like a\nperson's authorisation and is really just a config value. So a caller with no\nsigned-in user falls back to the pre-existing rule -- the newest stored entry must\nbe a human's -- and is refused with ``not_human_clocked`` if it is not. That is the\nsame answer the CLI gets, and the React app disables the button and says so rather\nthan letting someone press it into a refusal.",
        "operationId": "dispatch_task_endpoint_api_tasks__task_id__dispatch_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DispatchRequestBody",
                "default": {
                  "if_full": "refuse",
                  "over_ceiling": false
                }
              }
            }
          }
        },
        "responses": {
          "202": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DispatchStarted"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Dispatch Task Endpoint",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/finish-retry": {
      "post": {
        "description": "Retry a stopped scripted finish on the approval already given (task-575).\n\nThe repairing agent calls this instead of running ``agentjobs finish`` from its own\nshell, which the auto-mode classifier rightly refuses as a merge nobody reviewed.\nThe server judges the repair against the head the approver saw: when nothing they\nreviewed changed, it starts the finish itself in a detached process; otherwise it\nhands the task to human/review with each offending path stated. See\n``dispatch/finish_retry.py``.\n\n``task.verb`` rather than ``task.review``, because a run must be able to ask. What\nkeeps the approval meaningful is the judgement and the bound, not who may call.",
        "operationId": "request_finish_retry_api_tasks__task_id__finish_retry_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FinishRetryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinishRetryResult"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Request Finish Retry",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/handoff": {
      "post": {
        "description": "Move the ball, with its ask.",
        "operationId": "handoff_task_api_tasks__task_id__handoff_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/HandoffRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Handoff Task Api Tasks  Task Id  Handoff Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Handoff Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/hold": {
      "post": {
        "description": "Stop the task, with the release condition on the record (agent/hold).\n\nThe condition is prefixed rather than left bare, because this is the one send-back\nwhose prompt must not read as work: an agent that skims it and carries on has done\nthe opposite of what it was told. `hold` is also the one agent-side reason no\ndispatch path will act on, so a held task cannot be started by clicking Dispatch\nbeside the Hold button that stopped it.",
        "operationId": "hold_task_api_tasks__task_id__hold_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SendBackActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Hold Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/log": {
      "post": {
        "description": "Append a note/progress/decision/question/answer/instruction entry.",
        "operationId": "append_log_entry_api_tasks__task_id__log_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LogAppendRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Append Log Entry Api Tasks  Task Id  Log Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Append Log Entry",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/progress": {
      "post": {
        "description": "Append a progress update entry for the task.",
        "operationId": "post_progress_update_api_tasks__task_id__progress_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProgressUpdateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Post Progress Update Api Tasks  Task Id  Progress Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Post Progress Update",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/promote": {
      "post": {
        "description": "Declare a draft's spec finished: it becomes ready and claimable.",
        "operationId": "promote_task_api_tasks__task_id__promote_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PromoteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Promote Task Api Tasks  Task Id  Promote Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Promote Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/queue-keep": {
      "post": {
        "description": "Keep a place that was moved over a stated warning, and record why that is binding.\n\nThe other button on the queue-move notice. `undo` needs no route of its own -- it\nis an ordinary move back through the route above -- but keeping does, because what\nit writes is not a move: it is the strong anchor, a position a person defended\nagainst an objection they had read, which `reorder` may not overturn.\n\nRefused when the last move produced no warnings. An anchor claims \"informed, and\nkept anyway\", and one written where nothing was ever said would bind `reorder` to a\ndecision nobody took.",
        "operationId": "queue_keep_task_api_tasks__task_id__queue_keep_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueKeepRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Queue Keep Task Api Tasks  Task Id  Queue Keep Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Queue Keep Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/queue-move": {
      "post": {
        "description": "Change where a task stands in its band. The only way the order changes.\n\nA verb like every other one here, which is why it lives beside them rather than\nwith the read routes: attributed, retry-safe, refused against a stale read, and\nlogged. The route computes no position -- it names a placement and the manager\ndecides what number that is, because the arithmetic has to happen under the queue\nlock and a route holds no locks.\n\nThe move lands and is never blocked by what the check finds. Ask for\n``?envelope=true`` to be told what it found: ``queue_warnings`` carries the\nfindings, ``queue_undo`` the placement that puts the task back where it came from.\nWithout the envelope this answers with the bare task exactly as it always did.",
        "operationId": "queue_move_task_api_tasks__task_id__queue_move_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueueMoveRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Queue Move Task Api Tasks  Task Id  Queue Move Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Queue Move Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/redact": {
      "post": {
        "description": "Replace one prose region with a stated redaction, recording that it happened.",
        "operationId": "redact_task_region_api_tasks__task_id__redact_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RedactRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Redact Task Region Api Tasks  Task Id  Redact Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Redact Task Region",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/redirect": {
      "post": {
        "description": "Re-brief the agent and hand the ball back (agent/redirect).\n\nThe instructions changed; the work done so far stands. Recorded apart from `revise`\nbecause a reader who cannot tell a re-brief from a rejection has to reconstruct\nwhich it was from prose -- task-222 entry 14 had to supersede entry 13 to say\nexactly that.",
        "operationId": "redirect_task_api_tasks__task_id__redirect_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SendBackActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Redirect Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/reject": {
      "post": {
        "description": "Reject a task: closed as cancelled, archived, reason on the record.",
        "operationId": "reject_task_api_tasks__task_id__reject_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RejectActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Reject Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/release": {
      "post": {
        "description": "Return a claimed task to the pool.",
        "operationId": "release_task_api_tasks__task_id__release_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReleaseRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Release Task Api Tasks  Task Id  Release Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Release Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/reprioritize": {
      "post": {
        "description": "Change a task's band and its place in that band, in one decision.\n\nNot a `PATCH` of `priority`, deliberately. A band change moves a task between two\norderings, so it has to land somewhere in the new one -- and the generic patch has\nno way to say where, no queue lock, and no `queue_move` entry to record the\ndecision. Sending `priority` through `PATCH /tasks/{id}` still works and still\nrejoins the band at the bottom; this is the route that lets a caller say otherwise.",
        "operationId": "reprioritize_task_api_tasks__task_id__reprioritize_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          },
          {
            "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
            "in": "query",
            "name": "envelope",
            "required": false,
            "schema": {
              "default": false,
              "description": "Return a MutationResult with replayed/warnings instead of the bare task. Defaults to false, so existing callers see no change.",
              "title": "Envelope",
              "type": "boolean"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReprioritizeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/MutationResult-Output"
                    },
                    {
                      "$ref": "#/components/schemas/Task"
                    }
                  ],
                  "title": "Response Reprioritize Task Api Tasks  Task Id  Reprioritize Post"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Reprioritize Task",
        "tags": [
          "status"
        ]
      }
    },
    "/api/tasks/{task_id}/request-changes": {
      "post": {
        "description": "Record requested changes and hand the ball back to the agent (agent/revise).\n\nThe feedback is the payload of the handoff, so it rides in the ball_prompt and the\nlog entry verbatim.",
        "operationId": "request_changes_api_tasks__task_id__request_changes_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FeedbackActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Request Changes",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/tasks/{task_id}/resume": {
      "post": {
        "description": "Release a hold and put the task back to work (agent/work).\n\nThe counterpart to /hold, and the reason a hold is not a dead end. The note is\noptional because \"carry on\" is a complete instruction on its own; demanding a\nsentence to say it would push the human back onto the send-back controls, which is\nhow one reason came to carry four intents in the first place.",
        "operationId": "resume_task_api_tasks__task_id__resume_post",
        "parameters": [
          {
            "in": "path",
            "name": "task_id",
            "required": true,
            "schema": {
              "title": "Task Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NoteActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HumanActionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Resume Task",
        "tags": [
          "tasks"
        ]
      }
    },
    "/api/version": {
      "get": {
        "description": "Report the versions a client must match before it starts issuing calls.\n\nAdded for the MCP server's startup probe, which refuses to serve tools against a\nservice it cannot understand. The version is already in ``/openapi.json``, but\nreading it there makes every client parse a large document to learn two fields.\n\nThe React app polls it for a second reason: to notice that the process answering\nit is not the one its own bundle was built against, and to say so with the remedy\nrather than crashing somewhere unrelated when a response is not the shape its\ngenerated types promise.",
        "operationId": "api_version_api_version_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VersionResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Api Version",
        "tags": [
          "system"
        ]
      }
    },
    "/api/webhooks": {
      "get": {
        "description": "List all registered webhooks.",
        "operationId": "list_webhooks_api_webhooks_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Webhook"
                  },
                  "title": "Response List Webhooks Api Webhooks Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List Webhooks",
        "tags": [
          "webhooks"
        ]
      },
      "post": {
        "description": "Create a new webhook.",
        "operationId": "create_webhook_api_webhooks_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WebhookCreateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Webhook"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Create Webhook",
        "tags": [
          "webhooks"
        ]
      }
    },
    "/api/webhooks/{webhook_id}": {
      "delete": {
        "description": "Delete a webhook.",
        "operationId": "delete_webhook_api_webhooks__webhook_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "webhook_id",
            "required": true,
            "schema": {
              "title": "Webhook Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Delete Webhook",
        "tags": [
          "webhooks"
        ]
      },
      "get": {
        "description": "Get a specific webhook by ID.",
        "operationId": "get_webhook_api_webhooks__webhook_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "webhook_id",
            "required": true,
            "schema": {
              "title": "Webhook Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Webhook"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Webhook",
        "tags": [
          "webhooks"
        ]
      }
    },
    "/api/webhooks/{webhook_id}/test": {
      "post": {
        "description": "Send a test event to a webhook.",
        "operationId": "test_webhook_api_webhooks__webhook_id__test_post",
        "parameters": [
          {
            "in": "path",
            "name": "webhook_id",
            "required": true,
            "schema": {
              "title": "Webhook Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "title": "Response Test Webhook Api Webhooks  Webhook Id  Test Post",
                  "type": "object"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Test Webhook",
        "tags": [
          "webhooks"
        ]
      }
    },
    "/api/whoami": {
      "get": {
        "description": "Report who this request resolved as. Reports only; refuses nothing.",
        "operationId": "api_whoami_api_whoami_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WhoAmIResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Api Whoami",
        "tags": [
          "system"
        ]
      }
    }
  }
}
