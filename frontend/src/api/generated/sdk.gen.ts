// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AcknowledgeAttentionApiAttentionAckPostData, AcknowledgeAttentionApiAttentionAckPostErrors, AcknowledgeAttentionApiAttentionAckPostResponses, AcknowledgeAttentionApiProjectsProjectIdAttentionAckPostData, AcknowledgeAttentionApiProjectsProjectIdAttentionAckPostErrors, AcknowledgeAttentionApiProjectsProjectIdAttentionAckPostResponses, AnswerTaskApiProjectsProjectIdTasksTaskIdAnswerPostData, AnswerTaskApiProjectsProjectIdTasksTaskIdAnswerPostErrors, AnswerTaskApiProjectsProjectIdTasksTaskIdAnswerPostResponses, AnswerTaskApiTasksTaskIdAnswerPostData, AnswerTaskApiTasksTaskIdAnswerPostErrors, AnswerTaskApiTasksTaskIdAnswerPostResponses, ApiHealthCheckApiHealthGetData, ApiHealthCheckApiHealthGetResponses, ApiVersionApiVersionGetData, ApiVersionApiVersionGetResponses, ApiWhoamiApiWhoamiGetData, ApiWhoamiApiWhoamiGetResponses, AppendLogEntryApiProjectsProjectIdTasksTaskIdLogPostData, AppendLogEntryApiProjectsProjectIdTasksTaskIdLogPostErrors, AppendLogEntryApiProjectsProjectIdTasksTaskIdLogPostResponses, AppendLogEntryApiTasksTaskIdLogPostData, AppendLogEntryApiTasksTaskIdLogPostErrors, AppendLogEntryApiTasksTaskIdLogPostResponses, ApproveTaskApiProjectsProjectIdTasksTaskIdApprovePostData, ApproveTaskApiProjectsProjectIdTasksTaskIdApprovePostErrors, ApproveTaskApiProjectsProjectIdTasksTaskIdApprovePostResponses, ApproveTaskApiTasksTaskIdApprovePostData, ApproveTaskApiTasksTaskIdApprovePostErrors, ApproveTaskApiTasksTaskIdApprovePostResponses, ArchiveTaskApiProjectsProjectIdTasksTaskIdDeleteData, ArchiveTaskApiProjectsProjectIdTasksTaskIdDeleteErrors, ArchiveTaskApiProjectsProjectIdTasksTaskIdDeleteResponses, ArchiveTaskApiTasksTaskIdDeleteData, ArchiveTaskApiTasksTaskIdDeleteErrors, ArchiveTaskApiTasksTaskIdDeleteResponses, ArmPullModeApiDispatchArmPostData, ArmPullModeApiDispatchArmPostErrors, ArmPullModeApiDispatchArmPostResponses, ArmPullModeApiProjectsProjectIdDispatchArmPostData, ArmPullModeApiProjectsProjectIdDispatchArmPostErrors, ArmPullModeApiProjectsProjectIdDispatchArmPostResponses, AuthorizeTaskChainApiProjectsProjectIdTasksTaskIdChainPostData, AuthorizeTaskChainApiProjectsProjectIdTasksTaskIdChainPostErrors, AuthorizeTaskChainApiProjectsProjectIdTasksTaskIdChainPostResponses, AuthorizeTaskChainApiTasksTaskIdChainPostData, AuthorizeTaskChainApiTasksTaskIdChainPostErrors, AuthorizeTaskChainApiTasksTaskIdChainPostResponses, CancelDispatchRunApiDispatchRunsRunIdCancelPostData, CancelDispatchRunApiDispatchRunsRunIdCancelPostErrors, CancelDispatchRunApiDispatchRunsRunIdCancelPostResponses, CancelDispatchRunApiProjectsProjectIdDispatchRunsRunIdCancelPostData, CancelDispatchRunApiProjectsProjectIdDispatchRunsRunIdCancelPostErrors, CancelDispatchRunApiProjectsProjectIdDispatchRunsRunIdCancelPostResponses, CheckTaskAcceptanceApiProjectsProjectIdTasksTaskIdCheckPostData, CheckTaskAcceptanceApiProjectsProjectIdTasksTaskIdCheckPostErrors, CheckTaskAcceptanceApiProjectsProjectIdTasksTaskIdCheckPostResponses, CheckTaskAcceptanceApiTasksTaskIdCheckPostData, CheckTaskAcceptanceApiTasksTaskIdCheckPostErrors, CheckTaskAcceptanceApiTasksTaskIdCheckPostResponses, ClaimTaskApiProjectsProjectIdTasksTaskIdClaimPostData, ClaimTaskApiProjectsProjectIdTasksTaskIdClaimPostErrors, ClaimTaskApiProjectsProjectIdTasksTaskIdClaimPostResponses, ClaimTaskApiTasksTaskIdClaimPostData, ClaimTaskApiTasksTaskIdClaimPostErrors, ClaimTaskApiTasksTaskIdClaimPostResponses, CloseTaskApiProjectsProjectIdTasksTaskIdClosePostData, CloseTaskApiProjectsProjectIdTasksTaskIdClosePostErrors, CloseTaskApiProjectsProjectIdTasksTaskIdClosePostResponses, CloseTaskApiTasksTaskIdClosePostData, CloseTaskApiTasksTaskIdClosePostErrors, CloseTaskApiTasksTaskIdClosePostResponses, CompactQueueApiProjectsProjectIdQueueCompactPostData, CompactQueueApiProjectsProjectIdQueueCompactPostErrors, CompactQueueApiProjectsProjectIdQueueCompactPostResponses, CompactQueueApiQueueCompactPostData, CompactQueueApiQueueCompactPostErrors, CompactQueueApiQueueCompactPostResponses, CreateTaskApiProjectsProjectIdTasksPostData, CreateTaskApiProjectsProjectIdTasksPostErrors, CreateTaskApiProjectsProjectIdTasksPostResponses, CreateTaskApiTasksPostData, CreateTaskApiTasksPostErrors, CreateTaskApiTasksPostResponses, CreateWebhookApiProjectsProjectIdWebhooksPostData, CreateWebhookApiProjectsProjectIdWebhooksPostErrors, CreateWebhookApiProjectsProjectIdWebhooksPostResponses, CreateWebhookApiWebhooksPostData, CreateWebhookApiWebhooksPostErrors, CreateWebhookApiWebhooksPostResponses, DeleteWebhookApiProjectsProjectIdWebhooksWebhookIdDeleteData, DeleteWebhookApiProjectsProjectIdWebhooksWebhookIdDeleteErrors, DeleteWebhookApiProjectsProjectIdWebhooksWebhookIdDeleteResponses, DeleteWebhookApiWebhooksWebhookIdDeleteData, DeleteWebhookApiWebhooksWebhookIdDeleteErrors, DeleteWebhookApiWebhooksWebhookIdDeleteResponses, DisableDispatchApiDispatchDisablePostData, DisableDispatchApiDispatchDisablePostResponses, DisableDispatchApiProjectsProjectIdDispatchDisablePostData, DisableDispatchApiProjectsProjectIdDispatchDisablePostErrors, DisableDispatchApiProjectsProjectIdDispatchDisablePostResponses, DisarmPullModeApiDispatchDisarmPostData, DisarmPullModeApiDispatchDisarmPostResponses, DisarmPullModeApiProjectsProjectIdDispatchDisarmPostData, DisarmPullModeApiProjectsProjectIdDispatchDisarmPostErrors, DisarmPullModeApiProjectsProjectIdDispatchDisarmPostResponses, DispatchTaskEndpointApiProjectsProjectIdTasksTaskIdDispatchPostData, DispatchTaskEndpointApiProjectsProjectIdTasksTaskIdDispatchPostErrors, DispatchTaskEndpointApiProjectsProjectIdTasksTaskIdDispatchPostResponses, DispatchTaskEndpointApiTasksTaskIdDispatchPostData, DispatchTaskEndpointApiTasksTaskIdDispatchPostErrors, DispatchTaskEndpointApiTasksTaskIdDispatchPostResponses, DraftTaskSpecApiModelDraftPostData, DraftTaskSpecApiModelDraftPostErrors, DraftTaskSpecApiModelDraftPostResponses, DraftTaskSpecApiProjectsProjectIdModelDraftPostData, DraftTaskSpecApiProjectsProjectIdModelDraftPostErrors, DraftTaskSpecApiProjectsProjectIdModelDraftPostResponses, EnableDispatchApiDispatchEnablePostData, EnableDispatchApiDispatchEnablePostErrors, EnableDispatchApiDispatchEnablePostResponses, EnableDispatchApiProjectsProjectIdDispatchEnablePostData, EnableDispatchApiProjectsProjectIdDispatchEnablePostErrors, EnableDispatchApiProjectsProjectIdDispatchEnablePostResponses, ExplainNextTaskApiProjectsProjectIdTasksNextExplainGetData, ExplainNextTaskApiProjectsProjectIdTasksNextExplainGetErrors, ExplainNextTaskApiProjectsProjectIdTasksNextExplainGetResponses, ExplainNextTaskApiTasksNextExplainGetData, ExplainNextTaskApiTasksNextExplainGetErrors, ExplainNextTaskApiTasksNextExplainGetResponses, GetAllTasksApiAllTasksGetData, GetAllTasksApiAllTasksGetErrors, GetAllTasksApiAllTasksGetResponses, GetAnalyticsApiAnalyticsGetData, GetAnalyticsApiAnalyticsGetErrors, GetAnalyticsApiAnalyticsGetResponses, GetAnalyticsApiProjectsProjectIdAnalyticsGetData, GetAnalyticsApiProjectsProjectIdAnalyticsGetErrors, GetAnalyticsApiProjectsProjectIdAnalyticsGetResponses, GetAttachmentApiProjectsProjectIdTasksTaskIdAttachmentsFilenameGetData, GetAttachmentApiProjectsProjectIdTasksTaskIdAttachmentsFilenameGetErrors, GetAttachmentApiProjectsProjectIdTasksTaskIdAttachmentsFilenameGetResponses, GetAttachmentApiTasksTaskIdAttachmentsFilenameGetData, GetAttachmentApiTasksTaskIdAttachmentsFilenameGetErrors, GetAttachmentApiTasksTaskIdAttachmentsFilenameGetResponses, GetAttentionApiAttentionGetData, GetAttentionApiAttentionGetResponses, GetAttentionApiProjectsProjectIdAttentionGetData, GetAttentionApiProjectsProjectIdAttentionGetErrors, GetAttentionApiProjectsProjectIdAttentionGetResponses, GetClaimableTasksApiProjectsProjectIdTasksClaimableGetData, GetClaimableTasksApiProjectsProjectIdTasksClaimableGetErrors, GetClaimableTasksApiProjectsProjectIdTasksClaimableGetResponses, GetClaimableTasksApiTasksClaimableGetData, GetClaimableTasksApiTasksClaimableGetErrors, GetClaimableTasksApiTasksClaimableGetResponses, GetDashboardApiDashboardGetData, GetDashboardApiDashboardGetResponses, GetDashboardApiProjectsProjectIdDashboardGetData, GetDashboardApiProjectsProjectIdDashboardGetErrors, GetDashboardApiProjectsProjectIdDashboardGetResponses, GetDispatchStateApiDispatchGetData, GetDispatchStateApiDispatchGetResponses, GetDispatchStateApiProjectsProjectIdDispatchGetData, GetDispatchStateApiProjectsProjectIdDispatchGetErrors, GetDispatchStateApiProjectsProjectIdDispatchGetResponses, GetEmergencyStopApiRunsEmergencyStopGetData, GetEmergencyStopApiRunsEmergencyStopGetResponses, GetModelStatusApiModelGetData, GetModelStatusApiModelGetResponses, GetModelStatusApiProjectsProjectIdModelGetData, GetModelStatusApiProjectsProjectIdModelGetErrors, GetModelStatusApiProjectsProjectIdModelGetResponses, GetNextTaskApiProjectsProjectIdTasksNextGetData, GetNextTaskApiProjectsProjectIdTasksNextGetErrors, GetNextTaskApiProjectsProjectIdTasksNextGetResponses, GetNextTaskApiTasksNextGetData, GetNextTaskApiTasksNextGetErrors, GetNextTaskApiTasksNextGetResponses, GetPlaybookApiPlaybooksNameGetData, GetPlaybookApiPlaybooksNameGetErrors, GetPlaybookApiPlaybooksNameGetResponses, GetPlaybookApiProjectsProjectIdPlaybooksNameGetData, GetPlaybookApiProjectsProjectIdPlaybooksNameGetErrors, GetPlaybookApiProjectsProjectIdPlaybooksNameGetResponses, GetPlaybooksApiPlaybooksGetData, GetPlaybooksApiPlaybooksGetResponses, GetPlaybooksApiProjectsProjectIdPlaybooksGetData, GetPlaybooksApiProjectsProjectIdPlaybooksGetErrors, GetPlaybooksApiProjectsProjectIdPlaybooksGetResponses, GetProjectRevisionApiProjectsProjectIdRevisionGetData, GetProjectRevisionApiProjectsProjectIdRevisionGetErrors, GetProjectRevisionApiProjectsProjectIdRevisionGetResponses, GetProjectRevisionApiRevisionGetData, GetProjectRevisionApiRevisionGetResponses, GetProjectsApiProjectsGetData, GetProjectsApiProjectsGetResponses, GetPushStatusApiProjectsProjectIdPushGetData, GetPushStatusApiProjectsProjectIdPushGetErrors, GetPushStatusApiProjectsProjectIdPushGetResponses, GetPushStatusApiPushGetData, GetPushStatusApiPushGetResponses, GetQueueApiProjectsProjectIdQueueGetData, GetQueueApiProjectsProjectIdQueueGetErrors, GetQueueApiProjectsProjectIdQueueGetResponses, GetQueueApiQueueGetData, GetQueueApiQueueGetErrors, GetQueueApiQueueGetResponses, GetTaskApiProjectsProjectIdTasksTaskIdGetData, GetTaskApiProjectsProjectIdTasksTaskIdGetErrors, GetTaskApiProjectsProjectIdTasksTaskIdGetResponses, GetTaskApiTasksTaskIdGetData, GetTaskApiTasksTaskIdGetErrors, GetTaskApiTasksTaskIdGetResponses, GetTaskDetailApiProjectsProjectIdTasksTaskIdDetailGetData, GetTaskDetailApiProjectsProjectIdTasksTaskIdDetailGetErrors, GetTaskDetailApiProjectsProjectIdTasksTaskIdDetailGetResponses, GetTaskDetailApiTasksTaskIdDetailGetData, GetTaskDetailApiTasksTaskIdDetailGetErrors, GetTaskDetailApiTasksTaskIdDetailGetResponses, GetWebhookApiProjectsProjectIdWebhooksWebhookIdGetData, GetWebhookApiProjectsProjectIdWebhooksWebhookIdGetErrors, GetWebhookApiProjectsProjectIdWebhooksWebhookIdGetResponses, GetWebhookApiWebhooksWebhookIdGetData, GetWebhookApiWebhooksWebhookIdGetErrors, GetWebhookApiWebhooksWebhookIdGetResponses, HandoffTaskApiProjectsProjectIdTasksTaskIdHandoffPostData, HandoffTaskApiProjectsProjectIdTasksTaskIdHandoffPostErrors, HandoffTaskApiProjectsProjectIdTasksTaskIdHandoffPostResponses, HandoffTaskApiTasksTaskIdHandoffPostData, HandoffTaskApiTasksTaskIdHandoffPostErrors, HandoffTaskApiTasksTaskIdHandoffPostResponses, HoldTaskApiProjectsProjectIdTasksTaskIdHoldPostData, HoldTaskApiProjectsProjectIdTasksTaskIdHoldPostErrors, HoldTaskApiProjectsProjectIdTasksTaskIdHoldPostResponses, HoldTaskApiTasksTaskIdHoldPostData, HoldTaskApiTasksTaskIdHoldPostErrors, HoldTaskApiTasksTaskIdHoldPostResponses, InitializeAndRegisterProjectApiProjectsInitPostData, InitializeAndRegisterProjectApiProjectsInitPostErrors, InitializeAndRegisterProjectApiProjectsInitPostResponses, InspectProjectPathApiProjectsInspectPostData, InspectProjectPathApiProjectsInspectPostErrors, InspectProjectPathApiProjectsInspectPostResponses, ListBrokenTasksApiProjectsProjectIdTasksBrokenGetData, ListBrokenTasksApiProjectsProjectIdTasksBrokenGetErrors, ListBrokenTasksApiProjectsProjectIdTasksBrokenGetResponses, ListBrokenTasksApiTasksBrokenGetData, ListBrokenTasksApiTasksBrokenGetResponses, ListDispatchRunsApiDispatchRunsGetData, ListDispatchRunsApiDispatchRunsGetErrors, ListDispatchRunsApiDispatchRunsGetResponses, ListDispatchRunsApiProjectsProjectIdDispatchRunsGetData, ListDispatchRunsApiProjectsProjectIdDispatchRunsGetErrors, ListDispatchRunsApiProjectsProjectIdDispatchRunsGetResponses, ListFullTasksApiProjectsProjectIdTasksFullGetData, ListFullTasksApiProjectsProjectIdTasksFullGetErrors, ListFullTasksApiProjectsProjectIdTasksFullGetResponses, ListFullTasksApiTasksFullGetData, ListFullTasksApiTasksFullGetErrors, ListFullTasksApiTasksFullGetResponses, ListIdleSessionsApiSessionsIdleGetData, ListIdleSessionsApiSessionsIdleGetResponses, ListLiveRunsApiRunsLiveGetData, ListLiveRunsApiRunsLiveGetResponses, ListRecentClosuresApiRecentClosuresGetData, ListRecentClosuresApiRecentClosuresGetErrors, ListRecentClosuresApiRecentClosuresGetResponses, ListTasksApiProjectsProjectIdTasksGetData, ListTasksApiProjectsProjectIdTasksGetErrors, ListTasksApiProjectsProjectIdTasksGetResponses, ListTasksApiTasksGetData, ListTasksApiTasksGetErrors, ListTasksApiTasksGetResponses, ListWebhooksApiProjectsProjectIdWebhooksGetData, ListWebhooksApiProjectsProjectIdWebhooksGetErrors, ListWebhooksApiProjectsProjectIdWebhooksGetResponses, ListWebhooksApiWebhooksGetData, ListWebhooksApiWebhooksGetResponses, MarkDeliverableApiProjectsProjectIdTasksTaskIdDeliverablesDeliverablePathPatchData, MarkDeliverableApiProjectsProjectIdTasksTaskIdDeliverablesDeliverablePathPatchErrors, MarkDeliverableApiProjectsProjectIdTasksTaskIdDeliverablesDeliverablePathPatchResponses, MarkDeliverableApiTasksTaskIdDeliverablesDeliverablePathPatchData, MarkDeliverableApiTasksTaskIdDeliverablesDeliverablePathPatchErrors, MarkDeliverableApiTasksTaskIdDeliverablesDeliverablePathPatchResponses, PostProgressUpdateApiProjectsProjectIdTasksTaskIdProgressPostData, PostProgressUpdateApiProjectsProjectIdTasksTaskIdProgressPostErrors, PostProgressUpdateApiProjectsProjectIdTasksTaskIdProgressPostResponses, PostProgressUpdateApiTasksTaskIdProgressPostData, PostProgressUpdateApiTasksTaskIdProgressPostErrors, PostProgressUpdateApiTasksTaskIdProgressPostResponses, PressEmergencyStopApiRunsEmergencyStopPostData, PressEmergencyStopApiRunsEmergencyStopPostResponses, PromoteTaskApiProjectsProjectIdTasksTaskIdPromotePostData, PromoteTaskApiProjectsProjectIdTasksTaskIdPromotePostErrors, PromoteTaskApiProjectsProjectIdTasksTaskIdPromotePostResponses, PromoteTaskApiTasksTaskIdPromotePostData, PromoteTaskApiTasksTaskIdPromotePostErrors, PromoteTaskApiTasksTaskIdPromotePostResponses, QueueKeepTaskApiProjectsProjectIdTasksTaskIdQueueKeepPostData, QueueKeepTaskApiProjectsProjectIdTasksTaskIdQueueKeepPostErrors, QueueKeepTaskApiProjectsProjectIdTasksTaskIdQueueKeepPostResponses, QueueKeepTaskApiTasksTaskIdQueueKeepPostData, QueueKeepTaskApiTasksTaskIdQueueKeepPostErrors, QueueKeepTaskApiTasksTaskIdQueueKeepPostResponses, QueueMoveTaskApiProjectsProjectIdTasksTaskIdQueueMovePostData, QueueMoveTaskApiProjectsProjectIdTasksTaskIdQueueMovePostErrors, QueueMoveTaskApiProjectsProjectIdTasksTaskIdQueueMovePostResponses, QueueMoveTaskApiTasksTaskIdQueueMovePostData, QueueMoveTaskApiTasksTaskIdQueueMovePostErrors, QueueMoveTaskApiTasksTaskIdQueueMovePostResponses, ReadDispatchRunOutputApiDispatchRunsRunIdOutputGetData, ReadDispatchRunOutputApiDispatchRunsRunIdOutputGetErrors, ReadDispatchRunOutputApiDispatchRunsRunIdOutputGetResponses, ReadDispatchRunOutputApiProjectsProjectIdDispatchRunsRunIdOutputGetData, ReadDispatchRunOutputApiProjectsProjectIdDispatchRunsRunIdOutputGetErrors, ReadDispatchRunOutputApiProjectsProjectIdDispatchRunsRunIdOutputGetResponses, ReadDispatchRunTailApiDispatchRunsRunIdTailGetData, ReadDispatchRunTailApiDispatchRunsRunIdTailGetErrors, ReadDispatchRunTailApiDispatchRunsRunIdTailGetResponses, ReadDispatchRunTailApiProjectsProjectIdDispatchRunsRunIdTailGetData, ReadDispatchRunTailApiProjectsProjectIdDispatchRunsRunIdTailGetErrors, ReadDispatchRunTailApiProjectsProjectIdDispatchRunsRunIdTailGetResponses, ReadDispatchRunTranscriptApiDispatchRunsRunIdTranscriptGetData, ReadDispatchRunTranscriptApiDispatchRunsRunIdTranscriptGetErrors, ReadDispatchRunTranscriptApiDispatchRunsRunIdTranscriptGetResponses, ReadDispatchRunTranscriptApiProjectsProjectIdDispatchRunsRunIdTranscriptGetData, ReadDispatchRunTranscriptApiProjectsProjectIdDispatchRunsRunIdTranscriptGetErrors, ReadDispatchRunTranscriptApiProjectsProjectIdDispatchRunsRunIdTranscriptGetResponses, ReadTaskChainsApiProjectsProjectIdTasksTaskIdChainsGetData, ReadTaskChainsApiProjectsProjectIdTasksTaskIdChainsGetErrors, ReadTaskChainsApiProjectsProjectIdTasksTaskIdChainsGetResponses, ReadTaskChainsApiTasksTaskIdChainsGetData, ReadTaskChainsApiTasksTaskIdChainsGetErrors, ReadTaskChainsApiTasksTaskIdChainsGetResponses, ReadTaskFinishApiDispatchFinishesTaskIdGetData, ReadTaskFinishApiDispatchFinishesTaskIdGetErrors, ReadTaskFinishApiDispatchFinishesTaskIdGetResponses, ReadTaskFinishApiProjectsProjectIdDispatchFinishesTaskIdGetData, ReadTaskFinishApiProjectsProjectIdDispatchFinishesTaskIdGetErrors, ReadTaskFinishApiProjectsProjectIdDispatchFinishesTaskIdGetResponses, ReadTaskFinishOutputApiDispatchFinishesTaskIdOutputGetData, ReadTaskFinishOutputApiDispatchFinishesTaskIdOutputGetErrors, ReadTaskFinishOutputApiDispatchFinishesTaskIdOutputGetResponses, ReadTaskFinishOutputApiProjectsProjectIdDispatchFinishesTaskIdOutputGetData, ReadTaskFinishOutputApiProjectsProjectIdDispatchFinishesTaskIdOutputGetErrors, ReadTaskFinishOutputApiProjectsProjectIdDispatchFinishesTaskIdOutputGetResponses, RecordFinishHistoryApiHistoryFinishesFinishIdPutData, RecordFinishHistoryApiHistoryFinishesFinishIdPutErrors, RecordFinishHistoryApiHistoryFinishesFinishIdPutResponses, RecordFinishHistoryApiProjectsProjectIdHistoryFinishesFinishIdPutData, RecordFinishHistoryApiProjectsProjectIdHistoryFinishesFinishIdPutErrors, RecordFinishHistoryApiProjectsProjectIdHistoryFinishesFinishIdPutResponses, RecordGateHistoryApiHistoryGatesGateIdPutData, RecordGateHistoryApiHistoryGatesGateIdPutErrors, RecordGateHistoryApiHistoryGatesGateIdPutResponses, RecordGateHistoryApiProjectsProjectIdHistoryGatesGateIdPutData, RecordGateHistoryApiProjectsProjectIdHistoryGatesGateIdPutErrors, RecordGateHistoryApiProjectsProjectIdHistoryGatesGateIdPutResponses, RedactTaskRegionApiProjectsProjectIdTasksTaskIdRedactPostData, RedactTaskRegionApiProjectsProjectIdTasksTaskIdRedactPostErrors, RedactTaskRegionApiProjectsProjectIdTasksTaskIdRedactPostResponses, RedactTaskRegionApiTasksTaskIdRedactPostData, RedactTaskRegionApiTasksTaskIdRedactPostErrors, RedactTaskRegionApiTasksTaskIdRedactPostResponses, RedirectTaskApiProjectsProjectIdTasksTaskIdRedirectPostData, RedirectTaskApiProjectsProjectIdTasksTaskIdRedirectPostErrors, RedirectTaskApiProjectsProjectIdTasksTaskIdRedirectPostResponses, RedirectTaskApiTasksTaskIdRedirectPostData, RedirectTaskApiTasksTaskIdRedirectPostErrors, RedirectTaskApiTasksTaskIdRedirectPostResponses, RegisterProjectApiProjectsPostData, RegisterProjectApiProjectsPostErrors, RegisterProjectApiProjectsPostResponses, RejectTaskApiProjectsProjectIdTasksTaskIdRejectPostData, RejectTaskApiProjectsProjectIdTasksTaskIdRejectPostErrors, RejectTaskApiProjectsProjectIdTasksTaskIdRejectPostResponses, RejectTaskApiTasksTaskIdRejectPostData, RejectTaskApiTasksTaskIdRejectPostErrors, RejectTaskApiTasksTaskIdRejectPostResponses, RelayAuthorizationApiProjectsProjectIdTasksTaskIdAuthorizationPostData, RelayAuthorizationApiProjectsProjectIdTasksTaskIdAuthorizationPostErrors, RelayAuthorizationApiProjectsProjectIdTasksTaskIdAuthorizationPostResponses, RelayAuthorizationApiTasksTaskIdAuthorizationPostData, RelayAuthorizationApiTasksTaskIdAuthorizationPostErrors, RelayAuthorizationApiTasksTaskIdAuthorizationPostResponses, ReleaseTaskApiProjectsProjectIdTasksTaskIdReleasePostData, ReleaseTaskApiProjectsProjectIdTasksTaskIdReleasePostErrors, ReleaseTaskApiProjectsProjectIdTasksTaskIdReleasePostResponses, ReleaseTaskApiTasksTaskIdReleasePostData, ReleaseTaskApiTasksTaskIdReleasePostErrors, ReleaseTaskApiTasksTaskIdReleasePostResponses, RepairQueueApiProjectsProjectIdQueueRepairPostData, RepairQueueApiProjectsProjectIdQueueRepairPostErrors, RepairQueueApiProjectsProjectIdQueueRepairPostResponses, RepairQueueApiQueueRepairPostData, RepairQueueApiQueueRepairPostErrors, RepairQueueApiQueueRepairPostResponses, ReprioritizeTaskApiProjectsProjectIdTasksTaskIdReprioritizePostData, ReprioritizeTaskApiProjectsProjectIdTasksTaskIdReprioritizePostErrors, ReprioritizeTaskApiProjectsProjectIdTasksTaskIdReprioritizePostResponses, ReprioritizeTaskApiTasksTaskIdReprioritizePostData, ReprioritizeTaskApiTasksTaskIdReprioritizePostErrors, ReprioritizeTaskApiTasksTaskIdReprioritizePostResponses, RequestChangesApiProjectsProjectIdTasksTaskIdRequestChangesPostData, RequestChangesApiProjectsProjectIdTasksTaskIdRequestChangesPostErrors, RequestChangesApiProjectsProjectIdTasksTaskIdRequestChangesPostResponses, RequestChangesApiTasksTaskIdRequestChangesPostData, RequestChangesApiTasksTaskIdRequestChangesPostErrors, RequestChangesApiTasksTaskIdRequestChangesPostResponses, RequestFinishRetryApiProjectsProjectIdTasksTaskIdFinishRetryPostData, RequestFinishRetryApiProjectsProjectIdTasksTaskIdFinishRetryPostErrors, RequestFinishRetryApiProjectsProjectIdTasksTaskIdFinishRetryPostResponses, RequestFinishRetryApiTasksTaskIdFinishRetryPostData, RequestFinishRetryApiTasksTaskIdFinishRetryPostErrors, RequestFinishRetryApiTasksTaskIdFinishRetryPostResponses, ResetFinishEstimatorApiAnalyticsFinishEstimatorResetPostData, ResetFinishEstimatorApiAnalyticsFinishEstimatorResetPostResponses, ResetFinishEstimatorApiProjectsProjectIdAnalyticsFinishEstimatorResetPostData, ResetFinishEstimatorApiProjectsProjectIdAnalyticsFinishEstimatorResetPostErrors, ResetFinishEstimatorApiProjectsProjectIdAnalyticsFinishEstimatorResetPostResponses, ResumeAfterEmergencyStopApiRunsEmergencyStopResumePostData, ResumeAfterEmergencyStopApiRunsEmergencyStopResumePostResponses, ResumeTaskApiProjectsProjectIdTasksTaskIdResumePostData, ResumeTaskApiProjectsProjectIdTasksTaskIdResumePostErrors, ResumeTaskApiProjectsProjectIdTasksTaskIdResumePostResponses, ResumeTaskApiTasksTaskIdResumePostData, ResumeTaskApiTasksTaskIdResumePostErrors, ResumeTaskApiTasksTaskIdResumePostResponses, RevokeTaskChainApiProjectsProjectIdTasksTaskIdChainRevokePostData, RevokeTaskChainApiProjectsProjectIdTasksTaskIdChainRevokePostErrors, RevokeTaskChainApiProjectsProjectIdTasksTaskIdChainRevokePostResponses, RevokeTaskChainApiTasksTaskIdChainRevokePostData, RevokeTaskChainApiTasksTaskIdChainRevokePostErrors, RevokeTaskChainApiTasksTaskIdChainRevokePostResponses, RunPlaybookEndpointApiPlaybooksNameRunPostData, RunPlaybookEndpointApiPlaybooksNameRunPostErrors, RunPlaybookEndpointApiPlaybooksNameRunPostResponses, RunPlaybookEndpointApiProjectsProjectIdPlaybooksNameRunPostData, RunPlaybookEndpointApiProjectsProjectIdPlaybooksNameRunPostErrors, RunPlaybookEndpointApiProjectsProjectIdPlaybooksNameRunPostResponses, SearchTasksApiProjectsProjectIdSearchGetData, SearchTasksApiProjectsProjectIdSearchGetErrors, SearchTasksApiProjectsProjectIdSearchGetResponses, SearchTasksApiSearchGetData, SearchTasksApiSearchGetErrors, SearchTasksApiSearchGetResponses, SearchTasksFullApiProjectsProjectIdSearchFullGetData, SearchTasksFullApiProjectsProjectIdSearchFullGetErrors, SearchTasksFullApiProjectsProjectIdSearchFullGetResponses, SearchTasksFullApiSearchFullGetData, SearchTasksFullApiSearchFullGetErrors, SearchTasksFullApiSearchFullGetResponses, SendTestPushApiProjectsProjectIdPushTestPostData, SendTestPushApiProjectsProjectIdPushTestPostErrors, SendTestPushApiProjectsProjectIdPushTestPostResponses, SendTestPushApiPushTestPostData, SendTestPushApiPushTestPostErrors, SendTestPushApiPushTestPostResponses, SubscribePushDeviceApiProjectsProjectIdPushSubscribePostData, SubscribePushDeviceApiProjectsProjectIdPushSubscribePostErrors, SubscribePushDeviceApiProjectsProjectIdPushSubscribePostResponses, SubscribePushDeviceApiPushSubscribePostData, SubscribePushDeviceApiPushSubscribePostErrors, SubscribePushDeviceApiPushSubscribePostResponses, TestWebhookApiProjectsProjectIdWebhooksWebhookIdTestPostData, TestWebhookApiProjectsProjectIdWebhooksWebhookIdTestPostErrors, TestWebhookApiProjectsProjectIdWebhooksWebhookIdTestPostResponses, TestWebhookApiWebhooksWebhookIdTestPostData, TestWebhookApiWebhooksWebhookIdTestPostErrors, TestWebhookApiWebhooksWebhookIdTestPostResponses, UnsubscribePushDeviceApiProjectsProjectIdPushUnsubscribePostData, UnsubscribePushDeviceApiProjectsProjectIdPushUnsubscribePostErrors, UnsubscribePushDeviceApiProjectsProjectIdPushUnsubscribePostResponses, UnsubscribePushDeviceApiPushUnsubscribePostData, UnsubscribePushDeviceApiPushUnsubscribePostErrors, UnsubscribePushDeviceApiPushUnsubscribePostResponses, UpdateIdleSessionSettingsApiSessionsIdleSettingsPutData, UpdateIdleSessionSettingsApiSessionsIdleSettingsPutErrors, UpdateIdleSessionSettingsApiSessionsIdleSettingsPutResponses, UpdateTaskApiProjectsProjectIdTasksTaskIdPatchData, UpdateTaskApiProjectsProjectIdTasksTaskIdPatchErrors, UpdateTaskApiProjectsProjectIdTasksTaskIdPatchResponses, UpdateTaskApiTasksTaskIdPatchData, UpdateTaskApiTasksTaskIdPatchErrors, UpdateTaskApiTasksTaskIdPatchResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Get All Tasks
 *
 * Every task this caller may see, each tagged with the project it belongs to.
 *
 * Mounted at ``/api/all/tasks`` rather than as a magic id under ``/api/tasks/``,
 * because ``/api/tasks/all`` would be indistinguishable from a task whose id is
 * literally "all" and would shadow it.
 *
 * Read-only by design. Writes always address one project explicitly, so there stays
 * exactly one code path that mutates a file.
 *
 * **Filtered by exposure, not denied** (task-333). The audit that found this route
 * returning 3.9 MB of every project's tasks to the tailnet proposed blocking it at the
 * proxy; filtering is strictly better, because the route keeps working for the
 * projects a phone is meant to see and stays correct the day there is a second front
 * door -- a proxy rule protects only the door it is written on.
 */
export const getAllTasksApiAllTasksGet = <ThrowOnError extends boolean = false>(options?: Options<GetAllTasksApiAllTasksGetData, ThrowOnError>) => (options?.client ?? client).get<GetAllTasksApiAllTasksGetResponses, GetAllTasksApiAllTasksGetErrors, ThrowOnError>({ url: '/api/all/tasks', ...options });

/**
 * Get Analytics
 *
 * Backlog, throughput, aging and where work is stuck, over one window.
 *
 * **A project with no history is a 200, not an error** (§7.4): the totals are
 * populated from ``task``, every series is empty and ``coverage.baseline_at`` is null,
 * which is what lets the page tell "nothing has happened here" from "we do not know
 * what happened". Those are different sentences and the page renders them differently.
 */
export const getAnalyticsApiAnalyticsGet = <ThrowOnError extends boolean = false>(options?: Options<GetAnalyticsApiAnalyticsGetData, ThrowOnError>) => (options?.client ?? client).get<GetAnalyticsApiAnalyticsGetResponses, GetAnalyticsApiAnalyticsGetErrors, ThrowOnError>({ url: '/api/analytics', ...options });

/**
 * Reset Finish Estimator
 *
 * Forget the landing estimate's learned correction (task-586).
 *
 * Deletes nothing: every prediction and every finish stays, and the accuracy chart
 * keeps scoring them. Only landings that start after this moment teach the bias factor,
 * so the estimate returns to the uncorrected medians until three of them have ended.
 * An owner's act, because it changes what every Landing row shows.
 */
export const resetFinishEstimatorApiAnalyticsFinishEstimatorResetPost = <ThrowOnError extends boolean = false>(options?: Options<ResetFinishEstimatorApiAnalyticsFinishEstimatorResetPostData, ThrowOnError>) => (options?.client ?? client).post<ResetFinishEstimatorApiAnalyticsFinishEstimatorResetPostResponses, unknown, ThrowOnError>({ url: '/api/analytics/finish-estimator/reset', ...options });

/**
 * Get Attention
 *
 * Return how many tasks are stopped waiting on a person, plus the episode.
 *
 * The header polls this on every surface, so it deliberately answers with one
 * integer and one small object instead of the records behind them -- see
 * :class:`AttentionResponse`. It is the same predicate the dashboard's own tile and
 * the legacy header use, from the same function, because a badge that disagrees with
 * the page it links to is worse than no badge.
 *
 * **This read reconciles**, which is the one thing about it worth knowing. The
 * episode is a fact about the waiting set, so it is brought up to date here rather
 * than on a clock of its own: the header was polling anyway, and a reconcile is
 * idempotent, so polling cannot manufacture attention. Nothing in the answer depends
 * on a notification having been delivered.
 */
export const getAttentionApiAttentionGet = <ThrowOnError extends boolean = false>(options?: Options<GetAttentionApiAttentionGetData, ThrowOnError>) => (options?.client ?? client).get<GetAttentionApiAttentionGetResponses, unknown, ThrowOnError>({ url: '/api/attention', ...options });

/**
 * Acknowledge Attention
 *
 * Record that a person deliberately acted on the episode they were shown.
 *
 * Acknowledgment stops the *interruption*, not the indicator: the badge tracks the
 * waiting set and stays up until it empties. What it buys is that the next task to
 * stop on this person may interrupt again, which is the whole of the anti-fatigue
 * rule (task-422's decision entry).
 *
 * An id that is no longer current is not an error -- see
 * :func:`agentjobs.attention.acknowledge`. The caller gets the current state back and
 * renders it.
 */
export const acknowledgeAttentionApiAttentionAckPost = <ThrowOnError extends boolean = false>(options: Options<AcknowledgeAttentionApiAttentionAckPostData, ThrowOnError>) => (options.client ?? client).post<AcknowledgeAttentionApiAttentionAckPostResponses, AcknowledgeAttentionApiAttentionAckPostErrors, ThrowOnError>({
    url: '/api/attention/ack',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Dashboard
 *
 * Return the dashboard projection, including its single next action.
 *
 * ``identity`` rides along for the same reason it rides along on the task detail and
 * the playbook listing: the next-up panel offers a Dispatch button, a run has to be
 * attributed to a real person, and a button that cannot name one must be disabled with
 * the reason rather than pressable into a refusal.
 */
export const getDashboardApiDashboardGet = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardApiDashboardGetData, ThrowOnError>) => (options?.client ?? client).get<GetDashboardApiDashboardGetResponses, unknown, ThrowOnError>({ url: '/api/dashboard', ...options });

/**
 * Get Dispatch State
 *
 * Whether this project may dispatch right now, and which gate says otherwise.
 */
export const getDispatchStateApiDispatchGet = <ThrowOnError extends boolean = false>(options?: Options<GetDispatchStateApiDispatchGetData, ThrowOnError>) => (options?.client ?? client).get<GetDispatchStateApiDispatchGetResponses, unknown, ThrowOnError>({ url: '/api/dispatch', ...options });

/**
 * Arm Pull Mode
 *
 * Arm the pull mode for this project. Starts nothing; the server's tick does.
 *
 * **Human-only, and enforced in one place rather than here.** ``DISPATCH_ADMIN`` is not
 * in a run's grant, so a run credential is refused 403 by the capability dependency
 * before this function is entered -- which is the property that matters: a run must not
 * be able to arm the machine to keep starting runs, and that is a rule about the table
 * rather than about anyone remembering to check it in a handler.
 *
 * The ``user`` is checked against the principal the request resolved to, on the same
 * terms a dispatch's is (task-332): every run this arming buys will carry that person's
 * name, so naming somebody else would be signing their authorisation.
 */
export const armPullModeApiDispatchArmPost = <ThrowOnError extends boolean = false>(options?: Options<ArmPullModeApiDispatchArmPostData, ThrowOnError>) => (options?.client ?? client).post<ArmPullModeApiDispatchArmPostResponses, ArmPullModeApiDispatchArmPostErrors, ThrowOnError>({
    url: '/api/dispatch/arm',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Disable Dispatch
 *
 * Stop dispatching for this project. Takes nothing, asks nothing.
 *
 * Refuses only when there is no config file at all, which is already a state in which
 * nothing can dispatch -- so there is no reachable case where a human wants this off
 * and cannot have it.
 */
export const disableDispatchApiDispatchDisablePost = <ThrowOnError extends boolean = false>(options?: Options<DisableDispatchApiDispatchDisablePostData, ThrowOnError>) => (options?.client ?? client).post<DisableDispatchApiDispatchDisablePostResponses, unknown, ThrowOnError>({ url: '/api/dispatch/disable', ...options });

/**
 * Disarm Pull Mode
 *
 * Stop the pull mode starting anything more. **Kills nothing.**
 *
 * Takes nothing and asks nothing, like ``/disable`` and for the same reason: a switch
 * you cannot reach is not one, and one that argues with you is worse than none. Runs
 * already going are untouched -- each was authorised individually and has its own merge
 * gate, and destroying work somebody's arming already bought is not what "stop" means.
 *
 * Quiet when the project was not armed, which covers a second press and a press that
 * raced a bound running out.
 */
export const disarmPullModeApiDispatchDisarmPost = <ThrowOnError extends boolean = false>(options?: Options<DisarmPullModeApiDispatchDisarmPostData, ThrowOnError>) => (options?.client ?? client).post<DisarmPullModeApiDispatchDisarmPostResponses, unknown, ThrowOnError>({ url: '/api/dispatch/disarm', ...options });

/**
 * Enable Dispatch
 *
 * Enable dispatch for this project against an already-defined runner.
 */
export const enableDispatchApiDispatchEnablePost = <ThrowOnError extends boolean = false>(options?: Options<EnableDispatchApiDispatchEnablePostData, ThrowOnError>) => (options?.client ?? client).post<EnableDispatchApiDispatchEnablePostResponses, EnableDispatchApiDispatchEnablePostErrors, ThrowOnError>({
    url: '/api/dispatch/enable',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Read Task Finish
 *
 * What is happening to this task's branch, or last happened to it.
 *
 * Keyed on the task rather than on a finish id, because the question a page asks is
 * "what is happening to *this*", and the reader pressing Approve has no finish id to
 * ask with -- the finish that answers it does not exist yet at the moment they press.
 *
 * ``null`` is the ordinary answer for almost every task, and means no finish has run
 * for it on this machine. The page renders nothing at all for that, which is why it is
 * a null body rather than a 404: an absent finish is not a missing resource, and a
 * task page that logged a 404 every two seconds would teach its reader to ignore them.
 */
export const readTaskFinishApiDispatchFinishesTaskIdGet = <ThrowOnError extends boolean = false>(options: Options<ReadTaskFinishApiDispatchFinishesTaskIdGetData, ThrowOnError>) => (options.client ?? client).get<ReadTaskFinishApiDispatchFinishesTaskIdGetResponses, ReadTaskFinishApiDispatchFinishesTaskIdGetErrors, ThrowOnError>({ url: '/api/dispatch/finishes/{task_id}', ...options });

/**
 * Read Task Finish Output
 *
 * A finish's output in full, as text a browser tab can show.
 *
 * Text rather than JSON, for the same reason a run's is: this is read by a person, and
 * a step table wrapped in a JSON string escape is unreadable.
 */
export const readTaskFinishOutputApiDispatchFinishesTaskIdOutputGet = <ThrowOnError extends boolean = false>(options: Options<ReadTaskFinishOutputApiDispatchFinishesTaskIdOutputGetData, ThrowOnError>) => (options.client ?? client).get<ReadTaskFinishOutputApiDispatchFinishesTaskIdOutputGetResponses, ReadTaskFinishOutputApiDispatchFinishesTaskIdOutputGetErrors, ThrowOnError>({ url: '/api/dispatch/finishes/{task_id}/output', ...options });

/**
 * List Dispatch Runs
 *
 * Runs belonging to this project, newest first.
 *
 * Filtered by project rather than returning the machine's whole ledger: a run
 * directory records which project it belongs to, and a page about one project has no
 * business showing another's.
 */
export const listDispatchRunsApiDispatchRunsGet = <ThrowOnError extends boolean = false>(options?: Options<ListDispatchRunsApiDispatchRunsGetData, ThrowOnError>) => (options?.client ?? client).get<ListDispatchRunsApiDispatchRunsGetResponses, ListDispatchRunsApiDispatchRunsGetErrors, ThrowOnError>({ url: '/api/dispatch/runs', ...options });

/**
 * Cancel Dispatch Run
 *
 * Stop one run, or take one queued dispatch out of the line, and record it.
 *
 * **The queue is tried first, and only a *waiting* entry answers here.** A queued
 * dispatch that started a second ago is a run, its queue row says ``started``, and this
 * falls through to the run path with the run's own id -- which is the one that stops
 * something. The reverse ordering would report a cancellation over a live agent.
 *
 * Same route for both because they are the same act from where the person is standing:
 * the card they are cancelling is the same card, before and after a slot freed under it.
 */
export const cancelDispatchRunApiDispatchRunsRunIdCancelPost = <ThrowOnError extends boolean = false>(options: Options<CancelDispatchRunApiDispatchRunsRunIdCancelPostData, ThrowOnError>) => (options.client ?? client).post<CancelDispatchRunApiDispatchRunsRunIdCancelPostResponses, CancelDispatchRunApiDispatchRunsRunIdCancelPostErrors, ThrowOnError>({ url: '/api/dispatch/runs/{run_id}/cancel', ...options });

/**
 * Read Dispatch Run Output
 *
 * A run's output in full, as text a browser tab can show.
 *
 * Text rather than JSON because this is the one dispatch response a human reads
 * directly, and a transcript wrapped in a JSON string escape is unreadable.
 */
export const readDispatchRunOutputApiDispatchRunsRunIdOutputGet = <ThrowOnError extends boolean = false>(options: Options<ReadDispatchRunOutputApiDispatchRunsRunIdOutputGetData, ThrowOnError>) => (options.client ?? client).get<ReadDispatchRunOutputApiDispatchRunsRunIdOutputGetResponses, ReadDispatchRunOutputApiDispatchRunsRunIdOutputGetErrors, ThrowOnError>({ url: '/api/dispatch/runs/{run_id}/output', ...options });

/**
 * Read Dispatch Run Tail
 *
 * The end of a run's output, for a page watching it while it happens.
 *
 * A separate endpoint from ``/output`` rather than a parameter on it, because the two
 * are read by different things: ``/output`` is a link a human opens, so it answers in
 * text and gives them everything, while this is polled by a page and answers in the
 * shape the generated client already understands.
 *
 * **No subprocess is involved.** The text comes from a file the session poller wrote,
 * so a hundred people watching one run costs a hundred file reads and no more processes
 * than nobody watching it -- and the tail can never be fresher than the poller's own
 * interval, which is the point rather than a limitation.
 */
export const readDispatchRunTailApiDispatchRunsRunIdTailGet = <ThrowOnError extends boolean = false>(options: Options<ReadDispatchRunTailApiDispatchRunsRunIdTailGetData, ThrowOnError>) => (options.client ?? client).get<ReadDispatchRunTailApiDispatchRunsRunIdTailGetResponses, ReadDispatchRunTailApiDispatchRunsRunIdTailGetErrors, ThrowOnError>({ url: '/api/dispatch/runs/{run_id}/tail', ...options });

/**
 * Read Dispatch Run Transcript
 *
 * What the session recorded about itself, as entries rather than as a screen.
 *
 * Polled on the same clock as ``/tail`` and read the same way -- a file, no subprocess
 * -- but from a different file. ``/tail`` serves the pty capture, which is a *repaint*
 * of a terminal and loses every space and line break the moment its escape sequences
 * are removed. This serves the JSONL the runner writes beside it, where the agent's
 * prose, each tool call and whether it failed are all still separate things.
 *
 * ``source: 'none'`` is an ordinary answer, not an error: a batch run, a session that
 * has not reported its id yet, and a driver that keeps no such file all reach it. The
 * ``note`` says which, and the caller falls back to the tail.
 */
export const readDispatchRunTranscriptApiDispatchRunsRunIdTranscriptGet = <ThrowOnError extends boolean = false>(options: Options<ReadDispatchRunTranscriptApiDispatchRunsRunIdTranscriptGetData, ThrowOnError>) => (options.client ?? client).get<ReadDispatchRunTranscriptApiDispatchRunsRunIdTranscriptGetResponses, ReadDispatchRunTranscriptApiDispatchRunsRunIdTranscriptGetErrors, ThrowOnError>({ url: '/api/dispatch/runs/{run_id}/transcript', ...options });

/**
 * Api Health Check
 *
 * Simple health check endpoint for API consumers.
 */
export const apiHealthCheckApiHealthGet = <ThrowOnError extends boolean = false>(options?: Options<ApiHealthCheckApiHealthGetData, ThrowOnError>) => (options?.client ?? client).get<ApiHealthCheckApiHealthGetResponses, unknown, ThrowOnError>({ url: '/api/health', ...options });

/**
 * Record Finish History
 *
 * Index one scripted finish and the steps it has taken so far.
 *
 * ``written`` is false, with a reason, when an imported record met a row already there
 * or when the finish names a task this project does not have. Both are answers rather
 * than errors: the importer reports them, and the finisher never sends either.
 */
export const recordFinishHistoryApiHistoryFinishesFinishIdPut = <ThrowOnError extends boolean = false>(options: Options<RecordFinishHistoryApiHistoryFinishesFinishIdPutData, ThrowOnError>) => (options.client ?? client).put<RecordFinishHistoryApiHistoryFinishesFinishIdPutResponses, RecordFinishHistoryApiHistoryFinishesFinishIdPutErrors, ThrowOnError>({
    url: '/api/history/finishes/{finish_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Record Gate History
 *
 * Index one gate run and the stages it has finished so far.
 */
export const recordGateHistoryApiHistoryGatesGateIdPut = <ThrowOnError extends boolean = false>(options: Options<RecordGateHistoryApiHistoryGatesGateIdPutData, ThrowOnError>) => (options.client ?? client).put<RecordGateHistoryApiHistoryGatesGateIdPutResponses, RecordGateHistoryApiHistoryGatesGateIdPutErrors, ThrowOnError>({
    url: '/api/history/gates/{gate_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Model Status
 *
 * Whether a drafting call can be made on this machine, and why not when it cannot.
 *
 * Names no project: what is configured is a property of the machine, and a per-project
 * answer would imply a per-project credential, which is the one arrangement the design
 * rules out (``docs/model-access-design.md`` §4).
 */
export const getModelStatusApiModelGet = <ThrowOnError extends boolean = false>(options?: Options<GetModelStatusApiModelGetData, ThrowOnError>) => (options?.client ?? client).get<GetModelStatusApiModelGetResponses, unknown, ThrowOnError>({ url: '/api/model', ...options });

/**
 * Draft Task Spec
 *
 * Expand what a person typed into spec fields. Writes nothing.
 *
 * Project-scoped because the draft is told which project it is being filed into, which
 * is context a spec legitimately reflects. It reads nothing else about the project and
 * writes nothing to it.
 *
 * Every refusal comes back as a 200 with ``drafted: false``; see
 * :class:`~agentjobs.api.models.SpecDraftResponse` for why that is the shape.
 */
export const draftTaskSpecApiModelDraftPost = <ThrowOnError extends boolean = false>(options: Options<DraftTaskSpecApiModelDraftPostData, ThrowOnError>) => (options.client ?? client).post<DraftTaskSpecApiModelDraftPostResponses, DraftTaskSpecApiModelDraftPostErrors, ThrowOnError>({
    url: '/api/model/draft',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Playbooks
 *
 * Every playbook this project holds, with the files that would not load.
 */
export const getPlaybooksApiPlaybooksGet = <ThrowOnError extends boolean = false>(options?: Options<GetPlaybooksApiPlaybooksGetData, ThrowOnError>) => (options?.client ?? client).get<GetPlaybooksApiPlaybooksGetResponses, unknown, ThrowOnError>({ url: '/api/playbooks', ...options });

/**
 * Get Playbook
 *
 * One playbook by name, including its brief.
 *
 * 422 rather than 404 for a file that exists and does not validate, matching how a
 * stored task that will not parse is reported: the thing is there and repairable,
 * and "not found" would send its author looking for something already on disk.
 */
export const getPlaybookApiPlaybooksNameGet = <ThrowOnError extends boolean = false>(options: Options<GetPlaybookApiPlaybooksNameGetData, ThrowOnError>) => (options.client ?? client).get<GetPlaybookApiPlaybooksNameGetResponses, GetPlaybookApiPlaybooksNameGetErrors, ThrowOnError>({ url: '/api/playbooks/{name}', ...options });

/**
 * Run Playbook Endpoint
 *
 * Start an agent on a playbook: create its run task if it has one, then dispatch.
 *
 * 202 rather than 200, and for the same reason ``POST /tasks/{id}/dispatch`` is: the
 * run has started, and how it ends arrives later as ``dispatch_result`` entries on the
 * task rather than in this response.
 *
 * **Every dispatch gate binds, unchanged.** This endpoint opens nothing: it resolves a
 * file, checks the invocation's shape against the playbook's ``target``, and then
 * calls the same ``dispatch_task`` the Dispatch button calls, with a pointer attached
 * that no guard reads. A machine with dispatch off refuses here as it refuses there,
 * and refuses *before* a run task is created.
 *
 * ``user`` reaches the record by one of two routes, and which one depends on the
 * playbook rather than on this endpoint. A project-target run creates the run task
 * attributed to that human, and their creation entry is the newest stored entry the
 * human-clocked rule then reads. A task-target run creates nothing, so ``user`` is
 * passed to the dispatcher as task-188's ``authorized_by`` and its authorising entry
 * is written onto the task named. Neither path substitutes the project's
 * ``default_user`` for a human nobody named.
 */
export const runPlaybookEndpointApiPlaybooksNameRunPost = <ThrowOnError extends boolean = false>(options: Options<RunPlaybookEndpointApiPlaybooksNameRunPostData, ThrowOnError>) => (options.client ?? client).post<RunPlaybookEndpointApiPlaybooksNameRunPostResponses, RunPlaybookEndpointApiPlaybooksNameRunPostErrors, ThrowOnError>({
    url: '/api/playbooks/{name}/run',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Projects
 *
 * List every project this server can serve *this caller*, with task counts.
 *
 * A project whose directory has gone missing is reported with a null task_count
 * rather than failing the whole listing -- the registry is machine-local and a
 * checkout can legitimately disappear.
 *
 * A project marked ``visibility: local`` is simply not in the list for a remote
 * caller (task-333). Omitted rather than returned with a flag, because a flag would
 * disclose the name and the root path of the thing being hidden, and because a client
 * that has to know about a hidden-project state is a client that will one day render
 * one.
 */
export const getProjectsApiProjectsGet = <ThrowOnError extends boolean = false>(options?: Options<GetProjectsApiProjectsGetData, ThrowOnError>) => (options?.client ?? client).get<GetProjectsApiProjectsGetResponses, unknown, ThrowOnError>({ url: '/api/projects', ...options });

/**
 * Register Project
 *
 * Register an existing AgentJobs project without changing its files.
 */
export const registerProjectApiProjectsPost = <ThrowOnError extends boolean = false>(options: Options<RegisterProjectApiProjectsPostData, ThrowOnError>) => (options.client ?? client).post<RegisterProjectApiProjectsPostResponses, RegisterProjectApiProjectsPostErrors, ThrowOnError>({
    url: '/api/projects',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Initialize And Register Project
 *
 * Initialize an existing untracked directory and register the new project.
 */
export const initializeAndRegisterProjectApiProjectsInitPost = <ThrowOnError extends boolean = false>(options: Options<InitializeAndRegisterProjectApiProjectsInitPostData, ThrowOnError>) => (options.client ?? client).post<InitializeAndRegisterProjectApiProjectsInitPostResponses, InitializeAndRegisterProjectApiProjectsInitPostErrors, ThrowOnError>({
    url: '/api/projects/init',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Inspect Project Path
 *
 * Tell the UI whether confirmation should register or initialize a path.
 */
export const inspectProjectPathApiProjectsInspectPost = <ThrowOnError extends boolean = false>(options: Options<InspectProjectPathApiProjectsInspectPostData, ThrowOnError>) => (options.client ?? client).post<InspectProjectPathApiProjectsInspectPostResponses, InspectProjectPathApiProjectsInspectPostErrors, ThrowOnError>({
    url: '/api/projects/inspect',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Analytics
 *
 * Backlog, throughput, aging and where work is stuck, over one window.
 *
 * **A project with no history is a 200, not an error** (§7.4): the totals are
 * populated from ``task``, every series is empty and ``coverage.baseline_at`` is null,
 * which is what lets the page tell "nothing has happened here" from "we do not know
 * what happened". Those are different sentences and the page renders them differently.
 */
export const getAnalyticsApiProjectsProjectIdAnalyticsGet = <ThrowOnError extends boolean = false>(options: Options<GetAnalyticsApiProjectsProjectIdAnalyticsGetData, ThrowOnError>) => (options.client ?? client).get<GetAnalyticsApiProjectsProjectIdAnalyticsGetResponses, GetAnalyticsApiProjectsProjectIdAnalyticsGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/analytics', ...options });

/**
 * Reset Finish Estimator
 *
 * Forget the landing estimate's learned correction (task-586).
 *
 * Deletes nothing: every prediction and every finish stays, and the accuracy chart
 * keeps scoring them. Only landings that start after this moment teach the bias factor,
 * so the estimate returns to the uncorrected medians until three of them have ended.
 * An owner's act, because it changes what every Landing row shows.
 */
export const resetFinishEstimatorApiProjectsProjectIdAnalyticsFinishEstimatorResetPost = <ThrowOnError extends boolean = false>(options: Options<ResetFinishEstimatorApiProjectsProjectIdAnalyticsFinishEstimatorResetPostData, ThrowOnError>) => (options.client ?? client).post<ResetFinishEstimatorApiProjectsProjectIdAnalyticsFinishEstimatorResetPostResponses, ResetFinishEstimatorApiProjectsProjectIdAnalyticsFinishEstimatorResetPostErrors, ThrowOnError>({ url: '/api/projects/{project_id}/analytics/finish-estimator/reset', ...options });

/**
 * Get Attention
 *
 * Return how many tasks are stopped waiting on a person, plus the episode.
 *
 * The header polls this on every surface, so it deliberately answers with one
 * integer and one small object instead of the records behind them -- see
 * :class:`AttentionResponse`. It is the same predicate the dashboard's own tile and
 * the legacy header use, from the same function, because a badge that disagrees with
 * the page it links to is worse than no badge.
 *
 * **This read reconciles**, which is the one thing about it worth knowing. The
 * episode is a fact about the waiting set, so it is brought up to date here rather
 * than on a clock of its own: the header was polling anyway, and a reconcile is
 * idempotent, so polling cannot manufacture attention. Nothing in the answer depends
 * on a notification having been delivered.
 */
export const getAttentionApiProjectsProjectIdAttentionGet = <ThrowOnError extends boolean = false>(options: Options<GetAttentionApiProjectsProjectIdAttentionGetData, ThrowOnError>) => (options.client ?? client).get<GetAttentionApiProjectsProjectIdAttentionGetResponses, GetAttentionApiProjectsProjectIdAttentionGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/attention', ...options });

/**
 * Acknowledge Attention
 *
 * Record that a person deliberately acted on the episode they were shown.
 *
 * Acknowledgment stops the *interruption*, not the indicator: the badge tracks the
 * waiting set and stays up until it empties. What it buys is that the next task to
 * stop on this person may interrupt again, which is the whole of the anti-fatigue
 * rule (task-422's decision entry).
 *
 * An id that is no longer current is not an error -- see
 * :func:`agentjobs.attention.acknowledge`. The caller gets the current state back and
 * renders it.
 */
export const acknowledgeAttentionApiProjectsProjectIdAttentionAckPost = <ThrowOnError extends boolean = false>(options: Options<AcknowledgeAttentionApiProjectsProjectIdAttentionAckPostData, ThrowOnError>) => (options.client ?? client).post<AcknowledgeAttentionApiProjectsProjectIdAttentionAckPostResponses, AcknowledgeAttentionApiProjectsProjectIdAttentionAckPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/attention/ack',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Dashboard
 *
 * Return the dashboard projection, including its single next action.
 *
 * ``identity`` rides along for the same reason it rides along on the task detail and
 * the playbook listing: the next-up panel offers a Dispatch button, a run has to be
 * attributed to a real person, and a button that cannot name one must be disabled with
 * the reason rather than pressable into a refusal.
 */
export const getDashboardApiProjectsProjectIdDashboardGet = <ThrowOnError extends boolean = false>(options: Options<GetDashboardApiProjectsProjectIdDashboardGetData, ThrowOnError>) => (options.client ?? client).get<GetDashboardApiProjectsProjectIdDashboardGetResponses, GetDashboardApiProjectsProjectIdDashboardGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dashboard', ...options });

/**
 * Get Dispatch State
 *
 * Whether this project may dispatch right now, and which gate says otherwise.
 */
export const getDispatchStateApiProjectsProjectIdDispatchGet = <ThrowOnError extends boolean = false>(options: Options<GetDispatchStateApiProjectsProjectIdDispatchGetData, ThrowOnError>) => (options.client ?? client).get<GetDispatchStateApiProjectsProjectIdDispatchGetResponses, GetDispatchStateApiProjectsProjectIdDispatchGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch', ...options });

/**
 * Arm Pull Mode
 *
 * Arm the pull mode for this project. Starts nothing; the server's tick does.
 *
 * **Human-only, and enforced in one place rather than here.** ``DISPATCH_ADMIN`` is not
 * in a run's grant, so a run credential is refused 403 by the capability dependency
 * before this function is entered -- which is the property that matters: a run must not
 * be able to arm the machine to keep starting runs, and that is a rule about the table
 * rather than about anyone remembering to check it in a handler.
 *
 * The ``user`` is checked against the principal the request resolved to, on the same
 * terms a dispatch's is (task-332): every run this arming buys will carry that person's
 * name, so naming somebody else would be signing their authorisation.
 */
export const armPullModeApiProjectsProjectIdDispatchArmPost = <ThrowOnError extends boolean = false>(options: Options<ArmPullModeApiProjectsProjectIdDispatchArmPostData, ThrowOnError>) => (options.client ?? client).post<ArmPullModeApiProjectsProjectIdDispatchArmPostResponses, ArmPullModeApiProjectsProjectIdDispatchArmPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/dispatch/arm',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Disable Dispatch
 *
 * Stop dispatching for this project. Takes nothing, asks nothing.
 *
 * Refuses only when there is no config file at all, which is already a state in which
 * nothing can dispatch -- so there is no reachable case where a human wants this off
 * and cannot have it.
 */
export const disableDispatchApiProjectsProjectIdDispatchDisablePost = <ThrowOnError extends boolean = false>(options: Options<DisableDispatchApiProjectsProjectIdDispatchDisablePostData, ThrowOnError>) => (options.client ?? client).post<DisableDispatchApiProjectsProjectIdDispatchDisablePostResponses, DisableDispatchApiProjectsProjectIdDispatchDisablePostErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/disable', ...options });

/**
 * Disarm Pull Mode
 *
 * Stop the pull mode starting anything more. **Kills nothing.**
 *
 * Takes nothing and asks nothing, like ``/disable`` and for the same reason: a switch
 * you cannot reach is not one, and one that argues with you is worse than none. Runs
 * already going are untouched -- each was authorised individually and has its own merge
 * gate, and destroying work somebody's arming already bought is not what "stop" means.
 *
 * Quiet when the project was not armed, which covers a second press and a press that
 * raced a bound running out.
 */
export const disarmPullModeApiProjectsProjectIdDispatchDisarmPost = <ThrowOnError extends boolean = false>(options: Options<DisarmPullModeApiProjectsProjectIdDispatchDisarmPostData, ThrowOnError>) => (options.client ?? client).post<DisarmPullModeApiProjectsProjectIdDispatchDisarmPostResponses, DisarmPullModeApiProjectsProjectIdDispatchDisarmPostErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/disarm', ...options });

/**
 * Enable Dispatch
 *
 * Enable dispatch for this project against an already-defined runner.
 */
export const enableDispatchApiProjectsProjectIdDispatchEnablePost = <ThrowOnError extends boolean = false>(options: Options<EnableDispatchApiProjectsProjectIdDispatchEnablePostData, ThrowOnError>) => (options.client ?? client).post<EnableDispatchApiProjectsProjectIdDispatchEnablePostResponses, EnableDispatchApiProjectsProjectIdDispatchEnablePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/dispatch/enable',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read Task Finish
 *
 * What is happening to this task's branch, or last happened to it.
 *
 * Keyed on the task rather than on a finish id, because the question a page asks is
 * "what is happening to *this*", and the reader pressing Approve has no finish id to
 * ask with -- the finish that answers it does not exist yet at the moment they press.
 *
 * ``null`` is the ordinary answer for almost every task, and means no finish has run
 * for it on this machine. The page renders nothing at all for that, which is why it is
 * a null body rather than a 404: an absent finish is not a missing resource, and a
 * task page that logged a 404 every two seconds would teach its reader to ignore them.
 */
export const readTaskFinishApiProjectsProjectIdDispatchFinishesTaskIdGet = <ThrowOnError extends boolean = false>(options: Options<ReadTaskFinishApiProjectsProjectIdDispatchFinishesTaskIdGetData, ThrowOnError>) => (options.client ?? client).get<ReadTaskFinishApiProjectsProjectIdDispatchFinishesTaskIdGetResponses, ReadTaskFinishApiProjectsProjectIdDispatchFinishesTaskIdGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/finishes/{task_id}', ...options });

/**
 * Read Task Finish Output
 *
 * A finish's output in full, as text a browser tab can show.
 *
 * Text rather than JSON, for the same reason a run's is: this is read by a person, and
 * a step table wrapped in a JSON string escape is unreadable.
 */
export const readTaskFinishOutputApiProjectsProjectIdDispatchFinishesTaskIdOutputGet = <ThrowOnError extends boolean = false>(options: Options<ReadTaskFinishOutputApiProjectsProjectIdDispatchFinishesTaskIdOutputGetData, ThrowOnError>) => (options.client ?? client).get<ReadTaskFinishOutputApiProjectsProjectIdDispatchFinishesTaskIdOutputGetResponses, ReadTaskFinishOutputApiProjectsProjectIdDispatchFinishesTaskIdOutputGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/finishes/{task_id}/output', ...options });

/**
 * List Dispatch Runs
 *
 * Runs belonging to this project, newest first.
 *
 * Filtered by project rather than returning the machine's whole ledger: a run
 * directory records which project it belongs to, and a page about one project has no
 * business showing another's.
 */
export const listDispatchRunsApiProjectsProjectIdDispatchRunsGet = <ThrowOnError extends boolean = false>(options: Options<ListDispatchRunsApiProjectsProjectIdDispatchRunsGetData, ThrowOnError>) => (options.client ?? client).get<ListDispatchRunsApiProjectsProjectIdDispatchRunsGetResponses, ListDispatchRunsApiProjectsProjectIdDispatchRunsGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/runs', ...options });

/**
 * Cancel Dispatch Run
 *
 * Stop one run, or take one queued dispatch out of the line, and record it.
 *
 * **The queue is tried first, and only a *waiting* entry answers here.** A queued
 * dispatch that started a second ago is a run, its queue row says ``started``, and this
 * falls through to the run path with the run's own id -- which is the one that stops
 * something. The reverse ordering would report a cancellation over a live agent.
 *
 * Same route for both because they are the same act from where the person is standing:
 * the card they are cancelling is the same card, before and after a slot freed under it.
 */
export const cancelDispatchRunApiProjectsProjectIdDispatchRunsRunIdCancelPost = <ThrowOnError extends boolean = false>(options: Options<CancelDispatchRunApiProjectsProjectIdDispatchRunsRunIdCancelPostData, ThrowOnError>) => (options.client ?? client).post<CancelDispatchRunApiProjectsProjectIdDispatchRunsRunIdCancelPostResponses, CancelDispatchRunApiProjectsProjectIdDispatchRunsRunIdCancelPostErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/runs/{run_id}/cancel', ...options });

/**
 * Read Dispatch Run Output
 *
 * A run's output in full, as text a browser tab can show.
 *
 * Text rather than JSON because this is the one dispatch response a human reads
 * directly, and a transcript wrapped in a JSON string escape is unreadable.
 */
export const readDispatchRunOutputApiProjectsProjectIdDispatchRunsRunIdOutputGet = <ThrowOnError extends boolean = false>(options: Options<ReadDispatchRunOutputApiProjectsProjectIdDispatchRunsRunIdOutputGetData, ThrowOnError>) => (options.client ?? client).get<ReadDispatchRunOutputApiProjectsProjectIdDispatchRunsRunIdOutputGetResponses, ReadDispatchRunOutputApiProjectsProjectIdDispatchRunsRunIdOutputGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/runs/{run_id}/output', ...options });

/**
 * Read Dispatch Run Tail
 *
 * The end of a run's output, for a page watching it while it happens.
 *
 * A separate endpoint from ``/output`` rather than a parameter on it, because the two
 * are read by different things: ``/output`` is a link a human opens, so it answers in
 * text and gives them everything, while this is polled by a page and answers in the
 * shape the generated client already understands.
 *
 * **No subprocess is involved.** The text comes from a file the session poller wrote,
 * so a hundred people watching one run costs a hundred file reads and no more processes
 * than nobody watching it -- and the tail can never be fresher than the poller's own
 * interval, which is the point rather than a limitation.
 */
export const readDispatchRunTailApiProjectsProjectIdDispatchRunsRunIdTailGet = <ThrowOnError extends boolean = false>(options: Options<ReadDispatchRunTailApiProjectsProjectIdDispatchRunsRunIdTailGetData, ThrowOnError>) => (options.client ?? client).get<ReadDispatchRunTailApiProjectsProjectIdDispatchRunsRunIdTailGetResponses, ReadDispatchRunTailApiProjectsProjectIdDispatchRunsRunIdTailGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/runs/{run_id}/tail', ...options });

/**
 * Read Dispatch Run Transcript
 *
 * What the session recorded about itself, as entries rather than as a screen.
 *
 * Polled on the same clock as ``/tail`` and read the same way -- a file, no subprocess
 * -- but from a different file. ``/tail`` serves the pty capture, which is a *repaint*
 * of a terminal and loses every space and line break the moment its escape sequences
 * are removed. This serves the JSONL the runner writes beside it, where the agent's
 * prose, each tool call and whether it failed are all still separate things.
 *
 * ``source: 'none'`` is an ordinary answer, not an error: a batch run, a session that
 * has not reported its id yet, and a driver that keeps no such file all reach it. The
 * ``note`` says which, and the caller falls back to the tail.
 */
export const readDispatchRunTranscriptApiProjectsProjectIdDispatchRunsRunIdTranscriptGet = <ThrowOnError extends boolean = false>(options: Options<ReadDispatchRunTranscriptApiProjectsProjectIdDispatchRunsRunIdTranscriptGetData, ThrowOnError>) => (options.client ?? client).get<ReadDispatchRunTranscriptApiProjectsProjectIdDispatchRunsRunIdTranscriptGetResponses, ReadDispatchRunTranscriptApiProjectsProjectIdDispatchRunsRunIdTranscriptGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/dispatch/runs/{run_id}/transcript', ...options });

/**
 * Record Finish History
 *
 * Index one scripted finish and the steps it has taken so far.
 *
 * ``written`` is false, with a reason, when an imported record met a row already there
 * or when the finish names a task this project does not have. Both are answers rather
 * than errors: the importer reports them, and the finisher never sends either.
 */
export const recordFinishHistoryApiProjectsProjectIdHistoryFinishesFinishIdPut = <ThrowOnError extends boolean = false>(options: Options<RecordFinishHistoryApiProjectsProjectIdHistoryFinishesFinishIdPutData, ThrowOnError>) => (options.client ?? client).put<RecordFinishHistoryApiProjectsProjectIdHistoryFinishesFinishIdPutResponses, RecordFinishHistoryApiProjectsProjectIdHistoryFinishesFinishIdPutErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/history/finishes/{finish_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Record Gate History
 *
 * Index one gate run and the stages it has finished so far.
 */
export const recordGateHistoryApiProjectsProjectIdHistoryGatesGateIdPut = <ThrowOnError extends boolean = false>(options: Options<RecordGateHistoryApiProjectsProjectIdHistoryGatesGateIdPutData, ThrowOnError>) => (options.client ?? client).put<RecordGateHistoryApiProjectsProjectIdHistoryGatesGateIdPutResponses, RecordGateHistoryApiProjectsProjectIdHistoryGatesGateIdPutErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/history/gates/{gate_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Model Status
 *
 * Whether a drafting call can be made on this machine, and why not when it cannot.
 *
 * Names no project: what is configured is a property of the machine, and a per-project
 * answer would imply a per-project credential, which is the one arrangement the design
 * rules out (``docs/model-access-design.md`` §4).
 */
export const getModelStatusApiProjectsProjectIdModelGet = <ThrowOnError extends boolean = false>(options: Options<GetModelStatusApiProjectsProjectIdModelGetData, ThrowOnError>) => (options.client ?? client).get<GetModelStatusApiProjectsProjectIdModelGetResponses, GetModelStatusApiProjectsProjectIdModelGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/model', ...options });

/**
 * Draft Task Spec
 *
 * Expand what a person typed into spec fields. Writes nothing.
 *
 * Project-scoped because the draft is told which project it is being filed into, which
 * is context a spec legitimately reflects. It reads nothing else about the project and
 * writes nothing to it.
 *
 * Every refusal comes back as a 200 with ``drafted: false``; see
 * :class:`~agentjobs.api.models.SpecDraftResponse` for why that is the shape.
 */
export const draftTaskSpecApiProjectsProjectIdModelDraftPost = <ThrowOnError extends boolean = false>(options: Options<DraftTaskSpecApiProjectsProjectIdModelDraftPostData, ThrowOnError>) => (options.client ?? client).post<DraftTaskSpecApiProjectsProjectIdModelDraftPostResponses, DraftTaskSpecApiProjectsProjectIdModelDraftPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/model/draft',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Playbooks
 *
 * Every playbook this project holds, with the files that would not load.
 */
export const getPlaybooksApiProjectsProjectIdPlaybooksGet = <ThrowOnError extends boolean = false>(options: Options<GetPlaybooksApiProjectsProjectIdPlaybooksGetData, ThrowOnError>) => (options.client ?? client).get<GetPlaybooksApiProjectsProjectIdPlaybooksGetResponses, GetPlaybooksApiProjectsProjectIdPlaybooksGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/playbooks', ...options });

/**
 * Get Playbook
 *
 * One playbook by name, including its brief.
 *
 * 422 rather than 404 for a file that exists and does not validate, matching how a
 * stored task that will not parse is reported: the thing is there and repairable,
 * and "not found" would send its author looking for something already on disk.
 */
export const getPlaybookApiProjectsProjectIdPlaybooksNameGet = <ThrowOnError extends boolean = false>(options: Options<GetPlaybookApiProjectsProjectIdPlaybooksNameGetData, ThrowOnError>) => (options.client ?? client).get<GetPlaybookApiProjectsProjectIdPlaybooksNameGetResponses, GetPlaybookApiProjectsProjectIdPlaybooksNameGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/playbooks/{name}', ...options });

/**
 * Run Playbook Endpoint
 *
 * Start an agent on a playbook: create its run task if it has one, then dispatch.
 *
 * 202 rather than 200, and for the same reason ``POST /tasks/{id}/dispatch`` is: the
 * run has started, and how it ends arrives later as ``dispatch_result`` entries on the
 * task rather than in this response.
 *
 * **Every dispatch gate binds, unchanged.** This endpoint opens nothing: it resolves a
 * file, checks the invocation's shape against the playbook's ``target``, and then
 * calls the same ``dispatch_task`` the Dispatch button calls, with a pointer attached
 * that no guard reads. A machine with dispatch off refuses here as it refuses there,
 * and refuses *before* a run task is created.
 *
 * ``user`` reaches the record by one of two routes, and which one depends on the
 * playbook rather than on this endpoint. A project-target run creates the run task
 * attributed to that human, and their creation entry is the newest stored entry the
 * human-clocked rule then reads. A task-target run creates nothing, so ``user`` is
 * passed to the dispatcher as task-188's ``authorized_by`` and its authorising entry
 * is written onto the task named. Neither path substitutes the project's
 * ``default_user`` for a human nobody named.
 */
export const runPlaybookEndpointApiProjectsProjectIdPlaybooksNameRunPost = <ThrowOnError extends boolean = false>(options: Options<RunPlaybookEndpointApiProjectsProjectIdPlaybooksNameRunPostData, ThrowOnError>) => (options.client ?? client).post<RunPlaybookEndpointApiProjectsProjectIdPlaybooksNameRunPostResponses, RunPlaybookEndpointApiProjectsProjectIdPlaybooksNameRunPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/playbooks/{name}/run',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Push Status
 *
 * The application-server key, and every device registered for this project.
 */
export const getPushStatusApiProjectsProjectIdPushGet = <ThrowOnError extends boolean = false>(options: Options<GetPushStatusApiProjectsProjectIdPushGetData, ThrowOnError>) => (options.client ?? client).get<GetPushStatusApiProjectsProjectIdPushGetResponses, GetPushStatusApiProjectsProjectIdPushGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/push', ...options });

/**
 * Subscribe Push Device
 *
 * Register a device, or refresh one that already exists.
 *
 * **A device registered mid-episode is not immediately pushed.** The new row inherits
 * nothing, so ``last_episode_id`` is seeded with whatever episode is currently open:
 * a person who opts in from a page that is already showing "3 waiting" has seen the
 * three, and interrupting them about it a second later would be the alert fatigue the
 * episode model exists to prevent. The *next* episode wakes them, which is the thing
 * they subscribed for; ``POST /push/test`` is how they confirm it works before then.
 *
 * Re-subscribing an endpoint that is already registered keeps its id and its episode
 * id -- see :func:`agentjobs.push.subscriptions.upsert`.
 */
export const subscribePushDeviceApiProjectsProjectIdPushSubscribePost = <ThrowOnError extends boolean = false>(options: Options<SubscribePushDeviceApiProjectsProjectIdPushSubscribePostData, ThrowOnError>) => (options.client ?? client).post<SubscribePushDeviceApiProjectsProjectIdPushSubscribePostResponses, SubscribePushDeviceApiProjectsProjectIdPushSubscribePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/push/subscribe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send Test Push
 *
 * Prove delivery to one device, or to all of them.
 *
 * The answer to "did that actually work", which nothing else gives: a device that
 * opted in while nothing was waiting would otherwise find out whether push reaches it
 * at the moment it matters least. It does not touch the episode -- a test must not be
 * able to consume the interruption a real episode is owed.
 */
export const sendTestPushApiProjectsProjectIdPushTestPost = <ThrowOnError extends boolean = false>(options: Options<SendTestPushApiProjectsProjectIdPushTestPostData, ThrowOnError>) => (options.client ?? client).post<SendTestPushApiProjectsProjectIdPushTestPostResponses, SendTestPushApiProjectsProjectIdPushTestPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/push/test',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unsubscribe Push Device
 *
 * Forget a device, by id from the page or by endpoint from a service worker.
 *
 * Idempotent, and silent about whether anything matched. A device is routinely
 * removed from both ends at once -- the button here and the browser's own permission
 * reset -- and a 404 for the second of those would be an error message about a race.
 */
export const unsubscribePushDeviceApiProjectsProjectIdPushUnsubscribePost = <ThrowOnError extends boolean = false>(options: Options<UnsubscribePushDeviceApiProjectsProjectIdPushUnsubscribePostData, ThrowOnError>) => (options.client ?? client).post<UnsubscribePushDeviceApiProjectsProjectIdPushUnsubscribePostResponses, UnsubscribePushDeviceApiProjectsProjectIdPushUnsubscribePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/push/unsubscribe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Queue
 *
 * The whole ordered backlog, band by band. This is the list a human reviews.
 *
 * Every band appears, including the empty ones: "critical is empty" is a fact a
 * reader of an ordered backlog wants stated rather than inferred from a heading that
 * is not there. Every open task carries its claimability and, when it is not
 * claimable, the rule that excluded it -- the same sentence ``/tasks/next/explain``
 * gives, from the same place, so the two can never disagree.
 *
 * **This does not 409 on a broken queue**, unlike ``/tasks/next``. It is one of the
 * two deliberate exceptions in design section 8: you have to be able to see a broken
 * queue in order to fix it, so the offending bands still render and ``problems``
 * names what is wrong beside ``repair_command``.
 *
 * Each entry also carries ``last_move`` -- the move that set its place, with the
 * actor, that actor's kind from this project's vocabulary, the reason recorded with
 * it, and whether a human kept the place over a warning. That is the anchor evidence
 * ``reorder`` reads, and it is here so a run costs one request instead of one record
 * fetch per open task.
 */
export const getQueueApiProjectsProjectIdQueueGet = <ThrowOnError extends boolean = false>(options: Options<GetQueueApiProjectsProjectIdQueueGetData, ThrowOnError>) => (options.client ?? client).get<GetQueueApiProjectsProjectIdQueueGetResponses, GetQueueApiProjectsProjectIdQueueGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/queue', ...options });

/**
 * Compact Queue
 *
 * Renumber one band back to 100, 200, 300..., changing nobody's place.
 *
 * Purely cosmetic, and explicit only -- there is no automatic compaction, because a
 * background process quietly rewriting forty task files is exactly the kind of thing
 * that should require somebody to ask for it. One band per request for the same
 * reason: compacting the whole corpus is four decisions, not one.
 */
export const compactQueueApiProjectsProjectIdQueueCompactPost = <ThrowOnError extends boolean = false>(options: Options<CompactQueueApiProjectsProjectIdQueueCompactPostData, ThrowOnError>) => (options.client ?? client).post<CompactQueueApiProjectsProjectIdQueueCompactPostResponses, CompactQueueApiProjectsProjectIdQueueCompactPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/queue/compact',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Repair Queue
 *
 * Make a broken queue into a queue again, and say exactly what was guessed.
 *
 * Operates on a corrupt corpus by definition, so it reads the raw files rather than
 * loaded tasks -- the records it most needs are the ones the open-task-has-a-position
 * rule refuses to load. Open tasks with no usable position, and the losing claimants
 * of a shared one, go to the bottom of their band ordered by ``created`` then id.
 *
 * It never invents an opinion it does not have. A duplicate position carries no
 * record of who was meant to be first, so that tie-break is arbitrary by necessity --
 * and every task it touched comes back in ``assigned``, which is what makes the guess
 * reviewable rather than silent.
 */
export const repairQueueApiProjectsProjectIdQueueRepairPost = <ThrowOnError extends boolean = false>(options: Options<RepairQueueApiProjectsProjectIdQueueRepairPostData, ThrowOnError>) => (options.client ?? client).post<RepairQueueApiProjectsProjectIdQueueRepairPostResponses, RepairQueueApiProjectsProjectIdQueueRepairPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/queue/repair',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Project Revision
 *
 * Answer whether this project changed, without loading the task collection.
 */
export const getProjectRevisionApiProjectsProjectIdRevisionGet = <ThrowOnError extends boolean = false>(options: Options<GetProjectRevisionApiProjectsProjectIdRevisionGetData, ThrowOnError>) => (options.client ?? client).get<GetProjectRevisionApiProjectsProjectIdRevisionGetResponses, GetProjectRevisionApiProjectsProjectIdRevisionGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/revision', ...options });

/**
 * Search Tasks
 *
 * Search tasks using a case-insensitive substring query.
 *
 * Rows, in the same shape and for the same reason ``GET /tasks`` returns rows: a
 * result list draws a title, a badge and a place in line. It answered with whole
 * ``TaskRead`` records until task-495 -- every matched task's spec prose, acceptance
 * criteria and complete log -- which was 10,781 bytes per record and 5.2 MB for 480
 * matches. Whole records are at ``GET /search/full``.
 *
 * Rows carry the same computed dependency facts as ``GET /tasks`` -- ``actionable``,
 * ``unmet_needs``, ``open_children_count`` -- so a result can be acted on without a
 * second request per row.
 */
export const searchTasksApiProjectsProjectIdSearchGet = <ThrowOnError extends boolean = false>(options: Options<SearchTasksApiProjectsProjectIdSearchGetData, ThrowOnError>) => (options.client ?? client).get<SearchTasksApiProjectsProjectIdSearchGetResponses, SearchTasksApiProjectsProjectIdSearchGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/search', ...options });

/**
 * Search Tasks Full
 *
 * The same hits, in the same order, as whole records.
 *
 * Named so the expensive shape is the one a caller asks for by name -- task-484's
 * decision, applied to the second endpoint that had it by default.
 * ``TaskClient.search_tasks`` is the caller that needs it: it parses each item into a
 * ``Task``, and a row would parse with an empty log, which is a wrong answer rather
 * than an error.
 */
export const searchTasksFullApiProjectsProjectIdSearchFullGet = <ThrowOnError extends boolean = false>(options: Options<SearchTasksFullApiProjectsProjectIdSearchFullGetData, ThrowOnError>) => (options.client ?? client).get<SearchTasksFullApiProjectsProjectIdSearchFullGetResponses, SearchTasksFullApiProjectsProjectIdSearchFullGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/search/full', ...options });

/**
 * List Tasks
 *
 * List tasks filtered along the state axes, as listing rows.
 *
 * ``?ball=human`` is the human inbox: everything waiting on a person, each row
 * carrying its ``ball_prompt``. ``?ball=external`` is the blocked list.
 * ``?parent=task-063-schema-v2`` is one umbrella's children.
 *
 * **A row, not a record.** This used to answer with complete tasks -- spec prose,
 * acceptance criteria and the entire log of each -- which on the agentjobs backlog was
 * 10.4 MB and four full corpus loads to draw a column of titles, and grew with every
 * log entry appended to any task (task-484). It now answers with the fields a listing
 * reads. Open a task and ``/{task_id}/detail`` carries the rest, which is the fetch
 * every surface that shows a task already makes.
 *
 * Whole records are at ``/tasks/full``, unchanged. Narrowing *this* route rather than
 * adding a narrow sibling is deliberate: the expensive shape should be the one a caller
 * asks for by name, so a new surface that never thought about it gets the cheap one.
 */
export const listTasksApiProjectsProjectIdTasksGet = <ThrowOnError extends boolean = false>(options: Options<ListTasksApiProjectsProjectIdTasksGetData, ThrowOnError>) => (options.client ?? client).get<ListTasksApiProjectsProjectIdTasksGetResponses, ListTasksApiProjectsProjectIdTasksGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks', ...options });

/**
 * Create Task
 *
 * Create a new task record.
 *
 * With an operation_id the create runs under the project-wide creation lock and a
 * retry resolves to the task the first attempt made, rather than producing a second
 * one with a different generated id.
 */
export const createTaskApiProjectsProjectIdTasksPost = <ThrowOnError extends boolean = false>(options: Options<CreateTaskApiProjectsProjectIdTasksPostData, ThrowOnError>) => (options.client ?? client).post<CreateTaskApiProjectsProjectIdTasksPostResponses, CreateTaskApiProjectsProjectIdTasksPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Broken Tasks
 *
 * Files in the task directory that exist but cannot be loaded.
 *
 * Declared before /{task_id} so "broken" is not captured as a task id. These used to
 * be invisible: storage returned None for them and every listing simply omitted the
 * task. An unmigrated v1 file shows up here by filename.
 */
export const listBrokenTasksApiProjectsProjectIdTasksBrokenGet = <ThrowOnError extends boolean = false>(options: Options<ListBrokenTasksApiProjectsProjectIdTasksBrokenGetData, ThrowOnError>) => (options.client ?? client).get<ListBrokenTasksApiProjectsProjectIdTasksBrokenGetResponses, ListBrokenTasksApiProjectsProjectIdTasksBrokenGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/broken', ...options });

/**
 * Get Claimable Tasks
 *
 * Every task that may be worked now, in the queue's order. ``/next`` is its head.
 *
 * The epic walk needs the *set* rather than the winner: it starts every eligible child
 * up to its slot count, and asking for the winner repeatedly would either re-offer the
 * child it just claimed or report the epic as deadlocked. It is here because the CLI
 * is a service client now that only the server opens the store, and the walk must not
 * be the one caller that re-implements the claimability filter -- that is exactly how
 * the walk and the dashboard would come to disagree about what is next, with the walk
 * winning silently because it is the one that spends money.
 *
 * Declared before ``/{task_id}`` for the same reason ``/next`` is: otherwise
 * "claimable" is captured as a task id.
 */
export const getClaimableTasksApiProjectsProjectIdTasksClaimableGet = <ThrowOnError extends boolean = false>(options: Options<GetClaimableTasksApiProjectsProjectIdTasksClaimableGetData, ThrowOnError>) => (options.client ?? client).get<GetClaimableTasksApiProjectsProjectIdTasksClaimableGetResponses, GetClaimableTasksApiProjectsProjectIdTasksClaimableGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/claimable', ...options });

/**
 * List Full Tasks
 *
 * Every matching task as a complete record -- what ``GET /tasks`` used to return.
 *
 * Kept because two callers genuinely need whole records for a listing, and neither is
 * a browser: ``TaskClient.list_tasks`` is the surface the CLI's ``TaskManager``
 * equivalent is built on, so it must return ``Task`` and not a shape missing a log;
 * and ``agentjobs branches`` reads ``branches[]`` off every task, which no listing
 * projection carries.
 *
 * Expensive, and named so a caller has to choose it. Declared before ``/{task_id}``
 * for the reason ``/next`` and ``/broken`` are: otherwise "full" is captured as a task
 * id.
 */
export const listFullTasksApiProjectsProjectIdTasksFullGet = <ThrowOnError extends boolean = false>(options: Options<ListFullTasksApiProjectsProjectIdTasksFullGetData, ThrowOnError>) => (options.client ?? client).get<ListFullTasksApiProjectsProjectIdTasksFullGetResponses, ListFullTasksApiProjectsProjectIdTasksFullGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/full', ...options });

/**
 * Get Next Task
 *
 * The next claimable task: ready, eligible for the agent, no unmet needs.
 *
 * Answers 409 rather than guessing when the queue it would have to read is broken.
 */
export const getNextTaskApiProjectsProjectIdTasksNextGet = <ThrowOnError extends boolean = false>(options: Options<GetNextTaskApiProjectsProjectIdTasksNextGetData, ThrowOnError>) => (options.client ?? client).get<GetNextTaskApiProjectsProjectIdTasksNextGetResponses, GetNextTaskApiProjectsProjectIdTasksNextGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/next', ...options });

/**
 * Explain Next Task
 *
 * Why this task is next, and every open task it stands in front of.
 *
 * Declared before ``/{task_id}`` so "next" is not captured as a task id, exactly as
 * ``/next`` and ``/broken`` are. The body is design section 9's structure, produced
 * by the manager rather than assembled here: the route is a transcription.
 */
export const explainNextTaskApiProjectsProjectIdTasksNextExplainGet = <ThrowOnError extends boolean = false>(options: Options<ExplainNextTaskApiProjectsProjectIdTasksNextExplainGetData, ThrowOnError>) => (options.client ?? client).get<ExplainNextTaskApiProjectsProjectIdTasksNextExplainGetResponses, ExplainNextTaskApiProjectsProjectIdTasksNextExplainGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/next/explain', ...options });

/**
 * Archive Task
 *
 * Archive a task. An open task is closed as cancelled first.
 */
export const archiveTaskApiProjectsProjectIdTasksTaskIdDelete = <ThrowOnError extends boolean = false>(options: Options<ArchiveTaskApiProjectsProjectIdTasksTaskIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<ArchiveTaskApiProjectsProjectIdTasksTaskIdDeleteResponses, ArchiveTaskApiProjectsProjectIdTasksTaskIdDeleteErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/{task_id}', ...options });

/**
 * Get Task
 *
 * Retrieve a specific task by identifier.
 */
export const getTaskApiProjectsProjectIdTasksTaskIdGet = <ThrowOnError extends boolean = false>(options: Options<GetTaskApiProjectsProjectIdTasksTaskIdGetData, ThrowOnError>) => (options.client ?? client).get<GetTaskApiProjectsProjectIdTasksTaskIdGetResponses, GetTaskApiProjectsProjectIdTasksTaskIdGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/{task_id}', ...options });

/**
 * Update Task
 *
 * Apply a partial update to a task. State axes move through the verbs, not here.
 *
 * The ``actor`` is validated whenever one is sent, exactly as ``create_task``
 * validates its own (D2): this route writes an entry into an append-only log, and an
 * attribution nobody can resolve later is worse than a refused request. It was the one
 * submitted actor in this module that went to the manager unchecked, so a browser edit
 * could name anybody and a typo became permanent.
 */
export const updateTaskApiProjectsProjectIdTasksTaskIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateTaskApiProjectsProjectIdTasksTaskIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateTaskApiProjectsProjectIdTasksTaskIdPatchResponses, UpdateTaskApiProjectsProjectIdTasksTaskIdPatchErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Answer Task
 *
 * Supply what the agent was waiting for and hand the ball back (agent/answer).
 *
 * Not a revision: nothing the agent did was wrong, and a record that says otherwise
 * makes the next reader reconstruct which it was. The answer rides in the ball_prompt
 * and the log verbatim, exactly as requested changes do.
 *
 * Since task-017 it may also carry ``answers``, each naming the question entry it
 * answers. Those become ``answer`` entries threaded by ``re``, written in the same
 * mutation as the handoff, and the ball_prompt is composed from them when the human
 * tapped options and typed nothing -- which is the whole point of the feature, and
 * would otherwise leave the agent a handoff whose ask is blank.
 */
export const answerTaskApiProjectsProjectIdTasksTaskIdAnswerPost = <ThrowOnError extends boolean = false>(options: Options<AnswerTaskApiProjectsProjectIdTasksTaskIdAnswerPostData, ThrowOnError>) => (options.client ?? client).post<AnswerTaskApiProjectsProjectIdTasksTaskIdAnswerPostResponses, AnswerTaskApiProjectsProjectIdTasksTaskIdAnswerPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/answer',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Approve Task
 *
 * Record human approval and hand the ball back to the agent (agent/work).
 *
 * Nothing here merges anything. The GUI cannot run git, and a button that implied
 * otherwise would lie. It records the approval in the task record -- which is the
 * point, since approvals used to live only in chat -- and moves the ball: the agent
 * must now rebase, merge --no-ff, mark the branch merged, and close the task
 * (ENGINEERING.md, "The Merge Gate").
 *
 * ``note`` is optional and strictly additive (task-228). With none, this writes byte
 * for byte what it wrote before the field existed. With one, the note rides *after*
 * the merge clearance, verbatim and unsummarised, followed by a sentence saying it is
 * context to carry into the merge -- because an agent that reads an approval note as
 * a fresh review round has inverted the point of attaching one. Before this, an
 * approval carrying a sentence had to go through Request Changes: a round trip the
 * human did not ask for, and a record that said `revise` about work that was approved.
 */
export const approveTaskApiProjectsProjectIdTasksTaskIdApprovePost = <ThrowOnError extends boolean = false>(options: Options<ApproveTaskApiProjectsProjectIdTasksTaskIdApprovePostData, ThrowOnError>) => (options.client ?? client).post<ApproveTaskApiProjectsProjectIdTasksTaskIdApprovePostResponses, ApproveTaskApiProjectsProjectIdTasksTaskIdApprovePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/approve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Attachment
 *
 * Serve one image a log entry references.
 *
 * Resolved through the task's own record rather than straight off the filesystem, so
 * the only files this can ever return are ones an entry actually points at. That also
 * supplies the recorded hash, which is checked before the bytes are handed back: a
 * file edited or corrupted since it was stored is refused rather than rendered.
 */
export const getAttachmentApiProjectsProjectIdTasksTaskIdAttachmentsFilenameGet = <ThrowOnError extends boolean = false>(options: Options<GetAttachmentApiProjectsProjectIdTasksTaskIdAttachmentsFilenameGetData, ThrowOnError>) => (options.client ?? client).get<GetAttachmentApiProjectsProjectIdTasksTaskIdAttachmentsFilenameGetResponses, GetAttachmentApiProjectsProjectIdTasksTaskIdAttachmentsFilenameGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/{task_id}/attachments/{filename}', ...options });

/**
 * Relay Authorization
 *
 * Record that a human authorised a dispatch, as the agent they told (task-506).
 *
 * The third shape of an authorising entry, and the only one whose author and subject
 * are different people. A person clicking Dispatch gets an entry under their own name.
 * A person writing a note by hand gets the same. An agent told in a chat session to
 * start the task it has just filed had neither, and the workaround was to write the
 * note as *them* -- a signature in an append-only log that its owner did not put
 * there, indistinguishable by any reader from a click. This writes an entry that says
 * what actually happened: ``actor`` is the agent, ``authorized_by`` is the person,
 * ``body`` is what the agent was asked for in its own words.
 *
 * **It is a separate route because it is a separate capability.**
 * ``dispatch.relay_authorization`` is held by the two human kinds and by no ``run``,
 * and the route table is keyed by endpoint -- so putting this on ``POST /log``, which
 * every run may call, would have made the gate a condition inside a handler instead of
 * a row in the table ``tests/test_authorization.py`` checks for completeness.
 *
 * **It starts nothing.** It is not an approval, it releases no merge gate, it arms
 * nothing, and it spends no run slot. A dispatch afterwards is an ordinary ``manual``
 * dispatch, judged by every gate in ``dispatch/guards.py`` and counted against every
 * cap in ``dispatch/budget.py``. Nor is the entry consumed: it clocks exactly as many
 * runs as a human's own note would, which is to say as many as those caps allow.
 *
 * **What it is not is proof.** The server cannot tell whether the person said anything,
 * just as it cannot verify the ``user`` field on a dispatch (design section 2's P1-2).
 * What it buys is a record that is true about who typed it, and a write a dispatched
 * run is refused -- see ``docs/authorization.md``.
 */
export const relayAuthorizationApiProjectsProjectIdTasksTaskIdAuthorizationPost = <ThrowOnError extends boolean = false>(options: Options<RelayAuthorizationApiProjectsProjectIdTasksTaskIdAuthorizationPostData, ThrowOnError>) => (options.client ?? client).post<RelayAuthorizationApiProjectsProjectIdTasksTaskIdAuthorizationPostResponses, RelayAuthorizationApiProjectsProjectIdTasksTaskIdAuthorizationPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/authorization',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Authorize Task Chain
 *
 * Authorise a bounded chain of dispatches against this task's checks (task-150).
 *
 * **Classified under ``dispatch.start``, which no run holds**, and the alternative was
 * argued rather than assumed. A capability of its own -- ``CHAIN_AUTHORIZE`` -- would
 * draw exactly the same line for exactly the same principals, and this module's
 * coarseness is deliberate: *a capability per route would be a role system with extra
 * steps*. What a chain authorisation buys is up to twenty dispatches against one task
 * with nobody clicking again, which is the act ``dispatch.start`` already names, bought
 * in advance. ``task.review`` was the other candidate and is wrong for the opposite
 * reason: this is not a judgement about work somebody did, it is a purchase.
 *
 * The digest, the chain id and the covered criteria are computed here from the **stored
 * task**. There is no request field for any of them, so there is nothing a caller could
 * supply that would move the definition of done the chain converges on.
 *
 * ``user`` is validated the way a dispatch's is: the id must be one this project
 * configures with ``kind: human``. An agent may not authorise a chain any more than it
 * may authorise a dispatch, and this is the same refusal reached through the same
 * function.
 */
export const authorizeTaskChainApiProjectsProjectIdTasksTaskIdChainPost = <ThrowOnError extends boolean = false>(options: Options<AuthorizeTaskChainApiProjectsProjectIdTasksTaskIdChainPostData, ThrowOnError>) => (options.client ?? client).post<AuthorizeTaskChainApiProjectsProjectIdTasksTaskIdChainPostResponses, AuthorizeTaskChainApiProjectsProjectIdTasksTaskIdChainPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/chain',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke Task Chain
 *
 * Stop a chain before its next iteration. One click, no arguments required.
 *
 * Shares ``dispatch.start`` with the route above for the reason a kill switch always
 * shares a capability with its switch: everyone who may start it may stop it. The
 * asymmetric alternative -- anyone may stop, only some may start -- reads well and is
 * wrong here, because the principal it would newly admit is a dispatched run, and a run
 * able to revoke could stop a chain a person is relying on and then report that it
 * converged.
 *
 * **This does not stop a run that is already executing.** An iteration is a dispatch and
 * a dispatch is somebody's session; ``POST .../dispatch/runs/{id}/cancel`` ends one of
 * those. What this guarantees is that no further iteration begins.
 */
export const revokeTaskChainApiProjectsProjectIdTasksTaskIdChainRevokePost = <ThrowOnError extends boolean = false>(options: Options<RevokeTaskChainApiProjectsProjectIdTasksTaskIdChainRevokePostData, ThrowOnError>) => (options.client ?? client).post<RevokeTaskChainApiProjectsProjectIdTasksTaskIdChainRevokePostResponses, RevokeTaskChainApiProjectsProjectIdTasksTaskIdChainRevokePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/chain/revoke',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read Task Chains
 *
 * Every chain this task has had, oldest first, each with its iteration history.
 *
 * A read, and deliberately absent from ``ROUTE_CAPABILITIES``: it executes nothing and
 * answers with what the log already holds. What a caller may *do* with a chain is gated
 * at the two verbs below.
 */
export const readTaskChainsApiProjectsProjectIdTasksTaskIdChainsGet = <ThrowOnError extends boolean = false>(options: Options<ReadTaskChainsApiProjectsProjectIdTasksTaskIdChainsGetData, ThrowOnError>) => (options.client ?? client).get<ReadTaskChainsApiProjectsProjectIdTasksTaskIdChainsGetResponses, ReadTaskChainsApiProjectsProjectIdTasksTaskIdChainsGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/{task_id}/chains', ...options });

/**
 * Check Task Acceptance
 *
 * Run this task's executable acceptance checks and answer with what each one did.
 *
 * The same vector ``agentjobs check`` prints, written to the record the same way: the
 * criteria's statuses move and exactly one ``check_result`` entry records the pass.
 *
 * **A POST because it executes commands**, not because it writes -- and that is also
 * why it is classified under ``dispatch.start`` rather than under the ``task.*``
 * capabilities a run already holds. A run may edit its own task, so a run able to call
 * this could write a check and then have this machine run it, which is starting a
 * process of its own choosing on somebody else's hardware. That is the act the dispatch
 * gates bound, and this goes through the same gate: ``assert_dispatch_permitted`` runs
 * before any process starts, and its refusals render under their own reason codes.
 *
 * Refused with ``no_checks`` on a task whose criteria are all prose. Answering an empty
 * success would be indistinguishable, to any client, from every check having passed.
 */
export const checkTaskAcceptanceApiProjectsProjectIdTasksTaskIdCheckPost = <ThrowOnError extends boolean = false>(options: Options<CheckTaskAcceptanceApiProjectsProjectIdTasksTaskIdCheckPostData, ThrowOnError>) => (options.client ?? client).post<CheckTaskAcceptanceApiProjectsProjectIdTasksTaskIdCheckPostResponses, CheckTaskAcceptanceApiProjectsProjectIdTasksTaskIdCheckPostErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/{task_id}/check', ...options });

/**
 * Claim Task
 *
 * Claim a ready task: one winner, everyone else gets a 409.
 *
 * A claim that names its session also writes an interactive run record (task-354), so
 * the work shows as running on every surface that reads the ledger. Only after the
 * claim landed, only for an agent actor, and never a second one for a task that has a
 * live run -- which is what makes a replayed claim harmless.
 */
export const claimTaskApiProjectsProjectIdTasksTaskIdClaimPost = <ThrowOnError extends boolean = false>(options: Options<ClaimTaskApiProjectsProjectIdTasksTaskIdClaimPostData, ThrowOnError>) => (options.client ?? client).post<ClaimTaskApiProjectsProjectIdTasksTaskIdClaimPostResponses, ClaimTaskApiProjectsProjectIdTasksTaskIdClaimPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/claim',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Close Task
 *
 * End the task with an outcome.
 */
export const closeTaskApiProjectsProjectIdTasksTaskIdClosePost = <ThrowOnError extends boolean = false>(options: Options<CloseTaskApiProjectsProjectIdTasksTaskIdClosePostData, ThrowOnError>) => (options.client ?? client).post<CloseTaskApiProjectsProjectIdTasksTaskIdClosePostResponses, CloseTaskApiProjectsProjectIdTasksTaskIdClosePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/close',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Mark Deliverable
 *
 * Mark a deliverable as done for the task.
 */
export const markDeliverableApiProjectsProjectIdTasksTaskIdDeliverablesDeliverablePathPatch = <ThrowOnError extends boolean = false>(options: Options<MarkDeliverableApiProjectsProjectIdTasksTaskIdDeliverablesDeliverablePathPatchData, ThrowOnError>) => (options.client ?? client).patch<MarkDeliverableApiProjectsProjectIdTasksTaskIdDeliverablesDeliverablePathPatchResponses, MarkDeliverableApiProjectsProjectIdTasksTaskIdDeliverablesDeliverablePathPatchErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/{task_id}/deliverables/{deliverable_path}', ...options });

/**
 * Get Task Detail
 *
 * Return the complete resumption and review contract for one task.
 */
export const getTaskDetailApiProjectsProjectIdTasksTaskIdDetailGet = <ThrowOnError extends boolean = false>(options: Options<GetTaskDetailApiProjectsProjectIdTasksTaskIdDetailGetData, ThrowOnError>) => (options.client ?? client).get<GetTaskDetailApiProjectsProjectIdTasksTaskIdDetailGetResponses, GetTaskDetailApiProjectsProjectIdTasksTaskIdDetailGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/tasks/{task_id}/detail', ...options });

/**
 * Dispatch Task Endpoint
 *
 * Start an agent on this task.
 *
 * Deliberately **not** merged into any approval endpoint. Approving means "I agree";
 * dispatching means "spend money now", and collapsing the two would turn every
 * approval into an implicit purchase (design decision D1).
 *
 * 202 rather than 200: the run has started, and how it ends arrives later as
 * ``dispatch_result`` entries on the task, not in this response.
 *
 * The address handed to the agent is this server's own, taken from the socket the
 * request arrived on. Until 2026-08-19 nothing was passed and the runner's default
 * won, so a dashboard on any other port dispatched agents at ``:8765`` -- an address
 * that, on the machine this was built for, is deliberately dead (task-154).
 *
 * **``user`` makes this one click (task-188).** Supplied, the guard layer writes that
 * human's authorising entry onto the task and dispatches on it, so a person no longer
 * has to know to write a note by hand before every run. It is validated as a
 * configured human and it is not the dispatch's ``actor``; see
 * :class:`DispatchRequestBody` for why those are different things.
 *
 * **Omitted, this endpoint behaves exactly as it did before.** It does *not* quietly
 * substitute the project's ``default_user``: a run has to be signed for by whoever
 * asked for it, and a server-side default would produce an entry that looks like a
 * person's authorisation and is really just a config value. So a caller with no
 * signed-in user falls back to the pre-existing rule -- the newest stored entry must
 * be a human's -- and is refused with ``not_human_clocked`` if it is not. That is the
 * same answer the CLI gets, and the React app disables the button and says so rather
 * than letting someone press it into a refusal.
 */
export const dispatchTaskEndpointApiProjectsProjectIdTasksTaskIdDispatchPost = <ThrowOnError extends boolean = false>(options: Options<DispatchTaskEndpointApiProjectsProjectIdTasksTaskIdDispatchPostData, ThrowOnError>) => (options.client ?? client).post<DispatchTaskEndpointApiProjectsProjectIdTasksTaskIdDispatchPostResponses, DispatchTaskEndpointApiProjectsProjectIdTasksTaskIdDispatchPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/dispatch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Request Finish Retry
 *
 * Retry a stopped scripted finish on the approval already given (task-575).
 *
 * The repairing agent calls this instead of running ``agentjobs finish`` from its own
 * shell, which the auto-mode classifier rightly refuses as a merge nobody reviewed.
 * The server judges the repair against the head the approver saw: when nothing they
 * reviewed changed, it starts the finish itself in a detached process; otherwise it
 * hands the task to human/review with each offending path stated. See
 * ``dispatch/finish_retry.py``.
 *
 * ``task.verb`` rather than ``task.review``, because a run must be able to ask. What
 * keeps the approval meaningful is the judgement and the bound, not who may call.
 */
export const requestFinishRetryApiProjectsProjectIdTasksTaskIdFinishRetryPost = <ThrowOnError extends boolean = false>(options: Options<RequestFinishRetryApiProjectsProjectIdTasksTaskIdFinishRetryPostData, ThrowOnError>) => (options.client ?? client).post<RequestFinishRetryApiProjectsProjectIdTasksTaskIdFinishRetryPostResponses, RequestFinishRetryApiProjectsProjectIdTasksTaskIdFinishRetryPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/finish-retry',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Handoff Task
 *
 * Move the ball, with its ask.
 */
export const handoffTaskApiProjectsProjectIdTasksTaskIdHandoffPost = <ThrowOnError extends boolean = false>(options: Options<HandoffTaskApiProjectsProjectIdTasksTaskIdHandoffPostData, ThrowOnError>) => (options.client ?? client).post<HandoffTaskApiProjectsProjectIdTasksTaskIdHandoffPostResponses, HandoffTaskApiProjectsProjectIdTasksTaskIdHandoffPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/handoff',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Hold Task
 *
 * Stop the task, with the release condition on the record (agent/hold).
 *
 * The condition is prefixed rather than left bare, because this is the one send-back
 * whose prompt must not read as work: an agent that skims it and carries on has done
 * the opposite of what it was told. `hold` is also the one agent-side reason no
 * dispatch path will act on, so a held task cannot be started by clicking Dispatch
 * beside the Hold button that stopped it.
 */
export const holdTaskApiProjectsProjectIdTasksTaskIdHoldPost = <ThrowOnError extends boolean = false>(options: Options<HoldTaskApiProjectsProjectIdTasksTaskIdHoldPostData, ThrowOnError>) => (options.client ?? client).post<HoldTaskApiProjectsProjectIdTasksTaskIdHoldPostResponses, HoldTaskApiProjectsProjectIdTasksTaskIdHoldPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/hold',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Append Log Entry
 *
 * Append a note/progress/decision/question/answer/instruction entry.
 */
export const appendLogEntryApiProjectsProjectIdTasksTaskIdLogPost = <ThrowOnError extends boolean = false>(options: Options<AppendLogEntryApiProjectsProjectIdTasksTaskIdLogPostData, ThrowOnError>) => (options.client ?? client).post<AppendLogEntryApiProjectsProjectIdTasksTaskIdLogPostResponses, AppendLogEntryApiProjectsProjectIdTasksTaskIdLogPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/log',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Post Progress Update
 *
 * Append a progress update entry for the task.
 */
export const postProgressUpdateApiProjectsProjectIdTasksTaskIdProgressPost = <ThrowOnError extends boolean = false>(options: Options<PostProgressUpdateApiProjectsProjectIdTasksTaskIdProgressPostData, ThrowOnError>) => (options.client ?? client).post<PostProgressUpdateApiProjectsProjectIdTasksTaskIdProgressPostResponses, PostProgressUpdateApiProjectsProjectIdTasksTaskIdProgressPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/progress',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Promote Task
 *
 * Declare a draft's spec finished: it becomes ready and claimable.
 */
export const promoteTaskApiProjectsProjectIdTasksTaskIdPromotePost = <ThrowOnError extends boolean = false>(options: Options<PromoteTaskApiProjectsProjectIdTasksTaskIdPromotePostData, ThrowOnError>) => (options.client ?? client).post<PromoteTaskApiProjectsProjectIdTasksTaskIdPromotePostResponses, PromoteTaskApiProjectsProjectIdTasksTaskIdPromotePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/promote',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Queue Keep Task
 *
 * Keep a place that was moved over a stated warning, and record why that is binding.
 *
 * The other button on the queue-move notice. `undo` needs no route of its own -- it
 * is an ordinary move back through the route above -- but keeping does, because what
 * it writes is not a move: it is the strong anchor, a position a person defended
 * against an objection they had read, which `reorder` may not overturn.
 *
 * Refused when the last move produced no warnings. An anchor claims "informed, and
 * kept anyway", and one written where nothing was ever said would bind `reorder` to a
 * decision nobody took.
 */
export const queueKeepTaskApiProjectsProjectIdTasksTaskIdQueueKeepPost = <ThrowOnError extends boolean = false>(options: Options<QueueKeepTaskApiProjectsProjectIdTasksTaskIdQueueKeepPostData, ThrowOnError>) => (options.client ?? client).post<QueueKeepTaskApiProjectsProjectIdTasksTaskIdQueueKeepPostResponses, QueueKeepTaskApiProjectsProjectIdTasksTaskIdQueueKeepPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/queue-keep',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Queue Move Task
 *
 * Change where a task stands in its band. The only way the order changes.
 *
 * A verb like every other one here, which is why it lives beside them rather than
 * with the read routes: attributed, retry-safe, refused against a stale read, and
 * logged. The route computes no position -- it names a placement and the manager
 * decides what number that is, because the arithmetic has to happen under the queue
 * lock and a route holds no locks.
 *
 * The move lands and is never blocked by what the check finds. Ask for
 * ``?envelope=true`` to be told what it found: ``queue_warnings`` carries the
 * findings, ``queue_undo`` the placement that puts the task back where it came from.
 * Without the envelope this answers with the bare task exactly as it always did.
 */
export const queueMoveTaskApiProjectsProjectIdTasksTaskIdQueueMovePost = <ThrowOnError extends boolean = false>(options: Options<QueueMoveTaskApiProjectsProjectIdTasksTaskIdQueueMovePostData, ThrowOnError>) => (options.client ?? client).post<QueueMoveTaskApiProjectsProjectIdTasksTaskIdQueueMovePostResponses, QueueMoveTaskApiProjectsProjectIdTasksTaskIdQueueMovePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/queue-move',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redact Task Region
 *
 * Replace one prose region with a stated redaction, recording that it happened.
 */
export const redactTaskRegionApiProjectsProjectIdTasksTaskIdRedactPost = <ThrowOnError extends boolean = false>(options: Options<RedactTaskRegionApiProjectsProjectIdTasksTaskIdRedactPostData, ThrowOnError>) => (options.client ?? client).post<RedactTaskRegionApiProjectsProjectIdTasksTaskIdRedactPostResponses, RedactTaskRegionApiProjectsProjectIdTasksTaskIdRedactPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/redact',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redirect Task
 *
 * Re-brief the agent and hand the ball back (agent/redirect).
 *
 * The instructions changed; the work done so far stands. Recorded apart from `revise`
 * because a reader who cannot tell a re-brief from a rejection has to reconstruct
 * which it was from prose -- task-222 entry 14 had to supersede entry 13 to say
 * exactly that.
 */
export const redirectTaskApiProjectsProjectIdTasksTaskIdRedirectPost = <ThrowOnError extends boolean = false>(options: Options<RedirectTaskApiProjectsProjectIdTasksTaskIdRedirectPostData, ThrowOnError>) => (options.client ?? client).post<RedirectTaskApiProjectsProjectIdTasksTaskIdRedirectPostResponses, RedirectTaskApiProjectsProjectIdTasksTaskIdRedirectPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/redirect',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reject Task
 *
 * Reject a task: closed as cancelled, archived, reason on the record.
 */
export const rejectTaskApiProjectsProjectIdTasksTaskIdRejectPost = <ThrowOnError extends boolean = false>(options: Options<RejectTaskApiProjectsProjectIdTasksTaskIdRejectPostData, ThrowOnError>) => (options.client ?? client).post<RejectTaskApiProjectsProjectIdTasksTaskIdRejectPostResponses, RejectTaskApiProjectsProjectIdTasksTaskIdRejectPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/reject',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Release Task
 *
 * Return a claimed task to the pool.
 */
export const releaseTaskApiProjectsProjectIdTasksTaskIdReleasePost = <ThrowOnError extends boolean = false>(options: Options<ReleaseTaskApiProjectsProjectIdTasksTaskIdReleasePostData, ThrowOnError>) => (options.client ?? client).post<ReleaseTaskApiProjectsProjectIdTasksTaskIdReleasePostResponses, ReleaseTaskApiProjectsProjectIdTasksTaskIdReleasePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/release',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reprioritize Task
 *
 * Change a task's band and its place in that band, in one decision.
 *
 * Not a `PATCH` of `priority`, deliberately. A band change moves a task between two
 * orderings, so it has to land somewhere in the new one -- and the generic patch has
 * no way to say where, no queue lock, and no `queue_move` entry to record the
 * decision. Sending `priority` through `PATCH /tasks/{id}` still works and still
 * rejoins the band at the bottom; this is the route that lets a caller say otherwise.
 */
export const reprioritizeTaskApiProjectsProjectIdTasksTaskIdReprioritizePost = <ThrowOnError extends boolean = false>(options: Options<ReprioritizeTaskApiProjectsProjectIdTasksTaskIdReprioritizePostData, ThrowOnError>) => (options.client ?? client).post<ReprioritizeTaskApiProjectsProjectIdTasksTaskIdReprioritizePostResponses, ReprioritizeTaskApiProjectsProjectIdTasksTaskIdReprioritizePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/reprioritize',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Request Changes
 *
 * Record requested changes and hand the ball back to the agent (agent/revise).
 *
 * The feedback is the payload of the handoff, so it rides in the ball_prompt and the
 * log entry verbatim.
 */
export const requestChangesApiProjectsProjectIdTasksTaskIdRequestChangesPost = <ThrowOnError extends boolean = false>(options: Options<RequestChangesApiProjectsProjectIdTasksTaskIdRequestChangesPostData, ThrowOnError>) => (options.client ?? client).post<RequestChangesApiProjectsProjectIdTasksTaskIdRequestChangesPostResponses, RequestChangesApiProjectsProjectIdTasksTaskIdRequestChangesPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/request-changes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resume Task
 *
 * Release a hold and put the task back to work (agent/work).
 *
 * The counterpart to /hold, and the reason a hold is not a dead end. The note is
 * optional because "carry on" is a complete instruction on its own; demanding a
 * sentence to say it would push the human back onto the send-back controls, which is
 * how one reason came to carry four intents in the first place.
 */
export const resumeTaskApiProjectsProjectIdTasksTaskIdResumePost = <ThrowOnError extends boolean = false>(options: Options<ResumeTaskApiProjectsProjectIdTasksTaskIdResumePostData, ThrowOnError>) => (options.client ?? client).post<ResumeTaskApiProjectsProjectIdTasksTaskIdResumePostResponses, ResumeTaskApiProjectsProjectIdTasksTaskIdResumePostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/tasks/{task_id}/resume',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Webhooks
 *
 * List all registered webhooks.
 */
export const listWebhooksApiProjectsProjectIdWebhooksGet = <ThrowOnError extends boolean = false>(options: Options<ListWebhooksApiProjectsProjectIdWebhooksGetData, ThrowOnError>) => (options.client ?? client).get<ListWebhooksApiProjectsProjectIdWebhooksGetResponses, ListWebhooksApiProjectsProjectIdWebhooksGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/webhooks', ...options });

/**
 * Create Webhook
 *
 * Create a new webhook.
 */
export const createWebhookApiProjectsProjectIdWebhooksPost = <ThrowOnError extends boolean = false>(options: Options<CreateWebhookApiProjectsProjectIdWebhooksPostData, ThrowOnError>) => (options.client ?? client).post<CreateWebhookApiProjectsProjectIdWebhooksPostResponses, CreateWebhookApiProjectsProjectIdWebhooksPostErrors, ThrowOnError>({
    url: '/api/projects/{project_id}/webhooks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Webhook
 *
 * Delete a webhook.
 */
export const deleteWebhookApiProjectsProjectIdWebhooksWebhookIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteWebhookApiProjectsProjectIdWebhooksWebhookIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteWebhookApiProjectsProjectIdWebhooksWebhookIdDeleteResponses, DeleteWebhookApiProjectsProjectIdWebhooksWebhookIdDeleteErrors, ThrowOnError>({ url: '/api/projects/{project_id}/webhooks/{webhook_id}', ...options });

/**
 * Get Webhook
 *
 * Get a specific webhook by ID.
 */
export const getWebhookApiProjectsProjectIdWebhooksWebhookIdGet = <ThrowOnError extends boolean = false>(options: Options<GetWebhookApiProjectsProjectIdWebhooksWebhookIdGetData, ThrowOnError>) => (options.client ?? client).get<GetWebhookApiProjectsProjectIdWebhooksWebhookIdGetResponses, GetWebhookApiProjectsProjectIdWebhooksWebhookIdGetErrors, ThrowOnError>({ url: '/api/projects/{project_id}/webhooks/{webhook_id}', ...options });

/**
 * Test Webhook
 *
 * Send a test event to a webhook.
 */
export const testWebhookApiProjectsProjectIdWebhooksWebhookIdTestPost = <ThrowOnError extends boolean = false>(options: Options<TestWebhookApiProjectsProjectIdWebhooksWebhookIdTestPostData, ThrowOnError>) => (options.client ?? client).post<TestWebhookApiProjectsProjectIdWebhooksWebhookIdTestPostResponses, TestWebhookApiProjectsProjectIdWebhooksWebhookIdTestPostErrors, ThrowOnError>({ url: '/api/projects/{project_id}/webhooks/{webhook_id}/test', ...options });

/**
 * Get Push Status
 *
 * The application-server key, and every device registered for this project.
 */
export const getPushStatusApiPushGet = <ThrowOnError extends boolean = false>(options?: Options<GetPushStatusApiPushGetData, ThrowOnError>) => (options?.client ?? client).get<GetPushStatusApiPushGetResponses, unknown, ThrowOnError>({ url: '/api/push', ...options });

/**
 * Subscribe Push Device
 *
 * Register a device, or refresh one that already exists.
 *
 * **A device registered mid-episode is not immediately pushed.** The new row inherits
 * nothing, so ``last_episode_id`` is seeded with whatever episode is currently open:
 * a person who opts in from a page that is already showing "3 waiting" has seen the
 * three, and interrupting them about it a second later would be the alert fatigue the
 * episode model exists to prevent. The *next* episode wakes them, which is the thing
 * they subscribed for; ``POST /push/test`` is how they confirm it works before then.
 *
 * Re-subscribing an endpoint that is already registered keeps its id and its episode
 * id -- see :func:`agentjobs.push.subscriptions.upsert`.
 */
export const subscribePushDeviceApiPushSubscribePost = <ThrowOnError extends boolean = false>(options: Options<SubscribePushDeviceApiPushSubscribePostData, ThrowOnError>) => (options.client ?? client).post<SubscribePushDeviceApiPushSubscribePostResponses, SubscribePushDeviceApiPushSubscribePostErrors, ThrowOnError>({
    url: '/api/push/subscribe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send Test Push
 *
 * Prove delivery to one device, or to all of them.
 *
 * The answer to "did that actually work", which nothing else gives: a device that
 * opted in while nothing was waiting would otherwise find out whether push reaches it
 * at the moment it matters least. It does not touch the episode -- a test must not be
 * able to consume the interruption a real episode is owed.
 */
export const sendTestPushApiPushTestPost = <ThrowOnError extends boolean = false>(options: Options<SendTestPushApiPushTestPostData, ThrowOnError>) => (options.client ?? client).post<SendTestPushApiPushTestPostResponses, SendTestPushApiPushTestPostErrors, ThrowOnError>({
    url: '/api/push/test',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unsubscribe Push Device
 *
 * Forget a device, by id from the page or by endpoint from a service worker.
 *
 * Idempotent, and silent about whether anything matched. A device is routinely
 * removed from both ends at once -- the button here and the browser's own permission
 * reset -- and a 404 for the second of those would be an error message about a race.
 */
export const unsubscribePushDeviceApiPushUnsubscribePost = <ThrowOnError extends boolean = false>(options: Options<UnsubscribePushDeviceApiPushUnsubscribePostData, ThrowOnError>) => (options.client ?? client).post<UnsubscribePushDeviceApiPushUnsubscribePostResponses, UnsubscribePushDeviceApiPushUnsubscribePostErrors, ThrowOnError>({
    url: '/api/push/unsubscribe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Queue
 *
 * The whole ordered backlog, band by band. This is the list a human reviews.
 *
 * Every band appears, including the empty ones: "critical is empty" is a fact a
 * reader of an ordered backlog wants stated rather than inferred from a heading that
 * is not there. Every open task carries its claimability and, when it is not
 * claimable, the rule that excluded it -- the same sentence ``/tasks/next/explain``
 * gives, from the same place, so the two can never disagree.
 *
 * **This does not 409 on a broken queue**, unlike ``/tasks/next``. It is one of the
 * two deliberate exceptions in design section 8: you have to be able to see a broken
 * queue in order to fix it, so the offending bands still render and ``problems``
 * names what is wrong beside ``repair_command``.
 *
 * Each entry also carries ``last_move`` -- the move that set its place, with the
 * actor, that actor's kind from this project's vocabulary, the reason recorded with
 * it, and whether a human kept the place over a warning. That is the anchor evidence
 * ``reorder`` reads, and it is here so a run costs one request instead of one record
 * fetch per open task.
 */
export const getQueueApiQueueGet = <ThrowOnError extends boolean = false>(options?: Options<GetQueueApiQueueGetData, ThrowOnError>) => (options?.client ?? client).get<GetQueueApiQueueGetResponses, GetQueueApiQueueGetErrors, ThrowOnError>({ url: '/api/queue', ...options });

/**
 * Compact Queue
 *
 * Renumber one band back to 100, 200, 300..., changing nobody's place.
 *
 * Purely cosmetic, and explicit only -- there is no automatic compaction, because a
 * background process quietly rewriting forty task files is exactly the kind of thing
 * that should require somebody to ask for it. One band per request for the same
 * reason: compacting the whole corpus is four decisions, not one.
 */
export const compactQueueApiQueueCompactPost = <ThrowOnError extends boolean = false>(options: Options<CompactQueueApiQueueCompactPostData, ThrowOnError>) => (options.client ?? client).post<CompactQueueApiQueueCompactPostResponses, CompactQueueApiQueueCompactPostErrors, ThrowOnError>({
    url: '/api/queue/compact',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Repair Queue
 *
 * Make a broken queue into a queue again, and say exactly what was guessed.
 *
 * Operates on a corrupt corpus by definition, so it reads the raw files rather than
 * loaded tasks -- the records it most needs are the ones the open-task-has-a-position
 * rule refuses to load. Open tasks with no usable position, and the losing claimants
 * of a shared one, go to the bottom of their band ordered by ``created`` then id.
 *
 * It never invents an opinion it does not have. A duplicate position carries no
 * record of who was meant to be first, so that tie-break is arbitrary by necessity --
 * and every task it touched comes back in ``assigned``, which is what makes the guess
 * reviewable rather than silent.
 */
export const repairQueueApiQueueRepairPost = <ThrowOnError extends boolean = false>(options: Options<RepairQueueApiQueueRepairPostData, ThrowOnError>) => (options.client ?? client).post<RepairQueueApiQueueRepairPostResponses, RepairQueueApiQueueRepairPostErrors, ThrowOnError>({
    url: '/api/queue/repair',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Recent Closures
 *
 * The newest closed tasks this caller may see, newest first.
 *
 * **Each project is asked for ``limit`` rows and the merge keeps ``limit``.** Asking
 * for fewer per project would make the answer depend on how the closures happen to be
 * distributed -- one busy project would be cut short while a quiet one contributed a
 * week-old row ahead of it.
 *
 * **A project that cannot be read contributes nothing and does not fail the request.**
 * A store mid-migration, or one whose database has been moved, is a deployment fact;
 * taking the Dashboard down over it would be a worse answer than a list that is one
 * project short.
 */
export const listRecentClosuresApiRecentClosuresGet = <ThrowOnError extends boolean = false>(options?: Options<ListRecentClosuresApiRecentClosuresGetData, ThrowOnError>) => (options?.client ?? client).get<ListRecentClosuresApiRecentClosuresGetResponses, ListRecentClosuresApiRecentClosuresGetErrors, ThrowOnError>({ url: '/api/recent/closures', ...options });

/**
 * Get Project Revision
 *
 * Answer whether this project changed, without loading the task collection.
 */
export const getProjectRevisionApiRevisionGet = <ThrowOnError extends boolean = false>(options?: Options<GetProjectRevisionApiRevisionGetData, ThrowOnError>) => (options?.client ?? client).get<GetProjectRevisionApiRevisionGetResponses, unknown, ThrowOnError>({ url: '/api/revision', ...options });

/**
 * Get Emergency Stop
 *
 * Whether dispatch is stopped machine-wide. Read by the header on every page.
 */
export const getEmergencyStopApiRunsEmergencyStopGet = <ThrowOnError extends boolean = false>(options?: Options<GetEmergencyStopApiRunsEmergencyStopGetData, ThrowOnError>) => (options?.client ?? client).get<GetEmergencyStopApiRunsEmergencyStopGetResponses, unknown, ThrowOnError>({ url: '/api/runs/emergency-stop', ...options });

/**
 * Press Emergency Stop
 *
 * Refuse every new dispatch, then stop everything AgentJobs started. Takes nothing.
 *
 * The browser's twin of ``agentjobs dispatch stop``, and the same call underneath. No
 * body and no questions, for the reason ``disable_dispatch`` gives: a kill switch you
 * cannot reach is not one. It works whether or not dispatch is configured, because it
 * reads no configuration at all.
 *
 * A plain ``def`` so Starlette runs it on a worker thread: stopping a batch run may
 * wait out its grace period, and the event loop serving every other page must not.
 */
export const pressEmergencyStopApiRunsEmergencyStopPost = <ThrowOnError extends boolean = false>(options?: Options<PressEmergencyStopApiRunsEmergencyStopPostData, ThrowOnError>) => (options?.client ?? client).post<PressEmergencyStopApiRunsEmergencyStopPostResponses, unknown, ThrowOnError>({ url: '/api/runs/emergency-stop', ...options });

/**
 * Resume After Emergency Stop
 *
 * Lift the kill switch. Starts nothing: what the stop ended stays ended.
 *
 * Walks, pull armings and queued dispatches were ended by the stop rather than paused,
 * so resuming only means the next dispatch a person asks for is allowed again.
 */
export const resumeAfterEmergencyStopApiRunsEmergencyStopResumePost = <ThrowOnError extends boolean = false>(options?: Options<ResumeAfterEmergencyStopApiRunsEmergencyStopResumePostData, ThrowOnError>) => (options?.client ?? client).post<ResumeAfterEmergencyStopApiRunsEmergencyStopResumePostResponses, unknown, ThrowOnError>({ url: '/api/runs/emergency-stop/resume', ...options });

/**
 * List Live Runs
 *
 * Every run this caller may see, with what is left of the machine's capacity.
 *
 * One request answers both surfaces task-328 ships -- the Runs tab and the Dashboard's
 * capacity row -- which is why the ceiling and the occupied count are in the body
 * rather than left to a second call.
 *
 * **``occupied`` counts every run, including the ones not listed** (task-333). The two
 * numbers answer different questions and only one of them is about exposure: the rows
 * are "what may I read", while the capacity is "why can I not dispatch", and a machine
 * that is full because of a hidden project's run is still full. Subtracting the hidden
 * rows from the count would make this surface disagree with ``dispatch/guards.py``
 * about whether there is a slot, which is the one thing its docstring says it must
 * never do.
 *
 * **What the live-finish lookup adds (task-533), measured 2026-09-24** with
 * ``machine_live_finishes`` against a home of 311 finish directories, median of 15:
 * 0.09 ms with no lock held; 33 ms with three runs and no finish (the one shared scan
 * confirming none of them is merging); 65 ms with one of them finishing; 67 ms with six
 * runs and one finishing. The live home the same day -- three run locks, 311
 * directories, nothing finishing -- measured 68 ms. The cost follows live locks and
 * live finishes, never tasks or rows, and each live finish adds one store read for
 * whether its task is still open.
 */
export const listLiveRunsApiRunsLiveGet = <ThrowOnError extends boolean = false>(options?: Options<ListLiveRunsApiRunsLiveGetData, ThrowOnError>) => (options?.client ?? client).get<ListLiveRunsApiRunsLiveGetResponses, unknown, ThrowOnError>({ url: '/api/runs/live', ...options });

/**
 * Search Tasks
 *
 * Search tasks using a case-insensitive substring query.
 *
 * Rows, in the same shape and for the same reason ``GET /tasks`` returns rows: a
 * result list draws a title, a badge and a place in line. It answered with whole
 * ``TaskRead`` records until task-495 -- every matched task's spec prose, acceptance
 * criteria and complete log -- which was 10,781 bytes per record and 5.2 MB for 480
 * matches. Whole records are at ``GET /search/full``.
 *
 * Rows carry the same computed dependency facts as ``GET /tasks`` -- ``actionable``,
 * ``unmet_needs``, ``open_children_count`` -- so a result can be acted on without a
 * second request per row.
 */
export const searchTasksApiSearchGet = <ThrowOnError extends boolean = false>(options: Options<SearchTasksApiSearchGetData, ThrowOnError>) => (options.client ?? client).get<SearchTasksApiSearchGetResponses, SearchTasksApiSearchGetErrors, ThrowOnError>({ url: '/api/search', ...options });

/**
 * Search Tasks Full
 *
 * The same hits, in the same order, as whole records.
 *
 * Named so the expensive shape is the one a caller asks for by name -- task-484's
 * decision, applied to the second endpoint that had it by default.
 * ``TaskClient.search_tasks`` is the caller that needs it: it parses each item into a
 * ``Task``, and a row would parse with an empty log, which is a wrong answer rather
 * than an error.
 */
export const searchTasksFullApiSearchFullGet = <ThrowOnError extends boolean = false>(options: Options<SearchTasksFullApiSearchFullGetData, ThrowOnError>) => (options.client ?? client).get<SearchTasksFullApiSearchFullGetResponses, SearchTasksFullApiSearchFullGetErrors, ThrowOnError>({ url: '/api/search/full', ...options });

/**
 * List Idle Sessions
 *
 * Every Claude Code process on this machine, what the sweep makes of it, and its record.
 */
export const listIdleSessionsApiSessionsIdleGet = <ThrowOnError extends boolean = false>(options?: Options<ListIdleSessionsApiSessionsIdleGetData, ThrowOnError>) => (options?.client ?? client).get<ListIdleSessionsApiSessionsIdleGetResponses, unknown, ThrowOnError>({ url: '/api/sessions/idle', ...options });

/**
 * Update Idle Session Settings
 *
 * Switch enforcement on or off, or change the threshold. Stops nothing by itself.
 */
export const updateIdleSessionSettingsApiSessionsIdleSettingsPut = <ThrowOnError extends boolean = false>(options: Options<UpdateIdleSessionSettingsApiSessionsIdleSettingsPutData, ThrowOnError>) => (options.client ?? client).put<UpdateIdleSessionSettingsApiSessionsIdleSettingsPutResponses, UpdateIdleSessionSettingsApiSessionsIdleSettingsPutErrors, ThrowOnError>({
    url: '/api/sessions/idle/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Tasks
 *
 * List tasks filtered along the state axes, as listing rows.
 *
 * ``?ball=human`` is the human inbox: everything waiting on a person, each row
 * carrying its ``ball_prompt``. ``?ball=external`` is the blocked list.
 * ``?parent=task-063-schema-v2`` is one umbrella's children.
 *
 * **A row, not a record.** This used to answer with complete tasks -- spec prose,
 * acceptance criteria and the entire log of each -- which on the agentjobs backlog was
 * 10.4 MB and four full corpus loads to draw a column of titles, and grew with every
 * log entry appended to any task (task-484). It now answers with the fields a listing
 * reads. Open a task and ``/{task_id}/detail`` carries the rest, which is the fetch
 * every surface that shows a task already makes.
 *
 * Whole records are at ``/tasks/full``, unchanged. Narrowing *this* route rather than
 * adding a narrow sibling is deliberate: the expensive shape should be the one a caller
 * asks for by name, so a new surface that never thought about it gets the cheap one.
 */
export const listTasksApiTasksGet = <ThrowOnError extends boolean = false>(options?: Options<ListTasksApiTasksGetData, ThrowOnError>) => (options?.client ?? client).get<ListTasksApiTasksGetResponses, ListTasksApiTasksGetErrors, ThrowOnError>({ url: '/api/tasks', ...options });

/**
 * Create Task
 *
 * Create a new task record.
 *
 * With an operation_id the create runs under the project-wide creation lock and a
 * retry resolves to the task the first attempt made, rather than producing a second
 * one with a different generated id.
 */
export const createTaskApiTasksPost = <ThrowOnError extends boolean = false>(options: Options<CreateTaskApiTasksPostData, ThrowOnError>) => (options.client ?? client).post<CreateTaskApiTasksPostResponses, CreateTaskApiTasksPostErrors, ThrowOnError>({
    url: '/api/tasks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Broken Tasks
 *
 * Files in the task directory that exist but cannot be loaded.
 *
 * Declared before /{task_id} so "broken" is not captured as a task id. These used to
 * be invisible: storage returned None for them and every listing simply omitted the
 * task. An unmigrated v1 file shows up here by filename.
 */
export const listBrokenTasksApiTasksBrokenGet = <ThrowOnError extends boolean = false>(options?: Options<ListBrokenTasksApiTasksBrokenGetData, ThrowOnError>) => (options?.client ?? client).get<ListBrokenTasksApiTasksBrokenGetResponses, unknown, ThrowOnError>({ url: '/api/tasks/broken', ...options });

/**
 * Get Claimable Tasks
 *
 * Every task that may be worked now, in the queue's order. ``/next`` is its head.
 *
 * The epic walk needs the *set* rather than the winner: it starts every eligible child
 * up to its slot count, and asking for the winner repeatedly would either re-offer the
 * child it just claimed or report the epic as deadlocked. It is here because the CLI
 * is a service client now that only the server opens the store, and the walk must not
 * be the one caller that re-implements the claimability filter -- that is exactly how
 * the walk and the dashboard would come to disagree about what is next, with the walk
 * winning silently because it is the one that spends money.
 *
 * Declared before ``/{task_id}`` for the same reason ``/next`` is: otherwise
 * "claimable" is captured as a task id.
 */
export const getClaimableTasksApiTasksClaimableGet = <ThrowOnError extends boolean = false>(options?: Options<GetClaimableTasksApiTasksClaimableGetData, ThrowOnError>) => (options?.client ?? client).get<GetClaimableTasksApiTasksClaimableGetResponses, GetClaimableTasksApiTasksClaimableGetErrors, ThrowOnError>({ url: '/api/tasks/claimable', ...options });

/**
 * List Full Tasks
 *
 * Every matching task as a complete record -- what ``GET /tasks`` used to return.
 *
 * Kept because two callers genuinely need whole records for a listing, and neither is
 * a browser: ``TaskClient.list_tasks`` is the surface the CLI's ``TaskManager``
 * equivalent is built on, so it must return ``Task`` and not a shape missing a log;
 * and ``agentjobs branches`` reads ``branches[]`` off every task, which no listing
 * projection carries.
 *
 * Expensive, and named so a caller has to choose it. Declared before ``/{task_id}``
 * for the reason ``/next`` and ``/broken`` are: otherwise "full" is captured as a task
 * id.
 */
export const listFullTasksApiTasksFullGet = <ThrowOnError extends boolean = false>(options?: Options<ListFullTasksApiTasksFullGetData, ThrowOnError>) => (options?.client ?? client).get<ListFullTasksApiTasksFullGetResponses, ListFullTasksApiTasksFullGetErrors, ThrowOnError>({ url: '/api/tasks/full', ...options });

/**
 * Get Next Task
 *
 * The next claimable task: ready, eligible for the agent, no unmet needs.
 *
 * Answers 409 rather than guessing when the queue it would have to read is broken.
 */
export const getNextTaskApiTasksNextGet = <ThrowOnError extends boolean = false>(options?: Options<GetNextTaskApiTasksNextGetData, ThrowOnError>) => (options?.client ?? client).get<GetNextTaskApiTasksNextGetResponses, GetNextTaskApiTasksNextGetErrors, ThrowOnError>({ url: '/api/tasks/next', ...options });

/**
 * Explain Next Task
 *
 * Why this task is next, and every open task it stands in front of.
 *
 * Declared before ``/{task_id}`` so "next" is not captured as a task id, exactly as
 * ``/next`` and ``/broken`` are. The body is design section 9's structure, produced
 * by the manager rather than assembled here: the route is a transcription.
 */
export const explainNextTaskApiTasksNextExplainGet = <ThrowOnError extends boolean = false>(options?: Options<ExplainNextTaskApiTasksNextExplainGetData, ThrowOnError>) => (options?.client ?? client).get<ExplainNextTaskApiTasksNextExplainGetResponses, ExplainNextTaskApiTasksNextExplainGetErrors, ThrowOnError>({ url: '/api/tasks/next/explain', ...options });

/**
 * Archive Task
 *
 * Archive a task. An open task is closed as cancelled first.
 */
export const archiveTaskApiTasksTaskIdDelete = <ThrowOnError extends boolean = false>(options: Options<ArchiveTaskApiTasksTaskIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<ArchiveTaskApiTasksTaskIdDeleteResponses, ArchiveTaskApiTasksTaskIdDeleteErrors, ThrowOnError>({ url: '/api/tasks/{task_id}', ...options });

/**
 * Get Task
 *
 * Retrieve a specific task by identifier.
 */
export const getTaskApiTasksTaskIdGet = <ThrowOnError extends boolean = false>(options: Options<GetTaskApiTasksTaskIdGetData, ThrowOnError>) => (options.client ?? client).get<GetTaskApiTasksTaskIdGetResponses, GetTaskApiTasksTaskIdGetErrors, ThrowOnError>({ url: '/api/tasks/{task_id}', ...options });

/**
 * Update Task
 *
 * Apply a partial update to a task. State axes move through the verbs, not here.
 *
 * The ``actor`` is validated whenever one is sent, exactly as ``create_task``
 * validates its own (D2): this route writes an entry into an append-only log, and an
 * attribution nobody can resolve later is worse than a refused request. It was the one
 * submitted actor in this module that went to the manager unchecked, so a browser edit
 * could name anybody and a typo became permanent.
 */
export const updateTaskApiTasksTaskIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateTaskApiTasksTaskIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateTaskApiTasksTaskIdPatchResponses, UpdateTaskApiTasksTaskIdPatchErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Answer Task
 *
 * Supply what the agent was waiting for and hand the ball back (agent/answer).
 *
 * Not a revision: nothing the agent did was wrong, and a record that says otherwise
 * makes the next reader reconstruct which it was. The answer rides in the ball_prompt
 * and the log verbatim, exactly as requested changes do.
 *
 * Since task-017 it may also carry ``answers``, each naming the question entry it
 * answers. Those become ``answer`` entries threaded by ``re``, written in the same
 * mutation as the handoff, and the ball_prompt is composed from them when the human
 * tapped options and typed nothing -- which is the whole point of the feature, and
 * would otherwise leave the agent a handoff whose ask is blank.
 */
export const answerTaskApiTasksTaskIdAnswerPost = <ThrowOnError extends boolean = false>(options: Options<AnswerTaskApiTasksTaskIdAnswerPostData, ThrowOnError>) => (options.client ?? client).post<AnswerTaskApiTasksTaskIdAnswerPostResponses, AnswerTaskApiTasksTaskIdAnswerPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/answer',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Approve Task
 *
 * Record human approval and hand the ball back to the agent (agent/work).
 *
 * Nothing here merges anything. The GUI cannot run git, and a button that implied
 * otherwise would lie. It records the approval in the task record -- which is the
 * point, since approvals used to live only in chat -- and moves the ball: the agent
 * must now rebase, merge --no-ff, mark the branch merged, and close the task
 * (ENGINEERING.md, "The Merge Gate").
 *
 * ``note`` is optional and strictly additive (task-228). With none, this writes byte
 * for byte what it wrote before the field existed. With one, the note rides *after*
 * the merge clearance, verbatim and unsummarised, followed by a sentence saying it is
 * context to carry into the merge -- because an agent that reads an approval note as
 * a fresh review round has inverted the point of attaching one. Before this, an
 * approval carrying a sentence had to go through Request Changes: a round trip the
 * human did not ask for, and a record that said `revise` about work that was approved.
 */
export const approveTaskApiTasksTaskIdApprovePost = <ThrowOnError extends boolean = false>(options: Options<ApproveTaskApiTasksTaskIdApprovePostData, ThrowOnError>) => (options.client ?? client).post<ApproveTaskApiTasksTaskIdApprovePostResponses, ApproveTaskApiTasksTaskIdApprovePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/approve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Attachment
 *
 * Serve one image a log entry references.
 *
 * Resolved through the task's own record rather than straight off the filesystem, so
 * the only files this can ever return are ones an entry actually points at. That also
 * supplies the recorded hash, which is checked before the bytes are handed back: a
 * file edited or corrupted since it was stored is refused rather than rendered.
 */
export const getAttachmentApiTasksTaskIdAttachmentsFilenameGet = <ThrowOnError extends boolean = false>(options: Options<GetAttachmentApiTasksTaskIdAttachmentsFilenameGetData, ThrowOnError>) => (options.client ?? client).get<GetAttachmentApiTasksTaskIdAttachmentsFilenameGetResponses, GetAttachmentApiTasksTaskIdAttachmentsFilenameGetErrors, ThrowOnError>({ url: '/api/tasks/{task_id}/attachments/{filename}', ...options });

/**
 * Relay Authorization
 *
 * Record that a human authorised a dispatch, as the agent they told (task-506).
 *
 * The third shape of an authorising entry, and the only one whose author and subject
 * are different people. A person clicking Dispatch gets an entry under their own name.
 * A person writing a note by hand gets the same. An agent told in a chat session to
 * start the task it has just filed had neither, and the workaround was to write the
 * note as *them* -- a signature in an append-only log that its owner did not put
 * there, indistinguishable by any reader from a click. This writes an entry that says
 * what actually happened: ``actor`` is the agent, ``authorized_by`` is the person,
 * ``body`` is what the agent was asked for in its own words.
 *
 * **It is a separate route because it is a separate capability.**
 * ``dispatch.relay_authorization`` is held by the two human kinds and by no ``run``,
 * and the route table is keyed by endpoint -- so putting this on ``POST /log``, which
 * every run may call, would have made the gate a condition inside a handler instead of
 * a row in the table ``tests/test_authorization.py`` checks for completeness.
 *
 * **It starts nothing.** It is not an approval, it releases no merge gate, it arms
 * nothing, and it spends no run slot. A dispatch afterwards is an ordinary ``manual``
 * dispatch, judged by every gate in ``dispatch/guards.py`` and counted against every
 * cap in ``dispatch/budget.py``. Nor is the entry consumed: it clocks exactly as many
 * runs as a human's own note would, which is to say as many as those caps allow.
 *
 * **What it is not is proof.** The server cannot tell whether the person said anything,
 * just as it cannot verify the ``user`` field on a dispatch (design section 2's P1-2).
 * What it buys is a record that is true about who typed it, and a write a dispatched
 * run is refused -- see ``docs/authorization.md``.
 */
export const relayAuthorizationApiTasksTaskIdAuthorizationPost = <ThrowOnError extends boolean = false>(options: Options<RelayAuthorizationApiTasksTaskIdAuthorizationPostData, ThrowOnError>) => (options.client ?? client).post<RelayAuthorizationApiTasksTaskIdAuthorizationPostResponses, RelayAuthorizationApiTasksTaskIdAuthorizationPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/authorization',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Authorize Task Chain
 *
 * Authorise a bounded chain of dispatches against this task's checks (task-150).
 *
 * **Classified under ``dispatch.start``, which no run holds**, and the alternative was
 * argued rather than assumed. A capability of its own -- ``CHAIN_AUTHORIZE`` -- would
 * draw exactly the same line for exactly the same principals, and this module's
 * coarseness is deliberate: *a capability per route would be a role system with extra
 * steps*. What a chain authorisation buys is up to twenty dispatches against one task
 * with nobody clicking again, which is the act ``dispatch.start`` already names, bought
 * in advance. ``task.review`` was the other candidate and is wrong for the opposite
 * reason: this is not a judgement about work somebody did, it is a purchase.
 *
 * The digest, the chain id and the covered criteria are computed here from the **stored
 * task**. There is no request field for any of them, so there is nothing a caller could
 * supply that would move the definition of done the chain converges on.
 *
 * ``user`` is validated the way a dispatch's is: the id must be one this project
 * configures with ``kind: human``. An agent may not authorise a chain any more than it
 * may authorise a dispatch, and this is the same refusal reached through the same
 * function.
 */
export const authorizeTaskChainApiTasksTaskIdChainPost = <ThrowOnError extends boolean = false>(options: Options<AuthorizeTaskChainApiTasksTaskIdChainPostData, ThrowOnError>) => (options.client ?? client).post<AuthorizeTaskChainApiTasksTaskIdChainPostResponses, AuthorizeTaskChainApiTasksTaskIdChainPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/chain',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke Task Chain
 *
 * Stop a chain before its next iteration. One click, no arguments required.
 *
 * Shares ``dispatch.start`` with the route above for the reason a kill switch always
 * shares a capability with its switch: everyone who may start it may stop it. The
 * asymmetric alternative -- anyone may stop, only some may start -- reads well and is
 * wrong here, because the principal it would newly admit is a dispatched run, and a run
 * able to revoke could stop a chain a person is relying on and then report that it
 * converged.
 *
 * **This does not stop a run that is already executing.** An iteration is a dispatch and
 * a dispatch is somebody's session; ``POST .../dispatch/runs/{id}/cancel`` ends one of
 * those. What this guarantees is that no further iteration begins.
 */
export const revokeTaskChainApiTasksTaskIdChainRevokePost = <ThrowOnError extends boolean = false>(options: Options<RevokeTaskChainApiTasksTaskIdChainRevokePostData, ThrowOnError>) => (options.client ?? client).post<RevokeTaskChainApiTasksTaskIdChainRevokePostResponses, RevokeTaskChainApiTasksTaskIdChainRevokePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/chain/revoke',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read Task Chains
 *
 * Every chain this task has had, oldest first, each with its iteration history.
 *
 * A read, and deliberately absent from ``ROUTE_CAPABILITIES``: it executes nothing and
 * answers with what the log already holds. What a caller may *do* with a chain is gated
 * at the two verbs below.
 */
export const readTaskChainsApiTasksTaskIdChainsGet = <ThrowOnError extends boolean = false>(options: Options<ReadTaskChainsApiTasksTaskIdChainsGetData, ThrowOnError>) => (options.client ?? client).get<ReadTaskChainsApiTasksTaskIdChainsGetResponses, ReadTaskChainsApiTasksTaskIdChainsGetErrors, ThrowOnError>({ url: '/api/tasks/{task_id}/chains', ...options });

/**
 * Check Task Acceptance
 *
 * Run this task's executable acceptance checks and answer with what each one did.
 *
 * The same vector ``agentjobs check`` prints, written to the record the same way: the
 * criteria's statuses move and exactly one ``check_result`` entry records the pass.
 *
 * **A POST because it executes commands**, not because it writes -- and that is also
 * why it is classified under ``dispatch.start`` rather than under the ``task.*``
 * capabilities a run already holds. A run may edit its own task, so a run able to call
 * this could write a check and then have this machine run it, which is starting a
 * process of its own choosing on somebody else's hardware. That is the act the dispatch
 * gates bound, and this goes through the same gate: ``assert_dispatch_permitted`` runs
 * before any process starts, and its refusals render under their own reason codes.
 *
 * Refused with ``no_checks`` on a task whose criteria are all prose. Answering an empty
 * success would be indistinguishable, to any client, from every check having passed.
 */
export const checkTaskAcceptanceApiTasksTaskIdCheckPost = <ThrowOnError extends boolean = false>(options: Options<CheckTaskAcceptanceApiTasksTaskIdCheckPostData, ThrowOnError>) => (options.client ?? client).post<CheckTaskAcceptanceApiTasksTaskIdCheckPostResponses, CheckTaskAcceptanceApiTasksTaskIdCheckPostErrors, ThrowOnError>({ url: '/api/tasks/{task_id}/check', ...options });

/**
 * Claim Task
 *
 * Claim a ready task: one winner, everyone else gets a 409.
 *
 * A claim that names its session also writes an interactive run record (task-354), so
 * the work shows as running on every surface that reads the ledger. Only after the
 * claim landed, only for an agent actor, and never a second one for a task that has a
 * live run -- which is what makes a replayed claim harmless.
 */
export const claimTaskApiTasksTaskIdClaimPost = <ThrowOnError extends boolean = false>(options: Options<ClaimTaskApiTasksTaskIdClaimPostData, ThrowOnError>) => (options.client ?? client).post<ClaimTaskApiTasksTaskIdClaimPostResponses, ClaimTaskApiTasksTaskIdClaimPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/claim',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Close Task
 *
 * End the task with an outcome.
 */
export const closeTaskApiTasksTaskIdClosePost = <ThrowOnError extends boolean = false>(options: Options<CloseTaskApiTasksTaskIdClosePostData, ThrowOnError>) => (options.client ?? client).post<CloseTaskApiTasksTaskIdClosePostResponses, CloseTaskApiTasksTaskIdClosePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/close',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Mark Deliverable
 *
 * Mark a deliverable as done for the task.
 */
export const markDeliverableApiTasksTaskIdDeliverablesDeliverablePathPatch = <ThrowOnError extends boolean = false>(options: Options<MarkDeliverableApiTasksTaskIdDeliverablesDeliverablePathPatchData, ThrowOnError>) => (options.client ?? client).patch<MarkDeliverableApiTasksTaskIdDeliverablesDeliverablePathPatchResponses, MarkDeliverableApiTasksTaskIdDeliverablesDeliverablePathPatchErrors, ThrowOnError>({ url: '/api/tasks/{task_id}/deliverables/{deliverable_path}', ...options });

/**
 * Get Task Detail
 *
 * Return the complete resumption and review contract for one task.
 */
export const getTaskDetailApiTasksTaskIdDetailGet = <ThrowOnError extends boolean = false>(options: Options<GetTaskDetailApiTasksTaskIdDetailGetData, ThrowOnError>) => (options.client ?? client).get<GetTaskDetailApiTasksTaskIdDetailGetResponses, GetTaskDetailApiTasksTaskIdDetailGetErrors, ThrowOnError>({ url: '/api/tasks/{task_id}/detail', ...options });

/**
 * Dispatch Task Endpoint
 *
 * Start an agent on this task.
 *
 * Deliberately **not** merged into any approval endpoint. Approving means "I agree";
 * dispatching means "spend money now", and collapsing the two would turn every
 * approval into an implicit purchase (design decision D1).
 *
 * 202 rather than 200: the run has started, and how it ends arrives later as
 * ``dispatch_result`` entries on the task, not in this response.
 *
 * The address handed to the agent is this server's own, taken from the socket the
 * request arrived on. Until 2026-08-19 nothing was passed and the runner's default
 * won, so a dashboard on any other port dispatched agents at ``:8765`` -- an address
 * that, on the machine this was built for, is deliberately dead (task-154).
 *
 * **``user`` makes this one click (task-188).** Supplied, the guard layer writes that
 * human's authorising entry onto the task and dispatches on it, so a person no longer
 * has to know to write a note by hand before every run. It is validated as a
 * configured human and it is not the dispatch's ``actor``; see
 * :class:`DispatchRequestBody` for why those are different things.
 *
 * **Omitted, this endpoint behaves exactly as it did before.** It does *not* quietly
 * substitute the project's ``default_user``: a run has to be signed for by whoever
 * asked for it, and a server-side default would produce an entry that looks like a
 * person's authorisation and is really just a config value. So a caller with no
 * signed-in user falls back to the pre-existing rule -- the newest stored entry must
 * be a human's -- and is refused with ``not_human_clocked`` if it is not. That is the
 * same answer the CLI gets, and the React app disables the button and says so rather
 * than letting someone press it into a refusal.
 */
export const dispatchTaskEndpointApiTasksTaskIdDispatchPost = <ThrowOnError extends boolean = false>(options: Options<DispatchTaskEndpointApiTasksTaskIdDispatchPostData, ThrowOnError>) => (options.client ?? client).post<DispatchTaskEndpointApiTasksTaskIdDispatchPostResponses, DispatchTaskEndpointApiTasksTaskIdDispatchPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/dispatch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Request Finish Retry
 *
 * Retry a stopped scripted finish on the approval already given (task-575).
 *
 * The repairing agent calls this instead of running ``agentjobs finish`` from its own
 * shell, which the auto-mode classifier rightly refuses as a merge nobody reviewed.
 * The server judges the repair against the head the approver saw: when nothing they
 * reviewed changed, it starts the finish itself in a detached process; otherwise it
 * hands the task to human/review with each offending path stated. See
 * ``dispatch/finish_retry.py``.
 *
 * ``task.verb`` rather than ``task.review``, because a run must be able to ask. What
 * keeps the approval meaningful is the judgement and the bound, not who may call.
 */
export const requestFinishRetryApiTasksTaskIdFinishRetryPost = <ThrowOnError extends boolean = false>(options: Options<RequestFinishRetryApiTasksTaskIdFinishRetryPostData, ThrowOnError>) => (options.client ?? client).post<RequestFinishRetryApiTasksTaskIdFinishRetryPostResponses, RequestFinishRetryApiTasksTaskIdFinishRetryPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/finish-retry',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Handoff Task
 *
 * Move the ball, with its ask.
 */
export const handoffTaskApiTasksTaskIdHandoffPost = <ThrowOnError extends boolean = false>(options: Options<HandoffTaskApiTasksTaskIdHandoffPostData, ThrowOnError>) => (options.client ?? client).post<HandoffTaskApiTasksTaskIdHandoffPostResponses, HandoffTaskApiTasksTaskIdHandoffPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/handoff',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Hold Task
 *
 * Stop the task, with the release condition on the record (agent/hold).
 *
 * The condition is prefixed rather than left bare, because this is the one send-back
 * whose prompt must not read as work: an agent that skims it and carries on has done
 * the opposite of what it was told. `hold` is also the one agent-side reason no
 * dispatch path will act on, so a held task cannot be started by clicking Dispatch
 * beside the Hold button that stopped it.
 */
export const holdTaskApiTasksTaskIdHoldPost = <ThrowOnError extends boolean = false>(options: Options<HoldTaskApiTasksTaskIdHoldPostData, ThrowOnError>) => (options.client ?? client).post<HoldTaskApiTasksTaskIdHoldPostResponses, HoldTaskApiTasksTaskIdHoldPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/hold',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Append Log Entry
 *
 * Append a note/progress/decision/question/answer/instruction entry.
 */
export const appendLogEntryApiTasksTaskIdLogPost = <ThrowOnError extends boolean = false>(options: Options<AppendLogEntryApiTasksTaskIdLogPostData, ThrowOnError>) => (options.client ?? client).post<AppendLogEntryApiTasksTaskIdLogPostResponses, AppendLogEntryApiTasksTaskIdLogPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/log',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Post Progress Update
 *
 * Append a progress update entry for the task.
 */
export const postProgressUpdateApiTasksTaskIdProgressPost = <ThrowOnError extends boolean = false>(options: Options<PostProgressUpdateApiTasksTaskIdProgressPostData, ThrowOnError>) => (options.client ?? client).post<PostProgressUpdateApiTasksTaskIdProgressPostResponses, PostProgressUpdateApiTasksTaskIdProgressPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/progress',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Promote Task
 *
 * Declare a draft's spec finished: it becomes ready and claimable.
 */
export const promoteTaskApiTasksTaskIdPromotePost = <ThrowOnError extends boolean = false>(options: Options<PromoteTaskApiTasksTaskIdPromotePostData, ThrowOnError>) => (options.client ?? client).post<PromoteTaskApiTasksTaskIdPromotePostResponses, PromoteTaskApiTasksTaskIdPromotePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/promote',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Queue Keep Task
 *
 * Keep a place that was moved over a stated warning, and record why that is binding.
 *
 * The other button on the queue-move notice. `undo` needs no route of its own -- it
 * is an ordinary move back through the route above -- but keeping does, because what
 * it writes is not a move: it is the strong anchor, a position a person defended
 * against an objection they had read, which `reorder` may not overturn.
 *
 * Refused when the last move produced no warnings. An anchor claims "informed, and
 * kept anyway", and one written where nothing was ever said would bind `reorder` to a
 * decision nobody took.
 */
export const queueKeepTaskApiTasksTaskIdQueueKeepPost = <ThrowOnError extends boolean = false>(options: Options<QueueKeepTaskApiTasksTaskIdQueueKeepPostData, ThrowOnError>) => (options.client ?? client).post<QueueKeepTaskApiTasksTaskIdQueueKeepPostResponses, QueueKeepTaskApiTasksTaskIdQueueKeepPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/queue-keep',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Queue Move Task
 *
 * Change where a task stands in its band. The only way the order changes.
 *
 * A verb like every other one here, which is why it lives beside them rather than
 * with the read routes: attributed, retry-safe, refused against a stale read, and
 * logged. The route computes no position -- it names a placement and the manager
 * decides what number that is, because the arithmetic has to happen under the queue
 * lock and a route holds no locks.
 *
 * The move lands and is never blocked by what the check finds. Ask for
 * ``?envelope=true`` to be told what it found: ``queue_warnings`` carries the
 * findings, ``queue_undo`` the placement that puts the task back where it came from.
 * Without the envelope this answers with the bare task exactly as it always did.
 */
export const queueMoveTaskApiTasksTaskIdQueueMovePost = <ThrowOnError extends boolean = false>(options: Options<QueueMoveTaskApiTasksTaskIdQueueMovePostData, ThrowOnError>) => (options.client ?? client).post<QueueMoveTaskApiTasksTaskIdQueueMovePostResponses, QueueMoveTaskApiTasksTaskIdQueueMovePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/queue-move',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redact Task Region
 *
 * Replace one prose region with a stated redaction, recording that it happened.
 */
export const redactTaskRegionApiTasksTaskIdRedactPost = <ThrowOnError extends boolean = false>(options: Options<RedactTaskRegionApiTasksTaskIdRedactPostData, ThrowOnError>) => (options.client ?? client).post<RedactTaskRegionApiTasksTaskIdRedactPostResponses, RedactTaskRegionApiTasksTaskIdRedactPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/redact',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redirect Task
 *
 * Re-brief the agent and hand the ball back (agent/redirect).
 *
 * The instructions changed; the work done so far stands. Recorded apart from `revise`
 * because a reader who cannot tell a re-brief from a rejection has to reconstruct
 * which it was from prose -- task-222 entry 14 had to supersede entry 13 to say
 * exactly that.
 */
export const redirectTaskApiTasksTaskIdRedirectPost = <ThrowOnError extends boolean = false>(options: Options<RedirectTaskApiTasksTaskIdRedirectPostData, ThrowOnError>) => (options.client ?? client).post<RedirectTaskApiTasksTaskIdRedirectPostResponses, RedirectTaskApiTasksTaskIdRedirectPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/redirect',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reject Task
 *
 * Reject a task: closed as cancelled, archived, reason on the record.
 */
export const rejectTaskApiTasksTaskIdRejectPost = <ThrowOnError extends boolean = false>(options: Options<RejectTaskApiTasksTaskIdRejectPostData, ThrowOnError>) => (options.client ?? client).post<RejectTaskApiTasksTaskIdRejectPostResponses, RejectTaskApiTasksTaskIdRejectPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/reject',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Release Task
 *
 * Return a claimed task to the pool.
 */
export const releaseTaskApiTasksTaskIdReleasePost = <ThrowOnError extends boolean = false>(options: Options<ReleaseTaskApiTasksTaskIdReleasePostData, ThrowOnError>) => (options.client ?? client).post<ReleaseTaskApiTasksTaskIdReleasePostResponses, ReleaseTaskApiTasksTaskIdReleasePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/release',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reprioritize Task
 *
 * Change a task's band and its place in that band, in one decision.
 *
 * Not a `PATCH` of `priority`, deliberately. A band change moves a task between two
 * orderings, so it has to land somewhere in the new one -- and the generic patch has
 * no way to say where, no queue lock, and no `queue_move` entry to record the
 * decision. Sending `priority` through `PATCH /tasks/{id}` still works and still
 * rejoins the band at the bottom; this is the route that lets a caller say otherwise.
 */
export const reprioritizeTaskApiTasksTaskIdReprioritizePost = <ThrowOnError extends boolean = false>(options: Options<ReprioritizeTaskApiTasksTaskIdReprioritizePostData, ThrowOnError>) => (options.client ?? client).post<ReprioritizeTaskApiTasksTaskIdReprioritizePostResponses, ReprioritizeTaskApiTasksTaskIdReprioritizePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/reprioritize',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Request Changes
 *
 * Record requested changes and hand the ball back to the agent (agent/revise).
 *
 * The feedback is the payload of the handoff, so it rides in the ball_prompt and the
 * log entry verbatim.
 */
export const requestChangesApiTasksTaskIdRequestChangesPost = <ThrowOnError extends boolean = false>(options: Options<RequestChangesApiTasksTaskIdRequestChangesPostData, ThrowOnError>) => (options.client ?? client).post<RequestChangesApiTasksTaskIdRequestChangesPostResponses, RequestChangesApiTasksTaskIdRequestChangesPostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/request-changes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resume Task
 *
 * Release a hold and put the task back to work (agent/work).
 *
 * The counterpart to /hold, and the reason a hold is not a dead end. The note is
 * optional because "carry on" is a complete instruction on its own; demanding a
 * sentence to say it would push the human back onto the send-back controls, which is
 * how one reason came to carry four intents in the first place.
 */
export const resumeTaskApiTasksTaskIdResumePost = <ThrowOnError extends boolean = false>(options: Options<ResumeTaskApiTasksTaskIdResumePostData, ThrowOnError>) => (options.client ?? client).post<ResumeTaskApiTasksTaskIdResumePostResponses, ResumeTaskApiTasksTaskIdResumePostErrors, ThrowOnError>({
    url: '/api/tasks/{task_id}/resume',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Api Version
 *
 * Report the versions a client must match before it starts issuing calls.
 *
 * Added for the MCP server's startup probe, which refuses to serve tools against a
 * service it cannot understand. The version is already in ``/openapi.json``, but
 * reading it there makes every client parse a large document to learn two fields.
 *
 * The React app polls it for a second reason: to notice that the process answering
 * it is not the one its own bundle was built against, and to say so with the remedy
 * rather than crashing somewhere unrelated when a response is not the shape its
 * generated types promise.
 */
export const apiVersionApiVersionGet = <ThrowOnError extends boolean = false>(options?: Options<ApiVersionApiVersionGetData, ThrowOnError>) => (options?.client ?? client).get<ApiVersionApiVersionGetResponses, unknown, ThrowOnError>({ url: '/api/version', ...options });

/**
 * List Webhooks
 *
 * List all registered webhooks.
 */
export const listWebhooksApiWebhooksGet = <ThrowOnError extends boolean = false>(options?: Options<ListWebhooksApiWebhooksGetData, ThrowOnError>) => (options?.client ?? client).get<ListWebhooksApiWebhooksGetResponses, unknown, ThrowOnError>({ url: '/api/webhooks', ...options });

/**
 * Create Webhook
 *
 * Create a new webhook.
 */
export const createWebhookApiWebhooksPost = <ThrowOnError extends boolean = false>(options: Options<CreateWebhookApiWebhooksPostData, ThrowOnError>) => (options.client ?? client).post<CreateWebhookApiWebhooksPostResponses, CreateWebhookApiWebhooksPostErrors, ThrowOnError>({
    url: '/api/webhooks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Webhook
 *
 * Delete a webhook.
 */
export const deleteWebhookApiWebhooksWebhookIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteWebhookApiWebhooksWebhookIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteWebhookApiWebhooksWebhookIdDeleteResponses, DeleteWebhookApiWebhooksWebhookIdDeleteErrors, ThrowOnError>({ url: '/api/webhooks/{webhook_id}', ...options });

/**
 * Get Webhook
 *
 * Get a specific webhook by ID.
 */
export const getWebhookApiWebhooksWebhookIdGet = <ThrowOnError extends boolean = false>(options: Options<GetWebhookApiWebhooksWebhookIdGetData, ThrowOnError>) => (options.client ?? client).get<GetWebhookApiWebhooksWebhookIdGetResponses, GetWebhookApiWebhooksWebhookIdGetErrors, ThrowOnError>({ url: '/api/webhooks/{webhook_id}', ...options });

/**
 * Test Webhook
 *
 * Send a test event to a webhook.
 */
export const testWebhookApiWebhooksWebhookIdTestPost = <ThrowOnError extends boolean = false>(options: Options<TestWebhookApiWebhooksWebhookIdTestPostData, ThrowOnError>) => (options.client ?? client).post<TestWebhookApiWebhooksWebhookIdTestPostResponses, TestWebhookApiWebhooksWebhookIdTestPostErrors, ThrowOnError>({ url: '/api/webhooks/{webhook_id}/test', ...options });

/**
 * Api Whoami
 *
 * Report who this request resolved as. Reports only; refuses nothing.
 */
export const apiWhoamiApiWhoamiGet = <ThrowOnError extends boolean = false>(options?: Options<ApiWhoamiApiWhoamiGetData, ThrowOnError>) => (options?.client ?? client).get<ApiWhoamiApiWhoamiGetResponses, unknown, ThrowOnError>({ url: '/api/whoami', ...options });
