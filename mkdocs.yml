site_name: AgentJobs
site_description: AgentJobs task schema v2, with the retired v1 schema preserved for migration history.
repo_url: https://github.com/jeffposey/agentjobs
docs_dir: docs

theme:
  name: material
  features:
    - navigation.sections
    - navigation.top
    - navigation.indexes
    - content.code.copy
    - search.suggest
    - toc.follow
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      toggle:
        icon: material/brightness-4
        name: Switch to light mode

plugins:
  - search
  # Click-to-zoom/pan for the entity diagrams. The ER SVGs are ~2600px wide; without
  # a lightbox they render shrunk to the content column and cannot be read.
  - glightbox:
      touchNavigation: true
      effect: zoom
      zoomable: true
      draggable: true
      auto_caption: false

markdown_extensions:
  - admonition
  - attr_list
  - tables
  - toc:
      permalink: true
  - pymdownx.details
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format

nav:
  - Overview: index.md
  - Schema v2 (current):
      - Understanding the schema: schema/understanding.md
      - Entity diagram: schema/v2-erd.md
      - Design rationale: schema-design.md
      - Reference: schema/v2/index.md
  - Schema v1 (retired):
      - Entity diagram: schema/v1-erd.md
      - Reference: schema/v1/index.md
  - Project docs:
      - Agent workflow: agent-workflow.md
      - MCP server: mcp.md
      - Connecting a client: mcp-clients.md
      - Agent dispatch design: agent-dispatch-design.md
      - Agent loops design: agent-loops-design.md
      - Playbooks design: playbooks-design.md
      - Codex dispatch rollout: codex-dispatch.md
      - Durable Codex dispatch architecture: codex-dispatch-architecture.md
      - MCP integration design: mcp-integration-design.md
      - Analytics page design: analytics-design.md
      - The Tasks surface: tasks-shell.md
      - Queue position design: task-selection-design.md
      - Task schema reference: task-schema.md
      - API reference: api-reference.md
      - Measuring performance: performance.md
      - Webhooks: webhooks.md
      - Quickstart: quickstart.md
      - Installation: installation.md
      - Mobile and installed-app access: mobile-access.md
      - Migration guide: migration-guide.md
      - SQLite storage: storage-sqlite.md
      - Task corpus audit: task-corpus-audit.md
      - The context budget: context-budget.md
  - Access and identity:
      - Principals design: principals-design.md
      - Authorization: authorization.md
      - Exposure: exposure.md
      - Identity registry: identity-registry.md
      - Tailnet front door: tailnet-front-door.md

# The 160+ per-class/slot/enum/type pages under schema/v1 and schema/v2 are reached
# from each schema's Reference index rather than enumerated in the sidebar.
not_in_nav: |
  schema/*/classes/*
  schema/*/enums/*
  schema/*/slots/*
  schema/*/types/*
  schema/*/schemas/*
  integration/*
