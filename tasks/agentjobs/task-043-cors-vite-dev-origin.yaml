id: task-043-cors-vite-dev-origin
title: Allow Vite dev-server origin in CORS config
created: '2026-07-06T19:25:44.948348Z'
updated: '2026-07-29T18:34:28.679331Z'
status: waiting_for_human
priority: high
category: developer_experience
assigned_to: claude
estimated_effort: 15 minutes
description: "The upcoming React frontend will run on the Vite dev server at `http://localhost:5173`\
  \ during development. The FastAPI CORS middleware in `src/agentjobs/api/main.py`\
  \ (lines ~38-47) currently allows only `http://localhost:8765` and `http://127.0.0.1:8765`,\
  \ so browser requests from the React app would be blocked by CORS preflight.\n\n\
  Append `http://localhost:5173` and `http://127.0.0.1:5173` to `allow_origins`. Keep\
  \ the origins explicit \u2014 never switch to `\"*\"` while `allow_credentials=True`\
  \ is set (the browser rejects that combination, and it would be a security regression\
  \ anyway).\n\nNote: the React app will primarily use the Vite dev proxy (`/api`\
  \ \u2192 `localhost:8765`), which avoids CORS entirely, but the direct-origin path\
  \ should also work and this is a good opportunity to understand what CORS is."
phases: []
success_criteria:
- id: sc-1
  description: allow_origins includes http://localhost:5173 and http://127.0.0.1:5173;
    allow_credentials stays True with explicit origins
  status: completed
- id: sc-2
  description: OPTIONS preflight from Origin http://localhost:5173 returns access-control-allow-origin
    for that origin
  status: completed
- id: sc-3
  description: poetry run pytest passes
  status: completed
prompts:
  starter: "## Objective\nAdd the Vite dev-server origins to the CORS allowlist in\
    \ src/agentjobs/api/main.py.\n\n## Steps\n1. Create branch `feat/task-043-cors-vite-dev-origin`\
    \ from main. Record it in branches[] and mark the task in_progress.\n2. In src/agentjobs/api/main.py,\
    \ append `http://localhost:5173` and `http://127.0.0.1:5173` to `allow_origins`.\n\
    3. Restart the server and verify with a preflight request:\n   `curl.exe -i -X\
    \ OPTIONS http://localhost:8765/api/tasks -H \"Origin: http://localhost:5173\"\
    \ -H \"Access-Control-Request-Method: GET\"`\n   Expect `access-control-allow-origin:\
    \ http://localhost:5173` in the response headers.\n4. Run `poetry run pytest`.\n\
    \n## Git workflow (mandatory)\nSmall single-logical-change commits, Conventional\
    \ Commits format, tests green before each commit. When the branch is complete\
    \ and verified: STOP, set this task to waiting_for_human, and wait for explicit\
    \ human approval. On approval: rebase onto main, merge with --no-ff, mark the\
    \ branch merged in branches[], set task completed.\n\n## Study note (CORS primer\
    \ for the React work ahead)\nBrowsers enforce the same-origin policy: JS on origin\
    \ A may not read responses from origin B unless B opts in via Access-Control-*\
    \ response headers. The Vite proxy sidesteps this by making the browser talk only\
    \ to :5173 while Vite forwards to :8765 server-side. Both paths will work after\
    \ this task."
  followups: []
status_updates:
- timestamp: '2026-07-29T18:32:02.289871Z'
  author: claude
  status: in_progress
  summary: 'Started: adding Vite dev-server origins to the CORS allowlist'
  details: 'Dependency task-042 completed and merged to main, unblocking this task
    (moved straight from draft to in_progress). Branch feat/task-043-cors-vite-dev-origin
    created from main.


    Plan: append http://localhost:5173 and http://127.0.0.1:5173 to allow_origins
    in src/agentjobs/api/main.py (keeping origins explicit -- never ''*'' while allow_credentials=True),
    add a regression test for the preflight since no CORS test exists today, verify
    a live OPTIONS preflight, run pytest.'
- timestamp: '2026-07-29T18:34:28.679307Z'
  author: claude
  status: waiting_for_human
  summary: 'Branch complete, ready for review: Vite dev origins allowed in CORS'
  details: 'Done:

    - src/agentjobs/api/main.py: appended http://localhost:5173 and http://127.0.0.1:5173
    to allow_origins; allow_credentials stays True with explicit origins (added a
    comment recording why ''*'' is not usable here)

    - tests/test_api.py: added CORS coverage, which did not exist before -- parametrized
    preflight test over all four allowed origins, plus a negative test asserting an
    unknown origin gets no access-control-allow-origin header


    Verified live (started `agentjobs serve`, stopped it cleanly after):

    - OPTIONS preflight, Origin http://localhost:5173 -> 200, access-control-allow-origin:
    http://localhost:5173

    - OPTIONS preflight, Origin http://127.0.0.1:5173 -> 200, access-control-allow-origin:
    http://127.0.0.1:5173

    - OPTIONS preflight, Origin http://evil.example.com -> 400 with NO access-control-allow-origin
    header (the header''s absence is what actually makes the browser block it)

    - Actual GET with Origin http://localhost:5173 -> 200 with the allow-origin header
    present, so the non-preflight path works too


    - poetry run pytest: 71 passed (was 66; +5 new CORS tests)

    - ruff clean on both changed files; black flags only pre-existing regions in those
    files that this task did not touch (handle_validation_error in main.py, test_mark_deliverable_complete
    in test_api.py). Left alone deliberately to keep this diff to one logical change
    -- repo-wide formatting debt (12 files) is a separate concern.


    Commits on feat/task-043-cors-vite-dev-origin: mark in_progress/record branch,
    the CORS change, the tests. Ready for review; on approval rebase onto main + --no-ff
    merge, mark branch merged, set task completed.'
comments: []
deliverables:
- path: src/agentjobs/api/main.py
  status: completed
  description: Updated CORS allow_origins list
dependencies:
- task_id: task-042-relocate-demo-tasks
  type: depends_on
  note: Sequential Phase 0 ordering
external_links: []
issues: []
tags:
- react-frontend
- learning
- phase-0
branches:
- name: feat/task-043-cors-vite-dev-origin
  status: active
