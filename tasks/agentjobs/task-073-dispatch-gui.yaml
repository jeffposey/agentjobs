schema: 2
id: task-073-dispatch-gui
title: 'Dispatch in the web UI: the button, the run, and the toggle'
created: '2026-08-11T00:21:32.502467Z'
updated: '2026-08-11T00:21:32.503127Z'
lifecycle: draft
ball: human
ball_reason: spec
ball_prompt: Derived from the task-060 dispatch design pass. Do not start until that
  design is approved and merged -- the decisions here follow from it and would change
  with it.
archived: false
priority: high
category: ux
tags:
- dispatch
- architecture
assignment:
  eligible: []
spec:
  summary: 'The surface that removes the manual step: a Dispatch action beside Approve
    on a task whose ball is with an agent, live run status with a cancel button, a
    link to the run output, and the per-project enable/disable toggle. The toggle
    may enable and disable only -- it may never define what command runs. Design sections
    5 and 6, decision D2.'
  description: 'Derived from docs/agent-dispatch-design.md sections 5 and 6, and decision
    D2.


    This is the task that closes the loop the owner asked about on 2026-08-10: click
    in the browser, agent starts, no chat window.


    ## Scope


    - A **Dispatch** action, visually and semantically distinct from Approve, on tasks
    whose ball is `agent`. Approve means ''I agree''; Dispatch means ''spend money
    now''. Do not merge them into one button.

    - Live run indicator on the task: running/finished, elapsed time, outcome, cancel
    button, and a link to the run output.

    - Per-project dispatch toggle. **Enable and disable only.** The runner argv is
    not editable from the browser, and a project cannot be enabled against a runner
    that is not already defined machine-locally. Rationale: dispatch turns an unauthenticated
    localhost API into remote code execution, so the browser-reachable surface may
    switch a known capability on and off but must never widen it.

    - Disable is always available with no ceremony. A kill switch you cannot reach
    is not one.

    - Every refusal from task-071 renders as its specific reason, not ''dispatch failed''.


    ## Verification


    Exercise it against a freshly started server, not just in tests. Assert on rendered
    values -- a test that finds `data-run-state=` while the template emits `RunState.RUNNING`
    passes and proves nothing.'
  context: []
acceptance:
- id: sc-1
  text: 'A task with ball: agent in an enabled project shows a Dispatch action separate
    from Approve; clicking it starts a run and the page reflects that without a manual
    refresh'
  status: pending
- id: sc-2
  text: A live run shows elapsed time and a working cancel button; a finished run
    shows its outcome and links to its output
  status: pending
- id: sc-3
  text: The project toggle can enable and disable dispatch but exposes no way to define
    or edit a runner's argv, verified by inspecting the endpoints the page can reach
  status: pending
- id: sc-4
  text: Each refusal from the guard layer renders its specific reason; tests assert
    on the rendered text a user would read
  status: pending
- id: sc-5
  text: 'Exercised end to end against a freshly restarted server: approve a task,
    dispatch it, watch the agent run, see the result land on the task record'
  status: pending
deliverables:
- path: src/agentjobs/api/templates/
  note: Dispatch action, run status, project toggle
  status: pending
- path: src/agentjobs/api/routes/
  note: Toggle endpoint, run status endpoint
  status: pending
dependencies:
- task: task-071-dispatch-api-guards
  type: needs
  note: Derived from the same design pass; this task builds on it.
- task: task-072-dispatch-ledger-cancel
  type: needs
  note: Derived from the same design pass; this task builds on it.
- task: task-060-agent-dispatch
  type: related
  note: The design pass this task was derived from.
links:
- url: https://github.com/jeffposey/agentjobs/blob/main/docs/agent-dispatch-design.md
  rel: design
  title: Agent dispatch design pass (task-060)
branches: []
log: []
