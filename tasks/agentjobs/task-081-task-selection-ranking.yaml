schema: 2
id: task-081-task-selection-ranking
title: Design explicit queue positions so AgentJobs always knows what is next
created: '2026-08-11T04:56:38.397821Z'
updated: '2026-08-15T22:33:15.236503Z'
lifecycle: ready
ball: agent
ball_reason: available
archived: false
priority: high
category: architecture
tags:
- agent-dx
- design
- queue-order
- scheduling
- schema
- total-order
assignment:
  eligible: []
spec:
  summary: Design a schema-backed queue_position that gives every open task an explicit
    place within its priority band, so AgentJobs always has one durable answer to
    what comes next.
  intent: Priority levels are deliberately coarse, and dependencies answer eligibility
    rather than scheduling. AgentJobs currently falls back to updated timestamps,
    so grooming or logging silently changes the queue. The work order must be explicit
    task data that humans and every client can see and change intentionally.
  description: 'The core decision is settled: add a first-class `queue_position` to
    the task schema. `priority` remains the urgency band; `queue_position` is the
    authoritative order within that band. Together they form a total order for open
    work. This is not described as a tie-breaker because it is real scheduling data,
    not an incidental fallback.


    Dependencies, eligibility, lifecycle, ball state, and open-child rules determine
    whether a task is currently claimable. They do not encode which independent task
    should come first. `get_next_task()` filters by those rules and then selects the
    lowest `(priority rank, queue_position)`. Mutable timestamps are never part of
    the order.


    The design must settle readable storage and managed reorder mechanics: positive
    positions, uniqueness within each open priority band, default insertion at the
    bottom of a band, explicit before/after moves, priority changes with defined placement,
    concurrency under a project-level reorder lock, and sparse numbering or another
    human-readable strategy that avoids rewriting the corpus on every move. Missing/duplicate
    positions or a failed rebalance are reported as queue corruption; task selection
    must not silently fall back to timestamp or ID.


    The order must be visible and operable everywhere: REST and Python client, dedicated
    CLI commands, React list/drag and keyboard controls, MCP read results and a high-level
    reorder verb, portable validation, generated schema docs, and agent rules requiring
    the explicit queue rather than chat instructions or invented dependency edges.
    Creation defaults must always produce a valid position.


    Migration assigns every open task a deterministic initial position, then records
    the currently requested program order: task-109 MCP first, task-120 issue reporter
    second, and this queue-ordering program after those. The design produces implementation
    children; it does not hide implementation inside a one-line sort change.'
  constraints: '- Every open task has exactly one queue position within its priority
    band; no two open tasks in one band may share it.

    - Priority plus queue_position is the complete scheduling order after claimability
    filtering. No timestamp, filesystem order, or implicit ID order participates.

    - Dependencies remain semantic prerequisites only. Never add a needs edge merely
    to say one independent task should be done before another.

    - All create, import, migration, reprioritize, reorder, close, and archive paths
    preserve a valid total order through manager/domain operations.

    - Reordering is a managed domain verb, not a generic content patch or direct YAML
    edit.

    - The user-facing term is Queue position (or Work order), not tie-breaker.

    - The design must remain readable in YAML and safe under concurrent agents.'
  out_of_scope: Estimating effort from prose, automatic business-priority scoring,
    using dependency centrality as a substitute for explicit human order, or making
    one global queue span multiple separately configured projects.
  context:
  - path: src/agentjobs/manager.py
    why: Current claimability filter and timestamp-based ordering that must be replaced.
  - path: src/agentjobs/models_v2.py
    why: Schema and invariants for the new queue_position field.
  - path: docs/mcp-integration-design.md
    why: Managed agent surface that must expose and obey queue order without generic
      mutation.
  - path: frontend/src
    why: Task lists and accessible reorder interaction.
acceptance:
- id: sc-1
  text: The design specifies queue_position as first-class schema data and defines
    the total sort as priority band followed by queue position after claimability
    filtering.
  status: pending
- id: sc-2
  text: Every open task is guaranteed one unique position in its priority band; missing,
    duplicate, or invalid positions are loud corpus errors with no timestamp/ID fallback.
  status: pending
- id: sc-3
  text: Create, move before/after, reprioritize, close/archive, migration, compaction/rebalance,
    and concurrent reorder semantics are fully specified.
  status: pending
- id: sc-4
  text: REST, Python client, CLI, React UI, MCP, validation, and agent guidance all
    read or mutate the same managed queue order and explain why a task is next.
  status: pending
- id: sc-5
  text: The migration has a deterministic baseline and explicitly places task-109
    first, task-120 second, and the queue-ordering implementation after them without
    inventing needs dependencies.
  status: pending
- id: sc-6
  text: Implementation child tasks are derived for schema/storage/manager, API/CLI/React,
    and MCP/rules/validation, with acceptance tests that prove ordering never depends
    on updated timestamps.
  status: pending
deliverables:
- path: docs/task-selection-design.md
  note: Ranking signals, tie-breaking, and what /next explains
  status: pending
dependencies:
- task: task-074-auto-dispatch
  type: related
  note: Auto-dispatch consumes the ranking with no human reading it.
- task: task-078-agent-loops
  type: related
  note: A bounded loop re-asks for the next task on every iteration.
- task: task-045-subtask-support
  type: related
  note: '`parent` now records which tasks are one effort -- a ranking signal.'
links: []
branches: []
log:
- id: 1
  ts: '2026-08-11T06:05:20.474281Z'
  actor: claude
  type: note
  body: 'Live evidence for this task''s premise, produced accidentally on 2026-08-11
    and recorded here because it is a measurement, not an opinion.


    A backlog grooming pass closed two tasks and promoted four. Before it, `get_next_task()`
    returned **task-080**. After it, the same call returned **task-058**. No priority
    changed. No dependency closed. No one decided that 058 was more important than
    080.


    What changed is that grooming rewrote `updated`, and `updated` is the tie-breaker:
    the sort key is `(priority_rank(), -updated.timestamp())`. Eleven open tasks are
    claimable and most are `high`, so the first sort term separates almost nothing
    and the queue is effectively ordered by *most recently touched*.


    The consequence is worth stating plainly, because it is worse than arbitrary:
    **any edit to a task promotes it toward the front of the queue.** Logging progress,
    answering a question, fixing a typo in a spec, or a grooming pass that explicitly
    reasoned about priority and deliberately did not change it -- all of them reorder
    the work. The ranking rewards recency of *attention*, which is close to the opposite
    of what a queue should do, since the task nobody has touched in a month is the
    one most likely to be forgotten.


    This bears directly on two acceptance criteria already written here: that tie-breaking
    be deterministic and not depend on a mutable timestamp (it is a concrete demonstration
    of why), and that the design decide whether unblocking value belongs in the ranking.
    On the latter, the same pass produced a data point: ranking the dispatch hierarchy
    by hand, task-077 is the obvious next action because it gates five downstream
    tasks and may close two of them outright, while the tool''s answer -- first 080,
    then 058 -- reflects nothing but file-write order. A human ranked the queue better
    than the function did, in under a minute, using a signal the function does not
    have.'
  data: {}
- id: 2
  ts: '2026-08-15T22:33:15.236441Z'
  actor: codex
  type: decision
  body: 'Jeff settled the central requirement: priority levels alone are insufficient,
    and dependencies must not be abused as scheduling order. Design `queue_position`
    as authoritative first-class order within a priority band. Claimability filters
    first; priority + queue_position selects next; timestamps and IDs are not normal
    fallbacks. The product and agent rules must keep the order complete through every
    managed write path.'
  data: {}
