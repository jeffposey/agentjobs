schema: 2
id: task-093-mobile-and-pwa
title: Make the client usable on a phone, and installable
created: '2026-08-13T02:19:47.902546Z'
updated: '2026-08-13T09:06:01.195182Z'
lifecycle: active
ball: agent
ball_reason: work
ball_prompt: Execute the spec; log progress and hand off when done.
archived: false
priority: high
category: ux
tags:
- gui
- react-frontend
- mobile
- pwa
effort: a day
assignment:
  owner: codex
  eligible: []
parent: task-096-react-phase-2-parity
spec:
  summary: 'The web UI is unusable on a phone: wide tables, mouse-sized targets, and
    no way to keep it open as an app. Make the React client work on a phone and a
    tablet, installable as a PWA, reachable from another device without exposing the
    server to the network.'
  intent: Sequenced deliberately after the dashboard and before the remaining screens.
    After one real screen exists there is something to actually make usable on a phone
    rather than a guess at a pattern; before the other screens, so the card layout
    and the labelling convention are established once instead of retrofitted onto
    three ported pages.
  description: '## Why this is not a polish task


    The person who needs to see a review request is not always at the machine running

    the server. A merge gate that can only be answered from one desk delays every

    branch by however long it takes to get back to that desk. This is the difference

    between a dashboard you check and a dashboard that reaches you.


    ## Layout


    Below a breakpoint around 820px, stack each table into cards: one labelled line

    per field, controls full width at a 44px touch target. That covers a phone and
    a

    tablet in portrait; a landscape tablet is wide enough for the real table and

    should keep it.


    Drive the card labels from a `data-label` attribute the cell components set, not

    from a parallel list of column names. A new column then needs one attribute or
    it

    renders unlabelled on a phone -- which is a visible, cheap failure rather than
    a

    silent one.


    ## Installable


    A manifest, icons at 192/512 plus a maskable variant, and a service worker.

    Generate the icons from one committed SVG with a checked-in script, so they are

    reproducible rather than hand-exported binaries nobody can regenerate.


    **Cache the shell only.** A dashboard that shows yesterday''s ball assignments
    as

    though they were current is worse than one that says it is offline, because the

    whole point of the page is telling you what needs attention now. The offline

    state must be an explicit "cannot reach the server" screen, never stale content

    presented as live.


    Reload once when a new service worker takes over, or an installed app keeps

    serving the previous bundle indefinitely after an upgrade.


    ## Reaching it from another device


    **The server binds loopback only.** Cross-device access is a separate concern

    solved by a proxy in front of it -- a private overlay network such as Tailscale

    is the intended shape -- not by widening the bind address. A wildcard bind must

    be refused rather than merely discouraged: this application has no

    authentication (task-066 is unbuilt), so anything reachable is unauthenticated.


    **HTTPS is not optional.** Service workers only register in a secure context,
    so

    over plain HTTP the offline shell will not work and a browser will offer a

    bookmark rather than an installed app. Document that consequence where someone

    setting this up will read it.


    Keep a documented direct-bind escape hatch for a network without certificates,

    marked clearly as the fallback rather than the normal path, and still refusing
    a

    wildcard.


    ## Documentation


    The setup is not guessable and must be written down: which address is which,

    what the proxy command is, how to undo it, how to add a device, and what happens

    when the machine serving it is asleep.'
  constraints: '- The server binds loopback by default and refuses a wildcard bind.
    Cross-device access is a proxy in front of it, not a wider bind.

    - The service worker caches the shell only. Stale task data must never be presented
    as current.

    - Card labels come from an attribute the cell components set, so a new column
    cannot silently render unlabelled.

    - Icons are generated from a committed source by a committed script.

    - A new service worker taking over must not leave an installed app on the old
    bundle.

    - The setup steps and the HTTPS requirement are documented.'
  out_of_scope: Authentication and multi-user identity (task-066). Push notifications
    outside the browser, which are the webhook system's job. Any native app wrapper.
    Offline writes or queued actions -- the offline state is read-only and says so.
    Making the Jinja UI responsive.
  context:
  - path: src/agentjobs/cli.py
    why: serve --host, where the bind policy is enforced.
  - path: tasks/agentjobs/task-066-multiple-human-users.yaml
    why: Unbuilt authentication -- why reachable means unauthenticated.
acceptance:
- id: ac-1
  text: 'the dashboard is usable on a phone: tables stack into labelled cards below
    the breakpoint, controls meet a 44px touch target'
  status: pending
- id: ac-2
  text: the client installs as an app from a phone and launches standalone
  status: pending
- id: ac-3
  text: with the server unreachable the app says so explicitly and shows no stale
    task data as current
  status: pending
- id: ac-4
  text: the server still binds loopback by default and refuses a wildcard bind; cross-device
    access works through a proxy
  status: pending
- id: ac-5
  text: a deployed bundle upgrade reaches an already-installed app without a manual
    cache clear
  status: pending
- id: ac-6
  text: the setup, including why HTTPS is required, is documented well enough to add
    a second device from the docs alone
  status: pending
deliverables: []
dependencies:
- task: task-087-react-dashboard
  type: needs
  note: Needs one real screen to make usable, before the pattern is set for the rest.
links: []
branches:
- name: feat/task-093-mobile-and-pwa
  status: active
log:
- id: 1
  ts: '2026-08-13T08:55:40.062619Z'
  actor: codex
  type: transition
  body: Claimed by codex.
  data:
    lifecycle: active
    ball: agent
    ball_reason: work
- id: 2
  ts: '2026-08-13T08:55:40.094468Z'
  actor: codex
  type: progress
  body: 'Started mobile and PWA foundation after approved task 087 merge.


    Worktree: C:/projects/aj-093. Branch: feat/task-093-mobile-and-pwa. Plan: establish
    reusable responsive card/cell-label and 44px control patterns, add manifest plus
    reproducible 192/512/maskable icons, add shell-only service worker with explicit
    offline refusal and controller-change reload, enforce loopback/non-wildcard bind
    policy, and document HTTPS proxy setup/teardown/device onboarding/direct-bind
    fallback. Verification plan: focused component/API/CLI tests, generated icon checks,
    service-worker static/behavior checks, responsive browser emulation, full scripts/check.py
    gate, and a fresh loopback server. Human-only checks: real-phone installation/standalone
    launch, actual cross-device private-overlay HTTPS proxy, and deployed service-worker
    upgrade if those cannot be exercised here. The broader phase is not yet functional
    or done; task 093 establishes patterns for tasks 088/089. The required merged-main
    restart was attempted, but an orphaned inaccessible listener on 127.0.0.1:8765
    refused replacement; alternate loopback ports will be used for trustworthy verification.
    Usage-window telemetry is unavailable in this session; no estimate was invented.'
  data: {}
- id: 3
  ts: '2026-08-13T08:58:44.217501Z'
  actor: codex
  type: decision
  body: 'Reject unspecified/wildcard bind hosts in every server-starting CLI path
    (serve, restart, open), while allowing localhost and a specific interface address
    for the documented direct-bind fallback. Rejected alternative: print a warning
    but still bind 0.0.0.0/::; AgentJobs has no authentication, so a warning does
    not prevent accidental unauthenticated network exposure.'
  data: {}
- id: 4
  ts: '2026-08-13T08:58:44.233677Z'
  actor: codex
  type: progress
  body: 'Wildcard bind refusal committed.


    Feature commit 87ce0da adds a shared bind-host validator used by serve, restart,
    and open. It refuses 0.0.0.0, ::, [::], *, and + before starting or stopping anything;
    a specific address remains permitted for the explicitly documented fallback. Verified
    with poetry run pytest tests/test_cli.py -q (25 passed), Ruff (passed), and git
    diff --check (passed). Remaining: responsive component pattern, PWA shell/offline/update
    behavior, reproducible icons, proxy documentation, full checks, and browser/device
    evidence.'
  data: {}
- id: 5
  ts: '2026-08-13T09:06:01.178113Z'
  actor: codex
  type: decision
  body: 'Precache only the built application shell (HTML, hashed JS/CSS, manifest,
    icons) and keep every /api/ request network-only. On navigation failure, serve
    the shell, which immediately hides task data behind an explicit offline/unreachable
    screen. A new worker skips waiting, claims clients, deletes older shell caches,
    and triggers one client reload when it replaces an existing controller. Rejected
    alternative: cache dashboard/task API responses for richer offline reading; stale
    ball assignments would be presented as current and could cause the wrong human
    action.'
  data: {}
- id: 6
  ts: '2026-08-13T09:06:01.195086Z'
  actor: codex
  type: progress
  body: 'Responsive and PWA runtime foundation committed.


    Feature commit 605a43e adds the reusable ResponsiveTable/ResponsiveCell pattern
    with cell-owned data-label attributes, card layout below 820px, 44px touch targets,
    mobile dashboard adjustments, standalone manifest, source SVG plus standard-library
    icon generator and 192/512/maskable PNGs, shell-only revisioned service worker,
    explicit offline/server-unavailable screen, controller-change reload, and FastAPI
    routes for manifest/service worker/icons. Focused verification passed: npm run
    check (5 Vitest files / 13 tests, icons current, generated API current, lint,
    TypeScript, production build with 7 shell resources); 32 focused CLI/SPA/PWA pytest
    tests; Ruff; MyPy; diff check. Remaining: commit documentation, real-browser responsive/offline/PWA
    inspection, full repository gate, and human-only physical device/proxy/installed-upgrade
    checks.'
  data: {}
