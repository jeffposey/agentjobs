schema: 2
id: task-273
title: Build authoritative SQLite task storage alongside the AgentJobs server
created: '2026-08-22T19:00:47.987602Z'
updated: '2026-09-07T03:26:37.430265Z'
lifecycle: closed
outcome: completed
archived: false
priority: critical
category: infrastructure
tags:
- big-dawg-audit
- storage
- decision
assignment:
  eligible: []
parent: task-358
spec:
  summary: Replace canonical task YAML with authoritative SQLite on the AgentJobs
    server host, outside code worktrees. Preserve managed workflow semantics and make
    task state, relationships, and history directly queryable for analytics.
  intent: 'Owner decision, 2026-09-05: eliminate routine task-log Git commits, branch-dependent
    backlogs, and file-storage coordination workarounds. This supersedes earlier recommendations
    to retain canonical YAML with a disposable SQLite index.'
  description: 'Implement authoritative SQLite behind the existing AgentJobs service.
    All normal clients, including CLI, MCP, UI, dispatch and worktrees, address the
    same project-scoped service/store; no silent local database creation or YAML fallback
    if the service is unavailable. The database resides on the server host in a configurable
    durable data directory outside source checkouts. SQLite is embedded in AgentJobs,
    not a separate database server.


    Design the physical schema around real queries, not one opaque task JSON blob.
    Use indexed columns for project/task identity, title, lifecycle, ball/reason,
    outcome, priority, queue position, owner, parent, archived, created/updated and
    revision. Use relational rows for dependencies, tags and append-only log/history
    entries. Nested authoring content (spec sections, acceptance criteria and other
    value objects) may use validated JSON where it avoids unnecessary normalization;
    choose explicit columns/tables for fields that must be filtered, grouped or constrained.
    Specify one authoritative representation per field and assemble the existing task
    API shape from it.


    Write current state, attributable audit/history, revision and operation-id bookkeeping
    in the same transaction. Record structured before/after state for creation, close,
    reopen, handoff and other changes needed to reconstruct historical counts. Preserve
    log threading and original timestamps/actors. Index project/time and task/time
    queries. Task-212/213 require open-task counts over time, throughput, aging and
    waiting-state trends; demonstrate representative SQL queries and plans without
    scanning/parsing every task document. Define historical coverage and distinguish
    unknown pre-migration history from zero.


    Preserve optimistic conflicts, atomic claim, independent log appends, idempotent
    retries and queue semantics. Enforce project-scoped identity and open-task position
    uniqueness in SQL; multi-row reorders and their history must commit atomically.
    Domain validation, dependency/cycle rules and authorization remain enforced. Transactions
    do not automatically solve webhook delivery: preserve and test replay/side-effect
    behavior, coordinate task-255. Define WAL/durability settings, bounded busy handling,
    schema version upgrades, connection ownership and crash recovery. SQL storage
    does not require removing code worktrees.


    Declare a backend-neutral storage boundary. Revisit the old file-shaped Protocol
    proposal: do not require task_path/file locks from a SQL backend. Retain an explicit
    import/diagnostic path for malformed legacy records without allowing invalid live
    database state.


    Provide generic consistent backup/restore support and operator documentation:
    use SQLite''s online backup API or an equivalent consistent snapshot mechanism
    while live, cover attachments and required metadata, document paths/retention
    integration, and verify restore into an isolated instance. Do not recommend copying
    only a live database file while WAL writes continue. Product records contain generic
    backup support only.


    Task-311 owns reversible production import, cutover, and retirement of Git/file-specific
    workflows. Keep YAML as optional export/import, not an automatically committed
    mirror or second live authority. Update architecture documentation to distinguish
    the accepted target from the still-live YAML implementation. Earlier audit estimates
    are historical, not delivery promises.'
  constraints: Preserve every existing task, log, attachment reference, project boundary
    and managed workflow contract. No production cutover under this task; task-311
    owns it. Tests/benchmarks use isolated data, never the live database. Backups
    must be demonstrably restorable. Coordinate the schema/history contract with task-213
    before freezing it.
  out_of_scope: Disposable SQLite read index over canonical YAML; MongoDB/PostgreSQL/Dolt
    implementation; distributed database replication; analytics UI implementation;
    personal backup-provider configuration.
  context:
  - path: audits/2026-08-21/04-storage-manager.md
    why: 'Section 6 in full: inventory, accrual table, the weighing, and the trigger'
  - path: docs/schema-design.md
    why: Section 7, the stated philosophy and the existing SQLite-index escape hatch
  - path: src/agentjobs/storage.py
    why: The surface to declare as a Protocol
acceptance:
- id: ac-1
  text: SQLite is the authoritative implemented backend on the server host, with project-scoped
    service access and no per-worktree or unavailable-server fallback store.
  status: pending
- id: ac-2
  text: Documented schema uses indexed state columns, relational relationships/history
    and bounded JSON value objects; representative open-over-time, throughput and
    aging queries run without whole-task document scans.
  status: pending
- id: ac-3
  text: Concurrent claim/log/reorder, revision conflict, operation replay and crash
    tests prove atomic state plus audit persistence and no duplicate queue positions
    or lost entries.
  status: pending
- id: ac-4
  text: Generic consistent live backup and isolated restore are verified, including
    attachment coverage and documented recovery procedure.
  status: pending
- id: ac-5
  text: Current API/MCP task shape and validation/authorization semantics remain compatible;
    storage docs mark earlier canonical-YAML/index recommendations superseded by the
    owner decision.
  status: pending
- id: ac-6
  text: Repository gate and measured read/write performance checks pass on isolated
    realistic and larger corpora.
  status: pending
deliverables: []
dependencies:
- task: task-213
  type: related
  note: Coordinate queryable historical state and metrics contract; analytics design
    can proceed before backend implementation.
links: []
branches:
- name: feat/task-273-sqlite-store
  status: merged
  merged_at: '2026-09-07T03:26:37.246423Z'
log:
- id: 1
  ts: '2026-08-22T19:00:48.504023Z'
  actor: claude
  type: transition
  body: Created draft by claude.
  data:
    lifecycle: draft
    operation:
      id: 5a0796b0-bdec-4b7d-b0a9-d15849227fa6
      kind: create
      fingerprint: 7cddcbf97be059d2afc4a1f3e95ad7bd
- id: 2
  ts: '2026-09-04T03:42:03.348663Z'
  actor: claude
  type: note
  body: 'Measured 2026-09-03 against the live server on 8876 (CPU at 23%, so not a
    contention artefact), while answering Jeff''s SQL-vs-MongoDB question. The hybrid-index
    trigger this task names (listing p50 over 250 ms) has fired: GET /api/projects/agentjobs/tasks
    answered in 711, 739, 747, 767, 826 ms with X-Task-Parses 331 on every request;
    GET .../tasks/task-311/detail answered in 526-602 ms, also parsing all 331 files;
    /revision answered in 56 ms. Corpus is now 331 files / 6.1 MB / ~18.5 KB per task
    (audit figure on 2026-08-21 was 240 / 3.76 MB / 15.7 KB), largest file 132 KB
    (task-224). task-134''s own reopen trigger (~350 files) is also within reach.
    The second trigger, a writer that is not a process on this machine, has not fired:
    the tailnet proxy forwards to the same server process. Recommendation given to
    Jeff: SQLite as a derived, disposable index (schema-design.md section 7''s escape
    hatch), not a substrate swap and not MongoDB; the workflow coupling is task-311''s
    location question, not a storage-engine question. The atomic-write fix (task-247)
    is owed regardless of backend.'
  data:
    operation:
      id: 3f6b2a1e-9c4d-4e0f-8a7b-2d1c5e9f0a11
      kind: log_append
      fingerprint: c8d276bb9421e0018f432efa3517397b
- id: 3
  ts: '2026-09-05T20:14:06.337374Z'
  actor: codex
  type: note
  body: Updated acceptance, dependencies, spec, title.
  data:
    fields:
    - acceptance
    - dependencies
    - spec
    - title
    operation:
      id: 428e008d-fc79-4458-ab1a-b8e94f1a5290
      kind: update_content
      fingerprint: 0ac92b491312102b197f4fb2b9a6f699
- id: 4
  ts: '2026-09-05T20:17:03.912629Z'
  actor: codex
  type: decision
  body: 'Owner decision, 2026-09-05: use authoritative SQLite hosted alongside the
    AgentJobs server, outside code worktrees, accessed through the shared service.
    This supersedes prior recommendations for canonical YAML plus a disposable SQLite
    index or merely relocating canonical YAML. The purpose includes removing task-state
    Git commits and branch-local backlogs, not only faster reads. Keep queryable task
    state, relationships and timestamped state history structured for task-212/task-213
    analytics; JSON may hold appropriate nested content, not the entire query model.
    Generic consistent backup/restore support is part of the backend and cutover acceptance.
    Specifications updated; no storage implementation or cutover has occurred.'
  data:
    operation:
      id: d3f1092d-f332-4598-896c-ab9c2842e3ec
      kind: log_append
      fingerprint: 35dca2dfe94d61dd2efb399a6d33b39c
- id: 5
  ts: '2026-09-05T21:54:15.168428Z'
  actor: claude
  type: question
  body: 'Restart window, raised 2026-09-05 when Jeff reviewed the storage decision.
    The spec is right that there is no local fallback: with SQLite embedded in the
    server process, the server is the only thing that may open the database, so when
    it is down the CLI, MCP and every dispatched run''s task writes are down with
    it. Today that window is not hypothetical, it is scheduled: the merge gate''s
    step 6 restarts the server after every merge, the launcher restarts it when code
    changes underneath it, and `agentjobs restart` is the documented fix for a stale
    process. Under YAML those writes land on disk regardless. Under this design a
    run that handoffs, logs or closes during the restart gets a connection refused.


    Decide, before freezing the contract: (a) clients retry with bounded backoff against
    a refused connection and surface a clear "service unavailable, nothing written"
    error rather than a stack trace, with the retry budget documented; (b) the finisher''s
    restart step and the launcher drain in-flight requests or hold new ones (graceful
    shutdown on the ASGI side, and the front door returning 503 rather than a reset)
    so a restart is a pause, not an outage; (c) whether the CLI''s direct-to-storage
    path survives at all, or every CLI verb becomes an HTTP client like MCP already
    is, which also decides how `agentjobs serve` and `agentjobs restart` themselves
    address the store they are about to own. The idempotent operation_id replay the
    spec already requires is what makes (a) safe. Coordinate with task-126, which
    carries the same question from the MCP side.'
  data:
    operation:
      id: a4c1f8e2-3b7d-4e6a-9f21-5c8d0e7b2a41
      kind: log_append
      fingerprint: 31be9c7e0c5c4b73ed15509d63520885
- id: 6
  ts: '2026-09-05T21:56:10.271158Z'
  actor: claude
  type: decision
  body: 'Owner decision, Jeff Posey, 2026-09-05, answering entry 5. The restart window
    is handled on both sides of the connection and nowhere else:


    1. Clients retry. Every client of the service (CLI, MCP, the dispatch runner''s
    task writes, the finisher) retries a refused or reset connection with bounded
    backoff, keyed on the same operation_id so a retry can never write twice. When
    the budget is spent the client reports "service unavailable, nothing written"
    as a clean error, never a stack trace, and never a local write. The budget is
    a documented constant, not per-call configuration.

    2. Restarts drain. The server finishes in-flight requests before exiting (graceful
    ASGI shutdown), and the tailnet front door answers 503 while nothing is listening
    rather than resetting the connection. A restart is a pause a client rides through,
    not an outage it must reason about. The merge gate''s step 6, the launcher and
    `agentjobs restart` all get this for free once the server does it.

    3. Every CLI verb becomes a service client, the same as MCP already is. The only
    process that opens the database is the server, including `agentjobs serve` on
    startup. There is no direct-to-storage CLI path to keep.


    Rejected: a local task store or YAML fallback while the server is down (a second
    authority, and the spec already forbids it); the MCP server spawning the service
    when nothing answers (reintroduces the stale-server-on-the-wrong-port failure
    this repository has documented); leaving the CLI on a direct storage path (two
    processes opening one SQLite file is exactly the concurrency the design exists
    to remove). Task-126 is closed as superseded on this basis; its remaining concern,
    MCP behaviour during the window, is item 1 here.'
  re: 5
  data:
    operation:
      id: c3f7a0d9-8e21-4b5c-a6f4-1d9e7b3c5a02
      kind: log_append
      fingerprint: 8c0febab364a1168efa38d703e6103bb
- id: 7
  ts: '2026-09-06T06:08:22.694348Z'
  actor: claude
  type: queue_move
  body: Moved from the high band into the bottom of the critical band.
  data:
    band: critical
    from: 14000
    to: 1493
    placement:
      kind: bottom
    from_band: high
- id: 8
  ts: '2026-09-06T06:08:22.694365Z'
  actor: claude
  type: note
  body: Updated parent, priority.
  data:
    fields:
    - parent
    - priority
    operation:
      id: c1e5fd83-2a74-4b96-8158-5f9c3e7d2a68
      kind: update_content
      fingerprint: 0d4319f11828b1495a638da12c4dbd92
- id: 9
  ts: '2026-09-06T06:12:44.935212Z'
  actor: claude
  type: answer
  body: "Answered by entry 6 \u2014 Jeff's decision of 2026-09-05. Clients retry with\
    \ bounded backoff on the same operation_id; restarts drain gracefully and the\
    \ front door returns 503; every CLI verb becomes a service client, so only the\
    \ server opens the database.\n\nRecorded here as an `answer` because entry 6 was\
    \ typed `decision`, and only an `answer` closes a question thread. The GUI kept\
    \ asking Jeff a question he had already settled."
  re: 5
  data:
    operation:
      id: b6d0ec38-7f29-4a1b-8574-0c4e2f9d7a13
      kind: log_append
      fingerprint: e25918f0a7f7e281f1603f118530c3fc
- id: 10
  ts: '2026-09-06T06:28:34.562195Z'
  actor: claude
  type: handoff
  body: "Ball taken off the owner. The prior prompt was \"Finish specifying this task\"\
    , which asked a human to write a working specification \u2014 that is the agent's\
    \ work, and asking for it was the mistake, not his refusal.\n\nThe decisions here\
    \ are genuinely his: whether nested spec content is JSON or normalised, what historical\
    \ coverage is promised, how the restart window behaves (already answered, entry\
    \ 6 and 9). None of them require him to author prose. An agent drafts the schema\
    \ and comes back with the forks as choices.\n\nNothing else on this task changed."
  data:
    ball: agent
    ball_reason: work
    operation:
      id: a7c1de40-8b52-4f63-9074-2d6e0f9a5b31
      kind: handoff
      fingerprint: e82eadc7982ec51f44cadbac0754dae4
- id: 11
  ts: '2026-09-06T06:32:30.093776Z'
  actor: claude
  type: handoff
  body: 'Corrected the ball an hour after setting it wrong. `agent`/`work` renders
    as "In progress", which said an agent was working this when none was. `agent`/`hold`
    renders "On hold", which is true: parked behind two epics with nobody on it.


    Neither state is quite right, and that is a gap rather than a mistake to repeat:
    for a draft whose spec is the agent''s to write but which nobody has started,
    the vocabulary offers only `work` (reads as in progress), `available` (reads as
    "Ready", wrong for a draft) or `hold`. `hold` is the least misleading of the three.'
  data:
    ball: agent
    ball_reason: hold
    operation:
      id: b8d2ef51-9c63-4a74-8085-3e7f1a0b6c42
      kind: handoff
      fingerprint: e06e44313ed8398dc7a7af47331d386b
- id: 12
  ts: '2026-09-07T00:00:40.252207Z'
  actor: claude
  type: instruction
  body: "Owner instruction, Jeff Posey, 2026-09-06, on how much time this design pass\
    \ gets.\n\n**Get schema v3 right so it does not quickly have to be redone as v4.**\
    \ This is an explicit licence to spend longer here than the immediate cutover\
    \ needs, and it changes what \"done\" means for the drafting session: the schema\
    \ is not finished when it can hold today's task records, it is finished when the\
    \ forks that would force a v4 have been found and either designed for or consciously\
    \ deferred with the deferral written down.\n\nWhat that licenses, concretely:\n\
    \n- **Design for queries nobody has asked for yet.** The spec already requires\
    \ open-over-time, throughput and aging without whole-document scans. Go past that:\
    \ ask what task-212/213 will want second, what an operator debugging a stuck dispatch\
    \ will want, and what a question about a task's history at a past instant costs.\
    \ A column that is cheap to add now is a migration later.\n- **Coordinate with\
    \ task-213 before freezing the history contract.** This is already in the constraints\
    \ (\"Coordinate the schema/history contract with task-213 before freezing it\"\
    ) and it is the single most likely source of a v4: the analytics page is the consumer\
    \ most able to discover the store cannot answer something. Do not treat that coordination\
    \ as optional or as task-311's problem.\n- **Say what the upgrade path is.** A\
    \ schema version and a migration mechanism are part of this design, not a later\
    \ concern. Knowing how v4 would be applied is what makes it survivable if it comes\
    \ anyway.\n\nTwo things are settled and must NOT be re-asked:\n\n1. **Authoritative\
    \ SQLite, not a disposable index over canonical YAML.** Owner decision 2026-09-05,\
    \ recorded in entry 4 and named in task-358's out_of_scope as explicitly not to\
    \ be relitigated.\n2. **The restart window.** Answered in entries 5, 6 and 9 \u2014\
    \ clients retry with bounded backoff keyed on operation_id, restarts drain gracefully\
    \ with the front door returning 503, and every CLI verb becomes a service client\
    \ so only the server opens the database. Entry 9 exists only because entry 6 was\
    \ typed `decision` rather than `answer`, which left the GUI showing Jeff a question\
    \ he had already settled. Do not reopen it, and do not repeat that typing mistake:\
    \ an answer to a question thread is typed `answer` with `re` pointing at the question.\n\
    \n**Deliverable shape.** Draft the physical schema, then hand the ball to `human`/`decision`\
    \ with the real forks as structured `questions[]` \u2014 each with options carrying\
    \ a label, a description of what picking it means, and a `recommended` flag. Jeff\
    \ answers by tapping, on a phone. Do not hand back prose asking him to write a\
    \ specification; that mistake was already made and corrected here in entry 10."
  data:
    operation:
      id: 3f9c1a72-5d84-4e16-9b03-8a2e7c4f1d60
      kind: log_append
      fingerprint: e1f9038d25f669219b13f4f9d4bdb5f6
- id: 13
  ts: '2026-09-07T00:00:51.263233Z'
  actor: claude
  type: handoff
  body: 'Un-held on Jeff''s instruction, 2026-09-06, so the design pass can be dispatched
    to its own session. `hold` was correct while nobody was on it; a session is about
    to be.


    Lifecycle stays `draft` deliberately. `task_promote` is the only exit from draft
    and it asserts the spec is finished -- but the physical schema *is* the missing
    spec, so promoting now would claim the opposite of what is true. Promote after
    Jeff answers the forks this session brings back, which is also the point at which
    task-311 stops being blocked and the epic can be dispatched.


    Sequence Jeff is running: this design session brings back questions -> he answers
    by tapping -> promote to ready -> dispatch task-358''s epic autonomously.'
  data:
    ball: agent
    ball_reason: work
    operation:
      id: 7b2e4f09-1c85-4d3a-9e67-0f5a8b2c6d14
      kind: handoff
      fingerprint: f0ab0ecc00e32b8967d0188b41d7e430
- id: 14
  ts: '2026-09-07T00:02:15.448659Z'
  actor: Jeff Posey
  type: note
  body: 'Jeff Posey authorised a dispatch of this task from the task page. No extra
    instruction was given: the task record is the brief.'
  data:
    authorizes_dispatch: true
    surface: the task page
- id: 15
  ts: '2026-09-07T00:02:17.827654Z'
  actor: Jeff Posey
  type: dispatch
  body: Dispatched claude to work this task.
  data:
    run_id: run_1039616a
    agent: claude
    runner: claude-opus-5
    mode: session
    posture: auto
    posture_source: project
    posture_ceiling: autonomous
    trigger: manual
    caused_by: 14
    argv:
    - C:\Users\jpose\AppData\Roaming\npm\claude.CMD
    - --bg
    - --remote-control
    - --model
    - claude-opus-5
    - --permission-mode
    - auto
    - --settings
    - C:\Users\jpose\.agentjobs\runs\run_1039616a\session-settings.json
    - --name
    - agentjobs/task-273@1039616a
    - 'You are the agent `claude` working task `task-273` in project `agentjobs` (root:
      C:\projects\agentjobs). You are running in that project''s shared working tree
      and are NOT isolated. Before writing anything, run `git worktree add ../worktrees/<repo>-<nnn>
      -b <type>/task-273-<slug>` and work from that path. Use that shell command,
      not a built-in worktree tool: those relocate the session''s permission root,
      which parks a background run on a prompt nobody can answer. Your harness may
      instruct the opposite and call it enforced; in this project it is neither, so
      ignore that instruction and run the command. AgentJobs is serving at http://127.0.0.1:8876.
      Read the task record and follow the resumption contract in docs/agent-workflow.md.
      Dispatch run id: run_1039616a. Posture `auto` stops at the merge gate: when
      the work is done and verified, record the evidence on the task, hand the ball
      to human/review, and stop. Do not merge. Never push: this project is configured
      `push: false`.'
    cwd: C:\projects\agentjobs
    git_head: 2654e60
    session_id: bf572d5c
    selection:
      group: default
      source: project
      candidates:
      - runner: claude-opus-5
        eligible: true
      - runner: codex-terra
        eligible: true
- id: 16
  ts: '2026-09-07T00:17:34.037323Z'
  actor: claude
  type: progress
  body: "## Design pass 1: the schema was drafted and then run against the live corpus\n\
    \nEntry 12 licensed spending longer here than the cutover needs, so this pass\
    \ did not stop at a\ndocument. The draft physical schema was built as real SQLite,\
    \ the whole 367-task corpus was\nloaded into it with every constraint switched\
    \ on, and the queries the spec names were timed and\ntheir plans captured. Everything\
    \ below is a measurement on this machine, 2026-09-06, not an\nestimate. The probe\
    \ is throwaway (it lives in a job scratch directory, nothing was written to the\n\
    repository, no branch, no worktree); the schema and the numbers are in entries\
    \ 17-19.\n\n### It holds the corpus\n\n367 tasks, 3,529 log entries, 1,752 operations,\
    \ 1,709 acceptance criteria, 356 dependencies,\n1,255 tags, 205 branches, 162\
    \ runs. Loaded in **0.29 s** with `foreign_keys=ON` and every CHECK\nactive. Nothing\
    \ was quarantined and nothing was dropped. Database file **12.7 MB** against 9.0\
    \ MB\nof YAML -- the overhead is indexes, and it buys the numbers below.\n\n###\
    \ Before and after, measured\n\n| | today (YAML) | draft schema | |\n|---|---|---|---|\n\
    | read the whole corpus into task objects | **567 ms** (`list_tasks_uncached`,\
    \ libyaml) | **1.0 ms** (`SELECT` over 367 rows) | ~560x |\n| `GET /api/projects/agentjobs/tasks`\
    \ | **908 ms** p50, `X-Task-Parses: 367` | storage half is the 1.0 ms above |\
    \ |\n| `GET .../tasks/task-224/detail` | **736 ms** p50, still 367 parses | **0.12\
    \ ms** for the 71-entry log | |\n| `GET .../revision` | **47 ms**, hashes 9 MB\
    \ every poll | a counter read | |\n\nThe listing endpoint also serialises 7.0\
    \ MB of JSON, which SQL does not remove -- but a listing\nthat selects summary\
    \ columns instead of whole documents removes most of that too. The honest claim\n\
    is about the storage half, and it is the whole of the 567 ms.\n\n### The analytics\
    \ queries the spec requires (p50 of 25 runs)\n\n| query | rows | p50 |\n|---|---|---|\n\
    | open tasks over time, daily, running sum | 26 | **0.24 ms** |\n| throughput\
    \ per day, unique tasks and completion events | 21 | **0.25 ms** |\n| aging of\
    \ open work by bucket | 3 | **0.04 ms** |\n| where work is stuck: current ball\
    \ + days held | 4 | **0.57 ms** |\n| the board as it stood on a past date | 6\
    \ | **0.35 ms** |\n| runs in the air right now, across every task | 3 | **0.01\
    \ ms** |\n| open questions across the project | 24 | **0.05 ms** |\n| search (FTS5)\
    \ | 20 | **0.15 ms** |\n\nNo plan touches a task document. `EXPLAIN QUERY PLAN`\
    \ for the backlog series is\n`SEARCH task_event USING INDEX ix_event_backlog (project_id=?)`;\
    \ throughput is\n`SEARCH task_event USING INDEX ix_event_closed`; aging is\n`SEARCH\
    \ task USING INDEX ix_task_open_age`. Whole-document scans are not merely avoided\
    \ by\nconvention -- there is no JSON column in any of those plans.\n\n### The\
    \ invariants are unrepresentable, not merely checked\n\nEach of these was attempted\
    \ against the loaded corpus and **refused by the database**:\n\n- a second task\
    \ taking a queue slot already held in its band -- this is audit finding **F4**,\
    \ and\n  it stops being a race that a lock has to prevent\n- an open task with\
    \ no ball; a `ball_reason` from the wrong holder's vocabulary; a ball set with\
    \ a\n  blank `ball_prompt`; a closed task keeping its place in line; an active\
    \ task with no owner\n  (models_v2 consistency rules 1-6, as CHECK constraints)\n\
    - two log entries claiming the same per-task id\n- an `operation_id` reused within\
    \ a project\n- a `parent` pointing at a task that does not exist -- which today\
    \ needs `validation.py`\n\nThe one legal multi-row reorder was then exercised:\
    \ lifting a whole band out of the way and placing\nit, inside one transaction,\
    \ **succeeded** and left the band correctly ordered.\n\n### Concurrency\n\nEight\
    \ threads appending a log entry to the *same task* at once: **8 landed, 0 duplicate\
    \ entry ids,\n0 errors, 144 ms**. Under YAML that is a read-modify-write on one\
    \ file serialised by an advisory\nlock, and it is where audit findings F2 and\
    \ F4 live.\n\n### Backup and restore\n\n`VACUUM INTO` was run against the live\
    \ database *while a writer was committing continuously* (227\nwrites landed during\
    \ the window): **68 ms, 12.6 MB, one file, no WAL to copy**. The snapshot then\n\
    passed `PRAGMA integrity_check` = ok, `PRAGMA foreign_key_check` = 0 rows, matched\
    \ the live row\ncounts for every table except the one being hammered (where it\
    \ is correctly an earlier point in\ntime), and satisfied the backlog reconciliation\
    \ invariant. That is ac-4's mechanism, demonstrated\nrather than cited.\n\n###\
    \ The one thing that did not go well, and it matters\n\nReconstructing history\
    \ from the existing log is **not** faithful. Entry 18 has the numbers and what\n\
    follows from them. It is the reason `task_event` carries before *and* after for\
    \ every axis rather\nthan trusting a replay.\n"
  data:
    operation:
      id: 4d1e6a90-3c27-4b51-9e08-6a2f7c31d5b0
      kind: log_append
      fingerprint: 482342e8e436e91c19e77f9cea995651
- id: 17
  ts: '2026-09-07T00:18:50.175150Z'
  actor: claude
  type: decision
  body: "## The draft physical schema\n\nThis is the deliverable entry 12 asked for.\
    \ It is a draft, not a freeze: five forks are open in the\nhandoff. Everything\
    \ else here is settled, with the rejected alternative named.\n\n### The rule that\
    \ decides rows from JSON\n\nThe spec allows validated JSON for nested authoring\
    \ content and demands columns for anything\nfiltered, grouped or constrained.\
    \ That is a judgement per field, so it needs a rule a later\naddition can be tested\
    \ against:\n\n> **A value-object list becomes a table when something filters or\
    \ joins on it, or when a verb\n> mutates one element of it. Otherwise it is a\
    \ validated JSON column on `task`.**\n\nApplying it: `tags` (filtered), `dependencies`\
    \ (graph walks and `needs` satisfaction),\n`acceptance` (per-criterion status,\
    \ and \"which tasks have a failed criterion\" is a real question),\n`deliverables`\
    \ (`mark_deliverable_complete` mutates one), `branches` (`agentjobs branches`\
    \ scans\nthem across every task today by parsing the corpus), `log` and `attachments`\
    \ all become tables.\n`spec.context`, `links` and `assignment.eligible` stay JSON:\
    \ nothing filters them and no verb\ntouches one element. `json_each` remains available\
    \ for the occasional question over them, at 367\nrows.\n\n**Rejected: one opaque\
    \ task JSON blob with a few extracted columns.** It is what a naive port looks\n\
    like, it reads fast and it cannot answer any historical question without parsing\
    \ every document,\nwhich is the thing the spec exists to remove.\n\n**Rejected:\
    \ full normalisation of every value object** -- the `gen-sqlddl` shape the schema-design\n\
    doc measured at 14 tables including synthetic join tables for lists of strings.\
    \ Two of those exist\nonly because LinkML has no array type; they buy nothing\
    \ here.\n\n### One authoritative representation per field\n\nTwo places where\
    \ that principle bit:\n\n- **`task_run` is authoritative for run facts**, and\
    \ the `dispatch` / `dispatch_result` log\n  entries render their `data` payload\
    \ *from* it on read. The alternative -- a projection table\n  rebuilt from the\
    \ log -- is a second copy of a fact, which is what the principle forbids.\n- **`display_status`\
    \ is still computed, never stored.** Unchanged from the design doc, and the\n\
    \  reasoning is unchanged: a denormalised copy of three fields is a standing drift\
    \ bug.\n\nFour columns on `task` *are* denormalised (`closed_at`, `first_claimed_at`,\
    \ `last_activity_at`,\n`log_count`, `open_question_count`). Each is derivable\
    \ from the event or log table, and each is\ncarried because the alternative is\
    \ a join on the hottest read in the product. They are written in\nthe same transaction\
    \ as the thing that changes them, so they cannot drift the way a periodically\n\
    recomputed cache would, and a consistency check can assert each of them against\
    \ its source.\n\n### The DDL\n\n```sql\nPRAGMA journal_mode = WAL;\nPRAGMA foreign_keys\
    \ = ON;\nPRAGMA synchronous = FULL;\nPRAGMA busy_timeout = 5000;\n\nCREATE TABLE\
    \ schema_migration (\n  version INTEGER PRIMARY KEY, name TEXT NOT NULL, applied_at\
    \ TEXT NOT NULL,\n  agentjobs_version TEXT NOT NULL, duration_ms INTEGER);\n\n\
    CREATE TABLE project (\n  project_id            TEXT PRIMARY KEY,\n  root    \
    \              TEXT,\n  created_at            TEXT NOT NULL,\n  reporting_tz \
    \         TEXT NOT NULL DEFAULT 'UTC',\n  imported_from         TEXT,\n  imported_at\
    \           TEXT,\n  history_baseline_at   TEXT,\n  history_baseline_kind TEXT\
    \ NOT NULL DEFAULT 'native'\n     CHECK (history_baseline_kind IN ('native','reconstructed','unknown')));\n\
    \nCREATE TABLE task (\n  project_id        TEXT NOT NULL REFERENCES project(project_id)\
    \ ON DELETE RESTRICT,\n  task_id           TEXT NOT NULL,\n  seq             \
    \  INTEGER,\n  title             TEXT NOT NULL,\n  created_at        TEXT NOT\
    \ NULL,\n  updated_at        TEXT NOT NULL,\n  revision          INTEGER NOT NULL\
    \ DEFAULT 1,\n\n  lifecycle         TEXT NOT NULL CHECK (lifecycle IN ('draft','ready','active','closed')),\n\
    \  ball              TEXT CHECK (ball IN ('agent','human','external')),\n  ball_reason\
    \       TEXT,\n  ball_prompt       TEXT,\n  outcome           TEXT CHECK (outcome\
    \ IN ('completed','cancelled','superseded','duplicate')),\n  archived        \
    \  INTEGER NOT NULL DEFAULT 0 CHECK (archived IN (0,1)),\n\n  priority       \
    \   TEXT NOT NULL CHECK (priority IN ('low','medium','high','critical')),\n  priority_rank\
    \     INTEGER NOT NULL GENERATED ALWAYS AS (\n                      CASE priority\
    \ WHEN 'critical' THEN 0 WHEN 'high' THEN 1\n                                \
    \    WHEN 'medium'   THEN 2 ELSE 3 END) STORED,\n  queue_position    INTEGER CHECK\
    \ (queue_position IS NULL OR queue_position >= 1),\n  category          TEXT NOT\
    \ NULL,\n  effort            TEXT,\n  owner             TEXT,\n  parent_id   \
    \      TEXT,\n  posture           TEXT CHECK (posture IS NULL OR posture IN\n\
    \                      ('read_only','auto','supervised','autonomous')),\n\n  spec_summary\
    \      TEXT NOT NULL,\n  spec_intent       TEXT,\n  spec_description  TEXT NOT\
    \ NULL,\n  spec_constraints  TEXT,\n  spec_out_of_scope TEXT,\n  spec_context_json\
    \ TEXT NOT NULL DEFAULT '[]' CHECK (json_valid(spec_context_json)),\n  links_json\
    \        TEXT NOT NULL DEFAULT '[]' CHECK (json_valid(links_json)),\n  eligible_json\
    \     TEXT NOT NULL DEFAULT '[]' CHECK (json_valid(eligible_json)),\n\n  closed_at\
    \           TEXT,\n  first_claimed_at    TEXT,\n  last_activity_at    TEXT NOT\
    \ NULL,\n  log_count           INTEGER NOT NULL DEFAULT 0,\n  open_question_count\
    \ INTEGER NOT NULL DEFAULT 0,\n\n  PRIMARY KEY (project_id, task_id),\n  FOREIGN\
    \ KEY (project_id, parent_id) REFERENCES task(project_id, task_id) ON DELETE RESTRICT,\n\
    \  CHECK (parent_id IS NULL OR parent_id <> task_id),\n  CHECK ((lifecycle = 'closed')\
    \ = (ball IS NULL)),                      -- rule 1\n  CHECK ((lifecycle = 'closed')\
    \ = (outcome IS NOT NULL)),               -- rule 3\n  CHECK ((lifecycle = 'closed')\
    \ = (queue_position IS NULL)),            -- rule 6\n  CHECK ((ball IS NULL) =\
    \ (ball_reason IS NULL)),                       -- rule 2a\n  CHECK (ball IS NULL\
    \                                                   -- rule 2b\n      OR (ball='agent'\
    \    AND ball_reason IN\n            ('available','work','revise','answer','redirect','hold'))\n\
    \      OR (ball='human'    AND ball_reason IN\n            ('spec','review','decision','approval','input'))\n\
    \      OR (ball='external' AND ball_reason IN ('dependency','service'))),\n  CHECK\
    \ (ball IS NULL OR ball_reason = 'available'                      -- rule 4\n\
    \      OR (ball_prompt IS NOT NULL AND trim(ball_prompt) <> '')),\n  CHECK (lifecycle\
    \ NOT IN ('draft','ready') OR owner IS NULL),          -- rule 5a\n  CHECK (lifecycle\
    \ <> 'active' OR owner IS NOT NULL),                   -- rule 5b\n  CHECK ((lifecycle\
    \ = 'closed') = (closed_at IS NOT NULL)));\n\nCREATE UNIQUE INDEX ux_task_queue_slot\
    \ ON task(project_id, priority_rank, queue_position)\n  WHERE queue_position IS\
    \ NOT NULL;\nCREATE INDEX ix_task_queue_order ON task(project_id, priority_rank,\
    \ queue_position)\n  WHERE queue_position IS NOT NULL;\nCREATE INDEX ix_task_ball\
    \      ON task(project_id, ball, ball_reason) WHERE lifecycle <> 'closed';\nCREATE\
    \ INDEX ix_task_open_age  ON task(project_id, created_at)        WHERE lifecycle\
    \ <> 'closed';\nCREATE INDEX ix_task_parent    ON task(project_id, parent_id)\
    \ WHERE parent_id IS NOT NULL;\nCREATE INDEX ix_task_owner     ON task(project_id,\
    \ owner)     WHERE owner IS NOT NULL;\nCREATE INDEX ix_task_lifecycle ON task(project_id,\
    \ lifecycle, archived);\nCREATE INDEX ix_task_closed_at ON task(project_id, closed_at)\
    \ WHERE closed_at IS NOT NULL;\nCREATE INDEX ix_task_activity  ON task(project_id,\
    \ last_activity_at DESC);\n\nCREATE TABLE task_tag (\n  project_id TEXT NOT NULL,\
    \ task_id TEXT NOT NULL, tag TEXT NOT NULL,\n  PRIMARY KEY (project_id, task_id,\
    \ tag),\n  FOREIGN KEY (project_id, task_id) REFERENCES task ON DELETE CASCADE);\n\
    CREATE INDEX ix_tag_lookup ON task_tag(project_id, tag);\n\nCREATE TABLE task_dependency\
    \ (\n  project_id TEXT NOT NULL, task_id TEXT NOT NULL, other_id TEXT NOT NULL,\n\
    \  type TEXT NOT NULL CHECK (type IN ('needs','blocks','related')), note TEXT,\n\
    \  ord INTEGER NOT NULL,\n  PRIMARY KEY (project_id, task_id, other_id, type),\n\
    \  FOREIGN KEY (project_id, task_id) REFERENCES task ON DELETE CASCADE,\n  CHECK\
    \ (other_id <> task_id));\nCREATE INDEX ix_dep_reverse ON task_dependency(project_id,\
    \ other_id, type);\n\nCREATE TABLE task_acceptance (\n  project_id TEXT NOT NULL,\
    \ task_id TEXT NOT NULL, ac_id TEXT NOT NULL,\n  ord INTEGER NOT NULL, text TEXT\
    \ NOT NULL, verify TEXT,\n  status TEXT NOT NULL DEFAULT 'pending'\n         CHECK\
    \ (status IN ('pending','met','failed','dropped')),\n  PRIMARY KEY (project_id,\
    \ task_id, ac_id),\n  FOREIGN KEY (project_id, task_id) REFERENCES task ON DELETE\
    \ CASCADE);\nCREATE INDEX ix_acceptance_status ON task_acceptance(project_id,\
    \ status);\n\nCREATE TABLE task_deliverable (\n  project_id TEXT NOT NULL, task_id\
    \ TEXT NOT NULL, path TEXT NOT NULL,\n  ord INTEGER NOT NULL, note TEXT,\n  status\
    \ TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','done','dropped')),\n\
    \  PRIMARY KEY (project_id, task_id, path),\n  FOREIGN KEY (project_id, task_id)\
    \ REFERENCES task ON DELETE CASCADE);\n\nCREATE TABLE task_branch (\n  project_id\
    \ TEXT NOT NULL, task_id TEXT NOT NULL, name TEXT NOT NULL,\n  status TEXT NOT\
    \ NULL CHECK (status IN ('active','merged','abandoned')),\n  merged_at TEXT, ord\
    \ INTEGER NOT NULL,\n  PRIMARY KEY (project_id, task_id, name),\n  FOREIGN KEY\
    \ (project_id, task_id) REFERENCES task ON DELETE CASCADE);\nCREATE INDEX ix_branch_status\
    \ ON task_branch(project_id, status);\n\nCREATE TABLE log_entry (\n  project_id\
    \ TEXT NOT NULL, task_id TEXT NOT NULL, entry_id INTEGER NOT NULL,\n  ts TEXT\
    \ NOT NULL, actor TEXT NOT NULL,\n  type TEXT NOT NULL CHECK (type IN ('note','progress','transition','handoff','decision',\n\
    \       'question','answer','instruction','dispatch','dispatch_result','queue_move')),\n\
    \  body TEXT, re INTEGER,\n  data_json TEXT NOT NULL DEFAULT '{}' CHECK (json_valid(data_json)),\n\
    \  PRIMARY KEY (project_id, task_id, entry_id),\n  FOREIGN KEY (project_id, task_id)\
    \ REFERENCES task ON DELETE CASCADE,\n  FOREIGN KEY (project_id, task_id, re)\
    \ REFERENCES log_entry(project_id, task_id, entry_id)\n      DEFERRABLE INITIALLY\
    \ DEFERRED,\n  CHECK (re IS NULL OR re < entry_id));\nCREATE INDEX ix_log_project_ts\
    \ ON log_entry(project_id, ts DESC);\nCREATE INDEX ix_log_type_ts    ON log_entry(project_id,\
    \ type, ts DESC);\nCREATE INDEX ix_log_thread     ON log_entry(project_id, task_id,\
    \ re) WHERE re IS NOT NULL;\nCREATE INDEX ix_log_questions  ON log_entry(project_id,\
    \ task_id, entry_id) WHERE type='question';\n\nCREATE TABLE blob (\n  sha256 TEXT\
    \ PRIMARY KEY, media_type TEXT NOT NULL, size_bytes INTEGER NOT NULL,\n  content\
    \ BLOB, path TEXT, CHECK ((content IS NULL) <> (path IS NULL)));\n\nCREATE TABLE\
    \ attachment (\n  project_id TEXT NOT NULL, task_id TEXT NOT NULL, entry_id INTEGER\
    \ NOT NULL,\n  ord INTEGER NOT NULL, sha256 TEXT NOT NULL, media_type TEXT NOT\
    \ NULL,\n  size_bytes INTEGER NOT NULL CHECK (size_bytes >= 1), label TEXT NOT\
    \ NULL,\n  PRIMARY KEY (project_id, task_id, entry_id, ord),\n  FOREIGN KEY (project_id,\
    \ task_id, entry_id) REFERENCES log_entry ON DELETE CASCADE,\n  FOREIGN KEY (sha256)\
    \ REFERENCES blob(sha256) ON DELETE RESTRICT);\n\nCREATE TABLE task_run (\n  project_id\
    \ TEXT NOT NULL, run_id TEXT NOT NULL, task_id TEXT NOT NULL,\n  agent TEXT NOT\
    \ NULL, runner TEXT NOT NULL,\n  mode TEXT NOT NULL CHECK (mode IN ('session','batch','interactive')),\n\
    \  posture TEXT NOT NULL, posture_source TEXT, posture_ceiling TEXT, posture_requested\
    \ TEXT,\n  trigger TEXT NOT NULL CHECK (trigger IN ('manual','auto','child')),\n\
    \  caused_by INTEGER NOT NULL, playbook TEXT, playbook_hash TEXT, session_id TEXT,\n\
    \  git_head TEXT NOT NULL, cwd TEXT NOT NULL,\n  argv_json TEXT NOT NULL CHECK\
    \ (json_valid(argv_json)),\n  selection_json TEXT CHECK (selection_json IS NULL\
    \ OR json_valid(selection_json)),\n  started_at TEXT NOT NULL, ended_at TEXT,\
    \ outcome TEXT, exit_code INTEGER,\n  duration_seconds REAL, log_path TEXT,\n\
    \  PRIMARY KEY (project_id, run_id),\n  FOREIGN KEY (project_id, task_id) REFERENCES\
    \ task ON DELETE CASCADE);\nCREATE INDEX ix_run_live ON task_run(project_id, started_at)\
    \ WHERE ended_at IS NULL;\nCREATE INDEX ix_run_task ON task_run(project_id, task_id,\
    \ started_at DESC);\n\nCREATE TABLE task_event (\n  event_id     INTEGER PRIMARY\
    \ KEY,\n  project_id   TEXT NOT NULL, task_id TEXT NOT NULL,\n  ts           TEXT\
    \ NOT NULL, actor TEXT NOT NULL,\n  kind         TEXT NOT NULL CHECK (kind IN\
    \ ('create','claim','handoff','release','close',\n               'reopen','archive','unarchive','reprioritize','queue_move',\n\
    \               'update_content','import')),\n  log_entry_id INTEGER, operation_id\
    \ TEXT,\n  mechanical   INTEGER NOT NULL DEFAULT 0 CHECK (mechanical IN (0,1)),\n\
    \  source       TEXT NOT NULL DEFAULT 'native'\n               CHECK (source IN\
    \ ('native','reconstructed','backfilled')),\n  lifecycle_from   TEXT,    lifecycle_to\
    \   TEXT,\n  ball_from        TEXT,    ball_to        TEXT,\n  ball_reason_from\
    \ TEXT,    ball_reason_to TEXT,\n  outcome_from     TEXT,    outcome_to     TEXT,\n\
    \  archived_from    INTEGER, archived_to    INTEGER,\n  priority_from    TEXT,\
    \    priority_to    TEXT,\n  owner_from       TEXT,    owner_to       TEXT,\n\
    \  position_from    INTEGER, position_to    INTEGER,\n  parent_from      TEXT,\
    \    parent_to      TEXT,\n  detail_json      TEXT NOT NULL DEFAULT '{}' CHECK\
    \ (json_valid(detail_json)),\n  open_delta INTEGER NOT NULL GENERATED ALWAYS AS\
    \ (\n    CASE WHEN kind = 'create' THEN 1\n         WHEN lifecycle_to = 'closed'\n\
    \              AND (lifecycle_from IS NULL OR lifecycle_from <> 'closed') THEN\
    \ -1\n         WHEN lifecycle_from = 'closed'\n              AND lifecycle_to\
    \ IS NOT NULL AND lifecycle_to <> 'closed' THEN 1\n         ELSE 0 END) STORED,\n\
    \  FOREIGN KEY (project_id, task_id) REFERENCES task ON DELETE CASCADE);\nCREATE\
    \ INDEX ix_event_project_ts ON task_event(project_id, ts);\nCREATE INDEX ix_event_task_ts\
    \    ON task_event(project_id, task_id, ts);\nCREATE INDEX ix_event_backlog  \
    \  ON task_event(project_id, ts, open_delta) WHERE open_delta <> 0;\nCREATE INDEX\
    \ ix_event_closed     ON task_event(project_id, ts) WHERE lifecycle_to = 'closed';\n\
    CREATE INDEX ix_event_ball       ON task_event(project_id, ts) WHERE ball_to IS\
    \ NOT ball_from;\n\nCREATE TABLE operation (\n  project_id TEXT NOT NULL, operation_id\
    \ TEXT NOT NULL, kind TEXT NOT NULL,\n  actor TEXT NOT NULL, fingerprint TEXT\
    \ NOT NULL, task_id TEXT, log_entry_id INTEGER,\n  applied_at TEXT NOT NULL, result_revision\
    \ INTEGER,\n  PRIMARY KEY (project_id, operation_id));\nCREATE INDEX ix_operation_task\
    \ ON operation(project_id, task_id);\n\nCREATE TABLE webhook_outbox (\n  id INTEGER\
    \ PRIMARY KEY, project_id TEXT NOT NULL, task_id TEXT, event TEXT NOT NULL,\n\
    \  payload_json TEXT NOT NULL CHECK (json_valid(payload_json)), created_at TEXT\
    \ NOT NULL,\n  attempts INTEGER NOT NULL DEFAULT 0, next_attempt_at TEXT, delivered_at\
    \ TEXT,\n  last_error TEXT);\nCREATE INDEX ix_outbox_pending ON webhook_outbox(next_attempt_at)\
    \ WHERE delivered_at IS NULL;\n\nCREATE TABLE import_quarantine (\n  id INTEGER\
    \ PRIMARY KEY, project_id TEXT NOT NULL, source_path TEXT NOT NULL,\n  task_id_guess\
    \ TEXT, raw_text TEXT NOT NULL, error TEXT NOT NULL,\n  imported_at TEXT NOT NULL,\
    \ resolved_at TEXT);\n\nCREATE VIRTUAL TABLE task_fts USING fts5(\n  task_id UNINDEXED,\
    \ project_id UNINDEXED,\n  title, summary, description, ball_prompt, tags, tokenize\
    \ = 'unicode61');\n```\n\n### Assembling the existing API shape\n\nOne task document\
    \ is one `task` row plus five small keyed reads. The React listing does not\n\
    assemble documents at all -- it selects summary columns from `task` alone, which\
    \ is the 1.0 ms\nmeasured in entry 16 and where the 7 MB of JSON on the wire also\
    \ goes away.\n\n### What this removes from the codebase\n\n- `queue.py`'s gap/rebalance/compaction/repair\
    \ machinery. `docs/task-selection-design.md` section 12\n  chose sparse integers\
    \ *because* one-file-per-task makes a 47-file diff a merge hazard, and that\n\
    \  reason evaporates. `ux_task_queue_slot` plus a transaction is what the ~600\
    \ lines were buying.\n- `storage.py`'s three lock files, the corpus snapshot cache,\
    \ and `canonical_bytes`.\n- `validation.py`'s cross-task checks that are now constraints\
    \ (queue uniqueness, parent existence).\n- `project_revision` hashing 9 MB per\
    \ poll.\n"
  data:
    operation:
      id: 5e2f7ba1-4d38-4c62-af19-7b3a8d42e6c1
      kind: log_append
      fingerprint: 25c97686a96126478d49e5f2174f2a43
- id: 18
  ts: '2026-09-07T00:19:33.685751Z'
  actor: claude
  type: decision
  body: "## The history contract, and what the existing log genuinely cannot tell\
    \ us\n\nThis is the half task-213 consumes, so it is written to be quotable by\
    \ that design rather than\nre-derived. The constraint says coordinate before freezing;\
    \ the shape below is the offer, and the\nhandoff asks how task-213 should be sequenced\
    \ against it.\n\n### One row per state-changing event, carrying before *and* after\n\
    \n`task_event` is not a change feed of fields. It is one row per **event** --\
    \ create, claim, handoff,\nrelease, close, reopen, archive, reprioritize, queue_move,\
    \ update_content, import -- with a\n`_from` and a `_to` column for each of the\
    \ nine axes anyone has asked a question about. An axis the\nevent did not move\
    \ carries the same value on both sides, so a reader never has to look backwards\
    \ to\nfind out what something was.\n\n**Rejected: deriving history by replaying\
    \ the log at query time.** It was the obvious design, it is\nwhat the corpus superficially\
    \ supports, and the measurement below is why it does not survive.\n\n**Rejected:\
    \ a generic (entity, field, old, new) audit table.** It makes every question a\n\
    self-join per axis and gives up the generated `open_delta` column, which is the\
    \ whole of the\nbacklog chart.\n\n### The backlog chart is a running sum of one\
    \ indexed column\n\n`open_delta` is a `STORED` generated column: `+1` on create,\
    \ `-1` on the transition into `closed`,\n`+1` on the transition out of it, `0`\
    \ otherwise. The whole \"is the backlog growing\" question is then\n\n```sql\n\
    WITH d AS (SELECT date(ts, :tz) AS day, SUM(open_delta) AS delta\n           \
    \  FROM task_event WHERE project_id = :p AND open_delta <> 0 GROUP BY 1)\nSELECT\
    \ day, SUM(delta) OVER (ORDER BY day) AS open_count FROM d ORDER BY day;\n```\n\
    \n`SEARCH task_event USING INDEX ix_event_backlog`, **0.24 ms** over the whole\
    \ corpus. Throughput,\naging, waiting-state and point-in-time queries are in entry\
    \ 16 with their plans; all are under a\nmillisecond and none reads a task document.\n\
    \n**The invariant that makes it trustworthy**, and which should be a test rather\
    \ than a hope:\n\n> `SUM(open_delta)` over all events equals `COUNT(*)` of open\
    \ tasks, at all times.\n\nIt holds on the imported corpus: **125 = 125**, verified.\
    \ It also survives backup and restore.\n\n### Day buckets, and the timezone\n\n\
    `ts` is stored as ISO-8601 UTC; the bucket is computed with `date(ts, :offset)`\
    \ at query time and\nthe project's reporting offset lives in `project.reporting_tz`.\
    \ That is one authoritative place for\nthe answer to \"when does a day end\",\
    \ which is a question task-213 has to settle and which changes\nevery chart if\
    \ two components answer it differently. The cost of computing rather than storing\
    \ the\nbucket is a range scan instead of an equality probe; at 2,254 events it\
    \ is not measurable, and\nstoring it would freeze the timezone into the data.\n\
    \n### What the existing corpus can and cannot tell us -- measured\n\nThe whole\
    \ corpus was replayed forward from its log, per task, and the replayed final state\
    \ compared\nagainst the current row on six axes. This is the honest coverage statement:\n\
    \n| axis | tasks whose replay disagrees with the current row |\n|---|---|\n| owner\
    \ | **0 / 367** |\n| outcome | 22 / 367 |\n| lifecycle | 22 / 367 |\n| ball /\
    \ ball_reason | 35 / 367 |\n| **priority** | **337 / 367** |\n\n- **Priority history\
    \ does not exist.** A priority change is recorded as a `note` whose `data.fields`\n\
    \  says `[\"priority\"]` and never says the value. 409 content updates in the\
    \ corpus are that shape.\n  Nothing in the store can say what a task's priority\
    \ was last month, and no amount of care at\n  import invents it. This is the clearest\
    \ single argument for `priority_from`/`priority_to`.\n- **`archived` has the same\
    \ problem** and is not even in the table above, because there is nothing\n  to\
    \ compare against.\n- **79 tasks have no creation event at all**, and 60 carry\
    \ a `transition` whose only payload is\n  `v1_status` -- the 2026-08-10 schema\
    \ migration is a wall, and everything before it is a single\n  marker.\n- Lifecycle,\
    \ outcome and ball reconstruct well (**89-94%**) once the manager's own semantics\
    \ are\n  applied to entries that recorded a lifecycle without recording the ball\
    \ it implies.\n\n### Therefore: import reconciles, and says so\n\nThe import replays\
    \ what it can, then, for every task whose replay disagrees with the imported row,\n\
    writes **one `kind='import'`, `source='reconstructed'` event at `updated`** carrying\
    \ the true\ncurrent values. On this corpus that is **337 of 367 tasks**, and afterwards\
    \ the reconciliation\ninvariant holds exactly. Three properties follow, and they\
    \ are the point:\n\n1. **No task is left with a history that contradicts its own\
    \ row.** The disagreement is resolved in\n   the data, once, at import, rather\
    \ than surfacing later as a chart that does not add up.\n2. **The reconciliation\
    \ is visible.** `source` distinguishes `native` (written by a verb, exact)\n \
    \  from `reconstructed` (replayed or reconciled, timestamp is an upper bound)\
    \ from `backfilled`\n   (recovered from git). A chart can grey out or annotate\
    \ the reconstructed span; nothing has to\n   guess.\n3. **`project.history_baseline_at`\
    \ and `history_baseline_kind` name the boundary**, so\n   \"we do not know\" is\
    \ a value the API returns rather than a zero the page renders. That is the\n \
    \  spec's \"distinguish unknown pre-migration history from zero\", made concrete.\n\
    \n### Prose history: the thing cutover destroys unless it is designed in\n\nThe\
    \ audit names `git blame` on a field as one of three things files actively buy\
    \ and which are in\ndaily use. After cutover it is gone -- there is no commit\
    \ to blame. So: **an `update_content` event\ncarries, in `detail_json`, the previous\
    \ value of every prose field it changed.** One copy per\nchange, not two: the\
    \ \"after\" is the next event's \"before\", or the current row. That restores\n\
    \"what did this description say before Codex rewrote it\" as a query, and it costs\
    \ an estimated\n2-4 MB a year at the current rate of 409 content updates.\n\n\
    **Rejected: full bitemporal row versioning** (a `task_version` table holding every\
    \ column at every\nrevision). It answers more questions and roughly doubles the\
    \ store; nobody has asked one of the\nextra questions. The deferral is recorded\
    \ below with its trigger.\n\n### For task-213 specifically\n\nThe contract it\
    \ can build on: `task_event` with the eleven `kind` values and nine before/after\n\
    axes; `source` and the project baseline for coverage; `mechanical` to exclude\
    \ bulk renumbering from\n\"activity\"; `task_run` for anything about dispatch;\
    \ `date(ts, :tz)` as the bucket rule. **Adding a\ncolumn to `task_event` is a\
    \ migration, not a redesign** -- which is the reason the sequencing\nquestion\
    \ in the handoff has a recommended answer rather than a blocking dependency.\n\
    \n### Deferred, with the trigger written down\n\n- **`task_state_daily` rollup\
    \ table.** Designed, not built: `(project_id, day, open_count,\n  completed_count,\
    \ ...)`, rebuildable by one query, never an authority. **Trigger:** the backlog\n\
    \  query exceeding ~50 ms, which at today's 0.24 ms is about 200x away.\n- **Full\
    \ bitemporal versioning** (above). **Trigger:** a question that needs the *whole*\
    \ row as it\n  stood, not the axes plus prose before-values.\n- **Cross-project\
    \ analytics.** Out of scope for task-212 and not designed for, but the single-\n\
    \  database option in the handoff is what leaves the door open at no cost.\n"
  data:
    operation:
      id: 6a3b8cd2-5e49-4f73-b02a-8c4b9e53f7d2
      kind: log_append
      fingerprint: 4ae8bfd30a12ea20cff519f6859fce8f
- id: 19
  ts: '2026-09-07T00:20:21.250983Z'
  actor: claude
  type: decision
  body: "## The rest of the contract: concurrency, replay, webhooks, backup, the boundary,\
    \ and how v4 gets applied\n\nEverything here is settled with its rejected alternative.\
    \ The five open forks are in the handoff.\n\n### Connections and durability\n\n\
    - `journal_mode=WAL`, `synchronous=FULL`, `foreign_keys=ON`, `busy_timeout=5000`,\n\
    \  `PRAGMA optimize` on close.\n- **`FULL`, not the usual WAL recommendation of\
    \ `NORMAL`.** `NORMAL` can lose the last transaction\n  on power loss. At a few\
    \ writes a minute an fsync per commit is invisible, and a lost handoff is\n  expensive.\
    \ *Rejected: NORMAL* -- it optimises a write rate this product does not have.\n\
    - **One writer connection, serialised; a small pool of readers.** WAL makes readers\
    \ never block the\n  writer. Only the server process opens the database, which\
    \ is entry 6's decision.\n- **Every write is `BEGIN IMMEDIATE`.** Taking the write\
    \ lock up front avoids the deferred-to-write\n  upgrade, which is where `SQLITE_BUSY`\
    \ deadlocks come from. The 8-way concurrent append in entry 16\n  used exactly\
    \ this and lost nothing.\n- **Crash recovery is WAL replay on open.** There is\
    \ no lock file to reap, which is audit finding\n  **F2** ceasing to exist rather\
    \ than being fixed.\n\n### Optimistic conflicts, and what \"independent log appends\"\
    \ means\n\n- `updated_at` **remains** the revision token, compared to the microsecond,\
    \ so `expected_revision`\n  keeps working for every existing client. A `revision`\
    \ integer is added alongside as the token to\n  prefer going forward (and the\
    \ natural ETag). *Rejected: replacing the timestamp token* -- it is an\n  ac-5\
    \ compatibility break for a cosmetic gain.\n- A log append takes the next `entry_id`\
    \ inside the write transaction rather than rewriting a\n  document. Two appends\
    \ to one task therefore cannot clobber each other and neither has to fail --\n\
    \  proved in entry 16. It still moves `updated_at`, because a reader who read\
    \ before the append has\n  not seen it; \"independent\" means they do not collide,\
    \ not that they are invisible.\n\n### Idempotent replay\n\n- The `operation` ledger\
    \ is **project-scoped, not task-scoped**. That fixes audit **P3** twice\n  over:\
    \ `_find_created_by` stops being O(corpus x log) and becomes an index probe (**0.006\
    \ ms**),\n  and a bare create with no log entry becomes idempotent, which today\
    \ it is not.\n- A replayed operation returns the **current** task plus `replayed:\
    \ true` and the revision the\n  original produced. Audit **F5** observed that\
    \ \"replays the original result\" is not what happens;\n  this makes the contract\
    \ match the behaviour instead of the other way round. A retry after a\n  connection\
    \ reset -- which entry 6 makes the *normal* path -- needs to know its write landed,\
    \ and\n  `result_revision` answers that. *Rejected: storing the full original\
    \ response per operation* --\n  1,752 operations x a whole task document is roughly\
    \ doubling the store to serve a field nobody\n  reads.\n- A reused id with a different\
    \ fingerprint is still a hard refusal, unchanged.\n\n### Webhooks (task-255)\n\
    \nA state change and its notification go into the **same transaction**: the change\
    \ writes a\n`webhook_outbox` row, and a background sender drains it with backoff.\
    \ Two things fall out:\n\n- **Audit F3 disappears.** A replayed operation short-circuits\
    \ before the transaction body, so it\n  enqueues nothing and fires nothing.\n\
    - **The crash window closes.** Today a process that dies between the write and\
    \ the send loses the\n  notification silently, which is the shape of the bug task-047\
    \ hid for months.\n\nDelivered rows are pruned after 30 days; undelivered ones\
    \ are never pruned, because a webhook that\ngave up is a thing an operator needs\
    \ to find.\n\n### Backup and restore\n\n- **`VACUUM INTO <path>`** for a snapshot:\
    \ one consistent file, no `-wal`/`-shm` to copy, compacted.\n  Measured in entry\
    \ 16 at **68 ms while a writer was committing**, with `integrity_check` ok and\n\
    \  `foreign_key_check` clean on the result. `Connection.backup()` remains available\
    \ for a live\n  incremental copy.\n- **Never copy the live file while WAL writes\
    \ continue**, which the spec already forbids and which\n  is the failure mode\
    \ this replaces.\n- `agentjobs backup` produces the snapshot plus a `manifest.json`\
    \ (physical schema version, project\n  list, row counts per table, snapshot hash)\
    \ and, if blobs stay on disk, the attachment tree.\n  `agentjobs restore --verify`\
    \ opens the snapshot **as an isolated instance**, re-runs\n  `integrity_check`,\
    \ `foreign_key_check`, the manifest counts and the backlog reconciliation\n  invariant,\
    \ and reports before anything is swapped in.\n- Whether the attachment tree is\
    \ part of that at all is fork 2 in the handoff.\n\n### The storage boundary\n\n\
    The audit proposed declaring today's surface as a `Protocol` plus a `load_raw`.\
    \ The spec asks for\nthat to be revisited so a SQL backend is not made to supply\
    \ file-shaped methods. It is:\n\n**Drops:** `task_path`, `locked`, `creation_lock`,\
    \ `queue_lock`, `canonical_bytes`, `tasks_dir`.\nEvery one of those is a statement\
    \ about files.\n\n**Keeps or gains:**\n\n```\nget(task_id) -> Task | None    \
    \        list(filter) -> [Task]        search(query) -> [Task]\ncreate(task, *,\
    \ operation) -> Task     mutate(task_id, mutator, *, expected_revision, operation)\n\
    append_log(task_id, entry, *, operation)      reorder(moves, *, operation)\nrevision()\
    \ -> (token, count)           events(filter) -> Iterator[TaskEvent]\ntransaction()\
    \ -> context manager       attachments -> AttachmentStore\nquarantine() -> [QuarantinedRecord]\n\
    ```\n\n`transaction()` replaces all three locks: what the lock files were approximating\
    \ is what a\ntransaction *is*. `reorder()` exists as a first-class verb because\
    \ a multi-row move has to be one\ntransaction and one history burst, and hiding\
    \ that behind N calls to `mutate` would make it neither.\n\n**`load_raw` is replaced\
    \ by `quarantine()`.** Malformed records land in `import_quarantine`, which\n\
    has no foreign keys and no CHECKs, so the live tables keep every constraint while\
    \ an unreadable\nlegacy record is still inspectable and re-importable. The four\
    \ raw readers in `queue.py` and\n`validation.py` read quarantine; they never see\
    \ an invalid live row, because there cannot be one.\n*Rejected: `load_raw` over\
    \ the live tables* -- it would require the live tables to tolerate invalid\nstate,\
    \ which is the one thing the move to SQL is buying.\n\n### Two version axes, and\
    \ how v4 gets applied\n\n- **The document schema stays `schema: 2`.** Adding `revision`\
    \ is additive, and the evolution policy\n  in `docs/schema-design.md` section\
    \ 9 says additive changes bump nothing. *Rejected: bumping\n  documents to 3*\
    \ -- it would invalidate every exported YAML on every physical migration, coupling\n\
    \  two things that change for unrelated reasons. \"Schema v3\" in conversation\
    \ names this storage\n  generation, not the document version.\n- **The physical\
    \ schema has its own version** in `PRAGMA user_version`, starting at 1, with\n\
    \  `schema_migration` recording every applied step (version, name, when, which\
    \ AgentJobs version,\n  how long).\n- **Migrations are numbered SQL files, forward-only,\
    \ one transaction each.** On open: equal, do\n  nothing; lower, write a `VACUUM\
    \ INTO` snapshot beside the database *first*, then apply the pending\n  steps;\
    \ **higher, refuse to start and say so.** That last case is the stale-server hazard\
    \ this\n  repository already documents, and refusing is the same instinct as the\
    \ source-root check in\n  `agentjobs serve`. So the answer to \"how would v4 be\
    \ applied\" is: a numbered file, an automatic\n  pre-migration snapshot, and an\
    \ old binary that declines rather than corrupting.\n\n### Deferred, with triggers\n\
    \n- **Blob storage strategy is a single column pair** (`blob.content` / `blob.path`)\
    \ precisely so the\n  answer to fork 2 can change later without a schema migration.\n\
    - **`PRAGMA journal_size_limit` / WAL checkpoint tuning:** default until a measurement\
    \ says\n  otherwise. **Trigger:** the `-wal` file exceeding the database.\n- **Cross-project\
    \ queries:** no view, no API. **Trigger:** a second project asking for a combined\n\
    \  board, which task-212 explicitly excludes today.\n"
  data:
    operation:
      id: 7b4c9de3-6f5a-4083-c13b-9d5c0f64a8e3
      kind: log_append
      fingerprint: b052118daf5baef75115b8ba91bbefb9
- id: 20
  ts: '2026-09-07T00:21:03.373401Z'
  actor: claude
  type: handoff
  body: 'Design pass complete. The physical schema is drafted, built as real SQLite,
    and run against the live 367-task corpus; five forks are left for you.


    **What is in the record.** Entry 16 is the evidence, entry 17 the DDL and the
    rows-versus-JSON rule, entry 18 the history contract task-213 consumes, entry
    19 concurrency, replay, webhooks, backup, the storage boundary and the migration
    mechanism. Every claim in entry 16 is a measurement taken today, not an estimate.


    **Highlights.** Reading the whole corpus goes from 567 ms to 1.0 ms; the three
    analytics queries the spec names run in 0.24, 0.25 and 0.04 ms with no plan touching
    a task document. Nine invariants that today need validation passes or lock files
    -- including audit finding F4, the duplicate queue position -- were attempted
    and refused by the database. Eight concurrent log appends to one task all landed
    with no duplicate ids. A consistent backup was taken while a writer was committing
    (68 ms, one file) and verified on restore.


    **What did not go well, and it is the reason Q3 exists.** Reconstructing history
    from the existing log is not faithful. Priority history does not exist at all
    -- a change records that the field moved, never what it moved from -- and 79 tasks
    have no creation event. The schema answers this with before *and* after on every
    axis and a visible `reconstructed` marker, but the past cannot be invented, so
    how much to promise is your call.


    **Entry 12''s two settled items were not reopened.** Authoritative SQLite is assumed
    throughout, and the restart window is taken as decided in entries 5/6/9.


    **No worktree, no branch, no code** -- as the prompt required. The probe is throwaway
    and lives in a job scratch directory; nothing was written to the repository. Once
    you answer, this is ready to promote and the epic can be dispatched.'
  data:
    ball: human
    ball_reason: decision
    operation:
      id: 8c5d0ef4-7a6b-4194-d24c-0e6d1f75b9f4
      kind: handoff
      fingerprint: 2d13883cff31e75805a114b1da662f57
- id: 21
  ts: '2026-09-07T00:21:03.373528Z'
  actor: claude
  type: question
  body: '**One database for every project, or one per project?**


    Either works. It decides how backups and restores are scoped, and whether a cross-project
    question is ever possible.'
  re: 20
  data:
    options:
    - label: One database, all projects
      description: '`~/.agentjobs/data/agentjobs.db`, with `project_id` on every row
        and in every key. Project scoping becomes a foreign key rather than a convention.
        One backup, one migration run, one connection pool. A cross-project view stays
        possible later at no cost. Restoring one project means importing an export
        into the live database rather than swapping a file.'
      recommended: true
    - label: One per project
      description: A database under each project's own data directory. Restoring one
        project cannot touch another and a project can live on its own disk. Costs
        N connections, N migration runs, N backups, and closes the door on any query
        spanning projects.
      recommended: false
    multi_select: false
    placeholder: null
- id: 22
  ts: '2026-09-07T00:21:03.373550Z'
  actor: claude
  type: question
  body: '**Attachments: inside the database, or files beside it?**


    There are zero attachments in this project''s corpus today, so whichever you pick
    is free to adopt now and awkward to change later.'
  re: 20
  data:
    options:
    - label: Blobs in the database
      description: '"The backup is one file" becomes literally true -- one `VACUUM
        INTO` covers everything, with no attachment tree to keep in step. Content-addressed
        by hash, so the same screenshot twice is stored once and an orphan is a refcount
        of zero rather than a directory scan. Cost: the database grows by whatever
        images you attach (5 MB cap each, as now).'
      recommended: true
    - label: Files beside it
      description: 'The database stays small and holds only metadata; images stream
        from disk. Cost: a backup is two things that must be captured at the same
        instant, and orphan and missing-file states come back as real possibilities.'
      recommended: false
    multi_select: false
    placeholder: null
- id: 23
  ts: '2026-09-07T00:21:03.373565Z'
  actor: claude
  type: question
  body: '**How much pre-cutover history should the analytics page promise?**


    Measured on the real corpus: replaying the log recovers owner perfectly, lifecycle/outcome/ball
    at 89-94%, and priority not at all -- a priority change records that the field
    changed, never what it changed from. 79 tasks have no creation event; 60 carry
    only a v1 migration marker from 2026-08-10.'
  re: 20
  data:
    options:
    - label: Reconstruct, then reconcile
      description: Import replays the log, then writes one `import` event per task
        where the replay disagrees with the current row (337 of 367 today), marked
        `reconstructed` so its timestamp reads as an upper bound. Counts reconcile
        exactly, charts are trustworthy from roughly 2026-08-10, and the page shows
        a "history begins here" line rather than rendering unknown as zero. No extra
        cost to task-311.
      recommended: true
    - label: Also mine git history
      description: The same, plus a one-time pass over the git history of the task
        files to recover the priority and field timeline the log never wrote down.
        Buys a fuller chart back to the project's start. Costs task-311 real implementation
        time for a chart segment nobody has asked to see yet.
      recommended: false
    - label: Baseline only
      description: Everything before cutover becomes one `import` event per task at
        the migration instant. Charts start the day of the cutover and say so. Cheapest,
        and throws away the 89-94% that is genuinely recoverable.
      recommended: false
    multi_select: false
    placeholder: null
- id: 24
  ts: '2026-09-07T00:21:03.373579Z'
  actor: claude
  type: question
  body: '**How should task-213 (the analytics design) coordinate with this schema?**


    Your instruction was that this coordination is not optional. The question is whether
    it blocks the build.'
  re: 20
  data:
    options:
    - label: Run task-213 next, then build
      description: task-213 goes before implementation and consumes entry 18 as its
        input, so the consumer most able to find a gap finds it before any code is
        written. It does not become a `needs` edge -- adding a column to the event
        table is a migration, not a redesign -- so if task-213 stalls, the build is
        not held hostage.
      recommended: true
    - label: Make it a blocking dependency
      description: 'task-273''s implementation gets `needs: task-213`. Strongest guarantee
        the contract is frozen with its consumer in the room; puts the store behind
        a design task that has not started.'
      recommended: false
    - label: Build now, migrate later
      description: Implement against entry 18 as it stands and let task-213 request
        columns afterwards as ordinary migrations. Fastest to a working store; accepts
        that the first analytics session may ask for a schema change.
      recommended: false
    multi_select: false
    placeholder: null
- id: 25
  ts: '2026-09-07T00:21:03.373593Z'
  actor: claude
  type: question
  body: '**Search: keep exact substring matching, or move to full-text?**


    User-visible either way. Today''s search lowercases and substring-matches id,
    title, summary, intent, description, ball_prompt and tags.'
  re: 20
  data:
    options:
    - label: Move to FTS5
      description: 'Tokenised, ranked results with prefix matching, at 0.15 ms. "sqlite
        storage" would find tasks containing both words anywhere rather than only
        that exact phrase. The id-first rule is preserved. Differences you would notice:
        results are ranked rather than corpus-ordered, and a partial word mid-token
        ("ttach") stops matching.'
      recommended: true
    - label: Keep substring exactly
      description: A `LIKE` scan over 367 rows is about a millisecond anyway, so nothing
        is lost on speed and behaviour is bit-for-bit what you have today. Gives up
        ranking and word-level matching.
      recommended: false
    multi_select: false
    placeholder: null
- id: 26
  ts: '2026-09-07T00:21:54.354636Z'
  actor: dispatcher
  type: dispatch_result
  re: 15
  data:
    run_id: run_1039616a
    outcome: completed
    duration_seconds: 1177.631178
    log_path: C:\Users\jpose\.agentjobs\runs\run_1039616a
- id: 27
  ts: '2026-09-07T00:36:37.137443Z'
  actor: Jeff Posey
  type: handoff
  body: 'Answered by Jeff Posey:


    **One database for every project, or one per project?**


    Either works. It decides how backups and restores are scoped, and whether a cross-project
    question is ever possible.

    -> Chose: One database, all projects


    **Attachments: inside the database, or files beside it?**


    There are zero attachments in this project''s corpus today, so whichever you pick
    is free to adopt now and awkward to change later.

    -> Chose: Blobs in the database


    **How much pre-cutover history should the analytics page promise?**


    Measured on the real corpus: replaying the log recovers owner perfectly, lifecycle/outcome/ball
    at 89-94%, and priority not at all -- a priority change records that the field
    changed, never what it changed from. 79 tasks have no creation event; 60 carry
    only a v1 migration marker from 2026-08-10.

    -> Chose: Also mine git history


    **How should task-213 (the analytics design) coordinate with this schema?**


    Your instruction was that this coordination is not optional. The question is whether
    it blocks the build.

    -> Chose: Run task-213 next, then build


    **Search: keep exact substring matching, or move to full-text?**


    User-visible either way. Today''s search lowercases and substring-matches id,
    title, summary, intent, description, ball_prompt and tags.

    -> Chose: Move to FTS5'
  data:
    ball: agent
    ball_reason: answer
- id: 28
  ts: '2026-09-07T00:36:37.137465Z'
  actor: Jeff Posey
  type: answer
  body: 'Chose: One database, all projects'
  re: 21
  data:
    selected:
    - One database, all projects
    other: null
- id: 29
  ts: '2026-09-07T00:36:37.137482Z'
  actor: Jeff Posey
  type: answer
  body: 'Chose: Blobs in the database'
  re: 22
  data:
    selected:
    - Blobs in the database
    other: null
- id: 30
  ts: '2026-09-07T00:36:37.137496Z'
  actor: Jeff Posey
  type: answer
  body: 'Chose: Also mine git history'
  re: 23
  data:
    selected:
    - Also mine git history
    other: null
- id: 31
  ts: '2026-09-07T00:36:37.137505Z'
  actor: Jeff Posey
  type: answer
  body: 'Chose: Run task-213 next, then build'
  re: 24
  data:
    selected:
    - Run task-213 next, then build
    other: null
- id: 32
  ts: '2026-09-07T00:36:37.137515Z'
  actor: Jeff Posey
  type: answer
  body: 'Chose: Move to FTS5'
  re: 25
  data:
    selected:
    - Move to FTS5
    other: null
- id: 33
  ts: '2026-09-07T00:36:55.791208Z'
  actor: Jeff Posey
  type: note
  body: 'Jeff Posey authorised a dispatch of this task from the task page. No extra
    instruction was given: the task record is the brief.'
  data:
    authorizes_dispatch: true
    surface: the task page
- id: 34
  ts: '2026-09-07T00:36:58.266701Z'
  actor: Jeff Posey
  type: dispatch
  body: Resumed the session from run `run_1039616a` rather than starting a cold one,
    so this agent still has the worktree, the branch and the verification it established
    there. The ball prompt was delivered to it as its next turn.
  data:
    run_id: run_8ad284df
    agent: claude
    runner: claude-opus-5
    mode: session
    posture: auto
    posture_source: project
    posture_ceiling: autonomous
    trigger: manual
    caused_by: 33
    argv:
    - C:\Users\jpose\AppData\Roaming\npm\claude.CMD
    - --bg
    - --remote-control
    - --model
    - claude-opus-5
    - --permission-mode
    - auto
    - --settings
    - C:\Users\jpose\.agentjobs\runs\run_8ad284df\session-settings.json
    - --name
    - agentjobs/task-273@8ad284df
    - --resume
    - bf572d5c-0ced-4cf5-8650-1847f859b5c2
    cwd: C:\projects\agentjobs
    git_head: 0cc8790
    session_id: c3b3a806
    selection:
      group: default
      source: project
      candidates:
      - runner: claude-opus-5
        eligible: true
      - runner: codex-terra
        eligible: true
- id: 35
  ts: '2026-09-07T00:40:46.088628Z'
  actor: claude
  type: answer
  body: 'Chose: One database, all projects


    Owner decision, Jeff Posey, 2026-09-06. Settled and not to be reopened.


    Consequences now binding on the design:


    - **One file, `agentjobs.db`, in a configurable durable data directory** on the
    server host, defaulting to `~/.agentjobs/data/`. Overridable with `AGENTJOBS_DATA_DIR`
    for an install that wants it on another disk. It is outside every source checkout,
    which is the whole point of the move.

    - **`project_id` is the first column of every primary key and every index**, exactly
    as the drafted DDL in entry 17 already has it. Project scoping is now a foreign
    key that the database enforces, not a convention a caller can forget: a task row
    cannot exist without a `project` row, and `ON DELETE RESTRICT` means removing
    a project with tasks in it is refused rather than silently orphaning them.

    - **One migration run, one connection pool, one WAL, one backup.** `agentjobs
    backup` covers the whole installation in one `VACUUM INTO`.

    - **Per-project restore is an import, not a file swap.** `agentjobs restore --project
    <id>` reads that project''s rows out of a snapshot and replaces them inside a
    transaction. This is the one thing the per-database option would have made cheaper,
    and it is the accepted cost.

    - **Cross-project queries become possible** without a schema change. Nothing is
    built for them -- task-212 explicitly excludes cross-project analytics -- but
    the door is no longer nailed shut, and a task moving between projects becomes
    a single atomic UPDATE rather than an operation with no correct implementation.

    '
  re: 21
  data:
    operation:
      id: 9d6e1f05-8b7c-42a5-e35d-1f7e2086ca05
      kind: log_append
      fingerprint: 82b84c5419e3a3c8af940ef13fbcccf9
- id: 36
  ts: '2026-09-07T00:42:03.846034Z'
  actor: claude
  type: decision
  body: '## The five forks are answered. What each one changes, and what the git backfill
    can actually recover.


    Jeff Posey answered all five on 2026-09-06 (entries 28-32, threaded to questions
    21-25). Those are

    binding and must not be re-asked. Entry 35 is a duplicate answer on thread 21
    -- his tap had already

    closed it -- and can be ignored; the design consequences are here instead.


    ### Q1 -- One database, all projects


    `agentjobs.db` in a configurable durable data directory on the server host, default

    `~/.agentjobs/data/`, overridable with `AGENTJOBS_DATA_DIR`, outside every source
    checkout.

    `project_id` leads every primary key and every index, as entry 17''s DDL already
    has it, so project

    scoping is a foreign key the database enforces rather than a convention a caller
    can forget. One

    migration run, one WAL, one connection pool, one backup. **Accepted cost:** restoring
    a single

    project is an import into the live database inside a transaction, not a file swap.


    ### Q2 -- Blobs in the database


    `blob.content` becomes `BLOB NOT NULL` and the `path` column and its either-or
    CHECK come out of

    entry 17''s DDL. One `VACUUM INTO` is then genuinely the whole backup, with no
    attachment tree to

    capture at the same instant, and orphans and missing files stop being representable.
    Content is

    addressed by sha256 with a refcount, so the same screenshot twice is stored once.
    **If this is ever

    regretted, adding a nullable `path` back is an additive migration, not a redesign.**


    ### Q5 -- FTS5


    An external-content FTS5 table over title, summary, description, ball_prompt and
    tags, kept in step

    by triggers on `task`. The id-first rule survives: an exact id match is returned
    ahead of ranked

    results, because "058" meaning task-058 is the one query search must never get
    wrong.


    ### Q3 -- Also mine git history. It works, and the win is not the one it looked
    like.


    Measured before accepting it, since this was the one answer that went against
    the recommendation.

    1,418 commits touch the task directory, back to **2025-10-26**. A full-history
    extraction --

    `git log --reverse -p -U0 -M` over all of them -- costs **0.4 s and 11.2 MB of
    diff**, so the

    backfill is a cheap one-pass parse rather than a project.


    What comes out, separating a value present at file creation from a genuine before-to-after
    move:


    | field | initial values | genuine transitions | removals | tasks touched |

    |---|---|---|---|---|

    | priority | 376 | **37** | 1 | 369 |

    | archived | 355 | **0** | 0 | 355 |

    | queue_position | 242 | 151 | 117 | 240 |

    | parent | 167 | 14 | 3 | 167 |

    | category | 376 | 7 | 1 | 369 |


    **The value is the starting value, not a rich change stream.** Entry 18 reported
    337 of 367 tasks

    whose replay disagreed with their row, essentially all of it priority -- and the
    reason was that

    the log records no priority at creation, so the replay had no value to start from.
    Git supplies

    that starting value for **369 tasks**, which closes almost the whole gap. The
    intermediate history

    is genuinely thin: priority moved **37 times in the project''s life**, earliest
    2026-08-15. That is

    the truth about this backlog rather than a limitation of the method, and a priority-churn
    chart

    should be expected to look sparse because it is.


    **`archived` has never transitioned, and that was verified rather than inferred.**
    Both archived

    tasks took `archived: true` in the single 2026-08-10 schema-migration commit,
    as an initial value.

    So there is no archived history to recover and none is being lost.


    **365 of 367 files have a git "first added" instant**, which gives a real creation
    event to the 79

    tasks whose log has none.


    **The honest caveat, stated up front so no chart over-claims: git records commit
    time, not change

    time.** Records are committed promptly, so the skew is usually seconds, but a
    bulk commit collapses

    many changes into one instant -- the largest here are 132 task files in one commit
    (2026-08-22),

    then 93 and 87 (2026-08-21). Those are backlog reorganisations, and a backfilled
    `queue_position`

    history will correctly show them as simultaneous. Only 2 renames exist in the
    whole history, so

    `-M` keeps continuity.


    **Design consequences.** `task_event.source` already admits `backfilled`, so no
    schema change is

    needed -- but every backfilled row must carry `detail_json.git_commit` with the
    sha it came from,

    so any backfilled fact is traceable to the commit that is its evidence. `project

    .history_baseline_kind` becomes `backfilled` for this project. And a **sequencing
    constraint that

    belongs to task-311 and is easy to get fatally wrong: the backfill reads the git
    history of the

    task files, so it must run, and be verified, before those files are retired.**
    Retire first and the

    evidence is gone.


    ### Q4 -- task-213 runs next, without a blocking edge


    Confirmed on disk: **task-311 already carries `needs: task-273`**, which is the
    edge task-358 asked

    to have confirmed before running its walk. Nothing to add there.


    What is *not* enforced anywhere, and therefore has to be a human act: task-213''s
    parent is task-212,

    a different epic. **An epic walk on task-358 does not consult task-212''s queue
    order, so it will

    start task-273''s implementation whether or not task-213 has run.** No `needs`
    edge was added,

    because Jeff chose the option that explicitly declines one. So the sequencing
    is real but manual:

    **dispatch task-213 before dispatching task-358''s walk.** Recorded here rather
    than left in chat,

    because chat does not survive the session and this is the one part of his answer
    that nothing in

    the system will enforce for him.


    ### One observation this session produced by accident, worth carrying into the
    build


    This task record is now large enough that returning the whole document exceeds
    an MCP response

    budget -- every write this session came back over the limit, and the write still
    succeeded, so the

    failure is in the reply rather than the operation. It is a small, concrete argument
    for something

    the new store should do anyway: **the write verbs should return a summary plus
    the affected entry,

    not the entire task with its whole log**, and the log should be pageable. Worth
    a look during

    implementation; it is not in scope to fix here.

    '
  data:
    operation:
      id: a1f2c3d4-9e8b-4712-b563-2c4d8f19e7a6
      kind: log_append
      fingerprint: 32cd2ec006109016ddc3b7e6c5d6ca48
- id: 37
  ts: '2026-09-07T00:42:15.995982Z'
  actor: claude
  type: transition
  body: 'Promoted by claude: the physical schema was the missing spec, and it is now
    settled. Entry 17 is the DDL, 18 the history contract, 19 the concurrency/backup/boundary/migration
    contract, 36 the five owner answers folded in with their schema deltas. Entry
    16 is the measured evidence behind all of it.


    Before dispatching task-358''s walk: dispatch task-213 first. Jeff chose "task-213
    next, then build" without a blocking edge, and a walk on task-358 will not wait
    for a task that belongs to task-212''s epic. See entry 36.'
  data:
    lifecycle: ready
    ball: agent
    ball_reason: available
    operation:
      id: b2e3d4c5-0f9a-4823-c674-3d5e9f2ab8b7
      kind: promote
      fingerprint: 33c9350390f6c16f77a1c028a6dbc6a1
- id: 38
  ts: '2026-09-07T00:42:53.871052Z'
  actor: dispatcher
  type: dispatch_result
  re: 34
  data:
    run_id: run_8ad284df
    outcome: completed
    duration_seconds: 356.973437
    log_path: C:\Users\jpose\.agentjobs\runs\run_8ad284df
- id: 39
  ts: '2026-09-07T02:25:11.710750Z'
  actor: Jeff Posey
  type: note
  body: 'Jeff Posey authorised a dispatch of task-358, and this run is attempt 1 of
    2 at one of its children on that authorisation (entry 3 on task-358). They chose
    posture `autonomous` for the epic, so this run gets it too. No separate approval
    of this task was given or is required: the epic is what was authorised.'
  data:
    authorizes_dispatch: true
    surface: the epic walk
    epic:
      parent: task-358
      entry: 3
      attempt: 1
      limit: 2
- id: 40
  ts: '2026-09-07T02:25:12.386321Z'
  actor: claude
  type: transition
  body: Claimed by claude.
  data:
    lifecycle: active
    ball: agent
    ball_reason: work
- id: 41
  ts: '2026-09-07T02:25:13.971008Z'
  actor: Jeff Posey
  type: dispatch
  body: Resumed the session from run `run_8ad284df` rather than starting a cold one,
    so this agent still has the worktree, the branch and the verification it established
    there. The ball prompt was delivered to it as its next turn.
  data:
    run_id: run_8cd58e2f
    agent: claude
    runner: claude-opus-5
    mode: session
    posture: autonomous
    posture_source: epic
    posture_ceiling: autonomous
    trigger: child
    caused_by: 39
    argv:
    - C:\Users\jpose\AppData\Roaming\npm\claude.CMD
    - --bg
    - --remote-control
    - --model
    - claude-opus-5
    - --permission-mode
    - bypassPermissions
    - --name
    - agentjobs/task-273@8cd58e2f
    - --settings
    - C:\Users\jpose\.agentjobs\runs\run_8cd58e2f\session-settings.json
    - --resume
    - c3b3a806-342a-494e-9b83-95998f6b7970
    cwd: C:\projects\agentjobs
    git_head: fb69525b
    session_id: 0f33c184
    selection:
      group: default
      source: project
      candidates:
      - runner: claude-opus-5
        eligible: true
      - runner: codex-terra
        eligible: true
- id: 42
  ts: '2026-09-07T03:00:33.807738Z'
  actor: claude
  type: progress
  body: "## Built. Branch `feat/task-273-sqlite-store`, gate green on `73e07f92`.\n\
    \nThe store from entries 17-19 and 36 exists and is tested. **It is not switched\
    \ on** --\ntask-311 owns the cutover, and the live authority is still YAML.\n\n\
    ### What landed\n\n`src/agentjobs/sqlstore/` -- `migrations/001_initial.sql` (the\
    \ schema), `connection.py`\n(single writer, WAL, `FULL`, reentrant `BEGIN IMMEDIATE`\
    \ transactions), `migrations.py`\n(numbered forward-only steps, refuses a database\
    \ newer than the code), `store.py`\n(`SqlTaskStore`), `blobs.py`, `importer.py`,\
    \ `backfill.py`, `backup.py`.\n`src/agentjobs/storage_protocol.py` is the boundary.\
    \ `docs/storage-sqlite.md` is the\nprose; `docs/schema-design.md` section 7 now\
    \ carries a SUPERSEDED banner (ac-5).\n\n49 tests across `tests/test_sqlstore.py`\
    \ and `tests/test_sqlstore_analytics.py`.\n\n### Measured on the live 371-task\
    \ corpus\n\n| | |\n|---|---|\n| import with git backfill | 6.1 s, 2,026 events,\
    \ 7 blobs, **13.8 MB** |\n| backlog invariant after import | **128 = 128** |\n\
    | round-trip fidelity | **370 of 371 documents identical**; the one difference\
    \ is `merged_at` normalised from `-05:00` to the same instant in `Z` |\n| assemble\
    \ all 371 full documents | **84 ms** (YAML: 567 ms to parse the same corpus) |\n\
    | summary-column listing | **~1 ms** |\n| online snapshot | **59 ms**, one 13.6\
    \ MB file, verified restorable |\n\n### The design changed in three places, each\
    \ because data contradicted it\n\n1. **`task_run` is not authoritative for how\
    \ a run ended.** Entry 17 said it was. Two run\n   ids in this corpus carry *two*\
    \ `dispatch_result` entries each -- interrupted at 22:27,\n   completed at 22:31,\
    \ both true -- and one row cannot hold two outcomes. `dispatch` is\n   unique\
    \ per run id (167 of 167) and still renders from the row; the terminal payload\n\
    \   stays on the entry, and `task_run.ended_at` is a documented latest-observed\
    \ projection\n   for the \"runs in the air\" index.\n2. **`blob.content` is `NOT\
    \ NULL` and the `path` escape hatch is gone.** Entry 19 kept a\n   column pair\
    \ so fork 2 could be revisited without a migration. With the fork answered,\n\
    \   a permanently half-used CHECK is worse than the one-line additive migration\
    \ it saves.\n3. **History fidelity is 84%, not the 89% task-213 measured.** Reported\
    \ as measured\n   rather than rounded toward their figure; the residual is itemised\
    \ below.\n\n### History: what the past can honestly say\n\nReplaying the log and\
    \ comparing each task against its own record:\n\n| | tasks needing reconciliation\
    \ |\n|---|---|\n| log replay alone | 371 / 371 |\n| + manager semantics for entries\
    \ recording a lifecycle without its ball | 161 |\n| + git backfill | 84 |\n| +\
    \ close clears the queue position; a claim's actor is its owner | **59** |\n\n\
    Four bugs were found and fixed getting there, three of which would have shipped\
    \ silently:\n\n- **A missing `-` line in a diff is not evidence of a creation\
    \ value.** `archived:` and\n  `parent:` did not exist until the schema-v2 migration\
    \ added them to every record, so\n  each arrives as an addition with nothing on\
    \ the left, on a months-old file. Treating\n  those as creation values backdated\
    \ them and lost 39 real parent changes.\n- **Git emits a local offset, the manager\
    \ emits `Z`.** Storing both misordered events\n  under a string sort *and* put\
    \ changes in the wrong day bucket -- which would have\n  broken every chart on\
    \ the analytics page. Everything is normalised to UTC on entry.\n- **A synthesised\
    \ creation seeded `parent: None`, and `setdefault` then beat git to it**,\n  discarding\
    \ every backfilled parent.\n- **Closing never cleared `queue_position` in the\
    \ replay**, so every closed task\n  disagreed on that axis.\n\nThe **59 remaining**\
    \ are residual by design, and both categories are named in\n`docs/storage-sqlite.md`:\
    \ 35 tasks predate schema v2 and have no `ball` history anywhere\n(git could see\
    \ the field, but `ball` is not in the backfill contract agreed with\ntask-213,\
    \ and widening it unilaterally is the exact coordination that contract exists\
    \ to\nprevent -- recorded as a candidate, not done); 22 have `lifecycle`/`outcome`\
    \ git could\nsupply and must not, per Rule B.\n\n### task-213's four asks are\
    \ all in\n\n`ix_task_counts` (E), `ix_event_source` (F), the parent FK `DEFERRABLE\
    \ INITIALLY\nDEFERRED` (G), `reporting_tz` documented and used as an IANA zone\
    \ name rather than an\noffset (C), `mechanical` populated (H), and the reconciliation\
    \ invariant asserted in this\nsuite (D) -- including after a backup/restore round\
    \ trip, which their item names\nexplicitly. Rules A and B from their sections\
    \ 4.3 and 4.4 are implemented in\n`backfill.py` with the measurements next to\
    \ the code.\n\n### Two bugs the tests caught in my own code\n\nWorth naming because\
    \ they are the kind that pass review:\n\n- `snapshot()` reached for the shared\
    \ writer connection with no lock, so it raced any\n  in-flight transaction (`cannot\
    \ VACUUM from within a transaction` under load). Fixed\n  with `Database.exclusive()`,\
    \ which refuses rather than waits when the calling thread is\n  itself mid-transaction.\n\
    - The snapshot and its manifest were taken under **two** separate holds of that\
    \ lock, so\n  a concurrent write landed between them and the manifest described\
    \ a database one row\n  ahead of the file beside it -- surfacing later as a restore\
    \ refusing a good snapshot.\n\n### Against the acceptance criteria\n\n- **ac-1**\
    \ -- partial by design. The backend exists, is project-scoped and has no\n  fallback\
    \ path; it is not yet *the* authority, which is task-311's job and is excluded\n\
    \  by this task's own constraints.\n- **ac-2** -- met. Plans asserted, including\
    \ one test that the plan is unchanged at 20x.\n- **ac-3** -- met. Concurrent claim,\
    \ 8-way concurrent log append, atomic band reorder,\n  rolled-back transaction,\
    \ replay and duplicate-slot refusal.\n- **ac-4** -- met. Snapshot under load,\
    \ isolated verify, refusal of a corrupt snapshot,\n  restore keeping what it replaced.\n\
    - **ac-5** -- met for shape and docs; the API/MCP surface is unchanged because\
    \ nothing is\n  repointed at the store yet.\n- **ac-6** -- met. Full gate green;\
    \ measurements above and at 20x in the analytics tests.\n"
  data:
    operation:
      id: c3a4b5d6-1f2e-4839-9a7b-4e6c0d83f2b9
      kind: log_append
      fingerprint: acb0bdc5d0d6fd6ddc32fdf2293ac2cf
- id: 43
  ts: '2026-09-07T03:00:56.348415Z'
  actor: claude
  type: handoff
  body: "Built, tested and gated. Nothing merged; posture prompt said hand off.\n\n\
    **Branch** `feat/task-273-sqlite-store`, three commits, rebased on `main`, gate\
    \ green on `73e07f92` (10/10 stages, 272 s, receipt written). Worktree at `../worktrees/agentjobs-273`.\n\
    \n**What it is.** `src/agentjobs/sqlstore/` \u2014 schema, migrations, store,\
    \ blobs, importer, git backfill, backup/restore \u2014 plus `src/agentjobs/storage_protocol.py`\
    \ as the boundary and `docs/storage-sqlite.md` as the prose. `docs/schema-design.md`\
    \ \xA77 now carries a SUPERSEDED banner. 49 tests.\n\n**Measured on the real 371-task\
    \ corpus.** Import with backfill 6.1 s \u2192 13.8 MB; backlog invariant 128 =\
    \ 128; 370 of 371 documents round-trip byte-identical (the one difference is a\
    \ timezone normalisation of the same instant); assembling all 371 full documents\
    \ 84 ms against 567 ms to parse the same corpus as YAML; online snapshot 59 ms,\
    \ verified restorable.\n\n**Three design changes, each forced by data rather than\
    \ preference.** The one worth your attention: entry 17 claimed `task_run` was\
    \ authoritative for dispatch *results*. It cannot be \u2014 two run ids in this\
    \ corpus carry two `dispatch_result` entries each (interrupted, then completed)\
    \ and a row holds one outcome. Dispatch facts still render from the row; results\
    \ stay on the entry. The other two are in entry 42.\n\n**History is 84% exact,\
    \ not the 89% task-213 measured.** Reported as measured. Four bugs were found\
    \ getting there, three of which would have shipped silently \u2014 including git\
    \ and the manager writing timestamps in different formats, which would have put\
    \ changes in the wrong day bucket and broken every chart on the analytics page.\n\
    \n**What I did not do.** The cutover, the CLI-to-HTTP conversion, and backfilling\
    \ `ball` history \u2014 the last because `ball` is not in the backfill contract\
    \ agreed with task-213, and widening it unilaterally is the coordination that\
    \ contract exists to prevent. All three are named in the doc.\n\n**One thing to\
    \ decide at some point, not now.** This task's record is large enough that returning\
    \ it overruns an MCP response budget; every write this session came back over\
    \ the limit while succeeding. It is a concrete argument for the store's write\
    \ verbs returning a summary plus the affected entry rather than the whole task\
    \ and its log.\n"
  data:
    ball: human
    ball_reason: review
    operation:
      id: d4b5c6e7-2a3f-4940-8b6c-5f7d1e94a3ca
      kind: handoff
      fingerprint: 12cfb7413893562803dac1e63b1c6be0
- id: 44
  ts: '2026-09-07T03:01:13.770896Z'
  actor: claude
  type: note
  body: Updated branches.
  data:
    fields:
    - branches
    operation:
      id: e5c6d7f8-3b4a-4051-9c7d-6a8e2f05b4db
      kind: update_content
      fingerprint: a50d2f571d55b68e7f563f069e90833b
- id: 45
  ts: '2026-09-07T03:05:29.558690Z'
  actor: dispatcher
  type: dispatch_result
  re: 41
  data:
    run_id: run_8cd58e2f
    outcome: completed
    duration_seconds: 2416.131903
    log_path: C:\Users\jpose\.agentjobs\runs\run_8cd58e2f
- id: 46
  ts: '2026-09-07T03:22:11.490146Z'
  actor: Jeff Posey
  type: handoff
  body: Approved by Jeff Posey through the web UI.
  data:
    ball: agent
    ball_reason: work
- id: 47
  ts: '2026-09-07T03:22:13.082754Z'
  actor: finisher
  type: progress
  body: 'Scripted finish started on `feat/task-273-sqlite-store` (73e07f92).


    Rebasing onto `main` and running the full gate in C:\projects\worktrees\agentjobs-273.
    **Nothing is merged yet** and nothing will be unless the gate is green. Its output
    will be at C:\Users\jpose\.agentjobs\finishes\fin_60724860\gate.log.'
  data:
    finish_step: started
    branch: feat/task-273-sqlite-store
    finish_id: fin_60724860
- id: 48
  ts: '2026-09-07T03:26:20.512207Z'
  actor: finisher
  type: progress
  body: 'Merged `feat/task-273-sqlite-store` into `main` as `f1d9ffdd`.


    Rebased onto main and `scripts/check.py` ran green in C:\projects\worktrees\agentjobs-273
    before the merge. Approved by Jeff Posey; no model was in this loop. Delivery
    -- rebuild, restart, verification -- comes next, and this task stays open until
    it is verified.'
  data:
    finish_step: merge
    merge_commit: f1d9ffdd0d22a26554b3d2e2886e265a6cc8e57c
    branch: feat/task-273-sqlite-store
    base: main
- id: 49
  ts: '2026-09-07T03:26:37.429702Z'
  actor: finisher
  type: transition
  body: "Merged `feat/task-273-sqlite-store` into `main` as `f1d9ffdd` and verified\
    \ live. Finished by the scripted path with no agent session (task-241). Approved\
    \ by Jeff Posey; no model was in this loop.\n\nEverything up to and including\
    \ verification:\n\n```\n  preflight  ok         1.0s  feat/task-273-sqlite-store\
    \ at 73e07f92 in C:\\projects\\worktrees\\agentjobs-273\n  runway     ok     \
    \    0.0s  taken immediately\n  rebase     ok         0.2s  feat/task-273-sqlite-store\
    \ onto main: 73e07f92 -> cd42a5dd\n  gate       ok       246.8s  scripts/check.py\
    \ green\n  catch_up   skipped    0.0s  main did not move during the gate\n  merge\
    \      ok         0.2s  --no-ff into main as f1d9ffdd0d22a26554b3d2e2886e265a6cc8e57c\n\
    \  rebuild    skipped    0.0s  the merge touched no frontend/ path\n  restart\
    \    ok         0.5s  ran powershell -NoProfile -ExecutionPolicy Bypass -File\
    \ C:/ai/shared/launchers/open-agentjobs.ps1 -Force -NoOpen\n  verify     ok  \
    \       0.0s  http://127.0.0.1:8876 is serving 2d1a93e5, which contains the merge\
    \ f1d9ffdd, from C:\\projects\\agentjobs (started 2026-09-07T03:26:24.582325+00:00)\n\
    ```"
  data:
    lifecycle: closed
    outcome: completed
